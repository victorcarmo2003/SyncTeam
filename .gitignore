node_modules/
*.rbxm
*.rbxmx
*.vsix
dist/
out/
# dist-plugin/SyncTeam.rbxmx é rastreado de propósito (companion plugin pronto
# pro colega usar sem precisar buildar) — exceção às regras acima.
!dist-plugin/
!dist-plugin/SyncTeam.rbxmx
*.log
Tools/logs/*
!Tools/logs/.gitkeep

# Config/estado local de ferramentas de IA (não é conteúdo do SyncTeam)
.caveman/
.codex/
AGENTS.md
