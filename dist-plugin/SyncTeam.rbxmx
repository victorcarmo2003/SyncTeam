<roblox version="4">
  <Item class="Script" referent="0">
    <Properties>
      <string name="Name">SyncTeam</string>
      <token name="RunContext">0</token>
      <string name="Source"><![CDATA[-- SyncTeam — Plugin de produção (M2 + M3.1 + M3.2 + M4 + M4.5)
--
-- Ponte local mínima para 1 desenvolvedor: cliente WebSocket único da
-- extensão VS Code (a extensão hospeda o servidor; o plugin é sempre
-- cliente — decisão registrada em docs/DECISIONS.md), observação genérica
-- de Source nos containers fixos (SourceWatcher/Config) e handlers do
-- protocolo v2 (writeSource/readSource/listScripts endereçados por
-- UUID+ObjectValue via ScriptRegistry, não mais por path — ver
-- docs/MILESTONES.md seção M2) com handshake versionado. A partir do M3.1,
-- também inicia/para TeamCreateElection (Sessions + heartbeat + eleição de
-- líder sob TestService.SyncTeam) junto com SourceWatcher. A partir do
-- M3.2, também inicia/para TeamCreateLease (leases autoritativas por
-- script) e `handleWriteSource` em modo atualização passa a checar a lease
-- antes de escrever (ver TeamCreateLease.luau). A partir do M4, também
-- inicia/para TeamCreatePresence (passthrough de cursor/seleção/arquivo ativo
-- vindo da extensão para Sessions/<clientId>/Presence) e despacha a mensagem
-- espontânea `presenceUpdate` para `TeamCreatePresence.updateOwnPresence`
-- (ver TeamCreatePresence.luau). A partir do M4.5, a antiga toolbar de 3
-- botões (Conectar/Parar/Alternar porta) foi substituída por 1 botão só que
-- alterna um DockWidgetPluginGui (painel de status construído com Vide, ver
-- plugin/src/ui/PluginUI.luau) — este arquivo continua sendo o ÚNICO dono da
-- lógica de conexão/sessão; PluginUI só invoca callbacks (onConnect/
-- onDisconnect/onPortChange/onAutoReconnectToggle/onResyncRequest) e reflete
-- estado (setConnectionStatus/setPort/setResyncState), nunca reimplementa
-- nada de start()/stop()/troca de porta. 2026-07-20: a tela de configurações
-- do painel ganhou um toggle real para Config.AUTOSTART_SETTING_KEY
-- (onAutoReconnectToggle abaixo, perto de PluginUI.init) — a setting já
-- existia desde 2026-07-16, só sem UI dedicada. 2026-08-02 (docs/DECISIONS.md
-- "5ª rodada"): botão RESYNC — onResyncRequest envia a mensagem espontânea
-- resyncRequest e handleMessage trata a resposta resyncResult (ver os dois,
-- mais abaixo).
--
-- Fora de escopo aqui (ver docs/MILESTONES.md): UX de lease no VS Code
-- (M3.3, delegado a ui-dev); renderização de cursores/seleções na extensão
-- (M4, delegado a ui-dev — este arquivo só faz o passthrough de dados
-- opacos); schema versionado (SchemaVersion, nunca existiu no SyncTeam);
-- propriedades de instâncias não-script (decisão fixa do projeto, v1 só
-- scripts).
--
-- Porta do padrão de spikes/m0_5-local-pipeline/plugin/SyncTeamLab.lua
-- (checkSourceChanged polling+sinal) e spikes/m0-source-replication/
-- SyncTeamM0.lua (token de geração para start/stop seguro).
--
-- 2026-07-07: auto-descoberta de porta em runConnection (ver comentário na
-- própria função) — estopgap para o cenário de teste com 2 Studios na mesma
-- máquina/mesma pasta de Plugins, que hoje colidem na mesma porta sem isso.
-- O campo de porta do painel (M4.5, PluginUI.luau/onPortChange abaixo)
-- continua funcionando como fallback manual: uma porta setada
-- explicitamente é sempre a única tentada, nunca cicla pelas candidatas (ver
-- Config.resolveCandidatePorts).

local HttpService = game:GetService("HttpService")
local RunService = game:GetService("RunService")
local StudioService = game:GetService("StudioService")

local Config = require(script.Config)
local Logger = require(script.Logger)
local SourceWatcher = require(script.SourceWatcher)
local TeamCreateElection = require(script.TeamCreateElection)
local TeamCreateLease = require(script.TeamCreateLease)
local TeamCreatePresence = require(script.TeamCreatePresence)
local TeamCreateUndoGuard = require(script.TeamCreateUndoGuard)
local PluginUI = require(script.ui.PluginUI)
-- ReSync (2026-08-02, docs/DECISIONS.md "5ª rodada"): usado só para o toast
-- de erro do resyncResult (ok=false) abaixo, em handleMessage — chamada
-- DIRETA (Toast.show(text, severity)), não via Logger.notify/PluginUI.notify
-- (que é incondicional à preferência "Mostrar notificações" e sem conceito
-- de severidade): aqui é resposta imediata a uma ação explícita do usuário
-- (clique no botão RESYNC), então sempre aparece.
local Toast = require(script.ui.Toast)

local enabled = false
local currentToken = 0
local client = nil -- WebStreamClient conectado no momento (nil se desconectado)

-- Guarda de modo Run/Play (F8 Run / F5 Play), 2026-07-16 — pesquisa em
-- .claude/research/2026-07-16-runservice-isstudio-isrunning-plugin-detect-test.md.
-- `RunService:IsStudio()` sozinho NÃO distingue edição normal do Team Create de
-- um teste rodando dentro do próprio Studio (fica `true` nos dois casos) — a
-- condição correta de "devo sincronizar" é `IsStudio() and not IsRunning()`.
-- Checagem só no boot não basta: o dev pode apertar F8/F5 DEPOIS do plugin já
-- estar conectado, então isso precisa de polling contínuo detectando a
-- TRANSIÇÃO edição->teste (chama stop()) e teste->edição (chama start() de
-- novo, só se este guard foi quem parou). Reaproveita
-- Config.POLL_INTERVAL_SECONDS (mesmo intervalo já usado para o polling de
-- Source) em vez de um timer novo dedicado — loop próprio, module-level,
-- independente de `enabled`/`currentToken` (precisa rodar mesmo desconectado,
-- para detectar o retorno à edição e poder chamar start() de novo).
--
-- 2026-07-20 (BUG 1, ver docs/DECISIONS.md): a condição
-- `RunService:IsStudio() and RunService:IsRunning()` estava DUPLICADA (aqui e
-- no fim do arquivo, antes do autostart) e o botão CONNECT do painel
-- (onConnect, PluginUI.init abaixo) não tinha NENHUM guard — extraída para
-- esta função única, usada em TODOS os pontos de entrada de start()
-- (checkRunModeTransition, autostart, onConnect, e dentro do próprio start()
-- como defesa em profundidade).
local function isInRunOrPlayMode()
	return RunService:IsStudio() and RunService:IsRunning()
end

-- Limitação conhecida, não resolvida (documentada na pesquisa e em
-- docs/DECISIONS.md 2026-07-16): quando o teste é PAUSADO, a doc oficial
-- afirma que `IsRunning()` (e `IsEdit()`) ficam ambos `false` — ou seja, este
-- guard pode falso-negativo e achar que voltou para edição normal enquanto o
-- teste só está pausado, retomando o sync indevidamente. Não há solução limpa
-- documentada para isso; tratado como limitação aceita, não um hack.
local wasInRunOrPlayMode = isInRunOrPlayMode()
local runGuardStoppedSync = false -- true quando ESTE guard (não o usuário) chamou stop() por causa do Run/Play

-- Sinal EXPLÍCITO de rejeição de porta (2026-07-15): a extensão VS Code rejeita
-- um 2º cliente na mesma porta mandando uma mensagem de aplicação
-- {kind="connectionRejected", reason="port_in_use"} ANTES de fechar o WS. Como
-- WebStreamClient.Closed não expõe close code nem motivo
-- (.claude/research/2026-07-15-webstreamclient-close-code.md), essa mensagem é o
-- único jeito CONFIÁVEL de o plugin saber que o motivo real da queda é "porta
-- ocupada por outro Studio" — a heurística de tempo em runConnection só INFERE
-- isso. handleMessage seta `connectionRejected`; runConnection lê a flag para
-- tratar como rejeição confirmada sem esperar a janela de tempo. É module-level
-- porque handleMessage (dispatch genérico) não enxerga os locals de
-- runConnection — por isso runConnection reseta a flag a cada tentativa.
-- `activePort` = porta da tentativa em curso, usada só para o texto do toast em
-- handleMessage. `lastRejectedPortToasted` deduplica o toast: o Toast é
-- single-instance e re-show reinicia o timer (ver ui/Toast.luau), então sem
-- dedup reconectar numa porta explícita ocupada a cada RECONNECT_SECONDS
-- deixaria um toast permanente/indispensável — mesma disciplina anti-spam de
-- outageToasted/hasStabilizedOnce abaixo.
local connectionRejected = false
local activePort = nil
local lastRejectedPortToasted = nil

-- Guarda de reentrância da troca de porta pelo painel (2026-07-16, bug real
-- com log). FocusLost do campo de porta (ui/StatusPanel.luau, PortRow) pode
-- disparar MAIS DE UMA VEZ para o mesmo Enter/perda de foco (quirk conhecido de
-- TextBox no Roblox). O único guard contra repetição que existia é
-- `newPort ~= state.port()` DENTRO do PortRow, mas ele NÃO é seguro contra
-- reentrância: `state.port()` (portSource) só é atualizado de forma ASSÍNCRONA
-- por PluginUI.setPort, chamado lá dentro de runConnection (já no novo start()).
-- Existe portanto uma janela em que um 2º FocusLost lê o state.port() AINDA
-- ANTIGO contra o portBox.Text já NOVO, passa pelo guard e dispara um 2º
-- onPortChange. Esse 2º onPortChange roda stop()+start() de novo e o stop() mata
-- a sessão/conexão que o 1º start() acabou de criar (sintoma observado no log:
-- conecta em ws://127.0.0.1:1405 e ~40ms depois um stop() completo do MESMO
-- clientId recém-criado). Duas camadas de proteção, complementares:
--   (a) portChangeInFlight: enquanto o stop()+start() de uma troca ainda está
--       rodando (start() pode yieldar internamente), uma 2ª chamada é ignorada
--       — protege o caso de reentrância por yield.
--   (b) dedupe por VALOR+TEMPO: uma 2ª chamada com o MESMO newPort dentro de
--       PORT_CHANGE_DEDUPE_SECONDS é ignorada mesmo depois que a flag baixou —
--       cobre a janela entre start() retornar (flag=false) e o runConnection de
--       fato atualizar state.port(), que é EXATAMENTE quando o guard do PortRow
--       falha.
local PORT_CHANGE_DEDUPE_SECONDS = 1
local portChangeInFlight = false
local lastPortChangeValue = nil
local lastPortChangeAt = 0

local log = Logger.log
local notify = Logger.notify

-- sendMessage é a própria função que o Logger usa para encaminhar logs pela
-- conexão WS (injetada em Logger.init(sendMessage) dentro de start()) — o log
-- de falha de envio abaixo usa `print` PURO, nunca `log`/`Logger.log`. Motivo
-- (evitar recursão, ver Logger.luau): se usasse `log`, uma falha de :Send()
-- disparia Logger.log -> sendMessage -> (a MESMA falha de novo, ainda
-- quebrada) -> log da falha via Logger.log -> sendMessage de novo, sem saída
-- natural do ciclo (este branch não tem nenhum guard por `kind`).
--
-- 2026-08-02 (pedido do usuário: "limpar os prints do output, pode retirar
-- todos" — ver docs/DECISIONS.md): o branch "sem conexão" (client == nil)
-- abaixo DEIXOU de imprimir por completo — antes avisava "descartado (sem
-- conexão): <kind>" via print PURO para toda mensagem de protocolo (não-log)
-- descartada. Removido, e não só rebaixado, porque (a) é ALTA FREQUÊNCIA na
-- prática: dispara a cada mensagem espontânea de qualquer módulo
-- (sourceChanged, presenceUpdate, leaseChanged, scriptAdded...) sempre que o
-- plugin roda com a sessão Team Create ativa mas sem a extensão VS Code
-- conectada (ex.: antes do usuário abrir o VS Code, ou durante qualquer
-- janela de reconexão com um colega ativo do outro lado) — exatamente o
-- padrão "rotineiro" que o resto desta tarefa suprimiu, não um "erro raro";
-- e (b) não existe PERDA de observabilidade real: `client == nil` significa,
-- por definição, que o encaminhamento por WS já está indisponível NESTE
-- exato instante, então não havia nenhum canal (Output ou WS/Tools) capaz de
-- reportar isso de forma diferente de um print cru mesmo antes desta
-- mudança. Mantido só o early return (mensagem descartada em silêncio).
--
-- O branch de "falha ao enviar" logo abaixo é DIFERENTE e foi mantido como
-- print cru de propósito: `client` existe (parecia conectado) e a própria
-- chamada `:Send()` lançou erro — um caminho de erro genuíno e raro (WS
-- quebrou sem que Closed/Error tivessem disparado ainda), não ruído
-- rotineiro, e é o ÚNICO canal restante para relatar essa falha específica
-- (o próprio envio, inclusive de um log via WS, é o que está falhando no
-- instante exato do erro).
local function sendMessage(message)
	if client == nil then
		return
	end
	local ok, err = pcall(function()
		client:Send(HttpService:JSONEncode(message))
	end)
	if not ok then
		print(("[SyncTeam %s]"):format(os.date("%H:%M:%S")), "falha ao enviar:", tostring(err))
	end
end

-- --------------------------------------------------------------- handlers

-- Protocolo v2 (docs/MILESTONES.md, M2): writeSource tem dois modos,
-- distinguidos pela presença do campo `uuid` na mensagem recebida.
--
-- M3.2 (docs/MILESTONES.md): modo ATUALIZAÇÃO agora passa por checagem de
-- lease ANTES de qualquer escrita — registra/refresca o próprio intent
-- (TeamCreateLease.ensureIntent) e só prossegue se TeamCreateLease.canWrite
-- permitir (dono == eu, ou nenhuma lease arbitrada ainda: decisão explícita
-- de permitir otimisticamente, ver TeamCreateLease.luau e MILESTONES.md).
-- Modo CRIAÇÃO (sem uuid) NÃO passa por essa checagem nesta fatia (spec
-- M3.2, item 3 — fora de escopo).
local function handleWriteSource(message)
	if message.uuid ~= nil then
		-- Modo ATUALIZAÇÃO: {uuid, source, requestId} — script já conhecido.
		local instance = SourceWatcher.resolveByUuid(message.uuid)
		if instance == nil then
			sendMessage({
				kind = "writeAck",
				requestId = message.requestId,
				ok = false,
				error = "uuid desconhecido ou script inválido: " .. tostring(message.uuid),
			})
			log(("writeSource (update) uuid=%s -> ERRO: desconhecido"):format(tostring(message.uuid)))
			return
		end

		-- Pastas vendorizadas (Packages/ServerPackages/DevPackages, ver
		-- Config.luau/docs/DECISIONS.md 2026-07-16): edição normal (modo
		-- ATUALIZAÇÃO, script já existente) é bloqueada/ignorada — nunca
		-- arbitra lease nem escreve Source para esses scripts, para não
		-- empurrar ao Team Create compartilhado uma versão vendorizada
		-- divergente entre devs. Modo CRIAÇÃO (path abaixo, sem uuid) não é
		-- afetado por esta checagem — é assim que um pacote novo chega ao
		-- Studio pela 1ª vez.
		if Config.isInsideExcludedPackageFolder(instance) then
			local blockedMessage =
				"edição bloqueada: script dentro de pasta de pacote vendorizado (Packages/ServerPackages/DevPackages) — não sincronizado ao vivo"
			sendMessage({
				kind = "writeAck",
				requestId = message.requestId,
				ok = false,
				uuid = message.uuid,
				error = blockedMessage,
			})
			log(
				("writeSource (update) uuid=%s -> BLOQUEADO (pasta vendorizada): %s"):format(
					tostring(message.uuid),
					instance:GetFullName()
				)
			)
			return
		end

		TeamCreateLease.ensureIntent(message.uuid, instance)
		local canWrite, ownerClientId = TeamCreateLease.canWrite(message.uuid)
		if not canWrite then
			local ownerDisplay = TeamCreateLease.describeClient(ownerClientId)
			local leaseDeniedMessage = ("lease negada — script sendo editado por %s"):format(ownerDisplay)
			sendMessage({
				kind = "writeAck",
				requestId = message.requestId,
				ok = false,
				uuid = message.uuid,
				error = leaseDeniedMessage,
			})
			-- M4.5: toast-worthy (Logger.notify, não Logger.log) — recusa de
			-- escrita é um evento que o usuário precisa perceber sem precisar
			-- olhar o Output (spec da tarefa: writeAck ok=false/lease negada é
			-- um dos gatilhos sugeridos). Log incondicional continua
			-- acontecendo (Logger.notify sempre loga, ver Logger.luau) — só a
			-- exibição do toast depende da preferência "Mostrar notificações".
			notify(("%s (uuid=%s)"):format(leaseDeniedMessage, tostring(message.uuid)))
			return
		end

		local ok, detail = SourceWatcher.writeSource(instance, message.source or "")
		sendMessage({
			kind = "writeAck",
			requestId = message.requestId,
			ok = ok,
			uuid = if ok then message.uuid else nil,
			api = if ok then detail else nil,
			error = if ok then nil else detail,
		})
		-- 2026-07-20 (BUG 2, ruído de uso contínuo — docs/DECISIONS.md):
		-- rotineiro/alta frequência (dispara a cada escrita vinda do VS Code,
		-- não só no boot) -> Logger.debug. Continua encaminhado por WS
		-- (observabilidade de teste automatizado intacta); só não aparece mais
		-- no Output do Studio a cada edição. Erros de uuid desconhecido/bloqueio
		-- de pasta vendorizada (acima, ainda `log`) continuam visíveis — são
		-- raros/acionáveis, não rotina.
		Logger.debug(
			("writeSource (update) uuid=%s -> %s"):format(
				tostring(message.uuid),
				if ok then detail else "ERRO: " .. tostring(detail)
			)
		)
		return
	end

	-- Modo CRIAÇÃO: {path, source, className, requestId} — sem uuid, o
	-- plugin resolve/cria a Instance a partir de `game` e aloca um uuid novo.
	local instance, err = SourceWatcher.resolvePath(message.path, message.className or "Script")
	if instance == nil or not instance:IsA("LuaSourceContainer") then
		sendMessage({
			kind = "writeAck",
			requestId = message.requestId,
			ok = false,
			error = err or ("alvo não é um script: " .. tostring(message.path)),
		})
		return
	end
	-- writeSource já chama watchScript() de forma síncrona antes de escrever,
	-- o que já resolve/aloca o uuid no ScriptRegistry — getUuid abaixo lê o
	-- resultado dessa alocação. Se a Instance for genuinamente nova, o
	-- DescendantAdded (deferred) do container correspondente vai emitir
	-- também um scriptAdded espontâneo com o mesmo uuid — a extensão recebe
	-- os dois (writeAck + scriptAdded); é esperado, não é bug (spec M2).
	local ok, detail = SourceWatcher.writeSource(instance, message.source or "")
	local uuid = SourceWatcher.getUuid(instance)
	sendMessage({
		kind = "writeAck",
		requestId = message.requestId,
		ok = ok,
		uuid = if ok then uuid else nil,
		api = if ok then detail else nil,
		error = if ok then nil else detail,
	})
	-- 2026-07-20 (BUG 2, mesmo critério do writeSource (update) acima):
	-- rotineiro/alta frequência -> Logger.debug.
	Logger.debug(
		("writeSource (create) '%s' -> uuid=%s %s"):format(
			tostring(message.path),
			tostring(uuid),
			if ok then detail else "ERRO: " .. tostring(detail)
		)
	)
end

-- Contrato aditivo (extensão -> plugin, docs/DECISIONS.md): o dev deleta um
-- module localmente no VS Code -> a extensão detecta e manda
-- {kind="deleteScript", requestId, uuid}. Mesmo estilo/estrutura de
-- handleWriteSource modo ATUALIZAÇÃO acima (lógica quase paralela: resolve
-- por uuid, bloqueia pasta vendorizada, checa lease) — reusa o kind
-- "writeAck" na resposta (mesmo formato que writeSource já usa).
--
-- Checagem de lease aplicada aqui pela MESMA razão que updates já a exigem:
-- evita destruir um script que outro dev está com lease ativa de edição no
-- exato momento (consistência e segurança; a alternativa — permitir delete
-- sem lease — abriria uma forma de um dev apagar o trabalho em andamento de
-- outro sem qualquer arbitragem).
--
-- NÃO limpa ScriptRegistry nem emite scriptRemoved aqui: o ciclo de polling
-- que já existe (checkRegistryDrift em SourceWatcher.luau, docs/MILESTONES.md
-- M2) já detecta a Instance destruída (ScriptRegistry.isInstanceDestroyed —
-- Parent == nil + confirmação por pcall, NUNCA ObjectValue.Value == nil, ver
-- .claude/rules/luau.md) dentro de Config.POLL_INTERVAL_SECONDS (0.5s) e
-- emite scriptRemoved espontâneo sozinho, pelo MESMO caminho que já existe
-- para delete feito diretamente no Studio — inclusive o que propaga a
-- remoção para o OUTRO dev (Team Create replica o Destroy(), o Studio dele
-- detecta via o mesmo polling e o plugin dele manda scriptRemoved para a
-- extensão dele).
local function handleDeleteScript(message)
	local instance = SourceWatcher.resolveByUuid(message.uuid)
	if instance == nil then
		sendMessage({
			kind = "writeAck",
			requestId = message.requestId,
			ok = false,
			error = "uuid desconhecido ou script inválido: " .. tostring(message.uuid),
		})
		log(("deleteScript uuid=%s -> ERRO: desconhecido"):format(tostring(message.uuid)))
		return
	end

	-- Pastas vendorizadas (Packages/ServerPackages/DevPackages, ver
	-- Config.luau/docs/DECISIONS.md 2026-07-16): mesma checagem que
	-- handleWriteSource (modo atualização) já faz — delete via live sync
	-- também é bloqueado, para não destruir/perturbar uma árvore vendorizada
	-- que pode divergir entre devs.
	if Config.isInsideExcludedPackageFolder(instance) then
		local blockedMessage =
			"delete bloqueado: script dentro de pasta de pacote vendorizado (Packages/ServerPackages/DevPackages) — não sincronizado ao vivo"
		sendMessage({
			kind = "writeAck",
			requestId = message.requestId,
			ok = false,
			uuid = message.uuid,
			error = blockedMessage,
		})
		log(
			("deleteScript uuid=%s -> BLOQUEADO (pasta vendorizada): %s"):format(
				tostring(message.uuid),
				instance:GetFullName()
			)
		)
		return
	end

	TeamCreateLease.ensureIntent(message.uuid, instance)
	local canWrite, ownerClientId = TeamCreateLease.canWrite(message.uuid)
	if not canWrite then
		local ownerDisplay = TeamCreateLease.describeClient(ownerClientId)
		local leaseDeniedMessage = ("lease negada — script sendo editado por %s"):format(ownerDisplay)
		sendMessage({
			kind = "writeAck",
			requestId = message.requestId,
			ok = false,
			uuid = message.uuid,
			error = leaseDeniedMessage,
		})
		-- Mesmo padrão M4.5 de lease negada em handleWriteSource: toast-worthy
		-- (Logger.notify), não só log — o dev precisa perceber sem olhar o
		-- Output.
		notify(("%s (uuid=%s)"):format(leaseDeniedMessage, tostring(message.uuid)))
		return
	end

	-- Toda API de plataforma em pcall (.claude/rules/luau.md).
	local ok, err = pcall(function()
		instance:Destroy()
	end)
	sendMessage({
		kind = "writeAck",
		requestId = message.requestId,
		ok = ok,
		uuid = if ok then message.uuid else nil,
		error = if ok then nil else tostring(err),
	})
	-- Diferente de writeSource update/create (rotineiro/alta frequência,
	-- rebaixado a Logger.debug em 2026-07-20): delete é raro e consequente
	-- (evento destrutivo) — mesmo critério já estabelecido
	-- ("rotineiro/alta frequência = debug; raro/acionável = log") aponta para
	-- manter Logger.log tanto no sucesso quanto no erro.
	log(
		("deleteScript uuid=%s -> %s"):format(
			tostring(message.uuid),
			if ok then "destruído" else "ERRO: " .. tostring(err)
		)
	)
end

local function handleReadSource(message)
	local instance = SourceWatcher.resolveByUuid(message.uuid)
	if instance == nil then
		sendMessage({
			kind = "sourceContent",
			requestId = message.requestId,
			ok = false,
			error = "uuid desconhecido ou script inválido: " .. tostring(message.uuid),
		})
		return
	end
	sendMessage({
		kind = "sourceContent",
		requestId = message.requestId,
		ok = true,
		source = instance.Source,
	})
end

local function handleListScripts(message)
	sendMessage({
		kind = "scriptList",
		requestId = message.requestId,
		scripts = SourceWatcher.listScripts(),
	})
end

-- ReSync (2026-08-02, docs/DECISIONS.md "5ª rodada"): espontânea, sem
-- requestId (mesmo padrão de presenceUpdate/watchedRoots) — resposta da
-- extensão ao `resyncRequest` disparado pelo clique no botão RESYNC do painel
-- (ver `onResyncRequest` em PluginUI.init, mais abaixo). `message.ok == true`:
-- a extensão apagou e repuxou tudo do Studio com sucesso — painel vai a
-- "done" e volta sozinho a "idle" depois de RESYNC_DONE_HOLD_SECONDS (mesmo
-- timing do mockup aprovado). `message.ok == false`: painel volta DIRETO a
-- "idle" (nunca passa por "done") e um toast de erro avisa o motivo —
-- `message.reason` é "cancelled_by_user" (usuário recusou o modal de
-- confirmação do lado da extensão) ou uma mensagem de erro livre (falha real
-- durante o reset). Mensagem ADITIVA — não bumpa Config.PROTOCOL_VERSION
-- (mesmo precedente de watchedRoots/ping/leaseChanged).
local RESYNC_DONE_HOLD_SECONDS = 1.2

local function handleResyncResult(message)
	if message.ok == true then
		PluginUI.setResyncState("done")
		log(("resyncResult: sucesso (%s arquivo(s) apagado(s)/repuxado(s))"):format(tostring(message.deletedCount)))
		task.delay(RESYNC_DONE_HOLD_SECONDS, function()
			PluginUI.setResyncState("idle")
		end)
		return
	end

	PluginUI.setResyncState("idle")
	local reason = message.reason
	local friendlyMessage
	if reason == "cancelled_by_user" then
		friendlyMessage = "ReSync cancelado: confirmação recusada no VS Code."
	else
		friendlyMessage = ("ReSync falhou: %s"):format(tostring(reason or "motivo desconhecido"))
	end
	-- Chamada DIRETA a Toast.show (ver comentário do require de Toast, no
	-- topo do arquivo) — sempre visível, independente da preferência "Mostrar
	-- notificações" (que só governa o canal Logger.notify/PluginUI.notify):
	-- é resposta imediata a um clique explícito do usuário no botão RESYNC.
	Toast.show(friendlyMessage, "erro")
	log(("resyncResult: falha — %s"):format(tostring(reason)))
end

local function handleMessage(raw)
	local ok, message = pcall(function()
		return HttpService:JSONDecode(raw)
	end)
	if not ok or type(message) ~= "table" or type(message.kind) ~= "string" then
		log("mensagem inválida:", tostring(raw))
		return
	end

	if message.kind == "writeSource" then
		handleWriteSource(message)
	elseif message.kind == "deleteScript" then
		-- Mensagem ADITIVA (extensão -> plugin, docs/DECISIONS.md) — mesmo
		-- padrão de "ping": não bumpa Config.PROTOCOL_VERSION.
		handleDeleteScript(message)
	elseif message.kind == "readSource" then
		handleReadSource(message)
	elseif message.kind == "listScripts" then
		handleListScripts(message)
	elseif message.kind == "watchedRoots" then
		-- Extensão -> plugin, espontânea, 2026-07-29 (aditiva — NÃO bumpa
		-- Config.PROTOCOL_VERSION, mesmo precedente de
		-- ping/leaseChanged/presenceUpdate/connectionRejected). Contrato
		-- completo em docs/DECISIONS.md e vscode-extension/src/protocol.ts
		-- (WatchedRootsMessage): mandada 1x por conexão aceita, logo após o
		-- hello ser validado e ANTES do primeiro listScripts. `roots` é uma
		-- lista de nomes de Service (ex. "ReplicatedFirst") — ver
		-- SourceWatcher.applyWatchedRoots para o que o plugin faz com ela
		-- (substitui a fonte de verdade da lista fixa hardcoded de
		-- Config.getWatchedRoots() por uma união fixa+dinâmica).
		if type(message.roots) == "table" then
			SourceWatcher.applyWatchedRoots(message.roots)
		else
			log("watchedRoots: campo 'roots' ausente/inválido, ignorado.")
		end
	elseif message.kind == "presenceUpdate" then
		-- M4: espontânea, sem requestId, sem ack (spec docs/MILESTONES.md) —
		-- só grava a própria Presence; TeamCreatePresence.updateOwnPresence já
		-- degrada sem erro se a sessão local ainda não estiver pronta.
		TeamCreatePresence.updateOwnPresence(message)
	elseif message.kind == "ping" then
		-- Heartbeat da extensão (vscode-extension HeartbeatMonitor.ts): a
		-- extensão manda {kind:"ping"} a cada 5s enquanto conectada e responde-se
		-- pong imediatamente. Mensagem ADITIVA — não bumpa PROTOCOL_VERSION. Sem
		-- requestId, sem estado: é só liveness. O plugin NÃO origina ping próprio;
		-- só RECEBER este ping já conta como atividade (o handler de
		-- MessageReceived em runConnection atualiza lastMessageAt para QUALQUER
		-- mensagem, inclusive esta — a ausência dele é o que detecta a queda).
		sendMessage({ kind = "pong" })
	elseif message.kind == "connectionRejected" then
		-- A extensão rejeitou explicitamente esta conexão (2º cliente na mesma
		-- porta) e fechará o WS logo em seguida. Sinal DEFINITIVO de "porta
		-- ocupada por outro Studio" — runConnection lê a flag e trata como
		-- rejeição confirmada na hora (sem depender da heurística de tempo
		-- Config.PROBABLE_REJECTION_WINDOW_SECONDS, que passa a ser só o fallback
		-- para quando este sinal NÃO chega, ex.: extensão antiga sem este fix).
		-- Toast (Logger.notify, não Logger.log) só na 1ª vez por porta ocupada;
		-- rejeições repetidas na mesma porta só logam (ver dedupe no topo do
		-- arquivo). `reason` hoje é sempre "port_in_use" (único definido) — a
		-- flag/aviso assumem esse motivo.
		connectionRejected = true
		if lastRejectedPortToasted ~= activePort then
			lastRejectedPortToasted = activePort
			notify(("porta %d já está em uso por outro Studio"):format(activePort or 0))
		else
			log(
				("porta %d já está em uso por outro Studio (rejeição repetida; toast já exibido)"):format(
					activePort or 0
				)
			)
		end
	elseif message.kind == "resyncResult" then
		-- Mensagem ADITIVA (extensão -> plugin, espontânea, docs/DECISIONS.md
		-- "5ª rodada") — mesmo padrão de "ping"/"watchedRoots": não bumpa
		-- Config.PROTOCOL_VERSION.
		handleResyncResult(message)
	else
		log("kind desconhecido/ignorado:", tostring(message.kind))
	end
end

-- ------------------------------------------------------------- conexão ws

local function sendHello()
	local okUserId, userId = pcall(function()
		return StudioService:GetUserId()
	end)
	sendMessage({
		kind = "hello",
		protocolVersion = Config.PROTOCOL_VERSION,
		role = "studio",
		placeName = game.Name,
		userId = if okUserId then userId else nil,
		pluginVersion = Config.PLUGIN_VERSION,
		-- M3.3: clientId da eleição (TeamCreateElection.luau), para a extensão
		-- comparar contra o dono de uma lease. nil se a eleição ainda não
		-- rodou (start() chamado antes de TeamCreateElection.start() concluir
		-- o primeiro tick? não é o caso aqui — TeamCreateElection.start() já
		-- gera o clientId de forma síncrona antes de retornar; nil só seria
		-- possível se sendHello rodasse antes de start(), o que não acontece
		-- neste fluxo) — enviado como nil mesmo, sem erro, nesse caso.
		clientId = TeamCreateElection.getClientId(),
	})
end

-- Uma única conexão (o produto usa 1 conexão; spikes é que usavam até 2 —
-- ver .claude/rules/luau.md). Loop de reconexão guardado pelo token de
-- geração: stop()/start() repetidos não deixam loops antigos sobrepostos
-- (pegadinha real do M0, ver .claude/agent-memory/luau-dev.md).
--
-- Auto-descoberta de porta (2026-07-07, pedido do orquestrador): `ports` é
-- ou uma lista de 1 item (porta já explicitamente setada — manual via botão
-- "Alternar porta" ou persistida por uma descoberta automática anterior) ou
-- `Config.CANDIDATE_PORTS` inteiro (sem nenhuma setting ainda). O harness
-- (SyncServer.ts) rejeita uma 2ª conexão simultânea com
-- `socket.close(1013, "SyncTeam: já existe um plugin conectado")` — mas NÃO
-- há confirmação salva em .claude/research/ sobre o que
-- CreateWebStreamClient expõe desse close code/motivo através dos
-- parâmetros do evento Error, então o critério de "rejeição provável" abaixo
-- deliberadamente NÃO tenta interpretar `code`/`errorMessage` (só loga eles,
-- como antes) — usa só observação local: a conexão caiu SEM NUNCA ter
-- recebido nenhuma mensagem do harness E dentro de uma janela curta
-- (Config.PROBABLE_REJECTION_WINDOW_SECONDS) desde que foi aberta. Uma queda
-- que ficou de pé mais que essa janela, ou que já recebeu qualquer mensagem,
-- é tratada como queda normal (reconecta na MESMA porta, como antes desta
-- mudança).
local function runConnection(myToken, ports, explicitAtStart, pluginObject)
	local index = 1
	local attemptsInLap = 0
	-- Já explícita = nunca persistir (item 4 da tarefa: nunca sobrescrever
	-- uma porta que o usuário já escolheu, seja manual seja de uma sessão
	-- anterior). Só descobertas automáticas desta sessão chegam a persistir.
	local persistedAutoPort = explicitAtStart

	-- Estado visual do botão CONNECT/DISCONNECT (item 1 da tarefa): a cada
	-- volta deste loop o estado vai para "connecting" (laranja) e só passa a
	-- "connected" (vermelho) quando a conexão estabiliza (abaixo). O estado
	-- "disconnected" (azul) é setado SÓ por stop(), nunca aqui — assim um loop
	-- de ConnectFail fica laranja estável em vez de piscar azul/vermelho, que
	-- era o que o usuário via como confuso.
	--
	-- M4.5 (toast de "reconexão falhando"): `hasStabilizedOnce` vira true na
	-- primeira vez que esta runConnection (mesmo myToken, ou seja, mesmo
	-- ciclo start()/stop()) recebe de verdade qualquer mensagem do harness —
	-- só a partir daí uma queda é tratada como "algo que estava funcionando
	-- parou", digno de toast. Sem isso, o cenário normal "extensão VS Code
	-- ainda não foi aberta" (loop de reconexão tentando a cada
	-- RECONNECT_SECONDS sem nunca ter recebido nada) dispararia toast
	-- repetido desde o primeiro segundo — exatamente o "evento rotineiro"
	-- que a spec pede pra NÃO notificar. Isto é MAIS forte que o critério
	-- visual acima (que também aceita "ficou de pé além da janela") de
	-- propósito: um ConnectFail nunca recebe mensagem NEM fica de pé, então
	-- não vira nem laranja-confirmado nem toast — é o cenário "nunca conectou
	-- de verdade", corretamente silencioso. `outageToasted` garante só 1 toast
	-- por "episódio" de queda (reseta pra false assim que uma nova conexão
	-- estabiliza de novo, permitindo um novo toast se cair de novo depois).
	local hasStabilizedOnce = false
	local outageToasted = false

	while enabled and currentToken == myToken do
		local port = ports[index]
		activePort = port
		-- Reset por tentativa: `connectionRejected` é module-level (setado por
		-- handleMessage), então precisa nascer false a cada nova conexão para
		-- não vazar uma rejeição de uma tentativa anterior para a lógica de
		-- decisão abaixo.
		connectionRejected = false
		PluginUI.setPort(port)
		-- Cada volta do loop = uma tentativa de conexão → estado "connecting"
		-- (botão laranja). Dedupe no setter evita reexecutar effects quando já
		-- está "connecting" (o caso comum no loop de reconexão). Só vira
		-- "connected" (vermelho) quando a conexão estabiliza de fato, abaixo.
		PluginUI.setConnectionStatus("connecting")
		local url = ("ws://127.0.0.1:%d"):format(port)
		local ok, newClient = pcall(function()
			return HttpService:CreateWebStreamClient(Enum.WebStreamClientType.WebSocket, { Url = url })
		end)
		if not ok then
			log(
				("falha ao criar cliente WS (%s); nova tentativa em %ds"):format(
					tostring(newClient),
					Config.RECONNECT_SECONDS
				)
			)
			task.wait(Config.RECONNECT_SECONDS)
			continue
		end

		local closed = false
		local receivedAnyMessage = false
		local connectedAt = os.clock()
		-- Timestamp local da ÚLTIMA mensagem recebida de QUALQUER tipo (estende o
		-- rastreamento que `receivedAnyMessage` já fazia como booleano — agora
		-- também guardamos QUANDO, para detectar silêncio prolongado). Inicia em
		-- connectedAt: se nada chegar desde a abertura, o silêncio conta a partir
		-- daqui. Atualizado no handler de MessageReceived abaixo, para ping
		-- (heartbeat da extensão a cada 5s) e para toda outra mensagem por igual.
		local lastMessageAt = connectedAt
		local connections = {}
		table.insert(
			connections,
			newClient.MessageReceived:Connect(function(raw)
				receivedAnyMessage = true
				lastMessageAt = os.clock()
				handleMessage(raw)
			end)
		)
		table.insert(
			connections,
			newClient.Closed:Connect(function()
				closed = true
			end)
		)
		table.insert(
			connections,
			newClient.Error:Connect(function(code, errorMessage)
				-- 2026-07-16: Logger.debug (ver Logger.luau) — um ConnectFail isolado
				-- é ruído rotineiro do loop de reconexão (a linha visível de verdade
				-- é "desconectado; reconectando em Xs" logo abaixo, categoria
				-- "reconectando"); continua encaminhado por WS para diagnóstico.
				Logger.debug(("erro WS %s %s"):format(tostring(code), tostring(errorMessage)))
				closed = true
			end)
		)

		client = newClient
		sendHello()
		-- NÃO vira "connected" (vermelho) aqui: criar o WebStreamClient e
		-- mandar o hello é otimista (a conexão pode falhar logo em seguida com
		-- ConnectFail). O botão só passa a vermelho quando a conexão
		-- ESTABILIZA de fato (abaixo) — até lá fica laranja ("connecting").
		-- 2026-07-16: rebaixado para Logger.debug — esta tentativa ainda não é
		-- a categoria visível "conectado" (spec da tarefa: só 3 categorias de
		-- status, uma linha por MUDANÇA real); a linha Logger.log de verdade
		-- fica no ponto de estabilização abaixo (`shownConnected`).
		Logger.debug(("conectado em %s"):format(url))

		-- Vira "connected" (vermelho) só na 1ª vez que esta tentativa
		-- estabiliza — não repete o setter a cada 0.5s.
		local shownConnected = false

		while enabled and currentToken == myToken and not closed do
			task.wait(0.5)

			-- Garantia independente de Closed/Error: se ficamos silenciosos por
			-- mais de DEAD_CONNECTION_TIMEOUT_SECONDS (nenhuma mensagem de nenhum
			-- tipo — nem o ping periódico da extensão a cada 5s, nem qualquer
			-- outra), a extensão provavelmente caiu sem fechar o WS (ex.: "Reload
			-- Window" do VS Code) — cenário em que Closed/Error do WebStreamClient
			-- podem NÃO disparar (o bug que esta detecção existe para cobrir).
			-- Só setamos `closed = true`: EXATAMENTE o que os handlers de
			-- Closed/Error já fazem (o restante da iteração corrente termina
			-- inócuo e a condição do `while` encerra o loop na próxima checagem —
			-- mesmíssimo timing de quando um Closed dispara durante o task.wait),
			-- então caímos no MESMO teardown + ciclo de reconexão logo abaixo (que
			-- por sua vez repõe o painel em "connecting" sozinho — não duplicamos
			-- setConnectionStatus aqui).
			if os.clock() - lastMessageAt >= Config.DEAD_CONNECTION_TIMEOUT_SECONDS then
				log(
					("sem mensagens da extensão há %ds (provável queda sem close do WS); forçando reconexão"):format(
						Config.DEAD_CONNECTION_TIMEOUT_SECONDS
					)
				)
				closed = true
			end

			-- "Estabilizou" (item 4): recebeu qualquer mensagem do harness
			-- depois do hello (a extensão dispara um listScripts inicial em
			-- runInitialSync assim que valida o hello — ver vscode-extension/
			-- src/sync/SyncTeamService.ts), OU já ficou conectada além da
			-- janela curta de rejeição provável sem cair.
			-- `not connectionRejected` suprime "estabilizou": uma conexão
			-- rejeitada RECEBE sim uma mensagem (o próprio connectionRejected),
			-- então sem esta guarda ela contaria como estável e o plugin (a)
			-- piscaria o botão em "connected" e (b) PERSISTIRIA a porta ocupada
			-- como porta descoberta (SetSetting abaixo) — os dois errados. Ver
			-- handleMessage/connectionRejected.
			local stabilized = not connectionRejected
				and (receivedAnyMessage or (os.clock() - connectedAt) >= Config.PROBABLE_REJECTION_WINDOW_SECONDS)

			-- Estado visual (item 1 da tarefa): só vira "connected" (vermelho)
			-- ao estabilizar. Um ConnectFail nunca estabiliza (cai em
			-- <PROBABLE_REJECTION_WINDOW_SECONDS sem receber nada), então o
			-- botão fica laranja o loop inteiro em vez de piscar azul/vermelho.
			-- Nota: usa o critério FRACO (recebeu msg OU ficou de pé além da
			-- janela), igual à persistência de porta — mais permissivo que o
			-- critério de toast (hasStabilizedOnce, só recebeu msg). Ver
			-- .claude/agent-memory/ui-dev.md para o porquê da diferença.
			if stabilized and not shownConnected then
				shownConnected = true
				-- Conexão real estabeleceu: zera o dedupe de toast de rejeição,
				-- para que uma futura ocupação DESTA mesma porta (outro Studio a
				-- toma depois) volte a notificar.
				lastRejectedPortToasted = nil
				PluginUI.setConnectionStatus("connected")
				-- 2026-07-16 (consolidação de log de boot, item 3 da tarefa): ÚNICA
				-- linha visível (Logger.log) da categoria "conectado" — substitui a
				-- antiga rajada de ~7 linhas de subsistema (agora Logger.debug, ver
				-- acima e nos módulos SourceWatcher/TeamCreateElection/
				-- TeamCreateLease/TeamCreatePresence). Contagem de scripts vem de
				-- SourceWatcher.getWatchedCount() (novo, mesmo dado que a antiga
				-- linha "observação iniciada" já expunha, só lido sob demanda aqui
				-- em vez de guardado). Não inclui líder/term aqui de propósito: a
				-- eleição é assíncrona e pode não ter convergido ainda no instante
				-- em que a conexão WS estabiliza — "sou o líder agora (term N)"
				-- continua sendo logado separadamente por TeamCreateElection quando
				-- de fato acontecer.
				notify(("conectado em %s (%d scripts observados)"):format(url, SourceWatcher.getWatchedCount()))
			end

			-- Persistir assim que estabilizar (não espera a conexão cair) para
			-- reloads futuros já reconectarem direto nesta porta.
			if not persistedAutoPort and stabilized then
				local okSet, errSet = pcall(function()
					pluginObject:SetSetting(Config.PORT_SETTING_KEY, port)
				end)
				if okSet then
					log(("porta %d confirmada e salva"):format(port))
				else
					log(("falha ao salvar porta descoberta automaticamente (%d): %s"):format(port, tostring(errSet)))
				end
				persistedAutoPort = true
			end
			-- M4.5: ver comentário no topo de runConnection — só o recebimento
			-- REAL de uma mensagem do harness conta como "estabilizou" para
			-- fins de toast (mais forte que o critério visual/porta acima, que
			-- também aceita só "ficou de pé um tempo").
			-- `and not connectionRejected`: um connectionRejected É uma mensagem
			-- recebida, mas NÃO é "a conexão funcionou" — não deve armar o toast
			-- genérico de "conexão perdida" (já existe o toast específico de
			-- porta ocupada em handleMessage).
			if not hasStabilizedOnce and receivedAnyMessage and not connectionRejected then
				hasStabilizedOnce = true
				outageToasted = false
			end
		end

		local aliveSeconds = os.clock() - connectedAt
		client = nil
		for _, connection in connections do
			connection:Disconnect()
		end
		pcall(function()
			newClient:Close()
		end)

		if not (enabled and currentToken == myToken) then
			-- stop() já colocou o estado em "disconnected" (botão azul) — não
			-- sobrescreve com "connecting" aqui.
			return
		end

		-- Ainda habilitado: vamos tentar de novo → volta a "connecting"
		-- (laranja). Cobre a espera até a próxima tentativa (RECONNECT_SECONDS
		-- ou CANDIDATE_RETRY_DELAY_SECONDS) sem deixar o botão preso em
		-- "connected" (vermelho) depois de uma queda.
		PluginUI.setConnectionStatus("connecting")

		-- Rejeição CONFIRMADA pelo sinal explícito (connectionRejected) OU
		-- inferida pela heurística de tempo (fallback quando o sinal não chega:
		-- extensão antiga — "caiu cedo sem nunca receber mensagem"). O sinal
		-- explícito dispensa tanto a janela de tempo quanto o "não recebeu
		-- mensagem" (o próprio connectionRejected É uma mensagem). Só cicla
		-- candidatas se houver >1 porta; porta explícita (#ports==1) sempre
		-- reconecta na mesma — o toast de handleMessage já avisou o motivo e o
		-- usuário decide trocar de porta no painel (item 2 da tarefa).
		local rejected = connectionRejected
			or ((not receivedAnyMessage) and aliveSeconds < Config.PROBABLE_REJECTION_WINDOW_SECONDS)
		local probablyRejected = rejected and #ports > 1

		-- M4.5: toast só quando uma conexão que JÁ FUNCIONOU cai de um jeito
		-- que não é a rotina de "ciclar candidatas de porta" — evita o ruído
		-- de notificar o cenário normal "extensão ainda não foi aberta" (ver
		-- comentário no topo da função).
		if not probablyRejected and hasStabilizedOnce and not outageToasted then
			outageToasted = true
			notify("conexão com a extensão VS Code perdida; tentando reconectar...")
		end

		if probablyRejected then
			attemptsInLap += 1
			local nextIndex = (index % #ports) + 1
			if attemptsInLap >= #ports then
				-- Esgotou todas as candidatas nesta volta sem sucesso: volta
				-- pro início da lista, mas com o RECONNECT_SECONDS normal (evita
				-- busy-loop se nenhum harness estiver de pé em porta nenhuma).
				log(
					("porta %d parece ocupada; todas as %d candidatas falharam, aguardando %ds antes de recomeçar"):format(
						port,
						#ports,
						Config.RECONNECT_SECONDS
					)
				)
				attemptsInLap = 0
				index = nextIndex
				task.wait(Config.RECONNECT_SECONDS)
			else
				log(("porta %d parece ocupada, tentando %d..."):format(port, ports[nextIndex]))
				index = nextIndex
				task.wait(Config.CANDIDATE_RETRY_DELAY_SECONDS)
			end
		else
			attemptsInLap = 0
			log(("desconectado; reconectando em %ds"):format(Config.RECONNECT_SECONDS))
			task.wait(Config.RECONNECT_SECONDS)
		end
	end
end

-- ------------------------------------------------------------ start/stop

-- item 2a da tarefa 2026-07-21: `silent` é true só quando quem chama é o guard
-- de Run/Play (F8/F5) — suspender o sync por causa de um teste não é uma
-- desconexão que o usuário pediu, então fica só no log (sem toast). Os outros
-- 3 chamadores (Desconectar manual, o ciclo de troca de porta,
-- plugin.Unloading) chamam stop() sem argumento (silent fica falsy) e
-- continuam disparando o toast, já que são desconexões reais/deliberadas.
local function stop(silent)
	if not enabled then
		return
	end
	enabled = false
	currentToken += 1
	SourceWatcher.stop()
	-- TeamCreateLease.stop()/TeamCreatePresence.stop() antes de
	-- TeamCreateElection.stop(): ambos só limpam estado em memória (a própria
	-- Presence e as pastas de intent morrem em cascata quando
	-- TeamCreateElection.stop() destrói a própria sessão a seguir) — ordem
	-- não é estritamente necessária, mas segue a mesma disciplina "parar o
	-- que depende antes do que é dependido" já usada neste arquivo.
	TeamCreateLease.stop()
	TeamCreatePresence.stop()
	-- TeamCreateElection.stop() remove a própria entrada de Sessions/ de forma
	-- SÍNCRONA (M3.1) — mesmo cuidado do fix de WebStreamClient abaixo:
	-- plugin.Unloading pode não dar chance de uma coroutine assíncrona notar
	-- isso depois.
	TeamCreateElection.stop()
	-- Fecha o WebStreamClient de forma SÍNCRONA aqui, em vez de confiar no
	-- cleanup do loop runConnection (task.spawn): plugin.Unloading provavelmente
	-- mata a coroutine antes dela acordar do task.wait e rodar seu próprio
	-- newClient:Close(). Bug real confirmado em teste ao vivo, 2026-07-04
	-- (docs/DECISIONS.md): reloads repetidos do plugin vazavam um
	-- WebStreamClient cada, até atingir o limite de 6 por Studio
	-- (.claude/rules/luau.md) e travar toda nova conexão.
	if client ~= nil then
		pcall(function()
			client:Close()
		end)
	end
	client = nil
	-- Zera o dedupe de toast de rejeição de porta: um novo start() deve poder
	-- notificar de novo se reencontrar uma porta ocupada.
	lastRejectedPortToasted = nil
	-- Estado visual: "disconnected" (botão azul, ação = conectar) — o loop de
	-- runConnection já não vai sobrescrever porque enabled == false (ele volta
	-- cedo ao notar isso). runConnection NUNCA seta "disconnected" sozinho:
	-- esse estado parado é responsabilidade exclusiva do stop().
	PluginUI.setConnectionStatus("disconnected")
	-- BUG CORRIGIDO (2026-08-02, achado em auditoria de UI): sem isto, uma
	-- desconexão (WS cai, extensão fecha, DISCONNECT manual) NO MEIO de um
	-- RESYNC deixava o botão preso em "syncing" (amarelo, não clicável) pra
	-- sempre — só resyncResult ou o próprio clique setavam "idle", e nenhum
	-- dos dois chega se a conexão já caiu. stop() é o único ponto que sabe
	-- com certeza que nenhum resyncResult vai mais chegar para o resync em
	-- andamento, então é quem precisa resetar.
	PluginUI.setResyncState("idle")
	if silent then
		log("parado.")
	else
		notify("parado.")
	end
end

local function start(pluginObject)
	if enabled then
		log("já está rodando.")
		return
	end
	-- Defesa em profundidade (2026-07-20, BUG 1 — docs/DECISIONS.md): start()
	-- recusa a si mesmo em Run/Play, no MESMO padrão de guarda de idempotência
	-- já usado acima (`if enabled then ... return end`). Cobre qualquer
	-- chamador futuro que esqueça de checar antes. Os call-sites que já sabem
	-- de antemão que isRunOrPlayMode() é verdadeiro CONTINUAM checando por
	-- conta própria antes de chamar start() (ver onConnect abaixo) — só eles
	-- sabem o CONTEXTO da chamada (ação explícita do usuário) para decidir se
	-- vale um toast (Logger.notify); aqui dentro só loga (Logger.log), sem
	-- toast, porque start() não sabe se foi autostart, onConnect ou o guard de
	-- transição (que na prática nunca cai aqui: só chama start() de volta
	-- quando JÁ detectou o retorno à edição).
	if isInRunOrPlayMode() then
		log("start() recusado: Studio em modo Run/Play (F8/F5); conecte após voltar para edição normal.")
		return
	end
	enabled = true
	currentToken += 1
	local myToken = currentToken
	-- Estado visual imediato: o clique em CONNECT (ou o auto-start) já coloca o
	-- botão em "connecting" (laranja) antes mesmo da 1ª tentativa de WS em
	-- runConnection, sem esperar o loop começar.
	PluginUI.setConnectionStatus("connecting")
	local ports, explicitPort = Config.resolveCandidatePorts(pluginObject)
	local okUserId, resolvedUserId = pcall(function()
		return StudioService:GetUserId()
	end)
	-- Logger.init ANTES de SourceWatcher.start()/TeamCreateElection.start()/
	-- TeamCreateLease.start(): esses três já podem logar (via `log`/Logger.log)
	-- durante a própria inicialização (ex.: "observação iniciada", "sessão
	-- criada"), então o encaminhamento por WS precisa estar disponível o quanto
	-- antes — mesmo cuidado de ordem já seguido para SourceWatcher.init/
	-- TeamCreateLease.init abaixo.
	Logger.init(sendMessage)
	-- M4.5: injeta o callback de toast do painel — Logger.notify(...) (usado
	-- pelos poucos pontos "toast-worthy" deste arquivo e de
	-- TeamCreateSchema.luau) passa a acionar PluginUI.notify a partir de
	-- agora. Antes deste ponto (e se PluginUI.init tiver falhado, ver
	-- PluginUI.luau), Logger.notify se comporta como Logger.log.
	Logger.initNotify(PluginUI.notify)
	SourceWatcher.init(sendMessage)
	SourceWatcher.start()
	-- pluginObject repassado (M4.5+, "Nome de exibição customizado") — TeamCreateElection
	-- usa para Config.resolveCustomDisplayName no boot desta sessão de eleição.
	TeamCreateElection.start(if okUserId then resolvedUserId else 0, pluginObject)
	-- TeamCreateLease depois de TeamCreateElection: ensureIntent (chamado só
	-- durante handleWriteSource, não aqui) depende de
	-- TeamCreateElection.getSessionFolder() já existir. init() (M3.3) injeta o
	-- mesmo callback sendMessage que SourceWatcher já usa, para o ciclo de
	-- checkLeaseDrift poder emitir `leaseChanged` espontâneo. pluginObject
	-- repassado (2026-08-02, docs/DECISIONS.md "8ª rodada", handoff
	-- quase-instantâneo de lease) — mesmo padrão de TeamCreateElection.start
	-- acima: TeamCreateLease usa para Config.getPlaceSetting(...,
	-- "leaseStaleAfterSeconds") a cada ciclo do líder.
	TeamCreateLease.init(sendMessage)
	TeamCreateLease.start(pluginObject)
	-- TeamCreatePresence depois de TeamCreateElection, mesmo motivo de
	-- TeamCreateLease acima: updateOwnPresence/checkPresenceDrift dependem de
	-- TeamCreateElection.getSessionFolder()/getClientId() já existirem.
	TeamCreatePresence.init(sendMessage)
	TeamCreatePresence.start()
	task.spawn(runConnection, myToken, ports, explicitPort, pluginObject)
	-- 2026-07-16: Logger.debug (ruído rotineiro de boot, ver Logger.luau) —
	-- continua encaminhado por WS, só não polui o Output a cada conexão. O
	-- status visível de verdade ("conectado"/"reconectando"/"falha") é o
	-- consolidado dentro de runConnection (ver comentários lá).
	if explicitPort then
		Logger.debug(("iniciado. Conectando à porta %d..."):format(ports[1]))
	else
		Logger.debug(("iniciado. Sem porta salva ainda; testando candidatas (%s)..."):format(table.concat(ports, ", ")))
	end
end

-- --------------------------------------------------- guarda de Run/Play (F8/F5)
--
-- Checa a transição de RunService:IsRunning() a cada Config.POLL_INTERVAL_SECONDS
-- (ver comentário de wasInRunOrPlayMode acima). Loop próprio, roda pela vida
-- inteira do plugin (desconectado por plugin.Unloading abaixo, junto dos
-- outros).
local runModeMonitorEnabled = true

local function checkRunModeTransition()
	local nowInRunOrPlayMode = isInRunOrPlayMode()

	if nowInRunOrPlayMode and not wasInRunOrPlayMode then
		-- Transição edição -> Run/Play: suspende o sync se estava rodando.
		wasInRunOrPlayMode = true
		if enabled then
			runGuardStoppedSync = true
			log("F8/F5 (Run/Play) detectado; sync suspenso durante o teste.")
			stop(true)
		end
	elseif not nowInRunOrPlayMode and wasInRunOrPlayMode then
		-- Transição Run/Play -> edição (ressalva: também dispara em falso se o
		-- teste só foi PAUSADO, ver comentário de wasInRunOrPlayMode acima —
		-- limitação conhecida, não resolvida).
		wasInRunOrPlayMode = false
		if runGuardStoppedSync then
			runGuardStoppedSync = false
			log("retorno ao modo de edição detectado; retomando sync.")
			task.spawn(start, plugin)
		end
	end
end

task.spawn(function()
	while runModeMonitorEnabled do
		task.wait(Config.POLL_INTERVAL_SECONDS)
		if not runModeMonitorEnabled then
			break
		end
		checkRunModeTransition()
	end
end)

-- ------------------------------------------------------------------- UI (M4.5)
--
-- Substitui a antiga toolbar de 3 botões (Conectar/Parar/Alternar porta) por
-- 1 botão só que alterna o painel (PluginUI.luau cria toolbar+
-- DockWidgetPluginGui+monta a árvore Vide) — este arquivo continua sendo o
-- ÚNICO dono de start()/stop()/troca de porta; PluginUI só invoca os
-- callbacks abaixo e reflete estado via setConnectionStatus/setPort (chamados
-- dentro de runConnection/start/stop, ver acima).
--
-- onPortChange reusa EXATAMENTE a mesma sequência que o antigo botão
-- "Alternar porta" fazia (SetSetting + stop() + start(plugin)) — só
-- generalizada para um valor arbitrário digitado no painel em vez de
-- alternar entre 2 constantes fixas (34980/34981, que eram só uma
-- ferramenta de teste para 2 Studios na mesma máquina — o campo de porta do
-- painel substitui essa necessidade sem precisar de nenhum botão dedicado).
PluginUI.init(plugin, {
	onConnect = function()
		-- Guard de Run/Play no botão CONNECT (2026-07-20, BUG 1 — antes desta
		-- correção este era o ÚNICO ponto de entrada de start() sem NENHUM
		-- guard, documentado como "fora de escopo" na tarefa de 2026-07-16 que
		-- criou o guard de transição). Agora que autostart é opt-in (default
		-- OFF, ver Config.resolveAutoStartEnabled), CONNECT manual é o fluxo
		-- normal — clicar nele já em Run/Play (ex.: plugin recarregado durante
		-- um teste em andamento) faria start() rodar livre, porque o guard de
		-- TRANSIÇÃO (checkRunModeTransition) nunca veria a transição
		-- edição->Run/Play (o sync já nasceria dentro do Run/Play) e por isso
		-- nunca chamaria stop() de volta. Checagem feita AQUI, além da mesma
		-- checagem dentro de start() (defesa em profundidade, ver comentário
		-- lá): só este call-site sabe que é uma ação EXPLÍCITA do usuário —
		-- merece feedback visível (toast via Logger.notify), mesmo padrão dos
		-- outros pontos M4.5 que notificam recusas (ex.: lease negada).
		if isInRunOrPlayMode() then
			notify("CONNECT ignorado: Studio em modo Run/Play (F8/F5); conecte após voltar para edição normal.")
			return
		end
		task.spawn(start, plugin)
	end,
	onDisconnect = stop,
	onPortChange = function(newPort)
		-- Guarda de reentrância/dedupe (ver comentário no topo do arquivo):
		-- ignora uma 2ª chamada de FocusLost residual que dispararia um
		-- stop()+start() concorrente, matando a conexão recém-criada.
		if portChangeInFlight then
			log(("troca de porta para %d ignorada: outra troca ainda em andamento"):format(newPort))
			return
		end
		if lastPortChangeValue == newPort and (os.clock() - lastPortChangeAt) < PORT_CHANGE_DEDUPE_SECONDS then
			log(
				("troca de porta para %d ignorada: repetição do mesmo valor em <%ds"):format(
					newPort,
					PORT_CHANGE_DEDUPE_SECONDS
				)
			)
			return
		end
		portChangeInFlight = true
		lastPortChangeValue = newPort
		lastPortChangeAt = os.clock()
		task.spawn(function()
			local currentPort = Config.resolvePort(plugin)
			local okSet, errSet = pcall(function()
				plugin:SetSetting(Config.PORT_SETTING_KEY, newPort)
			end)
			if not okSet then
				log("falha ao mudar porta pelo painel:", tostring(errSet))
				-- Libera a guarda ANTES de sair: sem isso, uma falha de SetSetting
				-- deixaria portChangeInFlight travado em true para sempre,
				-- bloqueando toda troca de porta futura (bug pior que o que estamos
				-- corrigindo).
				portChangeInFlight = false
				return
			end
			log(("porta alterada pelo painel %d -> %d; reconectando..."):format(currentPort, newPort))
			-- stop()+start() em pcall só para GARANTIR que a guarda sempre baixa
			-- (um erro aqui deixaria portChangeInFlight travado, ver acima) — o
			-- erro, se houver, continua sendo logado, não engolido silenciosamente.
			local okCycle, errCycle = pcall(function()
				stop()
				start(plugin)
			end)
			-- Reinicia a janela de dedupe a partir do FIM do stop()+start() (que
			-- pode ter yieldado): é a partir daqui que o PortRow fica vulnerável de
			-- novo, até runConnection atualizar state.port(). Só então libera a
			-- flag de reentrância.
			lastPortChangeAt = os.clock()
			portChangeInFlight = false
			if not okCycle then
				log("erro ao reconectar após troca de porta:", tostring(errCycle))
			end
		end)
	end,
	-- 2026-07-20 (pedido do usuário): toggle real de "Reconectar
	-- automaticamente ao abrir a place" na tela de configurações do painel
	-- (ui/StatusPanel.luau) — REUSA Config.AUTOSTART_SETTING_KEY, que já
	-- existia desde 2026-07-16 (ver comentário de start()/auto-start no fim
	-- do arquivo) só sem UI dedicada; nenhuma setting nova. Diferente de
	-- onNotificationsToggle (preferência passiva, só persiste), este AGE na
	-- hora ao ligar — decisão de UX explícita do usuário: ligar o toggle
	-- enquanto desconectado deveria conectar já, não só valer "da próxima
	-- vez".
	onAutoReconnectToggle = function(newValue)
		local okSet, errSet = pcall(function()
			plugin:SetSetting(Config.AUTOSTART_SETTING_KEY, newValue)
		end)
		if not okSet then
			log("falha ao salvar preferência de auto-reconectar:", tostring(errSet))
			return
		end
		if not newValue then
			-- Desligar só afeta o PRÓXIMO carregamento do plugin/place
			-- (bloco de auto-start no fim deste arquivo) — NÃO derruba uma
			-- sessão já conectada agora: o usuário pediu pra parar de
			-- conectar sozinho no futuro, não pra desconectar o presente
			-- (isso é o que DISCONNECT/onDisconnect já faz, se for o que
			-- ele quiser).
			log("auto-reconectar desligado; só afeta o próximo carregamento do plugin/place.")
			return
		end
		-- Ligar: tenta conectar JÁ (mesma chamada que onConnect faz),
		-- respeitando o MESMO guard de Run/Play — nunca conecta livre por
		-- trás de um F8/F5 em andamento, mesmo padrão de feedback (toast)
		-- que onConnect já dá quando recusado por esse motivo. Se já
		-- conectado (enabled == true), start() se recusa sozinho
		-- (idempotência, ver guard no topo de start()) — nada extra a
		-- checar aqui além do SetSetting acima.
		if isInRunOrPlayMode() then
			notify(
				"Auto-reconectar ativado, mas o Studio está em modo Run/Play (F8/F5);"
					.. " conecte manualmente após voltar para edição normal."
			)
			return
		end
		task.spawn(start, plugin)
	end,
	-- ReSync (2026-08-02, docs/DECISIONS.md "5ª rodada"): clique no botão
	-- RESYNC do painel. Este arquivo só avisa visualmente ("syncing") e envia
	-- a mensagem espontânea (sem requestId, mesmo padrão de
	-- presenceUpdate/watchedRoots) pelo MESMO `sendMessage` que qualquer
	-- outra mensagem espontânea do plugin já usa — quem decide/confirma/
	-- executa o reset (apagar o Source já sincronizado no disco do dev e
	-- repuxar tudo de novo do Studio) é 100% a extensão
	-- (SyncTeamService.ts/extension-dev): é uma ação destrutiva do lado de
	-- quem vai perder o arquivo local, não deste lado.
	onResyncRequest = function()
		if client == nil then
			-- Sem esta guarda o painel ficaria travado em "syncing" para
			-- sempre: sem conexão a mensagem nunca chega à extensão, então
			-- nenhum resyncResult voltaria para tirar o estado visual de
			-- "syncing" (ver handleResyncResult acima).
			notify("RESYNC ignorado: sem conexão com a extensão VS Code no momento.")
			return
		end
		PluginUI.setResyncState("syncing")
		sendMessage({ kind = "resyncRequest" })
	end,
})
PluginUI.setPort(Config.resolvePort(plugin))

plugin.Unloading:Connect(stop)
-- PluginUI.stop() é um ciclo de vida SEPARADO do stop() de conexão acima
-- (ver PluginUI.luau): só desliga o refresh em segundo plano da tabela de
-- sessões; o painel em si (toolbar+widget) não precisa ser destruído, o
-- Studio cuida disso ao descarregar o plugin.
plugin.Unloading:Connect(PluginUI.stop)
-- Encerra o loop do guard de Run/Play (acima) junto com o resto do ciclo de
-- vida do plugin.
plugin.Unloading:Connect(function()
	runModeMonitorEnabled = false
end)

-- Mitigação de undo (2026-07-26, docs/DECISIONS.md — ver TeamCreateUndoGuard.luau
-- para o detalhe completo): escuta ChangeHistoryService.OnUndo/OnRedo pela
-- vida INTEIRA do plugin, independente de estar conectado (enabled) no
-- momento — Ctrl+Z pode acontecer a qualquer momento com a place aberta, e
-- cada checkIntegrity() já degrada sozinho pra no-op se o módulo
-- correspondente não estiver rodando. Por isso fica fora de start()/stop(),
-- no mesmo espírito do guard de Run/Play acima (loop independente do ciclo
-- de conexão).
TeamCreateUndoGuard.start()
plugin.Unloading:Connect(TeamCreateUndoGuard.stop)

log("plugin carregado. Toolbar 'SyncTeam' alterna o painel de status; use o botão CONNECT para conectar.")

-- ------------------------------------------------------------ auto-start
--
-- 2026-07-16 (pedido do usuário): autostart passou a ser OPT-IN, default
-- DESLIGADO (Config.AUTOSTART_SETTING_KEY/resolveAutoStartEnabled, default
-- false — oposto do padrão de NOTIFICATIONS_SETTING_KEY). Antes desta
-- mudança, `start(plugin)` era chamado incondicionalmente aqui (só sujeito ao
-- guard de Run/Play abaixo) — o dev não tinha como optar por conectar
-- manualmente. Agora o dev decide quando conectar clicando em CONNECT no
-- painel (onConnect acima já chama `task.spawn(start, plugin)`, mesmo
-- caminho); só quem ligar Config.AUTOSTART_SETTING_KEY volta a ter o
-- comportamento antigo — 2026-07-20: agora um toggle real ("Reconectar
-- automaticamente ao abrir a place", tela de configurações do painel, ver
-- onAutoReconnectToggle em PluginUI.init acima e ui/StatusPanel.luau); antes
-- disso só dava pra ligar via plugin:SetSetting(...) no Command Bar.
--
-- Mesmo padrão de carregamento validado no M0.5
-- (.claude/agent-memory/luau-dev.md): o Studio auto-executa o Script raiz do
-- plugin ao (re)carregar — seja como arquivo .lua solto na pasta de Plugins,
-- seja como .rbxm buildado via `rojo build`. start() tem guard de
-- idempotência (`if enabled then return end`) para tolerar reload/
-- reinstalação duplicada, o que segue relevante mesmo com autostart opt-in
-- (ex.: reload com a setting ligada).
--
-- Guarda de Run/Play (ver isInRunOrPlayMode() acima). Atualizado 2026-07-20
-- (BUG 1, docs/DECISIONS.md): antes desta correção só o autostart e o guard
-- de TRANSIÇÃO (checkRunModeTransition) tinham essa checagem — o botão
-- CONNECT (onConnect, PluginUI.init acima) ficava de fora, documentado como
-- "fora de escopo" na tarefa original de 2026-07-16. Isso deixou de ser
-- aceitável quando autostart virou opt-in/default OFF (mesma sessão de
-- 2026-07-16): CONNECT manual passou a ser o fluxo normal, não mais um atalho
-- raro, então precisava do mesmo guard. Agora os TRÊS pontos de entrada
-- (autostart aqui, onConnect, e start() como defesa em profundidade) usam a
-- MESMA função isInRunOrPlayMode().
if not Config.resolveAutoStartEnabled(plugin) then
	log("autostart desligado; use o painel (toggle na toolbar) pra conectar manualmente.")
elseif isInRunOrPlayMode() then
	log("Studio em modo Run/Play (RunService:IsRunning()==true) no carregamento; sync não iniciado automaticamente.")
else
	task.spawn(start, plugin)
end
]]></string>
    </Properties>
    <Item class="ModuleScript" referent="1">
      <Properties>
        <string name="Name">Config</string>
        <string name="Source"><![CDATA[-- SyncTeam — Config do plugin de produção (M1)
--
-- Constantes centralizadas: porta WS, timings de polling/reconexão, versão
-- de protocolo, versão do plugin, e a lista fixa de containers observados.
--
-- Fora de escopo aqui: constantes de coordenação do TestService.SyncTeam
-- (pulso 2s / stale 8s / cleanup 20s) — isso é M3, ver .claude/rules/luau.md
-- e docs/MILESTONES.md.

local Config = {}

-- Porta padrão do servidor WebSocket local hospedado pela extensão VS Code.
-- Escolhida nova (não reutiliza 34901/34902, usadas pelos spikes M0/M0.5) só
-- para não colidir se algum spike antigo ainda estiver instalado/rodando ao
-- mesmo tempo que o plugin de produto.
Config.DEFAULT_PORT = 1400

-- Chave de plugin:GetSetting/SetSetting para permitir porta configurável sem
-- reescrever código (regra de "porta configurável com default fixo" de
-- .claude/rules/typescript.md, que vale também do lado do plugin). Ainda sem
-- UI para setar isso — usuário avançado pode chamar
-- plugin:SetSetting("SyncTeam_WsPort", <porta>) pelo Command Bar do Studio.
Config.PORT_SETTING_KEY = "SyncTeam_WsPort"

-- Chave de plugin:GetSetting/SetSetting para a única opção real das
-- configurações do painel de status (M4.5, plugin/src/ui/): "Mostrar
-- notificações" — controla só se o TOAST aparece (Toast.luau); o log no
-- Output nunca é condicionado a isso (Logger.notify sempre loga, ver
-- Logger.luau). Default true (ligado) quando a setting nunca foi tocada.
Config.NOTIFICATIONS_SETTING_KEY = "SyncTeam_ShowNotifications"

-- Chave de plugin:GetSetting/SetSetting para autostart (2026-07-16, pedido do
-- usuário após teste real). Ao contrário de NOTIFICATIONS_SETTING_KEY
-- (default true/ligado), o default AQUI é false/DESLIGADO — decisão
-- deliberada: o comportamento anterior (auto-start incondicional ao carregar
-- o plugin, ver init.server.luau) conectava sem o dev pedir; a partir de
-- agora o dev decide quando conectar clicando em CONNECT no painel, e só quem
-- ligar esta setting (2026-07-20: toggle real "Reconectar automaticamente ao
-- abrir a place" na tela de configurações do painel, ver
-- plugin/src/ui/StatusPanel.luau — antes disso só via
-- plugin:SetSetting(...) no Command Bar) volta a ter auto-start.
Config.AUTOSTART_SETTING_KEY = "SyncTeam_AutoStart"

-- Chave de plugin:GetSetting/SetSetting para "Nome de exibição customizado"
-- (M4.5+, painel de configurações — antes um placeholder "em breve",
-- implementado de verdade em 2026-08-02). Quando não-vazio, substitui o nome
-- resolvido via Players:GetNameFromUserIdAsync como Username publicado em
-- Sessions/<clientId> (ver TeamCreateElection.luau) — é o que
-- TeamCreateLease.describeClient usa nas mensagens de lease negada e o que a
-- tabela de sessões do painel mostra na coluna USER. Default "" (string
-- vazia) = usar o nome do Roblox normalmente. GLOBAL à instalação do Studio,
-- mesmo grupo de NOTIFICATIONS_SETTING_KEY/AUTOSTART_SETTING_KEY acima — NÃO
-- confundir com o mecanismo por-place (PLACE_SETTINGS_KEY, mais abaixo),
-- outra fatia, não relacionada.
Config.CUSTOM_DISPLAY_NAME_SETTING_KEY = "SyncTeam_CustomDisplayName"

-- Lista de portas candidatas usada SÓ quando não há nenhuma porta
-- explicitamente persistida em plugin:GetSetting(PORT_SETTING_KEY) (nem por
-- SetSetting manual, nem por uma descoberta automática de sessão anterior —
-- ver Config.resolveCandidatePorts). ESTOPGAP para o cenário de teste de 2
-- Studios na mesma máquina compartilhando a MESMA pasta de Plugins (2 contas
-- via "Add Account" carregam o mesmo arquivo de plugin, então cada Studio
-- precisa descobrir sozinho qual harness/porta ainda está livre — ver
-- docs/PROJECT_STATUS.md). Em uso normal de produção (1 dev, 1 extensão) só
-- a primeira porta desta lista chega a importar; a segunda nunca é
-- alcançada porque a primeira tentativa já conecta.
Config.CANDIDATE_PORTS = { 1400, 1401 }

-- Janela curta (segundos) usada só para o heurístico de "rejeição
-- provável" da auto-descoberta de porta (init.server.luau, runConnection):
-- se a conexão cair SEM nunca ter recebido nenhuma mensagem do harness
-- dentro desta janela, é tratada como "porta provavelmente ocupada por
-- outro Studio" e o plugin avança para a próxima candidata em vez de
-- esperar RECONNECT_SECONDS. Deliberadamente NÃO depende de interpretar o
-- `code`/`errorMessage` do evento Error do WebStreamClient (não há
-- confirmação salva em .claude/research/ sobre o que CreateWebStreamClient
-- expõe para o close code 1013 do harness — ver SyncServer.ts) — só de
-- observação local (tempo decorrido + mensagens recebidas), que não exige
-- pesquisa de API nova.
Config.PROBABLE_REJECTION_WINDOW_SECONDS = 3

-- Atraso entre tentar uma candidata rejeitada e a próxima da lista —
-- deliberadamente bem menor que RECONNECT_SECONDS (não faz sentido esperar
-- muito antes de tentar a alternativa se a porta atual está ocupada por
-- OUTRO Studio), mas não é zero para não virar busy-loop se todas as
-- portas candidatas estiverem ocupadas ao mesmo tempo.
Config.CANDIDATE_RETRY_DELAY_SECONDS = 0.75

-- Caminho garantido de detecção de mudança de Source (ver DECISIONS.md
-- 2026-07-03): sinal é só fast-path, nunca a única via.
Config.POLL_INTERVAL_SECONDS = 0.5

Config.RECONNECT_SECONDS = 3

-- Detecção de conexão morta por AUSÊNCIA de mensagens (init.server.luau,
-- runConnection). Garantia independente dos eventos Closed/Error do
-- WebStreamClient, que podem NÃO disparar quando o servidor WS da extensão
-- morre sem enviar frame de close (ex.: "Reload Window" do VS Code mata o
-- processo da extensão). A extensão manda `{kind:"ping"}` a cada 5s enquanto
-- conectada (vscode-extension HeartbeatMonitor.ts) e ela mesma derruba a
-- conexão se ficar 15s sem receber NENHUMA mensagem do plugin. Do lado do
-- plugin: se passar este intervalo sem receber NENHUMA mensagem (nem o ping
-- periódico, nem qualquer outra), a conexão é tratada como morta e forçamos
-- reconexão. Escolhido 20s (4 intervalos de ping) em vez de 15s: dá 1 ping de
-- folga sobre o timeout da extensão, evitando um teardown falso por um único
-- ping atrasado/perdido, ao custo de no máximo ~5s a mais exibindo "conectado"
-- após uma queda real — aceitável (o bug original era ficar "conectado" por
-- MUITO mais que isso). Só o plugin RESPONDE ao ping (pong); nunca origina
-- ping próprio — a ausência do ping da extensão é o único sinal usado aqui.
Config.DEAD_CONNECTION_TIMEOUT_SECONDS = 20

-- Handshake versionado (M1: começou em 1). Bump para 2 no M2 (breaking change
-- deliberado, docs/MILESTONES.md): readSource/writeSource passam a endereçar
-- por UUID (ScriptRegistry) em vez de path; writeSource sem `uuid` continua
-- significando criação (agora aloca um uuid novo); nova mensagem espontânea
-- scriptMoved. Extensão decide o que fazer em mismatch de versão.
Config.PROTOCOL_VERSION = 2
Config.PLUGIN_VERSION = "0.2.0"

-- Nomes de pasta que marcam árvore de dependências vendorizadas por Wally
-- (https://wally.sh) — convenção padrão: `Packages/` é a mais comum, mas
-- projetos com dependências separadas por lado (server/client/dev) também
-- usam `ServerPackages`/`DevPackages`. Risco de arquitetura (2026-07-16,
-- aprovado pelo usuário, ver docs/DECISIONS.md): dois devs podem ter
-- `wally.lock`/pastas `Packages/` locais divergentes (versão desatualizada
-- de um lado); se o SyncTeam tratasse esses ModuleScripts como qualquer
-- outro, o Source do pacote vendorizado desatualizado de um dev poderia ser
-- empurrado ao Team Create compartilhado, alterando silenciosamente a
-- dependência de TODOS os devs. Usado por Config.isInsideExcludedPackageFolder
-- para excluir esses scripts do LIVE EDIT SYNC (watch de mudança de Source +
-- arbitragem de lease) nos dois sentidos — mas NÃO do discovery/identidade
-- (ScriptRegistry/listScripts continuam normais, para o "Refresh Sync" da
-- extensão poder detectar "esse pacote já existe, não duplicar"; criação de
-- instância nova continua funcionando igual, só depois de criada é que ela
-- entra em "modo vendorizado").
Config.SYNC_EXCLUDED_FOLDER_NAMES = {
	Packages = true,
	ServerPackages = true,
	DevPackages = true,
}

-- true se `instance` estiver, em qualquer profundidade, dentro de uma pasta
-- cujo Name é exatamente um dos SYNC_EXCLUDED_FOLDER_NAMES (sobe
-- instance.Parent até a raiz observada/game). Usado para pular a instância
-- APENAS do polling de mudança de Source e da arbitragem de lease — nunca do
-- registro de identidade/listScripts/criação (ver comentário acima e
-- docs/DECISIONS.md 2026-07-16).
function Config.isInsideExcludedPackageFolder(instance)
	local current = instance and instance.Parent
	while current ~= nil do
		if Config.SYNC_EXCLUDED_FOLDER_NAMES[current.Name] then
			return true
		end
		current = current.Parent
	end
	return false
end

-- Lista dinâmica de watched roots recebida via mensagem espontânea
-- `watchedRoots` (extensão -> plugin, 2026-07-29, docs/DECISIONS.md — ver
-- também vscode-extension/src/protocol.ts WatchedRootsMessage e
-- SourceWatcher.applyWatchedRoots). `nil` até a 1ª mensagem chegar nesta
-- sessão do plugin. Module-level: sobrevive a SourceWatcher.stop()/start()
-- de propósito (mesmo padrão de estado persistente já usado por
-- ScriptRegistry, ver .claude/agent-memory/luau-dev.md) — uma troca de porta
-- ou reconexão não deveria esquecer a última lista dinâmica conhecida; a
-- próxima conexão aceita manda watchedRoots de novo e substitui isto,
-- normalmente.
local dynamicRoots = nil

-- Recebe `roots` (lista de nomes de Service, ex.
-- {"ReplicatedFirst", "ServerScriptService"}) da mensagem watchedRoots e
-- resolve cada nome via game:GetService, cada chamada em pcall (regra
-- .claude/rules/luau.md — nome de Service desconhecido/inválido não pode
-- derrubar o plugin). Substitui INTEIRAMENTE a lista dinâmica anterior (não
-- acumula entre chamadas — cada watchedRoots é o snapshot completo e atual
-- dos mount points do project.json no momento em que a extensão a montou).
-- Devolve a lista de nomes INVÁLIDOS/desconhecidos (pulados, nunca incluídos
-- em getWatchedRoots) para o chamador (SourceWatcher.applyWatchedRoots) logar
-- — esta função em si não loga nada (mantém Config livre de Logger).
function Config.setDynamicWatchedRoots(names)
	local resolved = {}
	local invalidNames = {}
	if type(names) == "table" then
		local seenNames = {}
		for _, name in names do
			if type(name) == "string" and not seenNames[name] then
				seenNames[name] = true
				local ok, service = pcall(function()
					return game:GetService(name)
				end)
				if ok and service ~= nil then
					table.insert(resolved, service)
				else
					table.insert(invalidNames, name)
				end
			end
		end
	end
	dynamicRoots = resolved
	return invalidNames
end

-- Containers observados recursivamente por Script/LocalScript/ModuleScript.
-- Decisão M1 original (docs/MILESTONES.md): plugin "burro"/genérico, lista
-- fixa; a extensão é quem mapeia contra o default.project.json do projeto do
-- usuário e ignora caminhos fora de qualquer ponto de montagem.
--
-- 2026-07-29 (docs/DECISIONS.md, mensagem watchedRoots): bug real — um mount
-- point novo (ex. ReplicatedFirst/First) apontando para um Service fora
-- desta lista fixa simplesmente não sincronizava. Esta função agora devolve a
-- UNIÃO da lista fixa com Config.setDynamicWatchedRoots(...) (quando já
-- recebida nesta sessão) — nunca REMOVE um container fixo, só adiciona os que
-- a extensão reportou e a lista fixa não cobria. A lista fixa em si vira o
-- FALLBACK/default: se watchedRoots nunca chegar (extensão mais velha, erro),
-- dynamicRoots continua nil e esta função devolve só a fixa, como sempre
-- devolveu. Decisão de UNIÃO (não substituição total), justificada em
-- SourceWatcher.applyWatchedRoots: nunca arriscar "esquecer" um container já
-- observado só porque a mensagem mais recente não o lista.
function Config.getWatchedRoots()
	local StarterPlayer = game:GetService("StarterPlayer")
	local fixed = {
		game:GetService("ServerScriptService"),
		StarterPlayer.StarterPlayerScripts,
		game:GetService("ReplicatedStorage"),
		game:GetService("ReplicatedFirst"),
		game:GetService("ServerStorage"),
		game:GetService("StarterGui"),
		game:GetService("Workspace"),
	}
	if dynamicRoots == nil then
		return fixed
	end
	local seen = {}
	local merged = {}
	for _, root in fixed do
		if not seen[root] then
			seen[root] = true
			table.insert(merged, root)
		end
	end
	for _, root in dynamicRoots do
		if not seen[root] then
			seen[root] = true
			table.insert(merged, root)
		end
	end
	return merged
end

-- Porta efetiva: setting do plugin se presente e válida, senão o default.
function Config.resolvePort(pluginObject)
	local ok, value = pcall(function()
		return pluginObject:GetSetting(Config.PORT_SETTING_KEY)
	end)
	if ok and type(value) == "number" and value > 0 then
		return math.floor(value)
	end
	return Config.DEFAULT_PORT
end

-- Lista de portas a tentar + se ela veio de uma porta JÁ EXPLICITAMENTE
-- setada (manual via botão "Alternar porta", ou persistida por uma
-- descoberta automática de sessão anterior via runConnection). Quando
-- explícita, a lista tem 1 item só e é a ÚNICA tentada (nunca cicla) —
-- CANDIDATE_PORTS só entra em jogo quando NÃO há nenhuma setting ainda.
function Config.resolveCandidatePorts(pluginObject)
	local ok, value = pcall(function()
		return pluginObject:GetSetting(Config.PORT_SETTING_KEY)
	end)
	if ok and type(value) == "number" and value > 0 then
		return { math.floor(value) }, true
	end
	return Config.CANDIDATE_PORTS, false
end

-- Preferência efetiva de "Mostrar notificações" (M4.5): setting do plugin se
-- presente e booleana, senão default true. Só GetSetting pode devolver nil
-- (nunca setado) ou um valor de tipo errado (schema corrompido/versão
-- antiga) — os dois caem no default, nunca erram.
function Config.resolveNotificationsEnabled(pluginObject)
	local ok, value = pcall(function()
		return pluginObject:GetSetting(Config.NOTIFICATIONS_SETTING_KEY)
	end)
	if ok and type(value) == "boolean" then
		return value
	end
	return true
end

-- Preferência efetiva de autostart (2026-07-16): setting do plugin se
-- presente e booleana, senão default FALSE (oposto de
-- resolveNotificationsEnabled — ver comentário de AUTOSTART_SETTING_KEY
-- acima). GetSetting pode devolver nil (nunca setado, o caso normal para
-- quem nunca abriu o toggle de "Reconectar automaticamente ao abrir a
-- place" nas configurações do painel — que também nasce desligado) ou um
-- valor de tipo errado — ambos caem no default desligado, nunca erram para
-- o lado de conectar sozinho sem o dev pedir.
function Config.resolveAutoStartEnabled(pluginObject)
	local ok, value = pcall(function()
		return pluginObject:GetSetting(Config.AUTOSTART_SETTING_KEY)
	end)
	if ok and type(value) == "boolean" then
		return value
	end
	return false
end

-- Preferência efetiva de "Nome de exibição customizado" (M4.5+): setting do
-- plugin se presente e do tipo string, senão default "" (string vazia =
-- "usar o nome do Roblox resolvido normalmente"). Diferente de
-- resolveNotificationsEnabled/resolveAutoStartEnabled (booleanas): aqui
-- `type(value) == "boolean"` não se aplica — é `type(value) == "string"` —
-- mesmo esqueleto de validação (GetSetting em pcall; nunca setado ou tipo
-- errado caem no default), só o tipo esperado muda.
function Config.resolveCustomDisplayName(pluginObject)
	local ok, value = pcall(function()
		return pluginObject:GetSetting(Config.CUSTOM_DISPLAY_NAME_SETTING_KEY)
	end)
	if ok and type(value) == "string" then
		return value
	end
	return ""
end

-- ---------------------------------------------------- settings por-place (M4.5+)
--
-- docs/DECISIONS.md (2026-08-02, 3ª rodada, seção 4): usuário pediu novas
-- settings do painel (Studio) configuráveis "por espaço de trabalho" (por
-- lugar/place do Roblox) — mas `plugin:GetSetting`/`SetSetting` é GLOBAL à
-- INSTALAÇÃO do Studio, não por place (não existe API de setting por-place).
-- Contorno decidido: um ÚNICO slot GetSetting (PLACE_SETTINGS_KEY) guardando
-- um mapa `{[tostring(placeId)] = {chave -> valor}}`; toda leitura/escrita
-- passa por game.PlaceId do momento da chamada, nunca por um place
-- explicitamente passado — sempre "o place atual". As 3 settings GLOBAIS já
-- existentes (PORT_SETTING_KEY/NOTIFICATIONS_SETTING_KEY/
-- AUTOSTART_SETTING_KEY) NÃO são afetadas por isto — continuam globais, como
-- sempre foram; só as settings NOVAS desta rodada nascem por-place.
--
-- Escopo desta fatia (luau-dev): só a API de leitura/escrita abaixo. Consumir
-- de fato estes valores nos loops de runtime (SourceWatcher.pollLoop,
-- TeamCreatePresence — hoje ambos usam Config.POLL_INTERVAL_SECONDS fixo, ver
-- comentário de PLACE_SETTINGS_DEFAULTS abaixo) e construir a UI de settings
-- (StatusPanel.luau) ficam para tarefas separadas (ui-dev) — não tocados
-- aqui.
Config.PLACE_SETTINGS_KEY = "SyncTeam_PlaceSettings"

-- Chaves reconhecidas + default sensato de cada uma, usado sempre que a
-- chave nunca foi setada para o place atual (ou o valor salvo é de tipo
-- errado — schema corrompido/versão antiga da setting). Mínimo e extensível
-- por pedido explícito da tarefa: hoje só as 2 settings pedidas pelo usuário
-- além do toggle de autostart (que já existia antes desta tarefa, GLOBAL, não
-- por-place, ver AUTOSTART_SETTING_KEY acima). "Preferência sobre posse de
-- porta" (3ª setting mencionada no pedido original) FICA DE FORA desta lista
-- de propósito: docs/DECISIONS.md 2026-08-02 seção 4 decide explicitamente
-- que aquele diálogo roda do lado da EXTENSÃO (é ela quem faz o bind da
-- porta), não é uma setting persistente do painel do Studio — a extensão
-- pode guardar sua própria preferência local (ExtensionContext.globalState),
-- fora do escopo deste arquivo.
--
-- `sourcePollIntervalSeconds` ("intervalo de atualização de arquivo") e
-- `presencePollIntervalSeconds` ("intervalo de cursor") espelham hoje o MESMO
-- default de Config.POLL_INTERVAL_SECONDS porque, na implementação atual, as
-- duas cadências (SourceWatcher.pollLoop e
-- TeamCreatePresence.checkPresenceDrift) COMPARTILHAM essa mesma constante
-- fixa — esta tarefa não separa os dois loops nem faz nenhum dos dois ler
-- estas settings novas (ver nota de escopo acima); são expostas já agora
-- para a UI (ui-dev) e o wiring de runtime (futuro) terem uma chave estável
-- para desenhar/consumir.
--
-- `leaseStaleAfterSeconds` (2026-08-02, docs/DECISIONS.md "8ª rodada" —
-- handoff quase-instantâneo de lease): substitui, só para
-- TeamCreateLease.luau (arbitragem de lease por arquivo), o antigo valor
-- fixo `TeamCreateElection.STALE_AFTER_SECONDS = 8`. Default escolhido = 2
-- (não 3): o gatilho de detecção de edição do lado extensão passa a incluir
-- `onDidChangeTextDocument` throttled ~150ms (além do watcher de disco),
-- então o Pulse do intent de quem está digitando ativamente é renovado bem
-- mais rápido que antes — 2s já dá ~4 ciclos de folga sobre a nova cadência
-- de checagem de 0.5s (ver leaderTick em TeamCreateLease.luau) antes de
-- considerar o intent morto, sem exigir tanta margem quanto 3s pediria.
-- DELIBERADAMENTE não aplicado a TeamCreateElection.STALE_AFTER_SECONDS (elect/
-- applyJoinSequenceAssignments/tick) — aquele valor continua fixo em 8s: é
-- uma constante de SEGURANÇA de coordenação (detecção de sessão/líder morto,
-- valida o split-brain já corrigido em 2026-07-07), não uma constante de UX
-- de latência de arquivo; a decisão registrada fala especificamente de LEASE,
-- não de eleição — ver TeamCreateLease.luau para o ponto de leitura real.
Config.PLACE_SETTINGS_DEFAULTS = {
	sourcePollIntervalSeconds = Config.POLL_INTERVAL_SECONDS,
	presencePollIntervalSeconds = Config.POLL_INTERVAL_SECONDS,
	leaseStaleAfterSeconds = 2,
}

-- Lê a sub-tabela de settings do PLACE ATUAL (tostring(game.PlaceId)) dentro
-- do slot único GetSetting(PLACE_SETTINGS_KEY), já mesclada com
-- PLACE_SETTINGS_DEFAULTS para qualquer chave ausente. Toda a leitura em
-- pcall (regra .claude/rules/luau.md) — setting nunca setada, tipo errado, ou
-- qualquer falha de GetSetting degrada silenciosamente para os defaults,
-- nunca propaga erro nem derruba o plugin.
function Config.getPlaceSettings(pluginObject)
	local result = table.clone(Config.PLACE_SETTINGS_DEFAULTS)
	local ok, allPlaces = pcall(function()
		return pluginObject:GetSetting(Config.PLACE_SETTINGS_KEY)
	end)
	if not ok or type(allPlaces) ~= "table" then
		return result
	end
	local placeSettings = allPlaces[tostring(game.PlaceId)]
	if type(placeSettings) ~= "table" then
		return result
	end
	for key in Config.PLACE_SETTINGS_DEFAULTS do
		local value = placeSettings[key]
		if value ~= nil then
			result[key] = value
		end
	end
	return result
end

-- Getter de UMA chave só (conveniência para chamadores que só precisam de um
-- valor, sem montar a tabela inteira) — mesma resolução de default de
-- getPlaceSettings. Chave desconhecida (fora de PLACE_SETTINGS_DEFAULTS)
-- devolve nil sempre, nunca inventa um default para chave não registrada.
function Config.getPlaceSetting(pluginObject, key)
	if Config.PLACE_SETTINGS_DEFAULTS[key] == nil then
		return nil
	end
	return Config.getPlaceSettings(pluginObject)[key]
end

-- Escreve UMA chave na sub-tabela do place ATUAL, preservando (a) as outras
-- chaves do MESMO place já salvas e (b) as sub-tabelas de QUALQUER OUTRO
-- place já salvas no mesmo slot único — read-modify-write completo do mapa
-- inteiro, porque GetSetting/SetSetting não oferece update parcial (é tudo
-- ou nada por chamada). `value == nil` REMOVE a chave da sub-tabela do place
-- (a próxima leitura volta a usar o default) em vez de gravar nil
-- explicitamente. Validação leve e genérica (não hardcoded por nome de
-- chave, para continuar extensível a settings futuras de qualquer tipo):
-- chave fora de PLACE_SETTINGS_DEFAULTS é rejeitada (não deixa o slot
-- acumular lixo de nome errado); valor não-nil precisa bater o MESMO tipo Lua
-- do default, e se o default for number, precisa ser > 0 (todas as settings
-- numéricas hoje são intervalos em segundos — 0/negativo não faz sentido).
-- Toda a operação em pcall (regra .claude/rules/luau.md); retorna ok, err.
function Config.setPlaceSetting(pluginObject, key, value)
	local default = Config.PLACE_SETTINGS_DEFAULTS[key]
	if default == nil then
		return false, ("chave de setting desconhecida: %s"):format(tostring(key))
	end
	if value ~= nil then
		local defaultType = type(default)
		if type(value) ~= defaultType then
			return false, ("valor inválido para '%s': esperado %s, recebido %s"):format(key, defaultType, type(value))
		end
		if defaultType == "number" and value <= 0 then
			return false, ("valor inválido para '%s': precisa ser um número positivo"):format(key)
		end
	end

	local okRead, allPlaces = pcall(function()
		return pluginObject:GetSetting(Config.PLACE_SETTINGS_KEY)
	end)
	if not okRead or type(allPlaces) ~= "table" then
		allPlaces = {}
	end
	local placeKey = tostring(game.PlaceId)
	local placeSettings = allPlaces[placeKey]
	if type(placeSettings) ~= "table" then
		placeSettings = {}
	end
	placeSettings[key] = value -- value == nil aqui remove a chave (tabela Lua padrão)
	allPlaces[placeKey] = placeSettings

	local okWrite, errWrite = pcall(function()
		pluginObject:SetSetting(Config.PLACE_SETTINGS_KEY, allPlaces)
	end)
	if not okWrite then
		return false, tostring(errWrite)
	end
	return true, nil
end

return Config
]]></string>
      </Properties>
    </Item>
    <Item class="ModuleScript" referent="2">
      <Properties>
        <string name="Name">Logger</string>
        <string name="Source"><![CDATA[-- SyncTeam — Logger centralizado (observabilidade de teste automatizado)
--
-- Motivação (pedido do orquestrador, 2026-07-07): antes desta tarefa, 6
-- módulos do plugin (init.server.luau, TeamCreateElection.luau,
-- TeamCreateSchema.luau, TeamCreateLease.luau, ScriptRegistry.luau,
-- SourceWatcher.luau) tinham cada um sua PRÓPRIA função local `log(...)`,
-- todas idênticas:
--   local function log(...)
--       print(("[SyncTeam %s]"):format(os.date("%H:%M:%S")), ...)
--   end
-- Isso obrigava o orquestrador/IA a depender do usuário colar manualmente o
-- painel Output do Studio para saber o que acontece durante um teste — um
-- gargalo para testes autônomos. Este módulo centraliza log() e encaminha o
-- texto renderizado pela conexão WS já existente (mesmo `sendMessage`
-- injetado por SourceWatcher.init/TeamCreateLease.init) como
-- `{kind = "log", text = "<string>"}`. O lado da extensão/harness (tarefa
-- paralela de outro agente, extension-dev) grava isso em arquivo — permite ler
-- o log via Read sem depender do usuário nem do Studio Output.
--
-- 2026-08-02 (pedido do usuário: "limpar os prints do Output, pode retirar
-- todos"): `render()` (abaixo) PAROU de chamar `print()` para QUALQUER
-- caminho (log/notify/debug) — até esta data, só Logger.debug (ver histórico
-- mais abaixo) deixava de imprimir; agora NENHUM dos três imprime no Output
-- do Studio. O encaminhamento por WS (parágrafo acima) e o toast de
-- Logger.notify (ver mais abaixo) estão INTOCADOS — a observabilidade de
-- teste automatizado e a UX de notificação continuam idênticas, só o Output
-- cru ficou quieto. Ver docs/DECISIONS.md 2026-08-02 para a decisão
-- completa, incluindo o que foi feito com os dois `print()` crus de
-- init.server.luau (sendMessage) que NÃO passam por este módulo.
--
-- Formato do PREFIXO mantido IDÊNTICO ao original de propósito
-- ("[SyncTeam HH:MM:SS]") — docs/PROJECT_STATUS.md e docs/DECISIONS.md citam
-- esse prefixo literalmente em várias entradas. Até 2026-08-02, `render()`
-- também fazia `print(prefix, ...)` com essa MESMA chamada (não reescrita/
-- reformatada) — removida nesta data (ver histórico acima), mas o formato do
-- prefixo em si não mudou, já que o texto continua sendo enviado por WS e
-- reaproveitado por `Logger.notify`/`lastMessageText`.
--
-- Reconstrução do campo `text` enviado por WS: `prefix .. " " .. tostring(a1)
-- .. " " .. tostring(a2) ...` (espaço como separador). Isso é uma
-- RECONSTRUÇÃO best-effort do conteúdo, não uma cópia byte-a-byte do que o
-- Output do Studio renderizava internamente para múltiplos argumentos de
-- `print` (separador exato do Output não documentado/verificado nesta tarefa,
-- `[Hipótese]`) — irrelevante para o objetivo (observabilidade de teste), já
-- que hoje não há mais nenhum `print(...)` para comparar.
--
-- Sem fila/buffer: mensagens de log perdidas enquanto desconectado não são um
-- problema (objetivo é só observabilidade em tempo real durante teste, não
-- log persistente garantido) — mesmo espírito de `sendMessage` em
-- init.server.luau, que também descarta silenciosamente sem conexão.
--
-- CUIDADO DE RECURSÃO (ver init.server.luau): `sendMessage` (a função que
-- efetivamente envia pela conexão WS, definida em init.server.luau) loga
-- internamente quando a chamada real de `:Send()` lança erro (WS parecia
-- vivo mas quebrou). Esse log usa `print` PURO ali, NUNCA `Logger.log`/`log`
-- — se usasse, uma falha de envio disparia: Logger.log -> sendMessage ->
-- (a MESMA falha de novo, ainda quebrada) -> log da falha via Logger.log ->
-- sendMessage de novo -> ciclo sem saída natural (este branch não tem
-- nenhum guard por `kind` para interromper, ao contrário do branch de "sem
-- conexão" logo acima dele).
-- 2026-08-02: o OUTRO print cru que existia ali (mensagem descartada porque
-- `client == nil`) foi REMOVIDO por completo, não só mantido puro — ver
-- init.server.luau e docs/DECISIONS.md para a distinção entre os dois casos
-- (aquele era ruído de alta frequência sem perda real de observabilidade,
-- já que sem conexão o encaminhamento por WS também está indisponível por
-- definição; este continua sendo o único canal restante quando o próprio
-- envio falha).
--
-- M4.5 (painel de status, plugin/src/ui/): adicionado `Logger.notify(...)`,
-- uma segunda via de injeção (`Logger.initNotify`) paralela a `Logger.init`.
-- Motivo de não reusar o mesmo callback de `sendMessage`/`Logger.init`: aquele
-- é especificamente "encaminhar como {kind='log'} pela conexão WS" (canal de
-- observabilidade de teste, ver cabeçalho acima); este é "isto é
-- toast-worthy" (canal de UX dentro do próprio Studio, sem relação com WS).
-- `Logger.notify(...)` SEMPRE faz exatamente o que `Logger.log(...)` já
-- fazia (forward por WS + tracking da linha INFO do painel, incondicional —
-- spec da tarefa original: "o log NUNCA é condicionado" à preferência de
-- notificação; até 2026-08-02 isso incluía também o print no Output, ver
-- histórico no topo do arquivo) e, ADICIONALMENTE,
-- chama o callback de notify (se injetado) com o texto renderizado — quem
-- injeta (PluginUI.notify, ver plugin/src/ui/PluginUI.luau) decide se anima
-- um toast visível, olhando a preferência "Mostrar notificações"
-- (Config.NOTIFICATIONS_SETTING_KEY) — essa decisão fica INTEIRAMENTE do lado
-- de UI, nunca aqui. Só os poucos call-sites já identificados como "erro
-- genuíno" (não evento rotineiro) foram trocados de `log(...)` para
-- `notify(...)`: ver TeamCreateSchema.luau (reconciliação de duplicata) e
-- init.server.luau (lease negada, falha de reconexão WS).

local Logger = {}

-- callback(message: table), injetado por Logger.init — mesmo padrão já usado
-- em SourceWatcher.init(sendMessage)/TeamCreateLease.init(sendMessage). nil
-- até init.server.luau chamar Logger.init dentro de start(); nesse intervalo
-- (e sempre que a conexão cair), Logger.log só monta o texto e (se
-- `trackPanel`) atualiza a linha INFO do painel — sem erro, mesmo sem ter
-- para onde encaminhar.
local sendMessage = nil

-- callback(text: string), injetado por Logger.initNotify (M4.5) — nil até
-- init.server.luau chamar Logger.initNotify(PluginUI.notify), dentro de
-- start() (mesmo lugar/ordem de Logger.init). Nesse intervalo, Logger.notify
-- se comporta exatamente como Logger.log (só não há ninguém para avisar).
local onNotify = nil

-- Item 1 da tarefa 2026-07-21 (linha INFO do painel, no gap entre o título e
-- o campo Porta — ver StatusPanel.luau/PluginUI.luau): guarda a ÚLTIMA
-- mensagem de Logger.log/Logger.notify (parâmetro `trackPanel == true`
-- dentro de render() abaixo — exclui Logger.debug de propósito, já que
-- aquele nunca deveria alimentar a linha que o usuário vê no painel) e o
-- instante (os.clock()) em que foi registrada. 2026-08-02: até esta data o
-- mesmo parâmetro também controlava o `print()` no Output ("mensagem
-- IMPRESSA"); agora nenhuma das três funções imprime, então o parâmetro só
-- decide se a mensagem alimenta esta linha do painel — renomeado de
-- `shouldPrint` para `trackPanel` para não sugerir mais nenhuma relação com
-- o Output. Exposto só por getter (Logger.getLastMessage(), abaixo) em vez
-- de mais um callback injetado — quem consome (PluginUI.luau) faz POLLING
-- periódico (task.wait(1)), mesma disciplina de ".claude/rules/luau.md"
-- ("polling periódico... como caminho garantido") em vez de depender de um
-- push que precisaria de mais uma camada de wiring em start()/
-- init.server.luau. `lastMessageText` guarda só o texto da mensagem (SEM o
-- prefixo "[SyncTeam HH:MM:SS]") — o prefixo já é um timestamp absoluto,
-- redundante com o contador de segundos decorridos que a UI deriva de
-- `lastMessageAt`.
local lastMessageText = nil
local lastMessageAt = nil

-- Injeta o callback de envio espontâneo. Chamado por init.server.luau, no
-- mesmo lugar/ordem em que já chama SourceWatcher.init(sendMessage)/
-- TeamCreateLease.init(sendMessage), dentro de start().
function Logger.init(onMessage)
	sendMessage = onMessage
end

-- Injeta o callback de notificação visual (M4.5). Chamado por
-- init.server.luau dentro de start(), ao lado de Logger.init.
function Logger.initNotify(callback)
	onNotify = callback
end

-- Núcleo compartilhado de Logger.log/Logger.debug/Logger.notify: monta o
-- prefixo, NUNCA imprime no Output do Studio (2026-08-02 — antes desta data
-- só Logger.debug pulava o print(); ver histórico no topo do arquivo),
-- encaminha por WS se houver sendMessage injetado (SEMPRE, incondicional — a
-- observabilidade de teste automatizado nunca dependeu do Output do Studio,
-- ver Tools/README.md, e continua não dependendo), e devolve o texto
-- renderizado (para Logger.notify repassar ao callback de notify sem
-- reconstruir a string).
local function render(trackPanel, ...)
	local prefix = ("[SyncTeam %s]"):format(os.date("%H:%M:%S"))

	local count = select("#", ...)
	local text = prefix
	-- `messageOnly` é a MESMA concatenação que monta `text`, só sem o
	-- prefixo — ver comentário de `lastMessageText` acima sobre o porquê de
	-- guardar as duas formas separadas.
	local messageOnly = ""
	for index = 1, count do
		local value = select(index, ...)
		text = text .. " " .. tostring(value)
		messageOnly = messageOnly .. (if index > 1 then " " else "") .. tostring(value)
	end

	if sendMessage ~= nil then
		sendMessage({ kind = "log", text = text })
	end

	-- Só Logger.log/Logger.notify (`trackPanel == true`) alimentam a linha
	-- INFO do painel — Logger.debug nunca deveria aparecer pro usuário, nem
	-- aqui.
	if trackPanel then
		lastMessageText = messageOnly
		lastMessageAt = os.clock()
	end

	return text
end

-- Getter read-only (poll-based, ver comentário de `lastMessageText` acima)
-- usado por PluginUI.luau para alimentar a linha INFO do painel de status
-- (item 1 da tarefa 2026-07-21). Devolve `nil, nil` se nenhuma chamada de
-- Logger.log/Logger.notify (`trackPanel == true`) ainda ocorreu nesta sessão
-- do plugin — StatusPanel/PluginUI tratam isso com um placeholder (ver
-- PluginUI.luau).
function Logger.getLastMessage()
	return lastMessageText, lastMessageAt
end

-- Mesma assinatura/comportamento de todo `log(...)` que existia espalhado
-- pelos 6 arquivos originalmente, MENOS o print() no Output (removido
-- 2026-08-02, ver histórico no topo do arquivo): aceita qualquer número de
-- argumentos, formata com o mesmo prefixo de sempre, alimenta a linha INFO
-- do painel (`trackPanel = true`) e, se houver um `sendMessage` injetado,
-- encaminha o texto renderizado como mensagem espontânea `{kind = "log",
-- text = ...}` — melhor esforço, nunca lança erro nem enfileira nada.
function Logger.log(...)
	render(true, ...)
end

-- 2026-07-16 (consolidação de ruído de boot, pedido do usuário após teste
-- real: rajada de ~7 linhas de subsistema a cada conexão). Mesmo forward por
-- WS de Logger.log (Tools/README.md continua enxergando tudo, observabilidade
-- de teste automatizado intacta). Uso: eventos de inicialização de
-- subsistema que são informação ROTINEIRA (reconciliação de registry, ciclos
-- internos iniciados/parados, tentativa otimista de conexão antes de
-- estabilizar) — nunca para eventos que o usuário precisa perceber (erro
-- real, mudança de liderança, lease negada — esses continuam
-- Logger.log/Logger.notify).
-- 2026-08-02: até esta data, esta era a ÚNICA das três funções que não
-- imprimia no Output do Studio (Logger.log/Logger.notify imprimiam) — a
-- distinção agora é só `trackPanel` (`false` aqui, `true` em log/notify): se
-- a mensagem alimenta ou não a linha INFO do painel (ver StatusPanel.luau/
-- PluginUI.luau). Nenhuma das três imprime mais no Output — ver histórico no
-- topo do arquivo e docs/DECISIONS.md.
function Logger.debug(...)
	render(false, ...)
end

-- Igual a Logger.log (mesmo forward por WS, SEMPRE — nunca condicionado a
-- preferência nenhuma; até 2026-08-02 também imprimia, ver histórico no topo
-- do arquivo), e ADICIONALMENTE chama o callback de notify injetado (se
-- houver) com o texto renderizado — melhor esforço, protegido por pcall (a
-- UI pode falhar por qualquer motivo de Instance/Tween; uma falha ali nunca
-- pode derrubar quem chamou Logger.notify no meio de lógica de coordenação
-- real). Usar SÓ nos poucos pontos já identificados como "merece toast" (ver
-- cabeçalho do arquivo) — todo o resto continua `Logger.log`.
function Logger.notify(...)
	local text = render(true, ...)
	if onNotify ~= nil then
		local ok, err = pcall(onNotify, text)
		if not ok then
			print(("[SyncTeam %s]"):format(os.date("%H:%M:%S")), "falha ao notificar UI:", tostring(err))
		end
	end
end

return Logger
]]></string>
      </Properties>
    </Item>
    <Item class="ModuleScript" referent="3">
      <Properties>
        <string name="Name">ScriptRegistry</string>
        <string name="Source"><![CDATA[-- SyncTeam — Registry de identidade de script (M2)
--
-- TestService.SyncTeam.Scripts.<uuid> = Folder { InstanceRef: ObjectValue,
-- CanonicalPath: StringValue (display-only) }. Porta da IDEIA validada em
-- dois Studios reais no RojoCoop (TeamCreateSchema.lua/createScript+readScript
-- e TeamCreateCoordinator:__resolveScriptRegistry) — sem eleição/leases/
-- schema versionado (SchemaVersion, sessões, leases), que ficam para o M3
-- (docs/MILESTONES.md). Aqui é só a estrutura mínima Scripts/<uuid>.
--
-- Identidade nunca é path nem attribute (decisão registrada,
-- docs/DECISIONS.md 2026-07-02): sempre UUID (HttpService:GenerateGUID(false))
-- + ObjectValue apontando para a Instance.
--
-- CORREÇÃO (docs/DECISIONS.md 2026-07-04, bug real confirmado em teste com 2
-- Studios): `InstanceRef.Value == nil` NUNCA é sinal de "instância
-- destruída" — `ObjectValue.Value` não zera automaticamente quando a
-- Instance referenciada é destruída (comportamento intencional confirmado
-- por staff da Roblox no DevForum, ver
-- .claude/research/2026-07-04-objectvalue-destroy-detection.md; o teste do
-- RojoCoop citado antes aqui só validou isso contra um MOCK, nunca contra o
-- engine real). Detecção de "destruído de verdade" usa
-- `ScriptRegistry.isInstanceDestroyed` (Parent == nil + confirmação por pcall
-- de reatribuição de Parent), nunca `InstanceRef.Value == nil` isolado.
-- `DescendantRemoving` continua não sendo usado para essa decisão porque
-- também dispara por reparent para fora da árvore observada, sem a
-- instância ter sido destruída de verdade.

local HttpService = game:GetService("HttpService")

local Logger = require(script.Parent.Logger)
local TeamCreateSchema = require(script.Parent.TeamCreateSchema)

local ScriptRegistry = {}

local scriptsFolder = nil -- TestService.SyncTeam.Scripts

-- Estado em memória (escopo de sessão do plugin, reconstruído por reconcile()
-- a cada start()). uuidByInstance é o inverso de recordByUuid[uuid].instance,
-- mantido separado só para lookup O(1) por Instance.
local uuidByInstance = {} -- Instance -> uuid
local recordByUuid = {} -- uuid -> { folder, instanceRef, canonicalPath (StringValue), instance }

local log = Logger.log

-- Container raiz (TestService.SyncTeam) extraído para TeamCreateSchema.luau
-- nesta fatia (M3.1) porque TeamCreateElection.luau também precisa dele
-- (Sessions/<clientId> + LeaderClientId/LeaderTerm/NextJoinSequence, mesmo
-- nível de Scripts) — evita duplicar "criar Folder SyncTeam sob TestService
-- se não existir" em dois módulos.
local function ensureContainers()
	if scriptsFolder ~= nil then
		return
	end
	scriptsFolder = TeamCreateSchema.ensureFolder("Scripts")
end

-- Detecta "removida da árvore observada" — nome documentado como
-- `isInstanceDestroyed` mas, na prática, é `Parent == nil`, sem
-- confirmação por pcall.
--
-- CORREÇÃO 2 (docs/DECISIONS.md 2026-07-04, teste ao vivo): a primeira
-- versão desta função exigia `pcall(function() instance.Parent = instance.Parent end)`
-- falhar (só falha quando `Destroy()` real trava a propriedade `Parent`).
-- Em teste real repetido, apagar um script pelo Explorer do Studio NUNCA
-- fez essa confirmação falhar — hipótese: o "Delete" do Explorer aparenta
-- fazer um soft-delete (só reparenta pra nil, mantendo a Instance viva e
-- editável) para suportar Ctrl+Z/undo, sem chamar Instance:Destroy() de
-- verdade nesse momento. Isso é `[Hipótese]`, não confirmado contra
-- documentação oficial (pesquisa anterior só confirmou o comportamento de
-- `ObjectValue.Value` em `Destroy()`, não o comportamento do Explorer
-- delete). Reparentar de verdade (`instance.Parent = novoPai`) é uma
-- atribuição atômica que nunca passa por um estado intermediário
-- observável com Parent nil — então, para os containers que este plugin
-- observa (Services/pastas reais manipuladas por humano via Explorer, não
-- reparenting programático via nil intermediário), `Parent == nil` sozinho
-- já é um sinal prático suficiente de "não está mais na árvore observada",
-- sem precisar da confirmação por pcall.
function ScriptRegistry.isInstanceDestroyed(instance)
	if instance == nil then
		return true
	end
	return instance.Parent == nil
end

-- Varre scriptsFolder procurando uma entrada JÁ EXISTENTE (possivelmente
-- replicada de outro Studio via Team Create) cujo InstanceRef aponte pra essa
-- MESMA Instance. Usado por resolveOrAllocate para não gerar um uuid novo
-- quando outro Studio já alocou um para essa Instance — bug real confirmado
-- em teste com 2 Studios (docs/DECISIONS.md 2026-07-04): sem essa varredura,
-- dois Studios acabavam alocando uuids diferentes para o mesmo script
-- replicado. Entradas cuja Instance já foi destruída de verdade
-- (isInstanceDestroyed) são ignoradas — não fazem sentido reaproveitar.
local function findRegistryEntryFor(instance)
	for _, folder in ipairs(scriptsFolder:GetChildren()) do
		if not folder:IsA("Folder") then
			continue
		end
		local instanceRef = folder:FindFirstChild("InstanceRef")
		local canonicalPath = folder:FindFirstChild("CanonicalPath")
		if
			instanceRef == nil
			or not instanceRef:IsA("ObjectValue")
			or canonicalPath == nil
			or not canonicalPath:IsA("StringValue")
		then
			continue
		end
		local candidate = instanceRef.Value
		if candidate == instance and not ScriptRegistry.isInstanceDestroyed(candidate) then
			return folder.Name, instanceRef, canonicalPath
		end
	end
	return nil
end

-- Cria (ou reaproveita, se já existir sob o mesmo nome) a estrutura
-- Folder/InstanceRef/CanonicalPath para um uuid já decidido, e popula os
-- mapas em memória. Uso interno de resolveOrAllocate() e reconcile().
local function createOrAdoptRecord(uuid, instance, canonicalPathStr)
	local folder = scriptsFolder:FindFirstChild(uuid)
	if folder == nil then
		folder = Instance.new("Folder")
		folder.Name = uuid
		-- Mitigação de undo (2026-07-26, docs/DECISIONS.md): camada 1
		-- (fast-path best-effort) — ver comentário em TeamCreateSchema.luau.
		-- Sem checagem de integridade dedicada aqui (fora do escopo desta
		-- tarefa, que cobriu só sessões/leases/presença): se undo destruir
		-- esta pasta de metadados, a resolução uuid->Instance NESTE Studio
		-- continua funcionando (recordByUuid[uuid].instance já cacheia a
		-- Instance real em memória, independente da pasta sobreviver), mas a
		-- identidade fica sem replicar para outro Studio que ainda não a
		-- viu — risco residual aceito, registrar como fatia futura se
		-- aparecer como problema real.
		folder.Archivable = false
		folder.Parent = scriptsFolder
	end
	local instanceRef = folder:FindFirstChild("InstanceRef")
	if instanceRef == nil then
		instanceRef = Instance.new("ObjectValue")
		instanceRef.Name = "InstanceRef"
		instanceRef.Archivable = false
		instanceRef.Parent = folder
	end
	instanceRef.Value = instance
	local canonicalPath = folder:FindFirstChild("CanonicalPath")
	if canonicalPath == nil then
		canonicalPath = Instance.new("StringValue")
		canonicalPath.Name = "CanonicalPath"
		canonicalPath.Archivable = false
		canonicalPath.Parent = folder
	end
	canonicalPath.Value = canonicalPathStr or ""

	local record = { folder = folder, instanceRef = instanceRef, canonicalPath = canonicalPath, instance = instance }
	recordByUuid[uuid] = record
	uuidByInstance[instance] = uuid
	return record
end

function ScriptRegistry.init()
	ensureContainers()
end

-- Reconciliação: chamar 1x no início de cada SourceWatcher.start(), ANTES de
-- escanear/observar qualquer Instance. Percorre Scripts:GetChildren() e, para
-- cada entrada cuja Instance referenciada ainda for válida (não destruída de
-- verdade, ver isInstanceDestroyed) e observada (isWatched(instance) ==
-- true), reaproveita o uuid em memória — identidade sobrevive a reload do
-- plugin dentro da mesma sessão de place. Entradas cuja Instance já foi
-- destruída de verdade ou que não são mais observadas (fora dos containers de
-- Config.getWatchedRoots()) são destruídas na hora: mesma regra de "delete"
-- do steady-state, só que aplicada retroativamente ao que aconteceu enquanto
-- o plugin estava parado/recarregando.
function ScriptRegistry.reconcile(isWatched)
	ensureContainers()
	local reused, stale = 0, 0
	for _, folder in ipairs(scriptsFolder:GetChildren()) do
		if not folder:IsA("Folder") then
			continue
		end
		local instanceRef = folder:FindFirstChild("InstanceRef")
		local canonicalPath = folder:FindFirstChild("CanonicalPath")
		if
			instanceRef == nil
			or not instanceRef:IsA("ObjectValue")
			or canonicalPath == nil
			or not canonicalPath:IsA("StringValue")
		then
			-- Entrada corrompida/incompleta (schema versionado/validação é M3);
			-- não é escopo desta fatia decidir o que fazer com lixo malformado.
			continue
		end
		local instance = instanceRef.Value
		if ScriptRegistry.isInstanceDestroyed(instance) or not isWatched(instance) then
			folder:Destroy()
			stale += 1
			continue
		end
		recordByUuid[folder.Name] =
			{ folder = folder, instanceRef = instanceRef, canonicalPath = canonicalPath, instance = instance }
		uuidByInstance[instance] = folder.Name
		reused += 1
	end
	-- 2026-07-16: rebaixado para Logger.debug (ruído rotineiro de boot, ver
	-- Logger.luau) — continua encaminhado por WS para observabilidade de
	-- teste, só não aparece mais no Output do Studio a cada conexão.
	Logger.debug(("registry reconciliado: %d reaproveitados, %d removidos (obsoletos)."):format(reused, stale))
end

-- Resolve o uuid de uma Instance observada, alocando um novo (e criando a
-- entrada no registry) se ela ainda não tiver um — nem reconciliado do
-- registry existente, nem alocado antes nesta sessão. Retorna uuid, isNew.
--
-- CORREÇÃO (docs/DECISIONS.md 2026-07-04, bug real confirmado em teste com 2
-- Studios): antes de gerar um uuid novo, também busca no registry
-- COMPARTILHADO (findRegistryEntryFor) por uma entrada que já aponte pra essa
-- Instance — não só no mapa em memória local (uuidByInstance). Cobre o caso
-- de uma Instance replicada de outro Studio (que já alocou uuid pra ela)
-- aparecer via DescendantAdded DEPOIS que o reconcile() inicial já rodou;
-- sem essa varredura, os dois Studios alocavam uuids diferentes para o mesmo
-- script.
function ScriptRegistry.resolveOrAllocate(instance, canonicalPathStr)
	ensureContainers()
	local existing = uuidByInstance[instance]
	if existing ~= nil then
		return existing, false
	end
	local existingUuid, instanceRef, canonicalPath = findRegistryEntryFor(instance)
	if existingUuid ~= nil then
		local folder = scriptsFolder:FindFirstChild(existingUuid)
		recordByUuid[existingUuid] =
			{ folder = folder, instanceRef = instanceRef, canonicalPath = canonicalPath, instance = instance }
		uuidByInstance[instance] = existingUuid
		log(("resolveOrAllocate: reaproveitado uuid=%s já existente no registry compartilhado."):format(existingUuid))
		return existingUuid, false
	end
	local uuid = HttpService:GenerateGUID(false)
	createOrAdoptRecord(uuid, instance, canonicalPathStr)
	return uuid, true
end

-- Instance atual para um uuid, ou nil se desconhecido OU se a Instance já foi
-- destruída de verdade (ver isInstanceDestroyed — InstanceRef.Value sozinho
-- NÃO detecta isso, ver cabeçalho do arquivo).
function ScriptRegistry.getInstance(uuid)
	local record = recordByUuid[uuid]
	if record == nil then
		return nil
	end
	local instance = record.instanceRef.Value
	if ScriptRegistry.isInstanceDestroyed(instance) then
		return nil
	end
	return instance
end

function ScriptRegistry.getUuid(instance)
	return uuidByInstance[instance]
end

function ScriptRegistry.getCanonicalPath(uuid)
	local record = recordByUuid[uuid]
	if record == nil then
		return nil
	end
	return record.canonicalPath.Value
end

function ScriptRegistry.updateCanonicalPath(uuid, newPath)
	local record = recordByUuid[uuid]
	if record ~= nil then
		record.canonicalPath.Value = newPath
	end
end

-- Reatribui um uuid JÁ EXISTENTE no registry para apontar para uma Instance
-- NOVA, sem alocar um uuid novo nem tratar como delete+create. Uso
-- (docs/DECISIONS.md 2026-08-02, 3ª rodada, seção 2): conversão de classe
-- Folder -> Script/LocalScript/ModuleScript em
-- SourceWatcher.resolvePath quando uma pasta ganha init.*/init.server.*/
-- init.client.* (convenção Rojo) — a Instance antiga (Folder) é destruída e
-- uma nova (LuaSourceContainer) é criada em seu lugar. Se a Folder destruída
-- JÁ tinha entrada no registry, esta função garante que o MESMO uuid
-- continue valendo para a Instance nova, em vez de: (a) o registry antigo
-- ser varrido como "destruído" pelo próximo checkRegistryDrift (emitindo
-- scriptRemoved) e (b) um uuid novo, sem nenhuma relação, ser alocado para a
-- Instance nova por resolveOrAllocate — o que quebraria lease/presença
-- ativos no uuid antigo.
--
-- Nota honesta: no caminho comum de hoje, uma Folder pura (agrupando filhos,
-- sem init.*) NUNCA tem entrada no registry (só LuaSourceContainer é
-- registrado, ver isInstanceWatchable/watchScript em SourceWatcher.luau) —
-- então, na prática atual, o chamador quase sempre encontra uuid == nil
-- aqui e esta função não faz nada (é um no-op seguro, ver `return false`
-- abaixo). Ainda assim, a checagem é feita SEMPRE antes de destruir a
-- Instance antiga: é barata, e protege a identidade em qualquer cenário
-- futuro em que a Instance convertida já esteja registrada (ex.: se esta
-- mesma lógica de conversão for reaproveitada para outro caso no futuro).
--
-- Retorna true se reatribuiu (uuid existia), false se uuid é desconhecido
-- (chamador decide o que fazer nesse caso — hoje, aloca um uuid novo
-- normalmente via o caminho de sempre, resolveOrAllocate).
function ScriptRegistry.reassignInstance(uuid, newInstance)
	local record = recordByUuid[uuid]
	if record == nil then
		return false
	end
	uuidByInstance[record.instance] = nil
	record.instanceRef.Value = newInstance
	record.instance = newInstance
	uuidByInstance[newInstance] = uuid
	return true
end

-- Destrói a entrada do registry (Folder inteiro) e limpa os mapas em memória.
-- Não decide sozinho SE deve remover — quem chama (SourceWatcher, ciclo de
-- poll) já decidiu que a Instance foi destruída.
function ScriptRegistry.remove(uuid)
	local record = recordByUuid[uuid]
	if record == nil then
		return
	end
	uuidByInstance[record.instance] = nil
	recordByUuid[uuid] = nil
	record.folder:Destroy()
end

-- Itera um SNAPSHOT (tirado antes de qualquer callback rodar) de todas as
-- entradas vivas do registry, chamando callback(uuid, instance,
-- storedCanonicalPath). `instance` vem direto de InstanceRef.Value — pode ser
-- uma referência "morta" (Instance já destruída de verdade, ver cabeçalho do
-- arquivo); quem chama decide isso com ScriptRegistry.isInstanceDestroyed,
-- não `instance == nil` (nunca acontece por essa via). Snapshot é necessário
-- porque o callback do chamador (checkRegistryDrift em SourceWatcher) pode
-- chamar ScriptRegistry.remove() para o próprio uuid em processamento —
-- mutar recordByUuid durante a iteração de `pairs` sobre ele mesmo não é
-- seguro em Luau.
function ScriptRegistry.forEach(callback)
	local snapshot = {}
	for uuid, record in recordByUuid do
		table.insert(
			snapshot,
			{ uuid = uuid, instance = record.instanceRef.Value, storedPath = record.canonicalPath.Value }
		)
	end
	for _, entry in snapshot do
		callback(entry.uuid, entry.instance, entry.storedPath)
	end
end

return ScriptRegistry
]]></string>
      </Properties>
    </Item>
    <Item class="ModuleScript" referent="4">
      <Properties>
        <string name="Name">SourceWatcher</string>
        <string name="Source"><![CDATA[-- SyncTeam — Observação e escrita de Source (plugin de produção, M2)
--
-- Porta de spikes/m0_5-local-pipeline/plugin/SyncTeamLab.lua e
-- spikes/m0-source-replication/SyncTeamM0.lua para observação GENÉRICA dos
-- containers reais listados em Config.getWatchedRoots(), em vez de uma
-- única pasta sandbox.
--
-- Identidade de script (M2, docs/MILESTONES.md): UUID + ObjectValue via
-- ScriptRegistry (TestService.SyncTeam.Scripts/<uuid>), nunca mais só o
-- caminho no DataModel. `pathFor`/`resolvePath` continuam existindo — path é
-- usado para exibição (CanonicalPath, campo `path` nas mensagens) e para
-- resolver/criar Instances a partir do project.json do lado da extensão —
-- mas NUNCA mais como chave de endereçamento de leitura/escrita.
--
-- Detecção de mudança de Source: NUNCA só o sinal.
-- GetPropertyChangedSignal("Source") não dispara de forma confiável depois
-- de ScriptEditorService:UpdateSourceAsync (confirmado em teste real, M0.5,
-- 2026-07-03 — docs/DECISIONS.md, .claude/rules/luau.md). O sinal fica
-- conectado só como fast-path; o polling periódico (Config.POLL_INTERVAL_SECONDS)
-- é o caminho garantido. As duas vias chamam checkSourceChanged, que faz
-- dedupe por instância via lastSourceByInstance. O mesmo ciclo de polling
-- também detecta rename/move e delete via ScriptRegistry (checkRegistryDrift)
-- — ver comentário lá embaixo sobre por que delete não usa DescendantRemoving.

local ScriptEditorService = game:GetService("ScriptEditorService")

local Config = require(script.Parent.Config)
local Logger = require(script.Parent.Logger)
local ScriptRegistry = require(script.Parent.ScriptRegistry)

local SourceWatcher = {}

local sendMessage = nil -- callback(message: table), injetado por SourceWatcher.init
local enabled = false
local currentToken = 0 -- token de geração: invalida polling/observadores de uma geração anterior
-- (pegadinha real encontrada no M0: uma flag booleana sozinha não basta
-- quando start()/stop() pode ser chamado de novo antes de um task.wait
-- antigo acordar — ver .claude/agent-memory/luau-dev.md)

local watched = {} -- LuaSourceContainer -> RBXScriptConnection (sinal fast-path)
local lastSourceByInstance = {} -- LuaSourceContainer -> último Source visto (dedupe sinal+polling)
local recentWrites = {} -- LuaSourceContainer -> os.clock() da última escrita local via writeSource
local rootConnections = {} -- DescendantAdded/DescendantRemoving dos containers observados
-- Instance (Service) -> true para todo container já coberto por scanAndWatch
-- nesta geração (currentToken) — 2026-07-29 (mensagem watchedRoots,
-- docs/DECISIONS.md): permite SourceWatcher.applyWatchedRoots adicionar só os
-- containers NOVOS que a lista fixa ainda não cobria, sem re-escanear (e sem
-- duplicar conexões DescendantAdded/DescendantRemoving) os que start() já
-- tinha coberto.
local scannedRoots = {}

local log = Logger.log

-- ------------------------------------------------------------------ caminho

-- Caminho completo relativo a `game` (ex.: "ServerScriptService/Foo/Bar").
-- nil se a instância não estiver (mais) sob `game` (ex.: destruída antes do
-- Removing terminar de processar). Display-only a partir do M2 (ver cabeçalho).
-- Adicionado 2026-07-16 (consolidação de log de boot, ver init.server.luau/
-- Logger.luau): número de scripts atualmente observados, para compor a linha
-- única "conectado" exibida ao usuário quando a conexão estabiliza. Leitura
-- simples do tamanho de `watched` — sem custo além de percorrer a tabela (já
-- feito de igual forma dentro de SourceWatcher.start() para o log de debug
-- "observação iniciada").
function SourceWatcher.getWatchedCount()
	local count = 0
	for _ in watched do
		count += 1
	end
	return count
end

function SourceWatcher.pathFor(instance)
	local parts = {}
	local current = instance
	while current ~= nil and current ~= game do
		table.insert(parts, 1, current.Name)
		current = current.Parent
	end
	if current ~= game then
		return nil
	end
	return table.concat(parts, "/")
end

-- Resolve "ServerScriptService/Foo/Bar" a partir de `game`. Se
-- createClassName for passado, cria Folders intermediários e o nó final com
-- essa classe quando não existirem — mesma lógica do resolvePath dos
-- spikes, agora operando a partir de `game` em vez de uma pasta sandbox.
-- Usado só no modo criação do writeSource (sem uuid) — leitura/atualização
-- resolvem por uuid via resolveByUuid.
function SourceWatcher.resolvePath(path, createClassName)
	if type(path) ~= "string" or path == "" then
		return nil, "path vazio"
	end
	local parts = string.split(path, "/")
	local current = game
	for index, name in parts do
		local isFinal = index == #parts
		local child = current:FindFirstChild(name)
		if child == nil and index == 1 then
			-- Primeiro segmento deveria ser um serviço padrão (ServerScriptService,
			-- Workspace, etc.), sempre presente — GetService como fallback
			-- defensivo caso o serviço ainda não tenha sido tocado no DataModel.
			local ok, service = pcall(game.GetService, game, name)
			if ok then
				child = service
			end
		end
		if child == nil then
			if createClassName == nil then
				return nil, ("'%s' não encontrado em '%s'"):format(name, current:GetFullName())
			end
			local ok, created = pcall(Instance.new, if isFinal then createClassName else "Folder")
			if not ok then
				return nil, ("falha ao criar '%s': %s"):format(name, tostring(created))
			end
			created.Name = name
			created.Parent = current
			child = created
		elseif
			isFinal
			and index > 1 -- nunca converter o 1º segmento (sempre um Service do DataModel, ver fallback acima)
			and createClassName ~= nil
			and child.ClassName ~= createClassName
			and not child:IsA("LuaSourceContainer")
		then
			-- Bug real reportado pelo usuário (2026-07-26, docs/DECISIONS.md):
			-- convenção Rojo — uma pasta que já existia agrupando filhos (ex.:
			-- Folder "Interface" com scripts dentro, criada só pra agrupar) ganha
			-- um init.luau e a PRÓPRIA pasta vira o script (ModuleScript/Script/
			-- LocalScript com Source), mantendo os filhos dentro dela. Sem esta
			-- conversão, o segmento final resolvia sempre para a Folder já
			-- existente (o código nunca comparava classe do child reusado), e o
			-- chamador (init.server.luau handleWriteSource, modo criação) rejeitava
			-- com "alvo não é um script" — uma Folder nunca vira LuaSourceContainer
			-- sozinha, então IsA("LuaSourceContainer") sempre falhava.
			--
			-- CONVERTE preservando Name e Parent e reparentando TODOS os filhos
			-- atuais da Folder para a nova Instance, só então destrói a casca
			-- antiga — nunca perde os scripts que já viviam dentro dela. Os
			-- filhos preservam a MESMA Instance (não são recriados), então
			-- ScriptRegistry/watched (chaveados pela Instance, não pelo path)
			-- continuam válidos sem nenhuma limpeza extra — e como Name/Parent da
			-- Folder→script não mudam, o caminho canônico dos filhos nem muda de
			-- string (SourceWatcher.pathFor deles fica idêntico), então o próximo
			-- checkRegistryDrift não confunde isso com um scriptMoved.
			--
			-- ORDEM DAS OPERAÇÕES (corrigida 2026-08-02, continuação 3ª rodada
			-- §2 — bug confirmado pelo researcher em
			-- .claude/research/2026-08-02-reparent-descendantremoving-semantics.md,
			-- fonte oficial Roblox/creator-docs Instance.yaml): `newInstance` é
			-- parentado em `current` ANTES de mover os filhos pra dentro dela —
			-- nunca depois. A ordem antiga (montar newInstance inteira, órfã,
			-- fora da árvore, e só parentar no final) fazia cada `oldChild`
			-- deixar de ser descendente da raiz observada no instante em que
			-- virava filho de uma `newInstance` ainda sem Parent — a definição
			-- oficial de `DescendantRemoving` ("fires immediately before the
			-- parent Instance changes such that a descendant instance will no
			-- longer be a descendant") cobre exatamente esse caso, então o
			-- handler de `scanAndWatch` disparava `unwatchScript` incorretamente
			-- pra cada filho, e a reconexão via `DescendantAdded` quando
			-- `newInstance.Parent = current` rodava depois NÃO tinha garantia
			-- documentada de refire individual por descendente pré-existente de
			-- uma subárvore movida de uma vez (só relato de fórum, não-oficial,
			-- com ressalva explícita de "não garantido"). Com `newInstance` já
			-- dentro da árvore observada ANTES do loop, cada `oldChild.Parent =
			-- newInstance` é um reparent DIRETO dentro da mesma árvore já
			-- observada (nunca deixa de ser descendente da raiz) — não dispara
			-- `DescendantRemoving` nem depende de nenhum `DescendantAdded` de
			-- reconexão: a conexão de sinal (`watched[oldChild]`) e o registro
			-- em `ScriptRegistry` de cada filho nunca são tocados, sobrevivem
			-- intactos à conversão. Trade-off aceito: mais eventos de
			-- replicação Team Create (newInstance entra vazia primeiro, depois
			-- cada filho reparenta individualmente) em troca de nunca perder o
			-- watch de Source dos filhos — a ordem antiga era mais econômica em
			-- replicação, mas incorreta.
			--
			-- Só entra aqui quando a classe existente NÃO é LuaSourceContainer
			-- (ex.: Folder) — um LuaSourceContainer de OUTRA classe já existente
			-- ali (ex.: Script onde se queria ModuleScript) não é convertido por
			-- esta correção; mantém o comportamento herdado (reusa como está),
			-- fora do escopo deste bug. `index > 1` protege os Services do
			-- DataModel (1º segmento, resolvidos via GetService acima) de serem
			-- convertidos/destruídos por engano — nunca deveria acontecer na
			-- prática (Config.getWatchedRoots() nunca aponta pra um path de 1
			-- segmento só), mas o guard custa nada e evita um acidente catastrófico
			-- se algum dia acontecer.
			-- Preservação de identidade (docs/DECISIONS.md 2026-08-02, 3ª rodada,
			-- seção 2): captura o uuid da Folder ANTES de destruí-la — ver
			-- ScriptRegistry.reassignInstance para o porquê (no caminho comum de
			-- hoje isto é sempre nil, já que uma Folder pura nunca é registrada;
			-- a checagem é defensiva/futuro-prova, não corrige um caso hoje
			-- observável).
			local existingUuid = ScriptRegistry.getUuid(child)
			local ok, convertedOrErr = pcall(function()
				local newInstance = Instance.new(createClassName)
				newInstance.Name = child.Name
				-- Parenteia ANTES de mover os filhos (ver comentário acima) —
				-- newInstance nunca fica órfã enquanto os filhos entram nela.
				newInstance.Parent = current
				for _, oldChild in child:GetChildren() do
					oldChild.Parent = newInstance
				end
				child:Destroy()
				return newInstance
			end)
			if not ok then
				return nil,
					("falha ao converter '%s' de %s para %s: %s"):format(
						name,
						child.ClassName,
						createClassName,
						tostring(convertedOrErr)
					)
			end
			if existingUuid ~= nil then
				-- A Folder já tinha uuid registrado (ver nota de honestidade acima —
				-- hoje não deveria acontecer, mas se acontecer, o uuid antigo passa a
				-- apontar para a Instance nova em vez de ser varrido como "destruído"
				-- pelo próximo checkRegistryDrift + um uuid novo sem relação ser
				-- alocado por resolveOrAllocate (chamado logo em seguida por
				-- SourceWatcher.writeSource, via watchScript). Isso garante que
				-- watchScript/resolveOrAllocate reutilizem o MESMO uuid para a
				-- Instance nova (primeiro check de resolveOrAllocate é justamente
				-- `uuidByInstance[instance]`, já populado aqui).
				-- Nota sobre a reordenação acima: `newInstance.Parent = current` agora
				-- roda ANTES do loop de filhos (não depois) — mas isso não muda esta
				-- interação. Sob SignalBehavior Immediate, `DescendantAdded` do root já
				-- podia disparar `watchScript(newInstance)`/`resolveOrAllocate` de forma
				-- síncrona DENTRO do pcall mesmo na ordem antiga (só que no final, em vez
				-- de agora, no início) — se isso alocasse um uuid novo pra newInstance
				-- antes deste reassignInstance rodar, o registro desse uuid novo ficaria
				-- órfão (dormant hoje, porque existingUuid é sempre nil na prática — ver
				-- nota de honestidade acima). Pré-existente à reordenação, não introduzido
				-- por ela; não corrigido aqui (fora do escopo desta tarefa).
				ScriptRegistry.reassignInstance(existingUuid, convertedOrErr)
				log(
					("resolvePath: uuid=%s preservado na conversão de '%s' (Folder já registrada)."):format(
						existingUuid,
						name
					)
				)
			end
			log(
				("resolvePath: convertido '%s' de Folder para %s (pasta com filhos virando script via init.*, convenção Rojo)."):format(
					name,
					createClassName
				)
			)
			child = convertedOrErr
		end
		current = child
	end
	return current
end

-- Instance atual para um uuid, validada como LuaSourceContainer vivo. nil se
-- o uuid é desconhecido ou a Instance já foi destruída (delete detectado no
-- próximo ciclo de poll, se ainda não tiver sido).
function SourceWatcher.resolveByUuid(uuid)
	local instance = ScriptRegistry.getInstance(uuid)
	if instance == nil or not instance:IsA("LuaSourceContainer") then
		return nil
	end
	return instance
end

function SourceWatcher.getUuid(instance)
	return ScriptRegistry.getUuid(instance)
end

-- ------------------------------------------------------------- detecção

local function checkSourceChanged(instance, via)
	-- Pastas vendorizadas (Packages/ServerPackages/DevPackages, ver Config.luau
	-- 2026-07-16): nunca puxar Studio->disco uma edição nelas. Retorna ANTES de
	-- tocar lastSourceByInstance (nenhum estado de dedupe é criado para essas
	-- instâncias) — discovery/identidade (ScriptRegistry, watchScript) continua
	-- normal, só a detecção de mudança de Source é pulada aqui.
	if Config.isInsideExcludedPackageFolder(instance) then
		return
	end
	local current = instance.Source
	if current == lastSourceByInstance[instance] then
		return
	end
	lastSourceByInstance[instance] = current
	local wroteAt = recentWrites[instance]
	local origin = if wroteAt ~= nil and os.clock() - wroteAt < 1 then "plugin" else "studio"
	local path = SourceWatcher.pathFor(instance)
	if path == nil then
		return
	end
	local uuid = ScriptRegistry.getUuid(instance)
	-- Bug real reportado pelo usuário (2026-08-03, docs/DECISIONS.md): digitar
	-- rápido no VS Code + Ctrl+S fazia o arquivo "desfazer e reconstruir" ao
	-- longo de vários segundos. Causa raiz: `sendMessage` não tem roteamento
	-- por conexão (produto usa 1 conexão só) — toda escrita que o PRÓPRIO
	-- plugin faz por causa de um writeSource vindo da extensão (inclusive o
	-- pulse de buffer a cada ~150ms) ecoava de volta pro MESMO cliente que
	-- causou a escrita. A extensão não distingue esse eco de uma mudança
	-- genuína vinda de outro Studio via Team Create, e sua fila FIFO podia
	-- processar o eco DEPOIS que o contentCache já tinha avançado para um
	-- pulso mais novo, reescrevendo disco/buffer com conteúdo velho — repetido
	-- a cada eco atrasado, dando exatamente a aparência de revert/replay.
	-- origin=="plugin" (writeSource desta MESMA instância há <1s) é
	-- exatamente esse eco: suprime só o envio, nunca o dedupe acima
	-- (lastSourceByInstance já foi atualizado) nem o Logger.debug abaixo.
	-- writeAck (init.server.luau) já confirma a escrita para quem pediu, e
	-- refreshSync (reconciliação 3-way) continua como rede de segurança para
	-- qualquer deriva de normalização que UpdateSourceAsync introduza sem eco.
	if origin ~= "plugin" and sendMessage ~= nil then
		sendMessage({
			kind = "sourceChanged",
			uuid = uuid,
			path = path,
			source = current,
			className = instance.ClassName,
			origin = origin,
			via = via,
		})
	end
	-- 2026-07-20 (BUG 2, ruído de uso contínuo — docs/DECISIONS.md): rotineiro/
	-- alta frequência (dispara a cada edição de Source, tanto via sinal quanto
	-- polling, não só no boot) -> Logger.debug.
	Logger.debug(("sourceChanged uuid=%s '%s' via %s (origin=%s)"):format(tostring(uuid), path, via, origin))
end

-- Registra a observação de Source E resolve/aloca o uuid da Instance no
-- ScriptRegistry, nessa ordem, de forma síncrona. Retorna o uuid (nunca nil
-- para uma Instance que passa o IsA abaixo). Idempotente: se já observada,
-- só devolve o uuid já conhecido (sem reallocar nada).
local function watchScript(instance)
	if watched[instance] ~= nil then
		return ScriptRegistry.getUuid(instance)
	end
	if not instance:IsA("LuaSourceContainer") then
		return nil
	end
	-- Baseline capturada NA HORA de começar a observar, nunca esperando o
	-- próximo poll nem o DescendantAdded (evento adiado). Pegadinha real do
	-- M0.5: se a baseline só é capturada depois, uma escrita imediata
	-- seguinte pode já estar refletida nela, mascarando a própria mudança
	-- que se queria detectar. Ver .claude/agent-memory/luau-dev.md.
	lastSourceByInstance[instance] = instance.Source
	watched[instance] = instance:GetPropertyChangedSignal("Source"):Connect(function()
		checkSourceChanged(instance, "sinal")
	end)
	local uuid = ScriptRegistry.resolveOrAllocate(instance, SourceWatcher.pathFor(instance))
	return uuid
end

-- Só cuida da bookkeeping local (conexão de sinal, caches de dedupe). NÃO
-- toca no ScriptRegistry nem emite scriptRemoved: DescendantRemoving dispara
-- tanto para delete real (Instance:Destroy()) quanto para reparent para fora
-- da árvore observada (Instance ainda existe, só saiu dos containers de
-- Config.getWatchedRoots()) — os dois casos parecem idênticos daqui. A
-- decisão "isso foi delete de verdade" cabe só a checkRegistryDrift, que usa
-- ScriptRegistry.isInstanceDestroyed (Parent == nil + confirmação por pcall)
-- no próprio registry, no ciclo de poll — ver docs/MILESTONES.md (M2) e
-- ScriptRegistry.luau. NUNCA `InstanceRef.Value == nil`: correção registrada
-- em docs/DECISIONS.md 2026-07-04 (bug real confirmado em teste com 2
-- Studios) — `ObjectValue.Value` não zera quando a Instance referenciada é
-- destruída.
local function unwatchScript(instance)
	local connection = watched[instance]
	if connection ~= nil then
		connection:Disconnect()
	end
	watched[instance] = nil
	lastSourceByInstance[instance] = nil
	recentWrites[instance] = nil
end

-- Processa uma entrada JÁ CONFIRMADA como destruída de verdade
-- (ScriptRegistry.isInstanceDestroyed já checado por quem chama): remove do
-- registry e emite scriptRemoved. Compartilhado entre checkRegistryDrift
-- (ciclo de poll, caminho GARANTIDO) e checkDeadFastPath (fast-path opcional
-- via DescendantRemoving, ver scanAndWatch) — extraído para não duplicar o
-- trio remove+sendMessage+log em dois lugares, já que a mesma entrada pode
-- ser detectada como morta por qualquer um dos dois caminhos.
local function emitScriptRemoved(uuid, storedPath)
	ScriptRegistry.remove(uuid)
	if sendMessage ~= nil then
		sendMessage({ kind = "scriptRemoved", uuid = uuid, path = storedPath })
	end
	log(("scriptRemoved uuid=%s path='%s'"):format(uuid, tostring(storedPath)))
end

-- Detecção de rename/move e delete, no mesmo ciclo que já detecta mudança de
-- Source (spec M2). Para cada entrada viva do registry: se a Instance foi
-- destruída de verdade (ScriptRegistry.isInstanceDestroyed — NUNCA
-- `instance == nil`, que não é o sinal correto, ver docs/DECISIONS.md
-- 2026-07-04), emite scriptRemoved e destrói a entrada; senão, recomputa o
-- caminho canônico atual e compara com o guardado — se mudou, emite
-- scriptMoved e atualiza CanonicalPath.
--
-- CAMINHO GARANTIDO: roda a cada Config.POLL_INTERVAL_SECONDS
-- independentemente de qualquer sinal/evento ter disparado ou não — ver
-- checkDeadFastPath abaixo para o atalho opcional de latência.
local function checkRegistryDrift()
	ScriptRegistry.forEach(function(uuid, instance, storedPath)
		if ScriptRegistry.isInstanceDestroyed(instance) then
			emitScriptRemoved(uuid, storedPath)
			return
		end
		local currentPath = SourceWatcher.pathFor(instance)
		if currentPath == nil or currentPath == storedPath then
			return
		end
		ScriptRegistry.updateCanonicalPath(uuid, currentPath)
		if sendMessage ~= nil then
			sendMessage({
				kind = "scriptMoved",
				uuid = uuid,
				oldPath = storedPath,
				newPath = currentPath,
				className = instance.ClassName,
			})
		end
		log(("scriptMoved uuid=%s '%s' -> '%s'"):format(uuid, tostring(storedPath), currentPath))
	end)
end

-- Fast-path OPCIONAL de latência para delete, acionado por DescendantRemoving
-- (ver conexão em scanAndWatch). NUNCA é o caminho garantido — isso continua
-- sendo checkRegistryDrift, a cada ciclo de poll; se este fast-path nunca
-- disparar scriptRemoved na prática, não é regressão, só significa que essa
-- otimização de latência não está rendendo nada (o próximo poll cobre).
--
-- Cuidado de timing (registrado em .claude/agent-memory/luau-dev.md):
-- DescendantRemoving dispara ANTES da remoção terminar — é "Removing", não
-- "Removed". No momento exato do handler, `instance.Parent` ainda pode ser o
-- Parent ANTIGO (não nil), inclusive para um Destroy() real — então checar
-- ScriptRegistry.isInstanceDestroyed SINCRONAMENTE aqui tenderia a sempre
-- devolver falso, mesmo pra delete de verdade. O task.defer() abaixo cede um
-- resumption point pra dar chance da destruição real terminar de travar o
-- Parent antes da checagem. **[Hipótese, não confirmada em Studio real nesta
-- tarefa]**: não há como testar o timing exato (defer vs SignalBehavior
-- Immediate/Deferred) sem um Studio real — ver roteiro manual no
-- PROJECT_STATUS.md. Se a suposição estiver errada e o defer não for
-- suficiente, o fast-path simplesmente não emite nada e o polling entrega o
-- scriptRemoved no ciclo seguinte, exatamente como antes desta mudança.
--
-- Dedupe: reconfere ScriptRegistry.getUuid(instance) == uuid depois do defer,
-- porque enquanto o defer esperava, checkRegistryDrift (ciclo de poll) ou
-- outro disparo deste mesmo fast-path pode já ter processado e removido essa
-- entrada (ScriptRegistry.remove limpa uuidByInstance[instance]) — sem essa
-- reconfirmação, poderíamos emitir scriptRemoved duplicado para o mesmo uuid.
local function checkDeadFastPath(uuid, instance, storedPath)
	task.defer(function()
		if ScriptRegistry.getUuid(instance) ~= uuid then
			return -- já processado por checkRegistryDrift ou outro disparo enquanto o defer esperava
		end
		if ScriptRegistry.isInstanceDestroyed(instance) then
			emitScriptRemoved(uuid, storedPath)
		end
	end)
end

-- Escaneia um container observado (Service) recursivamente, registra a
-- observação de cada script já existente e conecta DescendantAdded/
-- DescendantRemoving para o resto da vida desta geração (myToken). Extraído
-- de dentro de start() (2026-07-29, mensagem watchedRoots, docs/DECISIONS.md)
-- para poder ser chamado tanto pelo scan inicial (start(), com a lista fixa
-- ou uma lista dinâmica já conhecida de uma conexão anterior) quanto por
-- SourceWatcher.applyWatchedRoots quando a extensão manda containers novos NO
-- MEIO de uma conexão já em andamento. Idempotente por root: se `root` já
-- estiver em scannedRoots (mesma geração), não re-escaneia nem duplica
-- conexões.
local function scanAndWatch(root, myToken)
	if scannedRoots[root] then
		return
	end
	scannedRoots[root] = true
	for _, descendant in root:GetDescendants() do
		watchScript(descendant)
	end
	table.insert(
		rootConnections,
		root.DescendantAdded:Connect(function(descendant)
			if currentToken ~= myToken then
				return
			end
			if descendant:IsA("LuaSourceContainer") then
				local uuid = watchScript(descendant)
				local path = SourceWatcher.pathFor(descendant)
				if path ~= nil and uuid ~= nil and sendMessage ~= nil then
					sendMessage({ kind = "scriptAdded", uuid = uuid, path = path, className = descendant.ClassName })
				end
			end
		end)
	)
	table.insert(
		rootConnections,
		root.DescendantRemoving:Connect(function(descendant)
			if currentToken ~= myToken then
				return
			end
			if watched[descendant] ~= nil then
				-- Fast-path opcional (ver checkDeadFastPath acima): captura
				-- uuid/storedPath ANTES de unwatchScript, que só faz
				-- bookkeeping local e nunca toca o ScriptRegistry.
				local uuid = ScriptRegistry.getUuid(descendant)
				if uuid ~= nil then
					checkDeadFastPath(uuid, descendant, ScriptRegistry.getCanonicalPath(uuid))
				end
				unwatchScript(descendant)
			end
		end)
	)
end

local function pollLoop(myToken)
	while enabled and currentToken == myToken do
		task.wait(Config.POLL_INTERVAL_SECONDS)
		if not enabled or currentToken ~= myToken then
			break
		end
		for instance in watched do
			checkSourceChanged(instance, "polling")
		end
		checkRegistryDrift()
	end
end

-- --------------------------------------------------------------- escrita

-- Escreve a Source garantindo que a instância já esteja observada (e com
-- uuid alocado) ANTES da escrita, de forma síncrona e na mesma pilha de
-- chamada (mesma pegadinha do M0.5 citada em watchScript acima).
function SourceWatcher.writeSource(instance, newSource)
	watchScript(instance)
	recentWrites[instance] = os.clock()
	local ok = pcall(function()
		ScriptEditorService:UpdateSourceAsync(instance, function()
			return newSource
		end)
	end)
	if ok then
		lastSourceByInstance[instance] = instance.Source
		return true, "UpdateSourceAsync"
	end
	local okDirect, errDirect = pcall(function()
		instance.Source = newSource
	end)
	if okDirect then
		lastSourceByInstance[instance] = instance.Source
		return true, ".Source"
	end
	return false, tostring(errDirect)
end

-- --------------------------------------------------------------- listagem

function SourceWatcher.listScripts()
	local scripts = {}
	for instance in watched do
		local path = SourceWatcher.pathFor(instance)
		local uuid = ScriptRegistry.getUuid(instance)
		if path ~= nil and uuid ~= nil then
			table.insert(scripts, { uuid = uuid, path = path, className = instance.ClassName })
		end
	end
	return scripts
end

-- ------------------------------------------------------------ start/stop

-- onMessage(message: table) é chamado para eventos espontâneos
-- (sourceChanged/scriptAdded/scriptRemoved/scriptMoved); quem injeta decide
-- como/se enviar pela conexão WS ativa.
function SourceWatcher.init(onMessage)
	sendMessage = onMessage
end

local function isInstanceWatchable(instance)
	if not instance:IsA("LuaSourceContainer") then
		return false
	end
	for _, root in Config.getWatchedRoots() do
		if instance:IsDescendantOf(root) then
			return true
		end
	end
	return false
end

function SourceWatcher.start()
	if enabled then
		return
	end
	enabled = true
	currentToken += 1
	local myToken = currentToken

	-- Reconciliação (item 2 do M2): reaproveita uuids do registry existente
	-- para Instances ainda observadas ANTES de qualquer scan tratar algo como
	-- "novo" — identidade sobrevive a reload do plugin na mesma sessão de
	-- place. ScriptRegistry.reconcile também já limpa entradas obsoletas
	-- (Instance destruída ou fora dos containers observados).
	ScriptRegistry.init()
	ScriptRegistry.reconcile(isInstanceWatchable)

	for _, root in Config.getWatchedRoots() do
		scanAndWatch(root, myToken)
	end

	task.spawn(pollLoop, myToken)

	local count = 0
	for _ in watched do
		count += 1
	end
	-- 2026-07-16: Logger.debug (ruído rotineiro de boot, ver Logger.luau) —
	-- continua encaminhado por WS, só não polui o Output a cada conexão.
	Logger.debug(("observação iniciada (%d scripts encontrados nos containers observados)."):format(count))
end

function SourceWatcher.stop()
	if not enabled then
		return
	end
	enabled = false
	currentToken += 1
	for _, connection in rootConnections do
		connection:Disconnect()
	end
	table.clear(rootConnections)
	table.clear(scannedRoots)
	for _, connection in watched do
		connection:Disconnect()
	end
	table.clear(watched)
	table.clear(lastSourceByInstance)
	table.clear(recentWrites)
	log("observação parada.")
end

-- ------------------------------------------------------- watchedRoots (extensão -> plugin)
--
-- Contrato completo em docs/DECISIONS.md (2026-07-29) e
-- vscode-extension/src/protocol.ts (WatchedRootsMessage): a extensão manda
-- {kind="watchedRoots", roots={"ReplicatedFirst", ...}} uma vez por conexão
-- aceita, logo após validar o hello e ANTES do primeiro listScripts. `roots`
-- são nomes de Service (primeiro segmento de cada mount point do
-- default.project.json do usuário) — em vez da lista fixa hardcoded de
-- Config.getWatchedRoots().
--
-- Timing escolhido (opção "b" das duas avaliadas na tarefa, ver
-- docs/DECISIONS.md para a justificativa completa): NÃO atrasamos o scan
-- inicial esperando esta mensagem. start() já escaneia com
-- Config.getWatchedRoots() (fixa, ou a dinâmica de uma conexão anterior desta
-- mesma sessão de plugin) de forma síncrona e imediata, exatamente como antes
-- desta tarefa — sem introduzir timeout/espera nova que poderia travar a
-- conexão inicial caso a extensão nunca mande watchedRoots (extensão mais
-- velha, erro). Quando/se watchedRoots chegar (via handleMessage em
-- init.server.luau, sincronamente, sem nenhum yield no caminho — GetDescendants/
-- Instance.new/Connect são todos síncronos), esta função só ADICIONA os
-- containers que a lista fixa ainda não cobria — scanAndWatch é idempotente
-- por root (scannedRoots), então containers já escaneados nunca são
-- reprocessados/duplicados. Isso termina de aplicar a lista nova ANTES do
-- handler retornar, e como cada mensagem WS é processada até o fim antes da
-- próxima (mesma premissa de ordering já usada por todo o resto do
-- dispatch de handleMessage), o listScripts que a extensão manda link
-- LOGO DEPOIS já reflete os containers novos.
--
-- Decisão de UNIÃO (fixa ∪ dinâmica), não substituição total: Config.
-- getWatchedRoots() nunca REMOVE um container da lista fixa quando a
-- dinâmica chega — só adiciona os que faltam. Motivo: remover um container
-- que já estava sendo observado arriscaria "perder de vista" scripts já
-- conhecidos (uuid alocado, possivelmente com lease ativa) só porque o
-- default.project.json do usuário não referencia mais aquele Service no
-- momento exato da mensagem — um risco desnecessário para o problema real
-- reportado (containers FALTANDO, nunca containers sobrando). Escanear um
-- Service fixo a mais que o necessário é barato (mesmo custo de hoje); nunca
-- vimos indicação de que isso seja um problema.
function SourceWatcher.applyWatchedRoots(names)
	local invalidNames = Config.setDynamicWatchedRoots(names)
	for _, name in invalidNames do
		-- Nome de Service desconhecido/inválido (game:GetService falhou dentro
		-- de Config.setDynamicWatchedRoots) — nunca derruba o plugin, só pula
		-- esse nome específico e loga (regra .claude/rules/luau.md).
		log(("watchedRoots: serviço desconhecido/inválido '%s' ignorado."):format(tostring(name)))
	end

	if not enabled then
		-- SourceWatcher ainda não iniciado (ou já parado) quando esta mensagem
		-- chegou — não deveria acontecer na prática (ver ordering acima), mas
		-- se acontecer: a lista dinâmica já ficou registrada em Config e será
		-- usada na união completa (fixa+dinâmica) na próxima vez que start()
		-- rodar. Nada mais a fazer aqui.
		Logger.debug(
			"watchedRoots recebido com SourceWatcher parado; lista dinâmica guardada para o próximo start()."
		)
		return
	end

	local myToken = currentToken
	local addedCount = 0
	for _, root in Config.getWatchedRoots() do
		if currentToken ~= myToken then
			return
		end
		if not scannedRoots[root] then
			scanAndWatch(root, myToken)
			addedCount += 1
		end
	end

	if addedCount > 0 then
		log(
			("watchedRoots aplicado: +%d container(es) novo(s) (%d scripts observados no total)."):format(
				addedCount,
				SourceWatcher.getWatchedCount()
			)
		)
	else
		Logger.debug("watchedRoots aplicado: nenhum container novo (já cobertos pela lista fixa).")
	end
end

return SourceWatcher
]]></string>
      </Properties>
    </Item>
    <Item class="ModuleScript" referent="5">
      <Properties>
        <string name="Name">TeamCreateElection</string>
        <string name="Source"><![CDATA[-- SyncTeam — Sessions + heartbeat + eleição de líder (M3.1)
--
-- Porte DIRETO do algoritmo já validado em 2 Studios reais no RojoCoop
-- (TeamCreateElection.lua: elect/observeCandidate/assignJoinSequences — copiados
-- sem alterar a lógica nem as constantes de tempo) e do ciclo de
-- heartbeat/tick que os conecta ao schema (TeamCreateCoordinator.lua:
-- __tick/__readSessions/__assignJoinSequences/__cleanupStaleSessions),
-- simplificado para o escopo desta fatia: SEM leases/intents/SchemaVersion
-- (isso é M3.2/M3.3, docs/MILESTONES.md) e sem comparação com servidor Rust
-- (não existe servidor no SyncTeam).
--
-- Schema sob TestService.SyncTeam/ (container raiz compartilhado com
-- ScriptRegistry via TeamCreateSchema.luau, novo nesta fatia):
--   LeaderClientId: StringValue, LeaderTerm: IntValue, NextJoinSequence: IntValue
--   Sessions/<clientId>/ (ClientId, UserId, Username, JoinSequence, Pulse,
--     ObservedRole: StringValue "leader"|"follower")
--
-- Decisões tomadas nesta fatia (registradas também em
-- docs/PROJECT_STATUS.md/luau-dev.md, não 100% especificadas na tarefa):
--
-- 1. ClientId é gerado de NOVO a cada start() (HttpService:GenerateGUID(false)),
--    não persistido entre reloads do plugin. Motivo: stop() já remove a
--    própria entrada de Sessions/ de forma SÍNCRONA (item 6 da tarefa, mesmo
--    cuidado do fix de WebStreamClient do M2) — não há entrada órfã
--    acumulando por reload normal, então persistir clientId só ganharia
--    continuidade de JoinSequence/ObservedRole entre reloads, o que não tem
--    valor aqui (o modelo trata cada start() como uma nova sessão de
--    participante, igual ao RojoCoop: TeamCreateCoordinator.new recebe um
--    clientId novo por instância do coordinator, nunca persistido em disco).
-- 2. Username via Players:GetNameFromUserIdAsync (pcall, fallback
--    tostring(userId)) — MESMO padrão já usado e validado no RojoCoop
--    (TeamCreateCoordinator:__resolveUsername), portado sem alteração. Não é
--    pesquisa de API nova: é porte de componente já exercitado em 2 Studios
--    reais (tabela de componentes validados do CLAUDE.md).
-- 3. Log claro de liderança ("sou o líder agora (term N)" / "líder atual: X
--    (term N)") sugerido como opcional pela tarefa — implementado, ajuda a
--    validar visualmente com 2 Studios reais depois.
--
-- CORREÇÃO (2026-07-07, docs/DECISIONS.md): primeiro teste real com 2
-- Studios confirmou split-brain de liderança (termos divergentes, 6 vs 1,
-- no mesmo tick). Causa raiz: `TeamCreateSchema.ensureRoot()`/
-- `ensureFolder("Sessions")` eram chamados só 1x dentro de start() (`tick()`
-- rodava sincronamente logo depois, ANTES de qualquer garantia de que a
-- replicação do outro Studio já tinha assentado) e o RESULTADO era cacheado
-- em `rootValues`/`sessionsFolder` para o resto da sessão — se duas
-- Instances-singleton (Folder ou Value) chegassem a ser criadas em paralelo
-- por dois Studios (clássica corrida "FindFirstChild nil -> Instance.new"),
-- cada Studio ficava preso lendo/escrevendo pra sempre na SUA cópia local,
-- mesmo depois da réplica do outro lado chegar como Instance irmã. Fix em
-- duas partes: (a) `TeamCreateSchema.getOrCreate` agora reconcilia
-- duplicatas sempre que é chamado, com desempate determinístico/replicado
-- (atributo `SyncTeamOrigin`) e merge que nunca perde estado (reparent de
-- filhos de Folder; máximo de contadores IntValue) — ver comentário de
-- cabeçalho de TeamCreateSchema.luau; (b) `tick()` (não só start()) agora
-- CHAMA DE NOVO `ensureRoot()`/`ensureFolder("Sessions")` a cada pulso e
-- reatribui `rootValues`/`sessionsFolder`, para este Studio sempre convergir
-- pra Instance canônica atual em vez de ficar preso na referência capturada
-- 1x. Continua sendo best-effort (replicação assíncrona nunca dá garantia
-- 100%), mas agora AUTOCORRIGE em vez de divergir permanentemente.

local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")

local Config = require(script.Parent.Config)
local Logger = require(script.Parent.Logger)
local TeamCreateSchema = require(script.Parent.TeamCreateSchema)

local TeamCreateElection = {}

-- Constantes validadas em 2 Studios reais no RojoCoop (incluindo failover
-- forçado) — NÃO alterar sem registro em docs/DECISIONS.md
-- (.claude/rules/luau.md).
TeamCreateElection.PULSE_INTERVAL_SECONDS = 2
TeamCreateElection.STALE_AFTER_SECONDS = 8
TeamCreateElection.CLEANUP_AFTER_SECONDS = 20
TeamCreateElection.PROMOTION_OBSERVATIONS = 2

local log = Logger.log

-- ------------------------------------------------- algoritmo de eleição
-- Porte 1:1 de RojoCoop/rojo-7.7.0-rc.1/plugin/src/TeamCreateElection.lua —
-- não redesenhar (docs/MILESTONES.md, M3.1).

local function isLive(session, staleAfter)
	return session.heartbeatAge <= staleAfter
end

local function compareAssigned(left, right)
	if left.joinSequence ~= right.joinSequence then
		return left.joinSequence < right.joinSequence
	end
	return left.clientId < right.clientId
end

-- Bootstrap (nenhuma sessão viva tem JoinSequence > 0 ainda): menor clientId
-- assume. Eleição normal: menor JoinSequence vivo, clientId como desempate.
function TeamCreateElection.elect(sessions, staleAfter)
	staleAfter = staleAfter or TeamCreateElection.STALE_AFTER_SECONDS

	local assigned = {}
	local unassigned = {}

	for _, session in ipairs(sessions) do
		if isLive(session, staleAfter) then
			if session.joinSequence > 0 then
				table.insert(assigned, session)
			else
				table.insert(unassigned, session)
			end
		end
	end

	if #assigned > 0 then
		table.sort(assigned, compareAssigned)
		return assigned[1].clientId
	end

	table.sort(unassigned, function(left, right)
		return left.clientId < right.clientId
	end)

	return if unassigned[1] then unassigned[1].clientId else nil
end

-- Promoção de líder só depois de N (PROMOTION_OBSERVATIONS) observações
-- consecutivas do MESMO candidato — evita flapping de líder por uma leitura
-- instável isolada de sessions.
function TeamCreateElection.observeCandidate(previous, candidate, requiredObservations)
	requiredObservations = requiredObservations or TeamCreateElection.PROMOTION_OBSERVATIONS

	if candidate == nil then
		return { candidate = nil, observations = 0, leader = nil }
	end

	local observations = 1
	if previous ~= nil and previous.candidate == candidate then
		observations = previous.observations + 1
	end

	return {
		candidate = candidate,
		observations = observations,
		leader = if observations >= requiredObservations then candidate else nil,
	}
end

function TeamCreateElection.assignJoinSequences(sessions, nextJoinSequence)
	local unassigned = {}
	for _, session in ipairs(sessions) do
		if session.joinSequence <= 0 then
			table.insert(unassigned, session)
		end
	end

	table.sort(unassigned, function(left, right)
		return left.clientId < right.clientId
	end)

	local assignments = {}
	for _, session in ipairs(unassigned) do
		assignments[session.clientId] = nextJoinSequence
		nextJoinSequence += 1
	end

	return assignments, nextJoinSequence
end

-- ------------------------------------------------------------- estado

local enabled = false
local currentToken = 0 -- token de geração (mesmo padrão de SourceWatcher/init.server.luau)

local sessionsFolder = nil -- TestService.SyncTeam.Sessions
local rootValues = nil -- {LeaderClientId, LeaderTerm, NextJoinSequence} (Instances de valor)

local clientId = nil
local userId = 0
local sessionFolder = nil -- TestService.SyncTeam.Sessions.<clientId>
local sessionValues = nil -- {ClientId, UserId, Username, JoinSequence, Pulse, ObservedRole}
-- Ultimo Username REALMENTE publicado nesta sessao de eleicao (nome do
-- Roblox resolvido por Players:GetNameFromUserIdAsync, ou o nome customizado
-- do dev). Existe por causa da auto-cura de 2026-09-19: quando
-- `ensureOwnSessionAlive` recria a pasta, os valores nascem zerados e o
-- Username NAO se republica sozinho — a resolucao acontece uma vez em
-- start(), num task.spawn que ja terminou ha muito. Sem este cache o colega
-- passa a ver a sessao recuperada com nome VAZIO (medido: "dev_Hakor" virou
-- "db8dc80c" depois da primeira recriacao). Nunca guarda string vazia, entao
-- a primeira chamada de ensureOwnSession (dentro de start(), antes de
-- qualquer resolucao) e no-op.
local publishedUsername = ""

local pulseObservations = {} -- clientId -> {pulse, observedAt}
local candidateObservation = nil -- {candidate, observations, leader}
local announcedLeader = nil

-- Item 2(b) da tarefa 2026-07-21 (toast de colaborador entrou/saiu): roster
-- local de OUTRAS sessões já vistas nesta sessão do plugin, p/ diff
-- tick-a-tick (ver checkSessionRoster abaixo). clientId -> displayName.
local knownOtherSessions = {}
-- Baseline (mesmo princípio de TeamCreatePresence.checkPresenceDrift): a 1ª
-- leitura de sessions nesta sessão do plugin nunca dispara toast (evita
-- rajada de "entrou" pra cada colaborador JÁ conectado quando ESTE Studio
-- conecta).
local rosterBaselined = false

-- ------------------------------------------------------------- schema (Sessions/<clientId>)

local SESSION_VALUE_CLASSES = {
	ClientId = "StringValue",
	UserId = "IntValue",
	Username = "StringValue",
	JoinSequence = "IntValue",
	Pulse = "IntValue",
	ObservedRole = "StringValue",
}

local function getOrCreate(parent, className, name)
	local existing = parent:FindFirstChild(name)
	if existing ~= nil then
		return existing
	end
	local value = Instance.new(className)
	value.Name = name
	-- Mitigação de undo (2026-07-26, docs/DECISIONS.md): camada 1 (fast-path
	-- best-effort) — ver comentário equivalente em TeamCreateSchema.luau.
	value.Archivable = false
	value.Parent = parent
	return value
end

-- Cria (ou reaproveita, se já existisse por algum motivo) a própria entrada
-- de sessão e popula os campos iniciais. Chamado 1x em start().
local function ensureOwnSession()
	local folder = sessionsFolder:FindFirstChild(clientId)
	if folder == nil then
		folder = Instance.new("Folder")
		folder.Name = clientId
		-- Mitigação de undo (2026-07-26, docs/DECISIONS.md): camada 1
		-- (fast-path best-effort) — ver comentário em TeamCreateSchema.luau.
		folder.Archivable = false
		folder.Parent = sessionsFolder
	end

	local values = {}
	for name, className in SESSION_VALUE_CLASSES do
		values[name] = getOrCreate(folder, className, name)
	end

	values.ClientId.Value = clientId
	values.UserId.Value = userId
	if values.ObservedRole.Value == "" then
		values.ObservedRole.Value = "follower"
	end
	-- Ver `publishedUsername`: republica o nome numa pasta recriada pela
	-- auto-cura. No boot ainda esta vazio (a resolucao vem depois, em
	-- start()), entao aqui e no-op na primeira passada.
	if publishedUsername ~= "" then
		values.Username.Value = publishedUsername
	end

	sessionFolder = folder
	sessionValues = values
end

-- CORREÇÃO (2026-09-19, docs/DECISIONS.md — "o colaborador saiu" com os dois
-- Studios abertos e vivos, relatado em teste real com 2 Studios em Team
-- Create): a própria Sessions/<clientId> pode ser destruída POR OUTRO STUDIO
-- a qualquer momento, não só por um Ctrl+Z local. `cleanupStaleSessions` é o
-- caminho normal e esperado: o líder destrói toda sessão com heartbeatAge >=
-- CLEANUP_AFTER_SECONDS. Basta um falso positivo — Studio em segundo plano
-- (a Roblox estrangula `task.wait` na janela sem foco, e com 2 Studios na
-- mesma máquina UM SEMPRE está em segundo plano), ou replicação de Pulse
-- atrasada pelo Team Create — para o líder concluir que o colega morreu e
-- apagar a pasta dele.
--
-- Antes desta correção isso era PERMANENTE, não transitório: `sessionFolder`
-- era capturado 1x em `ensureOwnSession()` dentro de `start()` e nunca mais
-- reavaliado, e o guard do topo de `tick()` (`sessionFolder.Parent == nil`)
-- fazia early-return silencioso para sempre. O lado apagado seguia
-- incrementando `Pulse` numa Instance órfã (Parent nil, não replica), sumia
-- de `Sessions/` dos dois lados, e `getSessionFolder()` passava a devolver
-- nil — levando junto presença (cursor/seleção, TeamCreatePresence) e leases
-- (TeamCreateLease). Resultado observado: cada Studio se vê sozinho e cada
-- um avisa que o colaborador saiu.
--
-- O comentário de `checkIntegrity` (2026-07-26) já descrevia exatamente este
-- estado terminal, mas a recuperação só estava ligada a
-- ChangeHistoryService.OnUndo/OnRedo — a porta rara. A porta comum é o
-- cleanup do outro Studio, e ela não passava por lá.
--
-- Com a recriação por pulso, um cleanup indevido custa <= PULSE_INTERVAL
-- (2s) de invisibilidade em vez de a sessão inteira. Por isso
-- CLEANUP_AFTER_SECONDS continua em 20s: com auto-cura o falso positivo é
-- barato, e afrouxar o prazo só atrasaria a limpeza de sessão realmente
-- morta.
--
-- `Parent ~= sessionsFolder` (e não `Parent == nil`) de propósito: a
-- reconciliação de duplicatas do TeamCreateSchema pode deixar a pasta
-- pendurada numa casca de `Sessions` que perdeu o desempate e ainda não foi
-- destruída — Parent não-nil, mas invisível para quem lê a canônica. Mesmo
-- órfão, sintoma idêntico.
--
-- Presence/ e LeaseIntents/ NÃO são recriados aqui: ambos se refazem sozinhos
-- na próxima mensagem do editor (`TeamCreatePresence.updateOwnPresence`,
-- `TeamCreateLease.ensureIntent`), e recriá-los vazios aqui só publicaria um
-- estado falso de "sem script ativo / sem intent".
local function ensureOwnSessionAlive(motivo)
	if sessionFolder ~= nil and sessionFolder.Parent == sessionsFolder then
		return false
	end
	-- Logger.notify (toast), não Logger.log: perder a própria sessão nunca é
	-- rotina — é sempre ou um undo destrutivo ou um cleanup indevido do outro
	-- Studio, e nos dois casos o usuário viu (ou vai ver) um "colaborador
	-- saiu" errado. Mesmo critério da reconciliação de duplicatas em
	-- TeamCreateSchema.
	Logger.notify(("sessão local havia sumido de Sessions/ (%s) — recriada"):format(motivo))
	ensureOwnSession()
	return true
end

-- Lê uma entrada de Sessions/<clientId> já existente (própria ou de outro
-- Studio, replicada via Team Create). nil se a entrada estiver incompleta
-- (schema versionado/validação é M3.2/M3.3, fora de escopo desta fatia).
local function readSessionEntry(folder)
	local values = {}
	for name, className in SESSION_VALUE_CLASSES do
		local value = folder:FindFirstChild(name)
		if value == nil or not value:IsA(className) then
			return nil
		end
		values[name] = value
	end
	return {
		folder = folder,
		values = values,
		clientId = values.ClientId.Value,
		joinSequence = values.JoinSequence.Value,
		pulse = values.Pulse.Value,
		userId = values.UserId.Value,
		username = values.Username.Value,
	}
end

-- Varre Sessions/ e devolve as entradas válidas com heartbeatAge calculado
-- por OBSERVAÇÃO local de mudança de Pulse (mesmo princípio já usado para
-- Source.Changed/scriptRemoved desde o M0.5/M2: nunca confiar só num
-- timestamp remoto, medir localmente desde a última vez que o Pulse mudou).
local function readSessions(now)
	local sessions = {}
	for _, folder in ipairs(sessionsFolder:GetChildren()) do
		if not folder:IsA("Folder") then
			continue
		end
		local entry = readSessionEntry(folder)
		if entry == nil or entry.clientId == "" then
			continue
		end

		local observation = pulseObservations[entry.clientId]
		if observation == nil or observation.pulse ~= entry.pulse then
			observation = { pulse = entry.pulse, observedAt = now }
			pulseObservations[entry.clientId] = observation
		end
		entry.heartbeatAge = math.max(0, now - observation.observedAt)

		table.insert(sessions, entry)
	end
	return sessions
end

-- Atribui JoinSequence a sessões vivas ainda sem uma (chamado só pelo líder).
local function applyJoinSequenceAssignments(sessions)
	local liveSessions = {}
	for _, session in ipairs(sessions) do
		if session.heartbeatAge <= TeamCreateElection.STALE_AFTER_SECONDS then
			table.insert(liveSessions, session)
		end
	end

	local assignments, newNext = TeamCreateElection.assignJoinSequences(liveSessions, rootValues.NextJoinSequence.Value)
	for _, session in ipairs(liveSessions) do
		local assigned = assignments[session.clientId]
		if assigned ~= nil and session.values.JoinSequence.Value <= 0 then
			session.values.JoinSequence.Value = assigned
			-- 2026-07-20 (BUG 2, ruído de uso contínuo — docs/DECISIONS.md):
			-- rotineiro (dispara por sessão que entra, não só no boot) ->
			-- Logger.debug.
			Logger.debug(("joinSequence atribuído clientId=%s sequence=%d"):format(session.clientId, assigned))
		end
	end
	rootValues.NextJoinSequence.Value = newNext
end

-- Remove (Folder:Destroy()) sessões (que não a própria) obsoletas há mais de
-- CLEANUP_AFTER_SECONDS — só o líder faz isso (item 5 da tarefa). A própria
-- sessão nunca é limpa por aqui: sai de Sessions/ só via stop() síncrono
-- (item 6) ou porque outro Studio a considera obsoleta se este Studio caiu
-- sem chamar stop() (plugin.Unloading cobre o caso normal).
local function cleanupStaleSessions(sessions)
	for _, session in ipairs(sessions) do
		if
			session.clientId ~= clientId
			and session.heartbeatAge >= TeamCreateElection.CLEANUP_AFTER_SECONDS
			and session.folder.Parent == sessionsFolder
		then
			log(
				("removendo sessão obsoleta clientId=%s heartbeatAge=%.1fs"):format(
					session.clientId,
					session.heartbeatAge
				)
			)
			session.folder:Destroy()
			pulseObservations[session.clientId] = nil
		end
	end
end

-- Item 2(b) da tarefa 2026-07-21: diff tick-a-tick entre `knownOtherSessions`
-- (o que já vimos) e `sessions` (o que Sessions/ tem agora) — toast de
-- "colaborador entrou"/"colaborador saiu" por sessão de OUTRO Studio que
-- aparece/some. Chamado incondicionalmente a cada tick (não só pelo líder):
-- cada Studio conectado detecta joins/leaves de forma independente, sem
-- depender de quem é o líder no momento. Nunca notifica a própria sessão
-- (clientId == a própria). Baseline (rosterBaselined) evita rajada de
-- "entrou" na primeira leitura desta sessão do plugin, quando outras sessões
-- já estavam conectadas antes deste Studio entrar.
local function checkSessionRoster(sessions)
	local seenThisTick = {}
	for _, session in ipairs(sessions) do
		if session.clientId == clientId then
			continue -- nunca notifica a própria sessão
		end
		seenThisTick[session.clientId] = true
		if knownOtherSessions[session.clientId] == nil then
			local displayName = if session.username ~= "" then session.username else session.clientId
			knownOtherSessions[session.clientId] = displayName
			if rosterBaselined then
				Logger.notify(("colaborador entrou: %s"):format(displayName))
			end
		end
	end

	for otherClientId, displayName in knownOtherSessions do
		if not seenThisTick[otherClientId] then
			knownOtherSessions[otherClientId] = nil
			if rosterBaselined then
				Logger.notify(("colaborador saiu: %s"):format(displayName))
			end
		end
	end

	rosterBaselined = true
end

-- ------------------------------------------------------------------- tick

local function tick()
	if not enabled then
		return
	end

	-- CORREÇÃO (2026-07-07, docs/DECISIONS.md — split-brain de liderança
	-- confirmado em teste real com 2 Studios): `rootValues`/`sessionsFolder`
	-- eram capturados 1x em start() e nunca mais reavaliados — se uma
	-- duplicata (ver TeamCreateSchema.luau) só fosse reconciliada DEPOIS
	-- desse instante (replicação do Team Create ainda assentando), este
	-- Studio continuava lendo/escrevendo pra sempre numa cópia local que
	-- podia já ter sido esvaziada/destruída pela reconciliação de outro
	-- Studio. `TeamCreateSchema.ensureRoot()`/`ensureFolder()` já reconciliam
	-- duplicatas sozinhos a cada chamada (idempotente e barato — poucos
	-- filhos) — chamar de novo AQUI, a cada pulso, é o que garante que este
	-- Studio sempre aponta pra Instance CANÔNICA mais recente antes de ler ou
	-- escrever `LeaderTerm`/`LeaderClientId`/`NextJoinSequence`/Sessions.
	local root = TeamCreateSchema.ensureRoot()
	rootValues = root.values
	sessionsFolder = TeamCreateSchema.ensureFolder("Sessions")

	-- Ver ensureOwnSessionAlive (2026-09-19): tem de vir DEPOIS do refresh
	-- acima (precisa da `sessionsFolder` canônica para comparar o Parent) e
	-- ANTES do Pulse (senão o incremento vai para a Instance órfã e some).
	ensureOwnSessionAlive("cleanup de outro Studio, ou reconciliação")

	local now = os.clock()
	sessionValues.Pulse.Value += 1

	local sessions = readSessions(now)
	checkSessionRoster(sessions)
	local candidate = TeamCreateElection.elect(sessions, TeamCreateElection.STALE_AFTER_SECONDS)
	candidateObservation =
		TeamCreateElection.observeCandidate(candidateObservation, candidate, TeamCreateElection.PROMOTION_OBSERVATIONS)
	local leaderClientId = candidateObservation.leader

	if leaderClientId == clientId then
		if rootValues.LeaderClientId.Value ~= clientId then
			rootValues.LeaderTerm.Value += 1
			rootValues.LeaderClientId.Value = clientId
			log(("sou o líder agora (term %d)"):format(rootValues.LeaderTerm.Value))
		end
		applyJoinSequenceAssignments(sessions)
		cleanupStaleSessions(sessions)
	end

	local localRole = if leaderClientId == clientId then "leader" else "follower"
	sessionValues.ObservedRole.Value = localRole

	if leaderClientId ~= nil and leaderClientId ~= announcedLeader then
		announcedLeader = leaderClientId
		if leaderClientId ~= clientId then
			log(("líder atual: %s (term %d)"):format(leaderClientId, rootValues.LeaderTerm.Value))
		end
	end
end

-- ------------------------------------------------------------ start/stop

-- Resolve o nome real do Roblox via Players:GetNameFromUserIdAsync (yield) e
-- publica em sessionValues.Username.Value. Extraído de dentro de start()
-- (M4.5+, "Nome de exibição customizado" — ver Config.resolveCustomDisplayName)
-- para ser reusado também por TeamCreateElection.setLocalUsername quando o
-- dev LIMPA o campo de nome customizado com uma sessão já conectada
-- (precisa voltar a resolver o nome do Roblox, não só publicar string
-- vazia). Roda em task.spawn próprio (a chamada em si dá yield) — reconfere
-- token/enabled/sessionValues antes de escrever de volta, mesmo cuidado de
-- todo yield neste projeto (start()/stop() pode ter rodado de novo enquanto
-- isso resolvia).
local function resolveAndPublishRobloxUsername(myToken)
	task.spawn(function()
		local fallback = tostring(userId)
		local username = fallback
		if userId > 0 then
			local ok, resolved = pcall(Players.GetNameFromUserIdAsync, Players, userId)
			if ok and type(resolved) == "string" and resolved ~= "" then
				username = resolved
			end
		end
		if enabled and currentToken == myToken and sessionValues ~= nil then
			sessionValues.Username.Value = username
			publishedUsername = username
		end
	end)
end

-- userIdParam: resultado de StudioService:GetUserId() (já resolvido pelo
-- chamador, init.server.luau — este módulo não chama StudioService
-- diretamente para não duplicar o pcall que init.server.luau já faz para o
-- `hello`).
-- pluginObject (M4.5+, novo parâmetro): só usado para resolver
-- Config.resolveCustomDisplayName no boot desta sessão — nenhum outro uso
-- neste módulo (não guardado module-level; TeamCreateElection.setLocalUsername,
-- abaixo, recebe o nome já resolvido pelo chamador, não o pluginObject).
function TeamCreateElection.start(userIdParam, pluginObject)
	if enabled then
		return
	end
	enabled = true
	currentToken += 1
	local myToken = currentToken

	local root = TeamCreateSchema.ensureRoot()
	rootValues = root.values
	sessionsFolder = TeamCreateSchema.ensureFolder("Sessions")

	-- Decisão 1 (ver cabeçalho do arquivo): clientId novo a cada start(), não
	-- persistido entre reloads — stop() remove a própria entrada de forma
	-- síncrona, então não há acúmulo de lixo por reload normal.
	clientId = HttpService:GenerateGUID(false)
	userId = userIdParam or 0
	publishedUsername = ""
	pulseObservations = {}
	candidateObservation = nil
	announcedLeader = nil
	-- Item 2(b) da tarefa 2026-07-21: reconexão (troca de porta, F8/F5, reload
	-- do plugin) re-baseline o roster em vez de disparar "saiu" pra sessões
	-- antigas (que não existem mais neste processo) e "entrou" em rajada pras
	-- sessões que já estavam lá.
	knownOtherSessions = {}
	rosterBaselined = false

	ensureOwnSession()
	-- 2026-07-16: Logger.debug (ruído rotineiro de boot, ver Logger.luau) —
	-- continua encaminhado por WS, só não polui o Output a cada conexão.
	Logger.debug(("sessão criada clientId=%s userId=%d"):format(clientId, userId))

	-- Nome de exibição customizado (M4.5+, implementado de verdade em
	-- 2026-08-02 — antes um placeholder "em breve" no painel): resolvido 1x
	-- aqui no boot desta sessão de eleição. Quando presente (string
	-- não-vazia), PULA POR COMPLETO a resolução assíncrona do nome real do
	-- Roblox (Players:GetNameFromUserIdAsync) e publica o nome customizado
	-- direto, de forma síncrona — decisão deliberada (documentada também na
	-- tarefa original): menos uma yield/chamada de rede que o resultado nem
	-- seria usado, já que o dev escolheu explicitamente um nome fixo.
	-- pluginObject pode ser nil (chamador antigo/defensivo) —
	-- Config.resolveCustomDisplayName já degrada para "" nesse caso (pcall
	-- interno falha ao indexar nil, cai no default).
	local customDisplayName = Config.resolveCustomDisplayName(pluginObject)
	if customDisplayName ~= "" then
		sessionValues.Username.Value = customDisplayName
		publishedUsername = customDisplayName
	else
		-- Username via Players:GetNameFromUserIdAsync (yield), então roda em
		-- task.spawn separado — decisão 2 do cabeçalho.
		resolveAndPublishRobloxUsername(myToken)
	end

	tick()
	task.spawn(function()
		while enabled and currentToken == myToken do
			task.wait(TeamCreateElection.PULSE_INTERVAL_SECONDS)
			if enabled and currentToken == myToken then
				tick()
			end
		end
	end)
end

-- Remove a própria entrada de Sessions/ de forma SÍNCRONA (item 6 da
-- tarefa) — não depende do loop de heartbeat (task.spawn) notar isso depois.
-- Mesmo cuidado já tomado com WebStreamClient:Close() no fix do M2
-- (docs/DECISIONS.md 2026-07-04): plugin.Unloading pode não dar chance de
-- uma coroutine assíncrona rodar cleanup.
function TeamCreateElection.stop()
	if not enabled then
		return
	end
	enabled = false
	currentToken += 1

	if sessionFolder ~= nil and sessionFolder.Parent ~= nil then
		sessionFolder:Destroy()
	end

	log(("sessão removida clientId=%s"):format(tostring(clientId)))

	sessionFolder = nil
	sessionValues = nil
	publishedUsername = ""
	clientId = nil
	candidateObservation = nil
	announcedLeader = nil
	pulseObservations = {}
	-- Item 2(b) da tarefa 2026-07-21: mesma simetria/higiene do reset em
	-- start() acima — evita que um start() seguinte reveja este roster velho.
	knownOtherSessions = {}
	rosterBaselined = false
end

-- Atualiza o Username publicado da PRÓPRIA sessão em runtime, sem exigir
-- reconectar (M4.5+, "Nome de exibição customizado" — chamado pelo callback
-- do TextBox correspondente em plugin/src/ui/StatusPanel.luau, via
-- PluginUI.luau, assim que o dev confirma um novo valor com a sessão já
-- conectada). `customName` não-vazio: publica direto. `customName` vazio
-- (dev limpou o campo, "voltar a usar o nome do Roblox"): dispara de novo a
-- resolução assíncrona via Players:GetNameFromUserIdAsync (mesmo caminho de
-- start(), extraído em resolveAndPublishRobloxUsername acima) — nunca
-- publica uma string vazia como Username. No-op silencioso se a eleição não
-- estiver rodando ainda (sessionValues == nil, ex.: dev editou a setting
-- antes de conectar pela 1ª vez nesta sessão do plugin) — Config já
-- persistiu o valor por conta do chamador; o próximo start() aplica o nome
-- customizado do zero, lendo a setting de novo.
function TeamCreateElection.setLocalUsername(customName)
	if sessionValues == nil then
		return
	end
	if type(customName) == "string" and customName ~= "" then
		sessionValues.Username.Value = customName
		publishedUsername = customName
		return
	end
	resolveAndPublishRobloxUsername(currentToken)
end

-- Exposto só para eventual UI/diagnóstico futuro (não usado nesta fatia).
function TeamCreateElection.getClientId()
	return clientId
end

-- Exposto para TeamCreateLease.luau (M3.2): o Folder
-- TestService.SyncTeam.Sessions.<clientId> da PRÓPRIA sessão, onde
-- TeamCreateLease cria a subpasta LeaseIntents/<uuid> ao registrar um
-- intent de edição. nil se a eleição não estiver rodando (start() não
-- chamado ainda, ou stop() já rodou) OU se a sessão tiver sido destruída —
-- quem chama trata nil como "degradar sem lease" (ver
-- TeamCreateLease.ensureIntent), nunca como erro fatal.
function TeamCreateElection.getSessionFolder()
	if sessionFolder == nil or sessionFolder.Parent == nil then
		return nil
	end
	return sessionFolder
end

function TeamCreateElection.isLeader()
	return rootValues ~= nil and clientId ~= nil and rootValues.LeaderClientId.Value == clientId
end

-- ------------------------------------------------------- undo guard (2026-07-26)
--
-- Self-healing reativo (camada 2 da mitigação de undo, ver
-- TeamCreateUndoGuard.luau/docs/DECISIONS.md): chamado após
-- ChangeHistoryService.OnUndo/OnRedo dispararem (não sabemos QUAIS instances
-- foram afetadas, só que ALGUM undo/redo aconteceu — checagem de integridade
-- é a única opção). Sem isto, um Ctrl+Z que destrua a PRÓPRIA
-- Sessions/<clientId> deixaria tick() preso para sempre no guard
-- `sessionFolder.Parent == nil` do topo (early return silencioso) —
-- heartbeat/eleição param, e getSessionFolder() (usado por
-- TeamCreateLease/TeamCreatePresence) passa a devolver nil pra sempre,
-- quebrando leases/presença junto. Reaproveita ensureOwnSession() (já
-- idempotente, mesma função usada em start()) em vez de duplicar lógica de
-- criação. No-op se a eleição não estiver rodando (enabled == false).
function TeamCreateElection.checkIntegrity()
	if not enabled then
		return
	end
	-- Mesmo refresh que tick() já faz a cada pulso (docs/DECISIONS.md
	-- 2026-07-07, split-brain de liderança): reconsulta TeamCreateSchema para
	-- convergir na Instance canônica atual antes de checar/recriar a sessão.
	local root = TeamCreateSchema.ensureRoot()
	rootValues = root.values
	sessionsFolder = TeamCreateSchema.ensureFolder("Sessions")

	-- Mesma recuperação que tick() faz a cada pulso desde 2026-09-19 (ver
	-- ensureOwnSessionAlive): aqui continua valendo a pena chamar na hora,
	-- sem esperar até PULSE_INTERVAL_SECONDS, porque o undo já aconteceu e
	-- ChangeHistoryService nos deu o gancho síncrono.
	ensureOwnSessionAlive("undo/redo")
end

-- ---------------------------------------------------------- leitura p/ UI (M4.5)
--
-- Getters read-only adicionados para o painel de status do plugin
-- (plugin/src/ui/), pedido explícito da tarefa ("se já existir algo assim,
-- use; se faltar, adicione um getter pequeno e read-only, não mude a lógica
-- interna"). Nenhuma das duas funções abaixo cria/muta Instance nenhuma nem
-- participa do ciclo de eleição — só leem o estado já mantido por tick().

-- ClientId do líder atual, ou nil se a eleição ainda não convergiu nenhuma
-- vez nesta sessão (rootValues ainda não lido) ou se LeaderClientId está
-- vazio (bootstrap, nenhum term concedido ainda).
function TeamCreateElection.getLeaderClientId()
	if rootValues == nil then
		return nil
	end
	local value = rootValues.LeaderClientId.Value
	return if value ~= "" then value else nil
end

-- Snapshot de todas as sessões atualmente em Sessions/ (própria incluída —
-- decisão desta fatia: a UI mostra "quem mais está na sessão" incluindo o
-- próprio Studio, já que ele também pode ser o líder ou estar editando algo).
-- Array de { clientId, username, userId, joinSequence }, em nenhuma ordem
-- garantida (quem chama decide como ordenar) — mesmo formato de campo de
-- readSessionEntry, só que sem heartbeatAge (a UI não filtra por liveness
-- nesta fatia: uma sessão que já saiu de Sessions/ simplesmente não aparece
-- mais no próximo refresh, não precisa de um terceiro estado "stale" na
-- tabela). nil-safe: devolve {} se a eleição não estiver rodando ainda.
function TeamCreateElection.listSessions()
	if sessionsFolder == nil then
		return {}
	end
	local sessions = {}
	for _, folder in ipairs(sessionsFolder:GetChildren()) do
		if not folder:IsA("Folder") then
			continue
		end
		local entry = readSessionEntry(folder)
		if entry ~= nil and entry.clientId ~= "" then
			table.insert(sessions, {
				clientId = entry.clientId,
				username = entry.username,
				userId = entry.userId,
				joinSequence = entry.joinSequence,
			})
		end
	end
	return sessions
end

return TeamCreateElection
]]></string>
      </Properties>
    </Item>
    <Item class="ModuleScript" referent="6">
      <Properties>
        <string name="Name">TeamCreateLease</string>
        <string name="Source"><![CDATA[-- SyncTeam — Leases autoritativas por script (M3.2)
--
-- Porte do algoritmo de decisão já validado em 2 Studios reais no RojoCoop:
-- `TeamCreateShadowLease.lua` (chooseWinner/assignRequestSequences/
-- groupByScript — cópia direta da lógica de comparação, sem alterar) e
-- `TeamCreateCoordinator.lua` (ciclo __assignRequestSequences/
-- __reconcileShadowLeases/__tick — mesmo ciclo, ignorando tudo sobre
-- comparação com servidor Rust e SchemaVersion, que não existem aqui).
--
-- DIFERENÇA DELIBERADA em relação ao RojoCoop (docs/DECISIONS.md
-- 2026-07-02, docs/ARCHITECTURE.md): lá as leases eram "shadow" (só
-- observação, nunca bloqueavam escrita — quem bloqueava de verdade era o
-- servidor Rust). Aqui as leases são AUTORITATIVAS desde o início: o plugin
-- recusa `writeSource` de quem não é o dono. Ver `TeamCreateLease.canWrite`
-- e a integração em `init.server.luau` (`handleWriteSource`).
--
-- OUTRA DIFERENÇA: o RojoCoop arbitrava a própria identidade do script
-- (scriptKey) dentro do ciclo de leases (`__resolveScriptRegistry`), porque
-- não tinha registry de UUID prévio. O SyncTeam já resolve identidade desde
-- o M2 (`ScriptRegistry.luau`) — os intents aqui já chegam com um uuid
-- conhecido (chave da pasta `LeaseIntents/<uuid>`), então não existe
-- equivalente a `__resolveScriptRegistry` nesta fatia.
--
-- Schema (sob TestService.SyncTeam, container raiz de TeamCreateSchema.luau):
--   NextLeaseRequestSequence: IntValue (adicionado a TeamCreateSchema.ROOT_VALUES)
--   Sessions/<clientId>/LeaseIntents/<uuid>/ (IntentId: StringValue GUID,
--     Pulse: IntValue, RequestSequence: IntValue, ScriptRef: ObjectValue)
--     — subpasta de Sessions/<clientId>, que é OWNED por TeamCreateElection;
--     este módulo só lê/escreve dentro dela via
--     TeamCreateElection.getSessionFolder() (accessor novo desta fatia).
--   Leases/<uuid>/ (OwnerClientId: StringValue, LeaseId: StringValue GUID,
--     LeaderTerm: IntValue, RequestSequence: IntValue)
--
-- Decisões tomadas nesta fatia (registradas também em
-- docs/PROJECT_STATUS.md/luau-dev.md, não 100% especificadas na tarefa):
--
-- 1. A checagem "tenho a lease deste uuid?" e a recusa de writeSource NÃO
--    ficam dentro de SourceWatcher.luau — ficam em init.server.luau
--    (handleWriteSource), no mesmo lugar que já distingue modo
--    atualização/criação pela presença de `uuid`. Motivo: manter
--    SourceWatcher.luau focado só em I/O de Source + ScriptRegistry (como
--    já documentado no cabeçalho dele); TeamCreateElection/TeamCreateLease
--    já são requeridos em init.server.luau, que é quem orquestra semântica
--    de protocolo — evita criar uma dependência nova de SourceWatcher para
--    TeamCreateLease só para isso.
-- 2. Ciclo do líder roda em `task.spawn` PRÓPRIO deste módulo (não
--    acoplado ao tick interno de TeamCreateElection), checando
--    `TeamCreateElection.isLeader()` no início de cada iteração — opção
--    explicitamente sugerida como alternativa mais simples pela tarefa, e
--    que evita tocar no loop de heartbeat já validado do M3.1.
-- 3. "Vivo" para um intent é decidido só pela observação local de mudança
--    do próprio `Pulse` (mesmo princípio de Source.Changed/scriptRemoved/
--    heartbeat desde M0.5/M2/M3.1) — NÃO verifica adicionalmente se a
--    SESSÃO dona está viva (RojoCoop fazia essa checagem extra em
--    `__readLiveIntents`). Simplificação deliberada: como o Pulse do
--    intent só é incrementado por um writeSource local (não por um timer
--    de heartbeat independente), se a sessão dona morrer o Pulse do
--    intent também para de mudar e ele fica "morto" pela ótica desta
--    checagem, no mesmo prazo (STALE_AFTER_SECONDS). Revisitar se teste
--    real mostrar um cenário onde isso diverge.
-- 4. Pastas de intent NUNCA são destruídas por este módulo quando ficam
--    obsoletas (só as entradas em `Leases/<uuid>` são criadas/atualizadas/
--    destruídas). Diferente do RojoCoop (`__pulseLocalIntents` removia
--    intents locais inativos expirados). Consequência aceita: uma sessão
--    de Studio longa, editando muitos scripts diferentes, acumula uma
--    pasta `LeaseIntents/<uuid>` por script já tocado, nunca removida
--    enquanto a sessão estiver viva — só é limpa quando a sessão inteira
--    morre (Folder:Destroy() em `TeamCreateElection.stop()` ou
--    `cleanupStaleSessions`, que destrói `Sessions/<clientId>` inteiro,
--    levando `LeaseIntents` junto). Não é escopo desta fatia (não pedido
--    na tarefa); registrar como possível polish futuro se virar problema
--    real.
--
-- M3.3 (expor identidade + mudança de lease pra extensão) — decisão tomada
-- aqui, não em SourceWatcher.luau (opção (b) das duas oferecidas pela
-- tarefa): `checkLeaseDrift`, ciclo PRÓPRIO deste módulo (task.spawn a
-- Config.POLL_INTERVAL_SECONDS, ver start()), varre
-- ScriptRegistry.forEach comparando TeamCreateLease.getOwner(uuid) contra o
-- último dono visto (lastOwnerByUuid, dedupe) e emite `leaseChanged
-- {uuid, ownerClientId, ownerDisplayName}` via callback injetado por
-- TeamCreateLease.init(onMessage) (mesmo padrão de SourceWatcher.init).
-- Motivo de NÃO usar a opção (a): o cabeçalho deste arquivo já documenta
-- (decisão 1 acima) que a integração de lease foi mantida FORA de
-- SourceWatcher.luau de propósito; colocar checkLeaseDrift lá criaria
-- exatamente a dependência nova (SourceWatcher -> TeamCreateLease) que essa
-- decisão evitou. Rodar a Config.POLL_INTERVAL_SECONDS (0.5s), não
-- PULSE_INTERVAL_SECONDS (2s): leaseChanged é sinal de UX (arquivo virou
-- read-only ou foi liberado), então a responsividade alvo é a mesma do
-- resto da UI de sincronização (Source), não a cadência de heartbeat de
-- eleição. Primeira observação de um uuid nesta sessão só grava baseline
-- sem emitir (evita rajada de leaseChanged para leases já existentes no
-- momento em que este Studio conecta) — ver comentário em checkLeaseDrift.
--
-- Handoff quase-instantâneo de lease (2026-08-02, docs/DECISIONS.md "8ª
-- rodada") — duas mudanças, ver comentários nos pontos exatos:
-- 1. O ciclo do líder (leaderTick, task.spawn em start()) parou de reusar
--    TeamCreateElection.PULSE_INTERVAL_SECONDS (2s) e passou a rodar em
--    Config.POLL_INTERVAL_SECONDS (0.5s) — só baixar o limiar de staleness
--    sem isso seria decorativo (piso de detecção preso em ~2-4s). O
--    heartbeat de eleição em si (TeamCreateElection.tick()) NÃO mudou.
-- 2. readLiveIntents troca TeamCreateElection.STALE_AFTER_SECONDS (8s fixo)
--    por Config.getPlaceSetting(pluginObjectRef, "leaseStaleAfterSeconds")
--    (default 2s, configurável no painel). Deliberadamente NÃO aplicado a
--    TeamCreateElection.elect/tick (detecção de sessão/líder morto continua
--    nos 8s fixos validados no RojoCoop, incluindo failover forçado) — a
--    decisão registrada fala de LEASE, não de eleição; ver comentário de
--    Config.PLACE_SETTINGS_DEFAULTS.leaseStaleAfterSeconds para o raciocínio
--    completo de por que os dois se desacoplaram.

local HttpService = game:GetService("HttpService")

local Config = require(script.Parent.Config)
local Logger = require(script.Parent.Logger)
local TeamCreateSchema = require(script.Parent.TeamCreateSchema)
local TeamCreateElection = require(script.Parent.TeamCreateElection)
local ScriptRegistry = require(script.Parent.ScriptRegistry)

local TeamCreateLease = {}

local log = Logger.log

-- M3.3: callback(message: table) injetado por TeamCreateLease.init, mesmo
-- padrão de SourceWatcher.init — usado só para emitir `leaseChanged`
-- espontâneo (checkLeaseDrift abaixo). Quem injeta (init.server.luau) decide
-- como/se enviar pela conexão WS ativa.
--
-- BUG CORRIGIDO (2026-07-04, code review): esta declaração `local` precisa
-- vir ANTES de `TeamCreateLease.init` abaixo. Na ordem original (init()
-- definida antes deste `local`), a atribuição `sendMessage = onMessage`
-- dentro de init() criava/escrevia uma variável GLOBAL solta em vez de
-- fechar sobre esta local — checkLeaseDrift (que fecha sobre a local
-- declarada aqui) nunca recebia o valor injetado, e nenhum `leaseChanged`
-- jamais era enviado. Ver docs/DECISIONS.md.
local sendMessage = nil

-- Injeta o callback usado por checkLeaseDrift para emitir `leaseChanged`
-- espontâneo (M3.3). Chamado por init.server.luau antes de
-- TeamCreateLease.start(), mesmo padrão de SourceWatcher.init.
function TeamCreateLease.init(onMessage)
	sendMessage = onMessage
end

-- ------------------------------------------------------------------ estado

local enabled = false
local currentToken = 0 -- token de geração (mesmo padrão de todos os outros módulos deste plugin)

local leasesFolder = nil -- TestService.SyncTeam.Leases
local sessionsFolder = nil -- TestService.SyncTeam.Sessions (lido, não é dono — TeamCreateElection é o dono)
local rootValues = nil -- {LeaderClientId, LeaderTerm, NextJoinSequence, NextLeaseRequestSequence}

local ownIntents = {} -- uuid -> { folder, values = {IntentId, Pulse, RequestSequence, ScriptRef} }
local intentPulseObservations = {} -- "clientId:uuid" -> {pulse, observedAt} (bookkeeping do ciclo do líder)

-- Handoff quase-instantâneo de lease (2026-08-02, docs/DECISIONS.md "8ª
-- rodada"): guardado module-level (diferente de TeamCreateElection.start, que
-- só usa pluginObject de forma síncrona dentro do próprio start() e não
-- precisa reter) porque readLiveIntents/leaderTick precisam consultar
-- Config.getPlaceSetting(pluginObjectRef, "leaseStaleAfterSeconds") em TODO
-- ciclo do líder, não só no boot — o dev pode editar o valor no painel com a
-- sessão já conectada (StatusPanel.luau/PluginUI.luau) e o próximo ciclo
-- (até 0.5s depois) já precisa refletir. nil-safe: Config.getPlaceSetting
-- degrada para o default (2s) se pluginObjectRef ainda for nil (start() não
-- chamado) ou se GetSetting falhar internamente (mesmo pcall de sempre).
local pluginObjectRef = nil

-- M3.3: uuid -> último OwnerClientId observado (dedupe), OU NO_OWNER_KEY
-- quando a última observação foi "sem lease". Mesmo princípio de dedupe por
-- último valor visto já usado para Source/registry/sessões desde
-- M0.5/M2/M3.1 (nunca reagir a estado, só a MUDANÇA de estado observada
-- localmente). Ausência de uma chave em lastOwnerByUuid significa "uuid
-- ainda não observado nesta sessão" — usado para não emitir leaseChanged na
-- primeira vez que um uuid é visto (baseline, evita rajada de mensagens para
-- leases já existentes no momento em que este Studio conecta/inicia).
local lastOwnerByUuid = {}
local NO_OWNER_KEY = "" -- sentinela: OwnerClientId real é sempre um GUID não-vazio

local INTENT_VALUE_CLASSES = {
	IntentId = "StringValue",
	Pulse = "IntValue",
	RequestSequence = "IntValue",
	ScriptRef = "ObjectValue",
}

local LEASE_VALUE_CLASSES = {
	OwnerClientId = "StringValue",
	LeaseId = "StringValue",
	LeaderTerm = "IntValue",
	RequestSequence = "IntValue",
}

local function getOrCreate(parent, className, name)
	local existing = parent:FindFirstChild(name)
	if existing ~= nil then
		return existing
	end
	local value = Instance.new(className)
	value.Name = name
	-- Mitigação de undo (2026-07-26, docs/DECISIONS.md): camada 1 (fast-path
	-- best-effort) — ver comentário em TeamCreateSchema.luau.
	value.Archivable = false
	value.Parent = parent
	return value
end

local function ensureContainers()
	if leasesFolder ~= nil then
		return
	end
	local root = TeamCreateSchema.ensureRoot()
	rootValues = root.values
	leasesFolder = TeamCreateSchema.ensureFolder("Leases")
	sessionsFolder = TeamCreateSchema.ensureFolder("Sessions")
end

-- ------------------------------------------------------- intent (todo Studio)

-- Cria/atualiza o próprio intent de edição para `uuid` sob
-- Sessions/<meuClientId>/LeaseIntents/<uuid>, incrementando Pulse. Chamado
-- ANTES de toda escrita em modo atualização (spec M3.2, item 1) — quem
-- decide QUANDO chamar é init.server.luau (handleWriteSource), não este
-- módulo. Degrada silenciosamente (sem erro) se a sessão local ainda não
-- estiver disponível (eleição não iniciada/reiniciando) — a checagem de
-- lease seguinte (canWrite) trata "sem intent registrado ainda" como
-- "sem lease" e permite otimisticamente (spec M3.2, item 2).
function TeamCreateLease.ensureIntent(uuid, instance)
	if not enabled or type(uuid) ~= "string" or uuid == "" then
		return
	end
	ensureContainers()

	local sessionFolder = TeamCreateElection.getSessionFolder()
	if sessionFolder == nil then
		return
	end

	local intentsFolder = sessionFolder:FindFirstChild("LeaseIntents")
	if intentsFolder == nil then
		intentsFolder = Instance.new("Folder")
		intentsFolder.Name = "LeaseIntents"
		-- Mitigação de undo (2026-07-26, docs/DECISIONS.md): camada 1
		-- (fast-path best-effort) — ver comentário em TeamCreateSchema.luau.
		intentsFolder.Archivable = false
		intentsFolder.Parent = sessionFolder
	end

	local record = ownIntents[uuid]
	if record == nil or record.folder.Parent == nil then
		local folder = intentsFolder:FindFirstChild(uuid)
		if folder == nil then
			folder = Instance.new("Folder")
			folder.Name = uuid
			folder.Archivable = false
			folder.Parent = intentsFolder
		end
		local values = {}
		for name, className in INTENT_VALUE_CLASSES do
			values[name] = getOrCreate(folder, className, name)
		end
		if values.IntentId.Value == "" then
			values.IntentId.Value = HttpService:GenerateGUID(false)
		end
		record = { folder = folder, values = values }
		ownIntents[uuid] = record
	end

	record.values.ScriptRef.Value = instance
	record.values.Pulse.Value += 1
end

-- ------------------------------------------------- checagem de lease (todo Studio)

-- OwnerClientId atual de Leases/<uuid>, ou nil se a entrada não existe
-- (ninguém foi arbitrado ainda) ou está malformada.
function TeamCreateLease.getOwner(uuid)
	ensureContainers()
	local folder = leasesFolder:FindFirstChild(uuid)
	if folder == nil then
		return nil
	end
	local ownerValue = folder:FindFirstChild("OwnerClientId")
	if ownerValue == nil or not ownerValue:IsA("StringValue") or ownerValue.Value == "" then
		return nil
	end
	return ownerValue.Value
end

-- true se o clientId local pode escrever em `uuid` agora. Spec M3.2 (item 2):
-- sem lease registrada ainda (ninguém arbitrado) OU dono == eu => permite
-- otimisticamente; dono == outro clientId => nega. Segundo valor de retorno
-- é o clientId do dono atual, só quando a escrita é negada (para a mensagem
-- de erro).
function TeamCreateLease.canWrite(uuid)
	local owner = TeamCreateLease.getOwner(uuid)
	if owner == nil then
		return true, nil
	end
	local myClientId = TeamCreateElection.getClientId()
	if owner == myClientId then
		return true, nil
	end
	return false, owner
end

-- Nome amigável para mensagens de erro: username da sessão do clientId, com
-- fallback pro próprio clientId se a sessão não tiver username resolvido
-- ainda (mesma corrida do M3.1: username é resolvido de forma assíncrona
-- via task.spawn separado, pode não estar pronto no primeiro tick).
function TeamCreateLease.describeClient(clientId)
	if clientId == nil then
		return "desconhecido"
	end
	ensureContainers()
	local folder = sessionsFolder:FindFirstChild(clientId)
	if folder ~= nil then
		local username = folder:FindFirstChild("Username")
		if username ~= nil and username:IsA("StringValue") and username.Value ~= "" then
			return username.Value
		end
	end
	return clientId
end

-- --------------------------------------------------- algoritmo de decisão
-- Porte 1:1 de RojoCoop/rojo-7.7.0-rc.1/plugin/src/TeamCreateShadowLease.lua
-- — não redesenhar (docs/MILESTONES.md, M3.2).

local function compareForSequencing(left, right)
	if left.joinSequence ~= right.joinSequence then
		return left.joinSequence < right.joinSequence
	end
	if left.clientId ~= right.clientId then
		return left.clientId < right.clientId
	end
	return left.intentId < right.intentId
end

local function assignRequestSequences(intents, nextSequence)
	local pending = {}
	for _, intent in ipairs(intents) do
		if intent.requestSequence <= 0 then
			table.insert(pending, intent)
		end
	end
	table.sort(pending, compareForSequencing)

	local assignments = {}
	for _, intent in ipairs(pending) do
		assignments[intent.intentId] = nextSequence
		nextSequence += 1
	end
	return assignments, nextSequence
end

local function compareIntent(left, right)
	if left.requestSequence ~= right.requestSequence then
		return left.requestSequence < right.requestSequence
	end
	if left.joinSequence ~= right.joinSequence then
		return left.joinSequence < right.joinSequence
	end
	if left.clientId ~= right.clientId then
		return left.clientId < right.clientId
	end
	return left.intentId < right.intentId
end

-- SEM PREEMPÇÃO: se o dono atual ainda tem intent vivo no grupo, continua
-- sendo o dono, independente de RequestSequence — só entra a comparação
-- completa quando o dono atual não está mais entre os candidatos vivos.
local function chooseWinner(currentOwnerClientId, intents)
	local ordered = table.clone(intents)
	table.sort(ordered, compareIntent)
	for _, intent in ipairs(ordered) do
		if intent.clientId == currentOwnerClientId then
			return intent
		end
	end
	return ordered[1]
end

local function groupByUuid(intents)
	local grouped = {}
	for _, intent in ipairs(intents) do
		grouped[intent.uuid] = grouped[intent.uuid] or {}
		table.insert(grouped[intent.uuid], intent)
	end
	return grouped
end

-- ------------------------------------------------------- ciclo do líder

-- Varre Sessions/*/LeaseIntents/* e devolve só os intents "vivos" (Pulse
-- mudou há menos de leaseStaleAfterSeconds, observado localmente — ver
-- decisão 3 no cabeçalho). joinSequence vem da sessão dona (Sessions/<clientId>/
-- JoinSequence), lido diretamente (não precisa de aliveness própria da
-- sessão, ver decisão 3).
--
-- 2026-08-02 (docs/DECISIONS.md "8ª rodada"): o limiar de staleness NÃO usa
-- mais TeamCreateElection.STALE_AFTER_SECONDS (8s fixo, constante de
-- coordenação de sessão/líder — ver Config.PLACE_SETTINGS_DEFAULTS,
-- comentário de leaseStaleAfterSeconds, para o porquê de LEASE e ELEIÇÃO
-- terem se desacoplado). Lido a cada chamada (não cacheado) — é barato
-- (1 leitura de GetSetting via Config.getPlaceSettings) e permite ao dev
-- editar o valor no painel com a sessão já conectada, refletindo no próximo
-- ciclo do líder.
local function readLiveIntents(now)
	local staleAfter = Config.getPlaceSetting(pluginObjectRef, "leaseStaleAfterSeconds")
	local intents = {}
	for _, sessionFolder in ipairs(sessionsFolder:GetChildren()) do
		if not sessionFolder:IsA("Folder") then
			continue
		end
		local ownerClientId = sessionFolder.Name
		local joinSequenceValue = sessionFolder:FindFirstChild("JoinSequence")
		local joinSequence = if joinSequenceValue ~= nil and joinSequenceValue:IsA("IntValue")
			then joinSequenceValue.Value
			else 0

		local intentsFolder = sessionFolder:FindFirstChild("LeaseIntents")
		if intentsFolder == nil then
			continue
		end

		for _, folder in ipairs(intentsFolder:GetChildren()) do
			if not folder:IsA("Folder") then
				continue
			end
			local uuid = folder.Name
			local values = {}
			local valid = true
			for name, className in INTENT_VALUE_CLASSES do
				local value = folder:FindFirstChild(name)
				if value == nil or not value:IsA(className) then
					valid = false
					break
				end
				values[name] = value
			end
			if not valid then
				continue
			end
			local scriptInstance = values.ScriptRef.Value
			if scriptInstance == nil then
				continue
			end

			local observationKey = ownerClientId .. ":" .. uuid
			local pulse = values.Pulse.Value
			local observation = intentPulseObservations[observationKey]
			if observation == nil or observation.pulse ~= pulse then
				observation = { pulse = pulse, observedAt = now }
				intentPulseObservations[observationKey] = observation
			end
			local heartbeatAge = math.max(0, now - observation.observedAt)
			if heartbeatAge > staleAfter then
				continue -- intent morto: dev parou de escrever nesse uuid
			end

			table.insert(intents, {
				uuid = uuid,
				clientId = ownerClientId,
				joinSequence = joinSequence,
				intentId = values.IntentId.Value,
				requestSequence = values.RequestSequence.Value,
				values = values,
				scriptInstance = scriptInstance,
			})
		end
	end
	return intents
end

-- Atribui RequestSequence (via NextLeaseRequestSequence) aos intents vivos
-- que ainda não têm um — porte de
-- TeamCreateCoordinator:__assignRequestSequences.
local function applyRequestSequenceAssignments(intents)
	local assignments, newNext = assignRequestSequences(intents, rootValues.NextLeaseRequestSequence.Value)
	for _, intent in ipairs(intents) do
		local assigned = assignments[intent.intentId]
		if assigned ~= nil and intent.values.RequestSequence.Value <= 0 then
			intent.values.RequestSequence.Value = assigned
			intent.requestSequence = assigned
			-- 2026-07-20 (BUG 2, ruído de uso contínuo — docs/DECISIONS.md):
			-- rotineiro/alta frequência (dispara por script tocado, não só no
			-- boot) -> Logger.debug. Continua encaminhado por WS.
			Logger.debug(("lease intentSequenced intentId=%s sequence=%d"):format(intent.intentId, assigned))
		end
	end
	rootValues.NextLeaseRequestSequence.Value = newNext
end

local function readLease(folder)
	local values = {}
	for name, className in LEASE_VALUE_CLASSES do
		local value = folder:FindFirstChild(name)
		if value == nil or not value:IsA(className) then
			return nil
		end
		values[name] = value
	end
	return { folder = folder, values = values, ownerClientId = values.OwnerClientId.Value }
end

-- Cria/atualiza Leases/<uuid> pro vencedor de cada grupo (agrupado por
-- uuid), rotacionando LeaseId quando dono OU termo mudam; remove
-- Leases/<uuid> cujo grupo não tem mais nenhum intent vivo (spec M3.2, itens
-- 3-5) — porte de TeamCreateCoordinator:__reconcileShadowLeases, sem a
-- checagem de ObjectValueIdentityValidated (não existe no SyncTeam — a
-- identidade UUID+ObjectValue já é a base do projeto desde o M2, não uma
-- feature opcional a validar em runtime).
local function reconcileLeases(intents, leaderTerm)
	local grouped = groupByUuid(intents)

	local existing = {}
	for _, folder in ipairs(leasesFolder:GetChildren()) do
		if folder:IsA("Folder") then
			existing[folder.Name] = folder
		end
	end

	for uuid, scriptIntents in grouped do
		local leaseFolder = existing[uuid]
		local currentOwner = nil
		if leaseFolder ~= nil then
			local lease = readLease(leaseFolder)
			currentOwner = lease and lease.ownerClientId
		end

		local winner = chooseWinner(currentOwner, scriptIntents)
		if winner == nil then
			continue
		end

		if leaseFolder == nil then
			leaseFolder = Instance.new("Folder")
			leaseFolder.Name = uuid
			-- Mitigação de undo (2026-07-26, docs/DECISIONS.md): camada 1
			-- (fast-path best-effort) — ver comentário em TeamCreateSchema.luau.
			leaseFolder.Archivable = false
			leaseFolder.Parent = leasesFolder
		end
		local values = {}
		for name, className in LEASE_VALUE_CLASSES do
			values[name] = getOrCreate(leaseFolder, className, name)
		end

		local ownerChanged = values.OwnerClientId.Value ~= winner.clientId
		local termChanged = values.LeaderTerm.Value ~= leaderTerm
		if ownerChanged or termChanged or values.LeaseId.Value == "" then
			values.LeaseId.Value = HttpService:GenerateGUID(false)
		end
		values.OwnerClientId.Value = winner.clientId
		values.LeaderTerm.Value = leaderTerm
		values.RequestSequence.Value = winner.requestSequence

		if ownerChanged or termChanged then
			-- 2026-07-20 (BUG 2, ruído de uso contínuo — docs/DECISIONS.md):
			-- rotineiro/alta frequência (troca de dono acontece a cada início
			-- de edição de script diferente, não só no boot) -> Logger.debug.
			Logger.debug(
				("lease concedida uuid=%s owner=%s term=%d requestSequence=%d"):format(
					uuid,
					winner.clientId,
					leaderTerm,
					winner.requestSequence
				)
			)
		end

		existing[uuid] = nil
	end

	-- Sobrou em `existing` = uuid sem NENHUM intent vivo neste ciclo: libera
	-- de verdade (spec M3.2, item 5).
	for uuid, folder in existing do
		-- 2026-07-20 (BUG 2, mesmo critério de "lease concedida" acima):
		-- rotineiro/alta frequência -> Logger.debug.
		Logger.debug(("lease liberada uuid=%s (sem intents vivos)"):format(uuid))
		folder:Destroy()
	end
end

local function leaderTick()
	if not TeamCreateElection.isLeader() then
		return
	end
	local now = os.clock()
	local intents = readLiveIntents(now)
	applyRequestSequenceAssignments(intents)
	reconcileLeases(intents, rootValues.LeaderTerm.Value)
end

-- ------------------------------------------------------ leaseChanged (M3.3)

-- Roda em TODO Studio (não só o líder): para cada uuid CONHECIDO localmente
-- (ScriptRegistry.forEach — o mesmo registry de identidade do M2), compara o
-- dono atual (getOwner) com o último dono visto (dedupe, lastOwnerByUuid) e
-- emite `leaseChanged {uuid, ownerClientId, ownerDisplayName}` só quando
-- muda. Mesmo padrão exato de checkSourceChanged/checkRegistryDrift em
-- SourceWatcher.luau (cache do último valor visto por chave, dedupe).
--
-- Decisão desta fatia (não 100% especificada na tarefa): a PRIMEIRA vez que
-- um uuid aparece no registry nesta sessão só grava a baseline, sem emitir —
-- evita uma rajada de `leaseChanged` para todo script já com lease existente
-- no exato momento em que este Studio conecta/inicia (informação que a
-- extensão não pediu proativamente ainda nesta fatia; fica para um futuro
-- "estado inicial" se necessário). Consequência aceita, análoga à limitação
-- já documentada para LeaseIntents: se um uuid é removido do registry
-- (script deletado) a entrada correspondente em lastOwnerByUuid nunca é
-- limpa — lixo pequeno e inofensivo em memória, não em replicação.
local function checkLeaseDrift()
	ScriptRegistry.forEach(function(uuid)
		local owner = TeamCreateLease.getOwner(uuid)
		local ownerKey = owner or NO_OWNER_KEY
		local lastKey = lastOwnerByUuid[uuid]

		if lastKey == nil then
			-- Primeira observação deste uuid nesta sessão: só baseline.
			lastOwnerByUuid[uuid] = ownerKey
			return
		end
		if lastKey == ownerKey then
			return
		end
		lastOwnerByUuid[uuid] = ownerKey

		local ownerDisplayName = TeamCreateLease.describeClient(owner)
		if sendMessage ~= nil then
			sendMessage({
				kind = "leaseChanged",
				uuid = uuid,
				ownerClientId = owner,
				ownerDisplayName = ownerDisplayName,
			})
		end
		-- 2026-07-20 (BUG 2, ruído de uso contínuo — docs/DECISIONS.md):
		-- rotineiro/alta frequência (dispara por script tocado a cada troca de
		-- dono, não só no boot) -> Logger.debug.
		Logger.debug(("leaseChanged uuid=%s owner=%s (%s)"):format(uuid, tostring(owner), ownerDisplayName))
	end)
end

-- ---------------------------------------------------------- leitura p/ UI (M4.5)
--
-- Read-only, pedido pelo painel de status do plugin (plugin/src/ui/): "quem
-- está editando o quê agora" — não muda nada do ciclo de arbitragem acima,
-- só itera Leases/<uuid> já concedidas. Reusa readLease (mesmo parser da
-- leaderTick). ensureContainers() garante leasesFolder mesmo se chamado
-- antes de qualquer start() (painel pode renderizar antes da eleição ligar).
-- ------------------------------------------------------- undo guard (2026-07-26)
--
-- Self-healing reativo (camada 2 da mitigação de undo, ver
-- TeamCreateUndoGuard.luau/docs/DECISIONS.md): chamado após
-- ChangeHistoryService.OnUndo/OnRedo. Corrige um gap JÁ DOCUMENTADO desde
-- 2026-07-07 (ver comentário em TeamCreateElection.luau daquela data,
-- "risco residual aceito"): `leasesFolder`/`sessionsFolder`/`rootValues` são
-- cacheados 1x em ensureContainers() (guard `if leasesFolder ~= nil then
-- return end`) e NUNCA refeitos depois — se undo destruir `Leases` (ou a
-- árvore acima dela), este módulo ficaria preso operando sobre uma Instance
-- morta (Parent == nil) pelo resto da sessão do plugin, sem nenhum ciclo
-- periódico que force o refresh (diferente de TeamCreateElection.tick(), que
-- já chama ensureRoot()/ensureFolder() a cada pulso). Fix: força
-- `leasesFolder = nil` (invalida o guard de cache-once) antes de chamar
-- ensureContainers() de novo, que então recria/reconcilia containers do
-- zero. Não recria intents/leases individuais aqui — `ensureIntent` já
-- recria a própria pasta de intent lazily no próximo write (checa
-- `record.folder.Parent == nil`), e `leaderTick`/`reconcileLeases` já recria
-- `Leases/<uuid>` no próximo ciclo de 2s para qualquer intent ainda vivo,
-- desde que os containers voltem a apontar pra Instances vivas. No-op se o
-- módulo não estiver rodando (enabled == false).
function TeamCreateLease.checkIntegrity()
	if not enabled then
		return
	end
	if leasesFolder == nil or leasesFolder.Parent == nil or sessionsFolder == nil or sessionsFolder.Parent == nil then
		leasesFolder = nil
		ensureContainers()
		log("integridade: containers de lease ausentes (undo/redo?) — recriados")
	end
end

function TeamCreateLease.forEachLease(callback)
	ensureContainers()
	for _, folder in ipairs(leasesFolder:GetChildren()) do
		if not folder:IsA("Folder") then
			continue
		end
		local lease = readLease(folder)
		if lease ~= nil and lease.ownerClientId ~= "" then
			callback(folder.Name, lease.ownerClientId)
		end
	end
end

-- ------------------------------------------------------------ start/stop

-- Roda seu PRÓPRIO task.spawn (decisão 2 do cabeçalho) — não duplica o loop
-- de heartbeat de TeamCreateElection, mas TAMBÉM NÃO reusa mais a constante
-- de intervalo dele desde 2026-08-02 (docs/DECISIONS.md "8ª rodada", handoff
-- quase-instantâneo de lease): antes rodava a TeamCreateElection.
-- PULSE_INTERVAL_SECONDS (2s) — mesma cadência do heartbeat de
-- eleição/sessão, que NÃO precisava mudar para esta feature. Baixar só
-- leaseStaleAfterSeconds sem desacoplar este loop seria decorativo (piso de
-- detecção preso em ~2-4s independente do número configurado) — por isso
-- agora roda em Config.POLL_INTERVAL_SECONDS (0.5s), mesma cadência que
-- checkLeaseDrift já usa logo abaixo. TeamCreateElection.PULSE_INTERVAL_SECONDS
-- continua intocado, sem nenhuma mudança no heartbeat de eleição em si.
--
-- pluginObject (novo parâmetro, mesmo contrato de TeamCreateElection.start):
-- guardado em pluginObjectRef (module-level, ver comentário na declaração)
-- para readLiveIntents poder consultar leaseStaleAfterSeconds a cada ciclo.
-- Pode ser nil (chamador antigo/defensivo) — Config.getPlaceSetting já
-- degrada para o default (2s) nesse caso.
function TeamCreateLease.start(pluginObject)
	if enabled then
		return
	end
	enabled = true
	currentToken += 1
	local myToken = currentToken

	pluginObjectRef = pluginObject
	ensureContainers()
	ownIntents = {}
	intentPulseObservations = {}
	lastOwnerByUuid = {}

	task.spawn(function()
		while enabled and currentToken == myToken do
			task.wait(Config.POLL_INTERVAL_SECONDS)
			if enabled and currentToken == myToken then
				leaderTick()
			end
		end
	end)

	-- Ciclo PRÓPRIO de checkLeaseDrift (M3.3), independente do leaderTick
	-- acima: roda em TODO Studio (não só líder) e usa
	-- Config.POLL_INTERVAL_SECONDS (0.5s, mesmo intervalo de
	-- checkSourceChanged/checkRegistryDrift em SourceWatcher.luau) em vez de
	-- PULSE_INTERVAL_SECONDS (2s) — leaseChanged é sinal de UX (arquivo
	-- ficou read-only/liberado), então vale a mesma responsividade já usada
	-- para o resto da UI de sincronização, não a cadência mais lenta de
	-- heartbeat/eleição.
	task.spawn(function()
		while enabled and currentToken == myToken do
			task.wait(Config.POLL_INTERVAL_SECONDS)
			if enabled and currentToken == myToken then
				checkLeaseDrift()
			end
		end
	end)

	-- 2026-07-16: Logger.debug (ruído rotineiro de boot, ver Logger.luau) —
	-- continua encaminhado por WS, só não polui o Output a cada conexão.
	Logger.debug("leases: ciclo iniciado.")
end

-- Não precisa destruir LeaseIntents/Leases explicitamente: pastas de intent
-- vivem sob Sessions/<clientId>, destruídas em cascata quando
-- TeamCreateElection.stop() destrói a própria sessão (síncrono, já
-- acontece independentemente desta chamada). Só limpa estado em memória.
function TeamCreateLease.stop()
	if not enabled then
		return
	end
	enabled = false
	currentToken += 1
	pluginObjectRef = nil
	ownIntents = {}
	intentPulseObservations = {}
	lastOwnerByUuid = {}
	log("leases: ciclo parado.")
end

return TeamCreateLease
]]></string>
      </Properties>
    </Item>
    <Item class="ModuleScript" referent="7">
      <Properties>
        <string name="Name">TeamCreatePresence</string>
        <string name="Source"><![CDATA[-- SyncTeam — Presença: cursor/seleção/arquivo ativo (M4)
--
-- PASSTHROUGH PURO de dados opacos vindos da extensão VS Code — decisão já
-- fechada na tarefa (docs/MILESTONES.md, M4): a origem do cursor/seleção é o
-- VS Code, nunca o editor de script do Studio. Este módulo NUNCA lê cursor de
-- uma Instance de Script; só grava o que a extensão manda na PRÓPRIA
-- Sessions/<clientId>/Presence (Team Create replica pro Studio remoto) e
-- observa as entradas Presence de OUTRAS sessões pra notificar a própria
-- extensão. Mesmo padrão exato de TeamCreateLease.luau: schema/ensure, ciclo
-- de poll a Config.POLL_INTERVAL_SECONDS, dedupe por último valor visto,
-- callback injetado via init() para emitir mensagem espontânea.
--
-- Schema (sob Sessions/<clientId>, dono é TeamCreateElection — este módulo só
-- lê/escreve dentro dela via TeamCreateElection.getSessionFolder() para a
-- PRÓPRIA sessão, e via sessionsFolder:GetChildren() [lido, não é dono] para
-- as demais — mesmo princípio de TeamCreateLease.ensureIntent/checkLeaseDrift):
--   Presence/ (ActiveScriptUuid: StringValue [""=nenhum script ativo],
--     CursorLine: IntValue [-1=null], CursorColumn: IntValue [-1=null],
--     SelectionStartLine: IntValue [-1=null], SelectionStartColumn: IntValue
--     [-1=null])
--
-- Contrato de protocolo fechado com ui-dev (não mudar nomes/tipos sem
-- registrar em docs/DECISIONS.md e avisar o outro lado):
--   1. `presenceUpdate` (extensão -> plugin, espontânea, sem requestId, sem
--      ack): grava a PRÓPRIA Presence (TeamCreatePresence.updateOwnPresence).
--      `uuid = null` (que chega como `message.uuid == nil` depois do
--      JSONDecode — Lua não distingui campo ausente de null, mesma limitação
--      já documentada em docs/DECISIONS.md 2026-07-04 para o sentido inverso)
--      significa "nenhum editor ativo correspondendo a um script sincronizado
--      — limpar minha presença".
--   2. `presenceChanged`/`presenceLeft` (plugin -> extensão, espontânea):
--      emitidas pelo MESMO ciclo de poll que já observa leases
--      (checkPresenceDrift, Config.POLL_INTERVAL_SECONDS), convertendo os
--      sentinelas -1/"" de volta para `null` no JSON — o lado JS nunca vê
--      sentinela.
--
-- Decisões desta fatia, não 100% especificadas na tarefa (ver também
-- .claude/agent-memory/luau-dev.md):
--
-- 1. **`updateOwnPresence` escreve IMEDIATAMENTE ao receber a mensagem**, não
--    é enfileirada para o ciclo de poll — só o LADO DE OBSERVAÇÃO (outras
--    sessões) usa o ciclo de Config.POLL_INTERVAL_SECONDS. Isso é simétrico ao
--    padrão já usado para writeSource (escreve na hora que a mensagem chega),
--    não ao padrão de ensureIntent+leaderTick (que tem um ciclo de decisão
--    separado porque envolve arbitragem entre Studios) — aqui não há
--    arbitragem nenhuma, é só "gravar o que me mandaram".
-- 2. **Dedupe por chave composta (string concatenada dos 5 campos), não por
--    tabela**: como Presence tem 5 campos (não 1 só, como o OwnerClientId de
--    TeamCreateLease), comparar tabelas por igualdade não funciona em Lua
--    (identidade de referência, não de valor) — a chave é
--    `uuid|cursorLine|cursorColumn|selStartLine|selStartCol` (sentinelas
--    crus, antes de converter pra null), mesmo princípio do NO_OWNER_KEY de
--    TeamCreateLease.checkLeaseDrift, só que estendido a múltiplos campos.
-- 3. **Baseline na primeira observação de um clientId nesta sessão**: só
--    grava, nunca emite — mesmo princípio (e mesmo motivo: evitar rajada de
--    mensagens para presença já existente no momento em que este Studio
--    conecta) já documentado e decidido em TeamCreateLease.checkLeaseDrift
--    para leaseChanged. Aplica-se tanto a presenceChanged quanto a
--    presenceLeft.
-- 4. **`presenceLeft` tem DOIS gatilhos distintos**: (a) a sessão observada
--    ZEROU a própria presença (ActiveScriptUuid virou "" — extensão limpou/
--    fechou o arquivo) — detectado dentro do loop principal de
--    checkPresenceDrift, mesma passada que detectaria um presenceChanged; (b)
--    a sessão SUMIU inteira de Sessions/ (Folder destruído — via stop()
--    síncrono do dev remoto, mesmo comportamento medido no M3.1 de ~3s em
--    shutdown gracioso, ou via cleanupStaleSessions do líder após 20s) —
--    detectado numa segunda passada comparando o snapshot desta rodada
--    (seenThisCycle) contra lastPresenceByClientId. Não foi necessário
--    duplicar nenhuma lógica de staleness própria (Pulse/heartbeatAge): o
--    módulo confia inteiramente no ciclo de vida já existente da sessão
--    (TeamCreateElection) para o Folder eventualmente desaparecer — mesmo
--    espírito de checkLeaseDrift, que também não reimplementa staleness de
--    sessão (RojoCoop fazia essa checagem dupla em __readLiveIntents; aqui,
--    como lá, decisão deliberada de não duplicar).
-- 5. **`describeClient` duplicado aqui** (não reusa
--    TeamCreateLease.describeClient) para não criar uma dependência cruzada
--    entre TeamCreatePresence e TeamCreateLease só por causa de ~5 linhas de
--    lookup de Username — mesmo raciocínio já registrado em docs/DECISIONS.md
--    (2026-07-04) para ScriptRegistry.reconcile/findRegistryEntryFor: duplicar
--    é mais legível que forçar uma abstração compartilhada entre dois módulos
--    que hoje não têm nenhuma outra razão para se conhecer.
-- 6. **Escrita de `updateOwnPresence` protegida por pcall**: diferente da
--    maioria das escritas internas deste plugin (que só manipulam dados já
--    conhecidos/gerados localmente), os 4 campos numéricos aqui vêm de uma
--    mensagie JSON EXTERNA e não confiável — `IntValue.Value` exige um número
--    íntegro; um valor malformado do lado JS lançaria erro em vez de
--    degradar. Seguindo `.claude/rules/luau.md` ("toda chamada em pcall;
--    falha degrada com log claro, nunca quebra o plugin inteiro"), a escrita
--    dos 5 valores é feita dentro de um pcall só, logando e descartando a
--    atualização inteira em caso de falha (nunca deixa a Presence num estado
--    parcialmente escrito).
-- 7. **Deletar a chave ATUAL de `lastPresenceByClientId` durante a própria
--    iteração de `pairs` é seguro em Lua** (documentado no manual da
--    linguagem: só inserir chaves NOVAS durante a travessia é
--    comportamento indefinido) — usado no loop de "sessão sumiu" abaixo.
--    Diferente do cuidado de snapshot em ScriptRegistry.forEach, que existe
--    porque ali o callback do CHAMADOR podia remover uma chave DIFERENTE da
--    que estava sendo iterada; aqui só removemos a própria chave da iteração
--    corrente, então não precisa de snapshot.

local Config = require(script.Parent.Config)
local Logger = require(script.Parent.Logger)
local TeamCreateSchema = require(script.Parent.TeamCreateSchema)
local TeamCreateElection = require(script.Parent.TeamCreateElection)

local TeamCreatePresence = {}

local log = Logger.log

-- callback(message: table) injetado por TeamCreatePresence.init, mesmo padrão
-- de SourceWatcher.init/TeamCreateLease.init — usado só para emitir
-- `presenceChanged`/`presenceLeft` espontâneos (checkPresenceDrift abaixo).
local sendMessage = nil

function TeamCreatePresence.init(onMessage)
	sendMessage = onMessage
end

-- ------------------------------------------------------------------ estado

local enabled = false
local currentToken = 0 -- token de geração (mesmo padrão de todos os outros módulos deste plugin)

local sessionsFolder = nil -- TestService.SyncTeam.Sessions (lido, não é dono — TeamCreateElection é o dono)

-- clientId -> chave composta do último valor visto (dedupe, ver decisão 2 no
-- cabeçalho). Chave ausente == "clientId ainda não observado nesta sessão"
-- (baseline, decisão 3).
local lastPresenceByClientId = {}

local PRESENCE_VALUE_CLASSES = {
	ActiveScriptUuid = "StringValue",
	CursorLine = "IntValue",
	CursorColumn = "IntValue",
	SelectionStartLine = "IntValue",
	SelectionStartColumn = "IntValue",
}

-- "Nenhuma presença" — mesmos sentinelas usados quando a extensão manda
-- uuid=null (ActiveScriptUuid="") e quando a Presence de outra sessão ainda
-- não existe/está incompleta.
local NONE_PRESENCE = {
	uuid = "",
	cursorLine = -1,
	cursorColumn = -1,
	selectionStartLine = -1,
	selectionStartColumn = -1,
}

local function getOrCreate(parent, className, name)
	local existing = parent:FindFirstChild(name)
	if existing ~= nil then
		return existing
	end
	local value = Instance.new(className)
	value.Name = name
	-- Mitigação de undo (2026-07-26, docs/DECISIONS.md): camada 1 (fast-path
	-- best-effort) — ver comentário em TeamCreateSchema.luau.
	value.Archivable = false
	value.Parent = parent
	return value
end

local function ensureContainers()
	if sessionsFolder ~= nil then
		return
	end
	sessionsFolder = TeamCreateSchema.ensureFolder("Sessions")
end

-- --------------------------------------------------- própria presença (write)

-- Grava a PRÓPRIA Presence a partir de uma mensagem `presenceUpdate` recebida
-- da extensão (chamado por init.server.luau, sem ack — spec M4, item 1).
-- Degrada sem erro se a sessão local ainda não estiver disponível (eleição
-- não iniciada/reiniciando), mesmo princípio de TeamCreateLease.ensureIntent.
function TeamCreatePresence.updateOwnPresence(message)
	if not enabled then
		return
	end

	local sessionFolder = TeamCreateElection.getSessionFolder()
	if sessionFolder == nil then
		return
	end

	local presenceFolder = sessionFolder:FindFirstChild("Presence")
	if presenceFolder == nil then
		presenceFolder = Instance.new("Folder")
		presenceFolder.Name = "Presence"
		-- Mitigação de undo (2026-07-26, docs/DECISIONS.md): camada 1
		-- (fast-path best-effort) — ver comentário em TeamCreateSchema.luau.
		presenceFolder.Archivable = false
		presenceFolder.Parent = sessionFolder
	end

	local values = {}
	for name, className in PRESENCE_VALUE_CLASSES do
		values[name] = getOrCreate(presenceFolder, className, name)
	end

	-- Decisão 6 (cabeçalho): dado numérico vem de mensagem externa não
	-- confiável — IntValue.Value exige inteiro, protegido por pcall.
	local ok, err = pcall(function()
		values.ActiveScriptUuid.Value = if type(message.uuid) == "string" then message.uuid else ""
		values.CursorLine.Value = if type(message.cursorLine) == "number" then message.cursorLine else -1
		values.CursorColumn.Value = if type(message.cursorColumn) == "number" then message.cursorColumn else -1
		values.SelectionStartLine.Value = if type(message.selectionStartLine) == "number"
			then message.selectionStartLine
			else -1
		values.SelectionStartColumn.Value = if type(message.selectionStartColumn) == "number"
			then message.selectionStartColumn
			else -1
	end)
	if not ok then
		log(("presenceUpdate: falha ao gravar (dado malformado?): %s"):format(tostring(err)))
	end
end

-- --------------------------------------------------- outras sessões (observação)

-- Nome amigável para presenceChanged: username da sessão do clientId, com
-- fallback pro próprio clientId (mesmo padrão de
-- TeamCreateLease.describeClient, duplicado aqui — decisão 5 no cabeçalho).
local function describeClient(clientId)
	ensureContainers()
	local folder = sessionsFolder:FindFirstChild(clientId)
	if folder ~= nil then
		local username = folder:FindFirstChild("Username")
		if username ~= nil and username:IsA("StringValue") and username.Value ~= "" then
			return username.Value
		end
	end
	return clientId
end

-- Lê a Presence de outra sessão, ou nil se ausente/incompleta (schema
-- malformado/em processo de criação — tratado como NONE_PRESENCE por quem
-- chama, mesmo espírito de readSessionEntry/readLease retornando nil).
local function readPresenceValues(presenceFolder)
	local values = {}
	for name, className in PRESENCE_VALUE_CLASSES do
		local value = presenceFolder:FindFirstChild(name)
		if value == nil or not value:IsA(className) then
			return nil
		end
		values[name] = value
	end
	return {
		uuid = values.ActiveScriptUuid.Value,
		cursorLine = values.CursorLine.Value,
		cursorColumn = values.CursorColumn.Value,
		selectionStartLine = values.SelectionStartLine.Value,
		selectionStartColumn = values.SelectionStartColumn.Value,
	}
end

local function presenceKey(presence)
	return table.concat({
		presence.uuid,
		tostring(presence.cursorLine),
		tostring(presence.cursorColumn),
		tostring(presence.selectionStartLine),
		tostring(presence.selectionStartColumn),
	}, "|")
end

-- Converte sentinelas (-1/"") de volta para null e emite presenceChanged, OU
-- emite presenceLeft se a presença zerou pra "nenhum" (decisão 4a do
-- cabeçalho) — chamado só quando a chave de dedupe mudou em relação à última
-- observada para este clientId.
local function emitPresence(clientId, presence)
	if presence.uuid == "" then
		if sendMessage ~= nil then
			sendMessage({ kind = "presenceLeft", clientId = clientId })
		end
		-- 2026-07-20 (BUG 2, ruído de uso contínuo — docs/DECISIONS.md):
		-- rotineiro/alta frequência (zera a cada troca/fechamento de arquivo no
		-- VS Code, não só saída real da sessão) -> Logger.debug.
		Logger.debug(("presenceLeft clientId=%s (presença zerada)"):format(clientId))
		return
	end

	local displayName = describeClient(clientId)
	if sendMessage ~= nil then
		sendMessage({
			kind = "presenceChanged",
			clientId = clientId,
			displayName = displayName,
			uuid = presence.uuid,
			cursorLine = if presence.cursorLine == -1 then nil else presence.cursorLine,
			cursorColumn = if presence.cursorColumn == -1 then nil else presence.cursorColumn,
			selectionStartLine = if presence.selectionStartLine == -1 then nil else presence.selectionStartLine,
			selectionStartColumn = if presence.selectionStartColumn == -1 then nil else presence.selectionStartColumn,
		})
	end
	-- 2026-07-20 (BUG 2, ruído de uso contínuo — docs/DECISIONS.md): rotineiro/
	-- alta frequência (dispara a cada movimento de cursor/seleção) -> Logger.debug.
	Logger.debug(
		("presenceChanged clientId=%s (%s) uuid=%s cursor=%d,%d"):format(
			clientId,
			displayName,
			presence.uuid,
			presence.cursorLine,
			presence.cursorColumn
		)
	)
end

-- Ciclo de observação (M4): para cada sessão viva que não seja a própria,
-- compara a Presence atual contra o último valor visto (dedupe) e emite
-- presenceChanged/presenceLeft na mudança; para sessões que tinham presença
-- conhecida e SUMIRAM desta varredura (Folder destruído), emite presenceLeft
-- (decisão 4b do cabeçalho). Roda no mesmo ciclo/cadência de
-- TeamCreateLease.checkLeaseDrift (Config.POLL_INTERVAL_SECONDS).
local function checkPresenceDrift()
	ensureContainers()
	local myClientId = TeamCreateElection.getClientId()
	local seenThisCycle = {}

	for _, folder in ipairs(sessionsFolder:GetChildren()) do
		if not folder:IsA("Folder") then
			continue
		end
		local clientId = folder.Name
		if clientId == myClientId then
			continue -- spec M4, item 3: nunca a própria sessão
		end
		seenThisCycle[clientId] = true

		local presence = NONE_PRESENCE
		local presenceFolder = folder:FindFirstChild("Presence")
		if presenceFolder ~= nil then
			local read = readPresenceValues(presenceFolder)
			if read ~= nil then
				presence = read
			end
		end

		local key = presenceKey(presence)
		local lastKey = lastPresenceByClientId[clientId]

		if lastKey == nil then
			-- Primeira observação deste clientId nesta sessão: só baseline
			-- (decisão 3 do cabeçalho, mesmo princípio de checkLeaseDrift).
			lastPresenceByClientId[clientId] = key
			continue
		end
		if lastKey == key then
			continue
		end
		lastPresenceByClientId[clientId] = key
		emitPresence(clientId, presence)
	end

	-- Sobrou em lastPresenceByClientId sem aparecer nesta varredura = sessão
	-- removida de Sessions/ (decisão 4b). Deletar a chave ATUAL durante esta
	-- iteração é seguro em Lua (decisão 7 do cabeçalho).
	for clientId in lastPresenceByClientId do
		if clientId ~= myClientId and not seenThisCycle[clientId] then
			lastPresenceByClientId[clientId] = nil
			if sendMessage ~= nil then
				sendMessage({ kind = "presenceLeft", clientId = clientId })
			end
			-- 2026-07-20 (BUG 2, mesmo critério do presenceLeft acima): sessão
			-- some de Sessions/ também por timeout/cleanupStaleSessions (20s),
			-- não só saída real -> Logger.debug.
			Logger.debug(("presenceLeft clientId=%s (sessão removida)"):format(clientId))
		end
	end
end

-- ---------------------------------------------------------- leitura p/ UI (M4.5)
--
-- Read-only, pedido pelo painel de status do plugin (plugin/src/ui/): "essa
-- sessão está vendo algum script agora (sem lease de escrita)?" — reusa
-- readPresenceValues, mesmo parser de checkPresenceDrift. Não participa do
-- ciclo de observação/dedupe acima (não mexe em lastPresenceByClientId).
-- nil se a sessão não existe, não tem Presence ainda, ou zerou (uuid == "").
-- ------------------------------------------------------- undo guard (2026-07-26)
--
-- Self-healing reativo (camada 2 da mitigação de undo, ver
-- TeamCreateUndoGuard.luau/docs/DECISIONS.md): chamado após
-- ChangeHistoryService.OnUndo/OnRedo. Mesmo gap/fix de
-- TeamCreateLease.checkIntegrity (`sessionsFolder` cacheado 1x em
-- ensureContainers(), nunca refeito) — força `sessionsFolder = nil` antes de
-- chamar ensureContainers() de novo. A PRÓPRIA Presence (Sessions/<clientId>/
-- Presence) não precisa de recriação forçada aqui: updateOwnPresence já a
-- recria lazily (FindFirstChild -> Instance.new se ausente) no próximo
-- `presenceUpdate` recebido da extensão (a cada movimento de cursor/seleção,
-- alta frequência) — duplicar isso aqui não ganha responsividade real. No-op
-- se o módulo não estiver rodando (enabled == false).
function TeamCreatePresence.checkIntegrity()
	if not enabled then
		return
	end
	if sessionsFolder == nil or sessionsFolder.Parent == nil then
		sessionsFolder = nil
		ensureContainers()
		log("integridade: containers de presença ausentes (undo/redo?) — recriados")
	end
end

function TeamCreatePresence.getActiveUuid(clientId)
	ensureContainers()
	local folder = sessionsFolder:FindFirstChild(clientId)
	if folder == nil then
		return nil
	end
	local presenceFolder = folder:FindFirstChild("Presence")
	if presenceFolder == nil then
		return nil
	end
	local values = readPresenceValues(presenceFolder)
	if values == nil or values.uuid == "" then
		return nil
	end
	return values.uuid
end

-- ------------------------------------------------------------ start/stop

-- Ciclo PRÓPRIO deste módulo (mesmo padrão de checkLeaseDrift em
-- TeamCreateLease.luau): task.spawn a Config.POLL_INTERVAL_SECONDS, guardado
-- por token de geração.
function TeamCreatePresence.start()
	if enabled then
		return
	end
	enabled = true
	currentToken += 1
	local myToken = currentToken

	ensureContainers()
	lastPresenceByClientId = {}

	task.spawn(function()
		while enabled and currentToken == myToken do
			task.wait(Config.POLL_INTERVAL_SECONDS)
			if enabled and currentToken == myToken then
				checkPresenceDrift()
			end
		end
	end)

	-- 2026-07-16: Logger.debug (ruído rotineiro de boot, ver Logger.luau) —
	-- continua encaminhado por WS, só não polui o Output a cada conexão.
	Logger.debug("presença: ciclo iniciado.")
end

-- Não precisa destruir a própria Presence explicitamente: vive sob
-- Sessions/<clientId>, destruída em cascata quando TeamCreateElection.stop()
-- destrói a própria sessão de forma síncrona (mesmo raciocínio já registrado
-- em TeamCreateLease.stop() para LeaseIntents). Só limpa estado em memória.
function TeamCreatePresence.stop()
	if not enabled then
		return
	end
	enabled = false
	currentToken += 1
	lastPresenceByClientId = {}
	log("presença: ciclo parado.")
end

return TeamCreatePresence
]]></string>
      </Properties>
    </Item>
    <Item class="ModuleScript" referent="8">
      <Properties>
        <string name="Name">TeamCreateSchema</string>
        <string name="Source"><![CDATA[-- SyncTeam — Container raiz compartilhado de TestService.SyncTeam (M3.1)
--
-- Extraído nesta fatia porque dois módulos agora precisam de
-- "criar Folder SyncTeam sob TestService se não existir": ScriptRegistry.luau
-- (Scripts/<uuid>, desde o M2) e TeamCreateElection.luau (Sessions/<clientId>,
-- LeaderClientId/LeaderTerm/NextJoinSequence, desta fatia M3.1). Antes desta
-- tarefa essa lógica só existia duplicada dentro de
-- ScriptRegistry.ensureContainers — TODO cumprido (ver comentário lá).
--
-- Escopo estritamente mínimo: só o container raiz e os valores de
-- coordenação de eleição (LeaderClientId/LeaderTerm/NextJoinSequence) que
-- MILESTONES.md (M3.1) pede no mesmo nível de TestService.SyncTeam.Scripts.
-- Nada de SchemaVersion/intents versionados aqui.
--
-- M3.2 (leases por script, ver plugin/src/TeamCreateLease.luau) adicionou
-- NextLeaseRequestSequence ao mesmo nível — reusa o padrão já existente de
-- "valor de coordenação direto sob a raiz", não um container próprio.
--
-- CORREÇÃO (2026-07-07, docs/DECISIONS.md — split-brain de liderança
-- confirmado em teste real com 2 Studios): o antigo `getOrCreate`
-- (`FindFirstChild` -> se nil, `Instance.new`+`.Parent`) é um clássico
-- "singleton preguiçoso" — se dois Studios chamam `ensureRoot()`/
-- `ensureFolder()` quase ao mesmo tempo, ANTES da réplica do Team Create de
-- um assentar no outro, os dois veem `FindFirstChild(...) == nil` e cada um
-- cria sua PRÓPRIA Instance (Roblox permite duas Instances irmãs com o mesmo
-- Name — não faz merge). Cada Studio então guarda a referência retornada
-- (cacheada por quem chama, ex. `TeamCreateElection.rootValues`) e passa a
-- ler/escrever SEMPRE nessa cópia local, mesmo depois que a réplica do outro
-- lado chegar como um segundo filho do mesmo Folder — dois "líderes"
-- genuinamente operando sobre Instances DIFERENTES, não um conflito de
-- escrita na mesma Instance. Reproduzido: `LeaderTerm` divergente (6 vs 1)
-- entre dois Studios no mesmo `tick()`.
--
-- Fix: `getOrCreate` agora RECONCILIA duplicatas sempre que é chamado (não só
-- na criação) — ver `getOrCreate` abaixo. Convergência determinística sem
-- depender de "quem viu o quê primeiro" (impossível garantir com replicação
-- assíncrona, só minimizar a janela): cada Instance criada por este módulo
-- ganha um atributo `SyncTeamOrigin` (GUID aleatório, gravado ANTES de
-- `.Parent` ser setado); se `getOrCreate` encontra mais de uma Instance com o
-- mesmo Name+ClassName sob o mesmo parent, a CANÔNICA é sempre a de menor
-- `SyncTeamOrigin` (ordem lexicográfica) — todo Studio que já tiver AMBAS as
-- duplicatas replicadas calcula a MESMA escolha, independente de qual viu
-- primeiro. As demais são MESCLADAS na canônica antes de destruídas (nunca
-- dropam estado):
--   - Folder: todo filho é reparentado pra dentro da canônica antes da casca
--     vazia ser destruída (cobre Scripts/Sessions/Leases e o próprio
--     SyncTeam) — se isso deixar netos com nomes repetidos sob a canônica
--     (ex.: duas sessões de clientId diferente por azar não colidem, mas dois
--     valores-filho tipo LeaderTerm duplicados sob a MESMA pasta raiz por uma
--     corrida separada, sim), a PRÓXIMA chamada de getOrCreate para aquele
--     nome específico reconcilia de novo — nunca perde dado, só pode levar
--     mais de um ciclo pra convergir de vez.
--   - Value numérica (IntValue): a canônica fica com o MAIOR valor entre
--     todas as duplicatas — os contadores deste projeto (LeaderTerm,
--     NextJoinSequence, NextLeaseRequestSequence) são estritamente
--     crescentes, "maior" nunca é retrocesso nem perde progresso real de
--     nenhum lado.
--   - Outras Values (StringValue como LeaderClientId): sem merge de
--     conteúdo — o ciclo de eleição já se autocorrige no tick seguinte (lê
--     Sessions/ já mesclado e reavalia liderança do zero); na pior hipótese
--     custa +1 incremento de term "desperdiçado" na convergência, nunca
--     viola exclusão mútua de líder.
-- Quem chama `ensureRoot()`/`ensureFolder()` UMA VEZ em `start()` e cacheia o
-- resultado (`TeamCreateElection.luau`) precisa continuar chamando de novo a
-- cada ciclo (`tick()`) para essa reconciliação ter efeito prático — só criar
-- a Instance duplicate-safe não helps se a referência local nunca for
-- atualizada depois de reconciliada (ver fix em TeamCreateElection.luau).

local HttpService = game:GetService("HttpService")
local TestService = game:GetService("TestService")

local Logger = require(script.Parent.Logger)

local TeamCreateSchema = {}

-- Valores de coordenação, direto sob TestService.SyncTeam (mesmo nível de
-- TestService.SyncTeam.Scripts). LeaderClientId/LeaderTerm/NextJoinSequence
-- são da eleição (M3.1); NextLeaseRequestSequence é das leases (M3.2).
local ROOT_VALUES = {
	LeaderClientId = "StringValue",
	LeaderTerm = "IntValue",
	NextJoinSequence = "IntValue",
	NextLeaseRequestSequence = "IntValue",
}

-- Atributo de desempate determinístico e REPLICADO (ver comentário de cabeçalho).
local ORIGIN_ATTRIBUTE = "SyncTeamOrigin"

local function originOf(instance)
	return instance:GetAttribute(ORIGIN_ATTRIBUTE) or ""
end

-- Desempate primário: SyncTeamOrigin (determinístico/replicado). Se os dois
-- candidatos ainda não tiverem o atributo (Instance duplicada criada ANTES
-- desta correção — caso legado, só deve ocorrer uma vez na primeira
-- reconciliação de um schema pré-existente), cai para um desempate por
-- conteúdo replicado: `.Value` para Values (comparável, replicado de
-- verdade), contagem de filhos para Folder. `[Hipótese]`: nesse caso legado
-- (ambos sem atributo), dois Studios com visões parcialmente diferentes de
-- replicação PODEM escolher canônicos diferentes num único ciclo — não perde
-- dado (merge sempre roda antes de destruir), só pode custar 1-2 ciclos
-- extras de convergência; nunca exercitado contra Studio real, só deduzido
-- por leitura de código.
local function byOrigin(a, b)
	local originA, originB = originOf(a), originOf(b)
	if originA ~= originB then
		return originA < originB
	end
	if a:IsA("Folder") and b:IsA("Folder") then
		return #a:GetChildren() > #b:GetChildren() -- mais estado real = mais "estabelecido"
	end
	local va, vb = a.Value, b.Value
	if typeof(va) == "number" and typeof(vb) == "number" then
		return va < vb
	end
	return tostring(va) < tostring(vb)
end

-- Devolve a Instance canônica dentre TODAS as crianças de `parent` com o
-- Name+ClassName dados, criando se não existir nenhuma e RECONCILIANDO se
-- houver mais de uma (ver comentário de cabeçalho do arquivo para a
-- justificativa completa do algoritmo). Chamado a cada tick (não só 1x) por
-- quem usa este módulo — é isso que garante autocorreção contínua enquanto a
-- replicação do Team Create ainda estiver assentando.
local function getOrCreate(parent, className, name)
	local candidates = {}
	for _, child in ipairs(parent:GetChildren()) do
		if child.Name == name and child.ClassName == className then
			table.insert(candidates, child)
		end
	end

	if #candidates == 0 then
		local value = Instance.new(className)
		value.Name = name
		-- Mitigação de undo (2026-07-26, docs/DECISIONS.md,
		-- .claude/research/2026-07-26-changehistoryservice-undo-exclusion.md):
		-- camada 1 (fast-path best-effort, NÃO garantida) contra Ctrl+Z do
		-- usuário destruindo instances de coordenação — relato de comunidade
		-- não confirmado por staff da Roblox, mas sem custo conhecido; a
		-- camada garantida é o self-healing reativo via
		-- ChangeHistoryService.OnUndo/OnRedo (ver TeamCreateUndoGuard.luau).
		value.Archivable = false
		-- Atributo gravado ANTES de .Parent: melhor esforço para replicar
		-- junto com a criação (mesmo princípio de "registrar antes de agir"
		-- já usado no projeto para baseline de Source/uuid) — não é garantia
		-- atômica de rede, só reduz a janela em que a Instance existe sem o
		-- atributo.
		value:SetAttribute(ORIGIN_ATTRIBUTE, HttpService:GenerateGUID(false))
		value.Parent = parent
		return value
	end

	if #candidates == 1 then
		local only = candidates[1]
		if only:GetAttribute(ORIGIN_ATTRIBUTE) == nil then
			only:SetAttribute(ORIGIN_ATTRIBUTE, HttpService:GenerateGUID(false))
		end
		return only
	end

	table.sort(candidates, byOrigin)
	local canonical = candidates[1]
	if canonical:GetAttribute(ORIGIN_ATTRIBUTE) == nil then
		canonical:SetAttribute(ORIGIN_ATTRIBUTE, HttpService:GenerateGUID(false))
	end

	local isFolder = canonical:IsA("Folder")
	local maxNumeric = nil
	if not isFolder and typeof(canonical.Value) == "number" then
		maxNumeric = canonical.Value
	end

	for i = 2, #candidates do
		local duplicate = candidates[i]
		if isFolder then
			-- Reparenta TODOS os filhos antes de destruir a casca — nunca
			-- perde estado aninhado (sessões/scripts/leases que nasceram por
			-- azar sob a pasta "perdedora"). Se algum nome colidir com um
			-- filho já existente sob `canonical`, fica temporariamente
			-- duplicado um nível abaixo — resolvido pela PRÓXIMA chamada de
			-- getOrCreate sobre aquele nome específico (mesma reconciliação,
			-- aplicada de novo a cada ciclo).
			for _, grandchild in ipairs(duplicate:GetChildren()) do
				grandchild.Parent = canonical
			end
		elseif maxNumeric ~= nil and typeof(duplicate.Value) == "number" then
			maxNumeric = math.max(maxNumeric, duplicate.Value)
		end
		pcall(function()
			duplicate:Destroy()
		end)
	end

	if maxNumeric ~= nil and canonical.Value ~= maxNumeric then
		canonical.Value = maxNumeric
	end

	-- M4.5: toast-worthy (Logger.notify, não Logger.log) — reconciliar
	-- duplicatas é sinal de uma corrida real de replicação do Team Create
	-- (ver cabeçalho do arquivo), não um evento rotineiro; ajuda o usuário a
	-- perceber que algo estranho (mas autocorrigido) aconteceu. Continua
	-- logando no Output incondicionalmente (Logger.notify sempre faz isso,
	-- ver Logger.luau) — só a exibição do toast depende da preferência.
	Logger.notify(
		("reconciliada(s) %d duplicata(s) de %s '%s' sob %s"):format(
			#candidates - 1,
			className,
			name,
			parent:GetFullName()
		)
	)

	return canonical
end

-- Idempotente: cria (ou reaproveita/reconcilia) TestService.SyncTeam e os
-- valores de coordenação (eleição + leases). Retorna { folder, values =
-- {LeaderClientId, LeaderTerm, NextJoinSequence, NextLeaseRequestSequence} }
-- (as próprias Instances de valor, não .Value já lido — quem chama decide
-- ler/escrever). Sem cache module-level: sempre reavalia contra
-- TestService:GetChildren() — barato (poucos filhos), e é isso que permite
-- reconciliar duplicatas criadas DEPOIS da primeira chamada (ver cabeçalho).
function TeamCreateSchema.ensureRoot()
	local rootFolder = getOrCreate(TestService, "Folder", "SyncTeam")

	local values = {}
	for name, className in ROOT_VALUES do
		values[name] = getOrCreate(rootFolder, className, name)
	end

	return { folder = rootFolder, values = values }
end

-- Cria (ou reaproveita/reconcilia) uma pasta filha direta de
-- TestService.SyncTeam — usado para "Scripts" (ScriptRegistry), "Sessions"
-- (TeamCreateElection) e "Leases" (TeamCreateLease), evitando duplicar a
-- chamada a ensureRoot() em cada módulo.
function TeamCreateSchema.ensureFolder(name)
	local root = TeamCreateSchema.ensureRoot()
	return getOrCreate(root.folder, "Folder", name)
end

return TeamCreateSchema
]]></string>
      </Properties>
    </Item>
    <Item class="ModuleScript" referent="9">
      <Properties>
        <string name="Name">TeamCreateUndoGuard</string>
        <string name="Source"><![CDATA[-- SyncTeam — Self-healing pós Ctrl+Z/Ctrl+Y (undo/redo), 2026-07-26
--
-- Contexto (bug real reportado pelo usuário; pesquisa completa em
-- .claude/research/2026-07-26-changehistoryservice-undo-exclusion.md,
-- decisão registrada em docs/DECISIONS.md): toda mudança no DataModel feita
-- por um plugin (Instance.new + .Parent = ...) entra no ChangeHistoryService
-- do usuário como qualquer edição manual — não existe API oficial pra excluir
-- uma Instance do histórico de undo/redo. `ChangeHistoryService:SetEnabled(false)`
-- existe mas desliga GLOBALMENTE e LIMPA todo o histórico do usuário
-- (inviável). `TryBeginRecording`/`FinishRecording` fazem o OPOSTO: tornam
-- uma mudança deliberadamente undo-ável (é o que o Rojo real usa para os
-- próprios patches, ver ServeSession.lua citado na pesquisa) — não servem
-- aqui. Resultado: um Ctrl+Z do usuário pode reverter/destruir as instances
-- de coordenação (sessões/heartbeats/leases/presença) sob
-- TestService.SyncTeam, quebrando o plugin e desconectando o usuário.
--
-- Achado colateral da pesquisa que REDUZ o escopo real do bug: mudanças em
-- Script/LocalScript/ModuleScript.Source são confirmadas por staff da Roblox
-- (abr/2026, "Working as Designed") como NUNCA capturadas pelo
-- ChangeHistoryService — o Ctrl+Z do usuário NUNCA reverte código
-- sincronizado. O risco é exclusivamente sobre a árvore de metadados.
--
-- Mitigação de 2 camadas, NENHUMA delas garantida sozinha — mesmo padrão já
-- usado no projeto para Source.Changed/detecção de delete
-- (.claude/rules/luau.md, "fast-path opcional + caminho garantido"):
--   1. Fast-path best-effort: `instance.Archivable = false` gravado ANTES de
--      `.Parent` em toda Instance criada sob TestService.SyncTeam
--      (TeamCreateSchema, TeamCreateElection, TeamCreateLease,
--      TeamCreatePresence, ScriptRegistry) — relato de comunidade NÃO
--      confirmado por staff, cobertura parcial (thread de 2015 citada na
--      pesquisa sugere que mudanças de PROPRIEDADE em cascata podem não ser
--      cobertas, só a criação).
--   2. Caminho garantido (ESTE MÓDULO): `ChangeHistoryService.OnUndo`/
--      `OnRedo` são eventos OFICIAIS e documentados (só entregam o NOME da
--      ação desfeita/refeita como string, nunca quais instances foram
--      afetadas — não dá pra ser seletivo). A cada disparo, roda uma
--      checagem de integridade dos módulos de coordenação, recriando o que
--      estiver faltando — "self-healing reativo", não prevenção.
--
-- Onde centralizar (decisão desta tarefa, deixada em aberto pelo
-- orquestrador): módulo pequeno dedicado, chamado 1x no boot de
-- init.server.luau (fora de start()/stop() — precisa escutar independente do
-- plugin estar conectado, já que Ctrl+Z pode acontecer a qualquer momento com
-- a place aberta). Cada módulo de coordenação expõe seu próprio
-- checkIntegrity() (TeamCreateElection/TeamCreateLease/TeamCreatePresence),
-- reaproveitando a lógica "criar se não existir" que cada um já tinha para o
-- próprio uso normal — nada duplicado aqui, este módulo só ORQUESTRA a
-- chamada dos três em resposta ao evento.
--
-- Escopo desta fatia: só os três módulos com checkIntegrity() dedicado
-- (Sessions/heartbeat, Leases, Presence — os citados explicitamente na
-- tarefa). ScriptRegistry.luau (Scripts/<uuid>) e TeamCreateSchema.luau
-- (root/ROOT_VALUES) ganharam só a camada 1 (Archivable=false); não há
-- checkIntegrity() dedicado pra eles nesta fatia — ver comentário em
-- ScriptRegistry.createOrAdoptRecord sobre o risco residual aceito.
--
-- Debounce: OnUndo/OnRedo podem disparar em rajada numa sequência de vários
-- Ctrl+Z seguidos. Sem fila/prioridade: só um `task.defer` por disparo (cede
-- um resumption point, suficiente pra coalescer disparos síncronos da mesma
-- rajada) — cada checkIntegrity() já é barata e idempotente (na maioria das
-- vezes só um FindFirstChild/checagem de Parent que não faz nada), então
-- disparos redundantes são inofensivos, não precisa de debounce por tempo.
local ChangeHistoryService = game:GetService("ChangeHistoryService")

local Logger = require(script.Parent.Logger)
local TeamCreateElection = require(script.Parent.TeamCreateElection)
local TeamCreateLease = require(script.Parent.TeamCreateLease)
local TeamCreatePresence = require(script.Parent.TeamCreatePresence)

local TeamCreateUndoGuard = {}

local log = Logger.log

local connections = {}

-- Ordem igual à de init.server.luau start(): Election antes de Lease/
-- Presence, porque getSessionFolder() (usado pelos outros dois) depende da
-- sessão já ter sido recriada. Cada checkIntegrity() já é no-op se o módulo
-- correspondente não estiver `enabled` (plugin desconectado) — protegido por
-- pcall individual: uma falha inesperada em um módulo não pode impedir os
-- outros dois de rodar (.claude/rules/luau.md, "falha degrada, nunca quebra
-- o plugin inteiro").
local function checkIntegrity()
	local ok1, err1 = pcall(TeamCreateElection.checkIntegrity)
	if not ok1 then
		log("undoGuard: falha checkIntegrity (Election):", tostring(err1))
	end
	local ok2, err2 = pcall(TeamCreateLease.checkIntegrity)
	if not ok2 then
		log("undoGuard: falha checkIntegrity (Lease):", tostring(err2))
	end
	local ok3, err3 = pcall(TeamCreatePresence.checkIntegrity)
	if not ok3 then
		log("undoGuard: falha checkIntegrity (Presence):", tostring(err3))
	end
end

local function onHistoryEvent(kindLabel)
	return function(waypointName)
		-- Rotineiro/alta frequência potencial (rajada de Ctrl+Z) e não
		-- necessariamente acionável (a grande maioria dos undos do usuário não
		-- tem nada a ver com o SyncTeam) -> Logger.debug, mesmo critério já
		-- usado no projeto (docs/DECISIONS.md 2026-07-20). O log VISÍVEL
		-- (Logger.log) só acontece dentro de cada checkIntegrity() quando algo
		-- de fato precisou ser recriado.
		Logger.debug(
			("undoGuard: %s detectado (%s) — verificando integridade de TestService.SyncTeam"):format(
				kindLabel,
				tostring(waypointName)
			)
		)
		task.defer(checkIntegrity)
	end
end

-- Chamado 1x no boot do plugin (init.server.luau), fora de start()/stop().
-- Idempotente: chamadas repetidas não duplicam conexões.
function TeamCreateUndoGuard.start()
	if #connections > 0 then
		return
	end

	-- Toda API de plataforma nova em pcall (.claude/rules/luau.md) — mesmo
	-- ChangeHistoryService sendo um serviço central do Studio, uma falha aqui
	-- (ambiente inesperado, versão antiga) degrada para "sem self-healing
	-- reativo", nunca quebra o resto do plugin.
	local okUndo, connOrErr = pcall(function()
		return ChangeHistoryService.OnUndo:Connect(onHistoryEvent("undo"))
	end)
	if okUndo then
		table.insert(connections, connOrErr)
	else
		log("undoGuard: falha ao conectar OnUndo:", tostring(connOrErr))
	end

	local okRedo, connOrErr2 = pcall(function()
		return ChangeHistoryService.OnRedo:Connect(onHistoryEvent("redo"))
	end)
	if okRedo then
		table.insert(connections, connOrErr2)
	else
		log("undoGuard: falha ao conectar OnRedo:", tostring(connOrErr2))
	end

	if #connections > 0 then
		Logger.debug("undoGuard: escutando ChangeHistoryService.OnUndo/OnRedo.")
	end
end

-- Chamado em plugin.Unloading, junto do resto do cleanup de ciclo de vida.
function TeamCreateUndoGuard.stop()
	for _, connection in connections do
		connection:Disconnect()
	end
	connections = {}
end

return TeamCreateUndoGuard
]]></string>
      </Properties>
    </Item>
    <Item class="Folder" referent="10">
      <Properties>
        <string name="Name">ui</string>
      </Properties>
      <Item class="ModuleScript" referent="11">
        <Properties>
          <string name="Name">PluginUI</string>
          <string name="Source"><![CDATA[-- SyncTeam — Orquestração do painel de status do plugin (M4.5)
--
-- Único ponto de contato entre init.server.luau (dono da lógica de
-- conexão/sessão/lease) e a UI (toolbar, DockWidgetPluginGui, Vide). Cria o
-- botão único de toolbar (toggle mostrar/esconder o painel — substitui os 3
-- botões antigos "Conectar"/"Parar"/"Alternar porta", spec da tarefa), o
-- DockWidgetPluginGui, monta a árvore Vide (StatusPanel.luau) dentro dele, e
-- expõe um API pequeno para init.server.luau empurrar mudanças de estado
-- (conectado/porta) e para Logger.notify acionar toasts.
--
-- Ciclo de vida DELIBERADAMENTE separado do start()/stop() de
-- init.server.luau: o painel (toolbar+widget) é criado 1x, no carregamento
-- do plugin, e sobrevive a reconexões/troca de porta (mesmo padrão que a
-- toolbar antiga já tinha — os botões não desapareciam a cada stop()+start()).
-- Só `PluginUI.stop()` (chamado em plugin.Unloading, separado da função
-- `stop()` de init.server.luau) desliga o refresh em segundo plano da
-- tabela de sessões.
--
-- Getters read-only usados (adicionados nesta mesma tarefa, ver
-- .claude/agent-memory/ui-dev.md para o raciocínio de cada um):
--   TeamCreateElection.getLeaderClientId() / .listSessions()
--   TeamCreateLease.forEachLease(callback)
--   TeamCreatePresence.getActiveUuid(clientId)
--   ScriptRegistry.getCanonicalPath(uuid) (já existia desde o M2)

local Config = require(script.Parent.Parent.Config)
local Logger = require(script.Parent.Parent.Logger)
local ScriptRegistry = require(script.Parent.Parent.ScriptRegistry)
local TeamCreateElection = require(script.Parent.Parent.TeamCreateElection)
local TeamCreateLease = require(script.Parent.Parent.TeamCreateLease)
local TeamCreatePresence = require(script.Parent.Parent.TeamCreatePresence)

local vide = require(script.Parent.Parent.Packages.Vide)
local StatusPanel = require(script.Parent.StatusPanel)
local Toast = require(script.Parent.Toast)

local PluginUI = {}

local log = Logger.log

-- ------------------------------------------------------------------ estado

local running = false
local currentToken = 0 -- mesmo idioma de token de geração do resto do plugin

local toastHandle = nil -- { show, destroy }, de Toast.mount() (ScreenGui em CoreGui)
local widget = nil -- DockWidgetPluginGui

-- Sources do Vide (estado reativo do painel). `connectionStatus`/`port` são
-- empurrados de fora (init.server.luau, via setConnectionStatus/setPort);
-- `view`/`notificationsEnabled`/`autoReconnectEnabled`/`sessionsMap` são
-- geridos aqui dentro. connectionStatus tem 3 valores (item 1 da tarefa):
-- "disconnected" (parado), "connecting" (tentando/reconectando), "connected"
-- (conexão confirmada). `autoReconnectEnabled` (2026-07-20) espelha
-- Config.AUTOSTART_SETTING_KEY — default `false` aqui só é o valor inicial
-- antes de PluginUI.init ler a setting real via
-- Config.resolveAutoStartEnabled (mesmo padrão de notificationsEnabledSource
-- abaixo, cujo default `true` também é só o placeholder pré-init).
local connectionStatusSource = vide.source("disconnected")
local portSource = vide.source(Config.DEFAULT_PORT)
local viewSource = vide.source("main")
local notificationsEnabledSource = vide.source(true)
local autoReconnectEnabledSource = vide.source(false)
-- "Nome de exibição customizado" (M4.5+, implementado de verdade em
-- 2026-08-02 — antes um placeholder "em breve"). Default "" aqui é só o
-- placeholder pré-init (mesmo padrão de notificationsEnabledSource/
-- autoReconnectEnabledSource acima); PluginUI.init lê o valor real via
-- Config.resolveCustomDisplayName.
local customDisplayNameSource = vide.source("")
-- "Liberar lease após (s)" (2026-08-02, docs/DECISIONS.md "8ª rodada",
-- handoff quase-instantâneo de lease). Default aqui é só o placeholder
-- pré-init (mesmo padrão dos demais Sources desta seção) — lido de
-- Config.PLACE_SETTINGS_DEFAULTS.leaseStaleAfterSeconds em vez de um número
-- mágico solto, para nunca divergir do default real se ele mudar em
-- Config.luau. PluginUI.init sobrescreve com o valor por-place de verdade via
-- Config.getPlaceSetting.
local leaseStaleAfterSecondsSource = vide.source(Config.PLACE_SETTINGS_DEFAULTS.leaseStaleAfterSeconds)
local sessionsMapSource = vide.source({})

-- ReSync (2026-08-02, docs/DECISIONS.md "5ª rodada"): estado visual do botão
-- RESYNC do painel. "idle" (padrão) -> "syncing" (empurrado por
-- init.server.luau assim que o clique dispara a mensagem espontânea
-- resyncRequest) -> "done" (init.server.luau recebe resyncResult ok=true,
-- volta sozinho a "idle" depois de ~1.2s) ou de volta direto a "idle" se
-- resyncResult vier com ok=false (e um Toast de erro separado, ver
-- init.server.luau). Mesmo padrão de setter externo que
-- connectionStatusSource/portSource já usam (PluginUI.setResyncState,
-- abaixo) — este módulo nunca decide sozinho quando mudar de estado, só
-- reflete o que init.server.luau empurra.
local resyncStateSource = vide.source("idle")
-- Modo do seletor entre CONNECT e RESYNC. "individual" e o default por ser o
-- que nao depende de mais ninguem estar online. Por enquanto so alimenta a UI:
-- nada no fluxo de sincronizacao le isto ainda.
local modeSource = vide.source("individual")

-- Item 1 da tarefa 2026-07-21 (linha INFO no gap entre título e Porta, ver
-- StatusPanel.luau): `infoTextSource` reflete a última mensagem IMPRESSA por
-- Logger.log/Logger.notify (Logger.getLastMessage(), sem prefixo de
-- timestamp) e `infoElapsedSecondsSource` os segundos decorridos desde
-- então — refreshInfoRow() (abaixo) recalcula os dois a cada 1s. Placeholder
-- inicial até a 1ª mensagem impressa desta sessão do plugin (deve ser quase
-- imediato: PluginUI.init loga "painel: inicializado." perto do fim de si
-- mesma, antes do 1º refreshInfoRow()).
local infoTextSource = vide.source("aguardando eventos...")
local infoElapsedSecondsSource = vide.source(0)

-- --------------------------------------------------------- tabela de sessões

-- Monta { [clientId] = RowData } a partir do estado já mantido por
-- TeamCreateElection/TeamCreateLease/TeamCreatePresence/ScriptRegistry — só
-- LEITURA, nenhum destes módulos é mutado aqui. RowData = { clientId,
-- username, joinSequence, isLeader, info }.
--
-- Decisão desta fatia (não 100% especificada na tarefa): inclui a PRÓPRIA
-- sessão na tabela (não só as remotas) — "quem mais está na sessão" também
-- se beneficia de mostrar o próprio status (sou eu o líder? o que eu estou
-- editando?) num relance só, sem precisar de uma seção separada "eu".
--
-- Prioridade INFO (spec da tarefa, em ordem): lease ativa no uuid ("editando
-- <path>") > presença sem lease ("vendo <path>") > "ocioso". Se uma sessão
-- tiver leases em MAIS de um uuid (extremo raro, ver TeamCreateLease.luau —
-- leases não expiram por troca de arquivo, só por inatividade), mostra a
-- PRIMEIRA encontrada por TeamCreateLease.forEachLease (ordem de iteração de
-- Leases/*, não garantida) — simplificação aceita: a tabela mostra "editando
-- algo", não necessariamente TUDO que a sessão está editando; sem essa
-- limitação a coluna INFO precisaria virar lista, fora do escopo desta v1
-- (spec: "texto único").
local function buildSessionsMap()
	local leaderClientId = TeamCreateElection.getLeaderClientId()
	local sessions = TeamCreateElection.listSessions()

	local editingUuidByClient = {}
	TeamCreateLease.forEachLease(function(uuid, ownerClientId)
		if editingUuidByClient[ownerClientId] == nil then
			editingUuidByClient[ownerClientId] = uuid
		end
	end)

	local map = {}
	for _, session in ipairs(sessions) do
		local info
		local editingUuid = editingUuidByClient[session.clientId]
		if editingUuid ~= nil then
			local path = ScriptRegistry.getCanonicalPath(editingUuid)
			info = ("editando %s"):format(path or editingUuid)
		else
			local viewingUuid = TeamCreatePresence.getActiveUuid(session.clientId)
			if viewingUuid ~= nil then
				local path = ScriptRegistry.getCanonicalPath(viewingUuid)
				info = ("vendo %s"):format(path or viewingUuid)
			else
				info = "ocioso"
			end
		end

		map[session.clientId] = {
			clientId = session.clientId,
			username = session.username,
			joinSequence = session.joinSequence,
			isLeader = leaderClientId ~= nil and session.clientId == leaderClientId,
			info = info,
		}
	end

	return map
end

local function refreshSessionsMap()
	local ok, result = pcall(buildSessionsMap)
	if ok then
		sessionsMapSource(result)
	else
		-- Nunca deixa uma leitura malformada (schema em transição de
		-- replicação, por exemplo) derrubar o loop de refresh inteiro —
		-- mesma disciplina de pcall em torno de leitura de estado
		-- compartilhado já usada no resto do plugin.
		log("painel: falha ao atualizar tabela de sessões:", tostring(result))
	end
end

-- ------------------------------------------------------------------- info row

-- Item 1 da tarefa 2026-07-21: poll de Logger.getLastMessage() (SEM callback
-- injetado — leitura passiva, mesma disciplina de polling de
-- .claude/rules/luau.md) chamado a cada 1s por um task.spawn próprio em
-- PluginUI.init (cadência independente de Config.POLL_INTERVAL_SECONDS/
-- refreshSessionsMap, que não precisa ser tão fina). `text == nil` (nenhuma
-- mensagem impressa ainda nesta sessão do plugin) deixa o placeholder
-- inicial de infoTextSource intacto. O contador nunca é "resetado"
-- explicitamente aqui: `os.clock() - updatedAt` já dá ~0 na primeira leitura
-- depois de uma mensagem nova, então só recalcular a cada 1s a partir do
-- MESMO `updatedAt` já produz o crescimento contínuo pedido — ele só some
-- para 0 de novo quando Logger.getLastMessage() devolver um `updatedAt` mais
-- recente (ou seja, uma mensagem nova de verdade chegou).
local function refreshInfoRow()
	local text, updatedAt = Logger.getLastMessage()
	if text == nil then
		return
	end
	if text ~= infoTextSource() then
		infoTextSource(text)
	end
	infoElapsedSecondsSource(math.max(0, math.floor(os.clock() - updatedAt)))
end

-- ------------------------------------------------------------------ notify

-- Injetado em Logger.initNotify por init.server.luau (dentro de start()) —
-- chamado por Logger.notify(...) para os poucos eventos "toast-worthy"
-- (lease negada, reconciliação de duplicata do schema, falha de reconexão
-- WS). O log no Output já aconteceu (Logger.notify faz isso incondicionalmente,
-- ANTES de chamar este callback) — aqui só decide se anima o toast visível,
-- olhando a preferência "Mostrar notificações".
--
-- Não força mais o painel (DockWidgetPluginGui) a abrir: desde que o toast
-- virou um ScreenGui flutuante independente em CoreGui (ver Toast.luau), ele
-- aparece sozinho sobre o Studio, sem depender do painel estar aberto — abrir
-- o painel à força seria intrusivo (o usuário pode tê-lo fechado de
-- propósito). Ressalva: a pesquisa indica que o overlay via CoreGui talvez só
-- cubra o viewport 3D; o encaminhamento por WS (Tools/) continua sendo o
-- registro garantido (Logger não imprime mais no Output desde 2026-08-02).
function PluginUI.notify(text)
	if not notificationsEnabledSource() then
		return
	end
	if toastHandle ~= nil then
		toastHandle.show(text)
	end
end

-- --------------------------------------------------------- setters (init.server.luau)

-- Empurrado por init.server.luau nas transições de conexão dentro de
-- runConnection/start/stop — puramente refletir estado, nenhuma lógica de
-- conexão mora aqui. `status` ∈ {"disconnected","connecting","connected"};
-- valores inválidos são ignorados (defensivo). Dedupe: só escreve a Source se
-- o valor mudou de verdade — runConnection chama "connecting" repetidamente a
-- cada volta do loop, e escrever o mesmo valor reexecutaria os effects do
-- botão à toa.
local VALID_STATUSES = { disconnected = true, connecting = true, connected = true }
function PluginUI.setConnectionStatus(status)
	if VALID_STATUSES[status] and status ~= connectionStatusSource() then
		connectionStatusSource(status)
	end
end

-- Empurrado por init.server.luau sempre que uma porta é tentada/confirmada/
-- persistida (auto-descoberta ou manual) — o campo de porta do painel
-- reflete isso ao vivo.
function PluginUI.setPort(port)
	if type(port) == "number" then
		portSource(math.floor(port))
	end
end

-- Empurrado por init.server.luau nas transições do fluxo ReSync (clique no
-- botão -> "syncing"; resyncResult ok=true -> "done" -> de volta a "idle"
-- sozinho depois de ~1.2s; resyncResult ok=false -> de volta a "idle" direto).
-- Mesmo padrão de dedupe/validação de PluginUI.setConnectionStatus acima.
local VALID_RESYNC_STATES = { idle = true, syncing = true, done = true }
function PluginUI.setResyncState(state)
	if VALID_RESYNC_STATES[state] and state ~= resyncStateSource() then
		resyncStateSource(state)
	end
end

-- ------------------------------------------------------------------ init/stop

-- Chamado 1x por init.server.luau, fora do ciclo start()/stop() (mesmo
-- lugar/momento em que a toolbar antiga era criada) — cria toolbar+widget,
-- monta a árvore Vide, e inicia o refresh em segundo plano da tabela de
-- sessões. `callbacks` = { onConnect, onDisconnect, onPortChange(newPort),
-- onAutoReconnectToggle(newValue), onResyncRequest() } (mesma
-- nomenclatura/contrato de start()/stop()/troca de porta/ReSync já
-- existentes em init.server.luau — este módulo só invoca, nunca reimplementa
-- a lógica de conexão/sincronização).
-- `onAutoReconnectToggle` é repassado DIRETO pro StatusPanel sem lógica
-- própria aqui (diferente de onNotificationsToggle, implementado abaixo só
-- com plugin:SetSetting): a ação de "ligar auto-reconectar tenta conectar já
-- respeitando o guard de Run/Play" precisa de start()/isInRunOrPlayMode(),
-- que só init.server.luau conhece.
function PluginUI.init(pluginObject, callbacks)
	if running then
		return
	end

	-- API de Studio nunca antes usada neste projeto (DockWidgetPluginGuiInfo/
	-- CreateDockWidgetPluginGui, ver .claude/research/2026-07-15-*)
	-- protegida por pcall — uma falha aqui degrada para "sem painel visível"
	-- em vez de derrubar o restante do plugin (conexão/sync continuam
	-- funcionando via init.server.luau independente disso).
	local ok, err = pcall(function()
		notificationsEnabledSource(Config.resolveNotificationsEnabled(pluginObject))
		autoReconnectEnabledSource(Config.resolveAutoStartEnabled(pluginObject))
		customDisplayNameSource(Config.resolveCustomDisplayName(pluginObject))
		leaseStaleAfterSecondsSource(Config.getPlaceSetting(pluginObject, "leaseStaleAfterSeconds"))

		local toolbar = pluginObject:CreateToolbar("SyncTeam")
		local toggleButton = toolbar:CreateButton(
			"syncteam-toggle-panel",
			"Mostra/esconde o painel de status do SyncTeam",
			"rbxassetid://84898837758979",
			"SyncTeam"
		)
		toggleButton.ClickableWhenViewportHidden = true

		local widgetInfo = DockWidgetPluginGuiInfo.new(
			Enum.InitialDockState.Right,
			false, -- initialEnabled: só vale na primeiríssima instalação; Studio persiste o resto
			false, -- não força esse valor por cima do estado persistido em runs futuros
			300, -- floatingXSize
			400, -- floatingYSize
			260, -- minWidth
			320 -- minHeight
		)
		widget = pluginObject:CreateDockWidgetPluginGui("SyncTeamPanel", widgetInfo)
		widget.Title = "SyncTeam"
		widget.Name = "SyncTeamPanel"

		toggleButton.Click:Connect(function()
			widget.Enabled = not widget.Enabled
		end)
		local function syncToggleButtonActiveState()
			pcall(function()
				toggleButton:SetActive(widget.Enabled)
			end)
		end
		syncToggleButtonActiveState()
		widget:GetPropertyChangedSignal("Enabled"):Connect(syncToggleButtonActiveState)

		local statusPanelCallbacks = {
			onConnect = callbacks.onConnect,
			onDisconnect = callbacks.onDisconnect,
			onPortSubmit = callbacks.onPortChange,
			-- Persistência da preferência é responsabilidade da UI (não de
			-- init.server.luau) — só afeta exibição de toast, nada de
			-- conexão/protocolo.
			onNotificationsToggle = function(newValue)
				local okSet, errSet = pcall(function()
					pluginObject:SetSetting(Config.NOTIFICATIONS_SETTING_KEY, newValue)
				end)
				if not okSet then
					log("falha ao salvar preferência de notificações:", tostring(errSet))
				end
			end,
			-- Passthrough puro (ver comentário de PluginUI.init acima): a
			-- persistência da setting E a tentativa de conectar já (quando
			-- ligado) vivem em init.server.luau, o único lugar que conhece
			-- start()/isInRunOrPlayMode().
			onAutoReconnectToggle = callbacks.onAutoReconnectToggle,
			-- "Nome de exibição customizado" (2026-08-02, saiu da lista "em
			-- breve"): SELF-CONTAINED aqui, não passthrough — diferente de
			-- onAutoReconnectToggle (que depende de start()/isInRunOrPlayMode(),
			-- só em init.server.luau), a ação completa (persistir a setting +
			-- atualizar o Username já publicado na sessão atual) só depende de
			-- Config/TeamCreateElection, os dois já requeridos no topo deste
			-- módulo — nenhuma lógica exclusiva de init.server.luau envolvida.
			onCustomDisplayNameSubmit = function(newValue)
				local okSet, errSet = pcall(function()
					pluginObject:SetSetting(Config.CUSTOM_DISPLAY_NAME_SETTING_KEY, newValue)
				end)
				if not okSet then
					log("falha ao salvar nome de exibição customizado:", tostring(errSet))
					return
				end
				-- Atualiza o Username já publicado na sessão atual (se houver) sem
				-- exigir reconectar — TeamCreateElection.setLocalUsername já degrada
				-- em no-op se a eleição não estiver rodando (start() ainda não
				-- chamado / desconectado no momento); o próximo start() lê a setting
				-- de novo do zero.
				TeamCreateElection.setLocalUsername(newValue)
			end,
			-- "Liberar lease após (s)" (2026-08-02, docs/DECISIONS.md "8ª rodada"):
			-- SELF-CONTAINED aqui, mesmo raciocínio de onCustomDisplayNameSubmit
			-- acima (Config já requerido no topo deste módulo, nenhuma lógica
			-- exclusiva de init.server.luau envolvida) — MAS, diferente de
			-- onCustomDisplayNameSubmit, não precisa de nenhuma chamada extra de
			-- "republicar agora": TeamCreateLease.readLiveIntents já relê
			-- Config.getPlaceSetting(..., "leaseStaleAfterSeconds") a cada ciclo
			-- do líder (até Config.POLL_INTERVAL_SECONDS = 0.5s depois), então só
			-- persistir a setting já é suficiente. StatusPanel.luau já validou/
			-- rejeitou não-positivo antes de chamar este callback — Config.
			-- setPlaceSetting valida de novo por conta própria (defesa em
			-- profundidade, não confia cegamente no chamador).
			onLeaseStaleAfterSecondsSubmit = function(newValue)
				local okSet, errSet = Config.setPlaceSetting(pluginObject, "leaseStaleAfterSeconds", newValue)
				if not okSet then
					log("falha ao salvar 'liberar lease após (s)':", tostring(errSet))
				end
			end,
			-- Passthrough puro (ReSync, 2026-08-02, docs/DECISIONS.md "5ª
			-- rodada" — mesmo padrão de onConnect/onDisconnect acima): o envio
			-- da mensagem espontânea `resyncRequest` e o estado visual
			-- "syncing" vivem em init.server.luau, o único lugar que conhece
			-- sendMessage/o client WS. Este módulo só invoca o callback e
			-- reflete o estado de volta via resyncState/PluginUI.setResyncState.
			onResyncRequest = callbacks.onResyncRequest,
			-- O seletor guarda a escolha e avisa quem estiver ouvindo. A
			-- decisao do que cada modo faz nao mora aqui — quando existir,
			-- entra por `callbacks.onModeChange`, do mesmo jeito que
			-- onResyncRequest.
			onModeChange = function(mode)
				if mode ~= modeSource() then
					modeSource(mode)
					if callbacks.onModeChange then
						callbacks.onModeChange(mode)
					end
				end
			end,
		}

		local state = {
			connectionStatus = connectionStatusSource,
			port = portSource,
			view = viewSource,
			notificationsEnabled = notificationsEnabledSource,
			autoReconnectEnabled = autoReconnectEnabledSource,
			customDisplayName = customDisplayNameSource,
			leaseStaleAfterSeconds = leaseStaleAfterSecondsSource,
			sessionsMap = sessionsMapSource,
			infoText = infoTextSource,
			infoElapsedSeconds = infoElapsedSecondsSource,
			resyncState = resyncStateSource,
			mode = modeSource,
		}

		-- `StatusPanel.build` (e tudo que ele cria por dentro — `vide.create`
		-- com propriedades reativas, `vide.indexes`, `vide.switch`) precisa
		-- rodar DENTRO do escopo de `vide.root()` que `vide.mount` empurra —
		-- essas funções chamam `assert_stable_scope()` internamente e lançam
		-- erro fora de um root/effect ativo (bug real corrigido nesta sessão:
		-- construir a árvore ANTES de `vide.mount` deixava o widget vazio).
		vide.mount(function()
			return StatusPanel.build(state, statusPanelCallbacks)
		end, widget)
	end)

	if not ok then
		log("painel: falha ao inicializar UI (toolbar/widget):", tostring(err))
		-- `warn` ALEM do log (2026-09-19): desde 2026-08-02 nenhuma das tres
		-- funcoes do Logger imprime no Output — todas so alimentam a linha INFO
		-- do painel e encaminham por WebSocket. Para ESTA falha especifica os
		-- dois canais estao mortos por definicao: o painel nao existe (foi o que
		-- falhou) e o WS ainda nao foi conectado (Logger.init roda dentro de
		-- start(), depois daqui). O resultado era um plugin que carrega sem erro
		-- nenhum, sem painel e sem uma palavra em lugar nenhum — custou uma
		-- investigacao inteira achar que a UI tinha morrido dentro deste pcall.
		-- O Output e o unico canal que sobra, entao ele e obrigatorio aqui.
		warn(
			("[SyncTeam %s] painel: falha ao inicializar UI (toolbar/widget): %s"):format(
				os.date("%H:%M:%S"),
				tostring(err)
			)
		)
		return
	end

	-- Toast flutuante (ScreenGui em CoreGui, ver Toast.luau) criado FORA do
	-- vide.mount do painel (é independente dele) e em pcall ISOLADO: uma falha
	-- de acesso a CoreGui degrada para "sem toast visível" sem impedir o
	-- refresh da tabela de sessões abaixo (o painel já montou com sucesso).
	local okToast, errToast = pcall(function()
		toastHandle = Toast.mount(pluginObject)
	end)
	if not okToast then
		log("painel: falha ao criar overlay de notificação (CoreGui):", tostring(errToast))
		-- Mesmo motivo do `warn` acima: falha de UI antes de existir painel ou WS.
		warn(
			("[SyncTeam %s] painel: falha ao criar overlay de notificação (CoreGui): %s"):format(
				os.date("%H:%M:%S"),
				tostring(errToast)
			)
		)
	end

	running = true
	currentToken += 1
	local myToken = currentToken

	refreshSessionsMap()
	task.spawn(function()
		while running and currentToken == myToken do
			task.wait(Config.POLL_INTERVAL_SECONDS)
			if running and currentToken == myToken then
				refreshSessionsMap()
			end
		end
	end)

	log("painel: inicializado.")

	-- Item 1 da tarefa 2026-07-21: cadência PRÓPRIA de 1s (o contador "Ns" da
	-- linha INFO precisa de granularidade de segundo, independente de
	-- Config.POLL_INTERVAL_SECONDS usado acima para a tabela de sessões) —
	-- populamento imediato (sem esperar o 1º task.wait) para o painel já
	-- abrir refletindo a linha "painel: inicializado." de log acima em vez de
	-- um placeholder por 1s.
	refreshInfoRow()
	task.spawn(function()
		while running and currentToken == myToken do
			task.wait(1)
			if running and currentToken == myToken then
				refreshInfoRow()
			end
		end
	end)
end

-- Chamado em plugin.Unloading (init.server.luau) — separado do stop() da
-- lógica de conexão/sessão, já que o painel deve sobreviver a
-- reconexões/troca de porta ao longo da vida do plugin.
function PluginUI.stop()
	if not running then
		return
	end
	running = false
	currentToken += 1
	-- Destrói o ScreenGui do toast em CoreGui — não é auto-limpo no unload do
	-- plugin (ao contrário do DockWidgetPluginGui), então sem isso cada reload
	-- vazaria um ScreenGui em CoreGui (mesma classe de vazamento do
	-- WebStreamClient documentada em docs/DECISIONS.md).
	if toastHandle ~= nil then
		pcall(function()
			toastHandle.destroy()
		end)
		toastHandle = nil
	end
end

return PluginUI
]]></string>
        </Properties>
      </Item>
      <Item class="ModuleScript" referent="12">
        <Properties>
          <string name="Name">StatusPanel</string>
          <string name="Source"><![CDATA[-- SyncTeam — Árvore de componentes Vide do painel de status (M4.5)
--
-- Constrói o conteúdo do DockWidgetPluginGui (painel principal: barra de
-- título com indicador de status, INFO, campo de porta, botão CONNECT,
-- botão RESYNC, tabela USER | INFO) mais a tela de configurações (troca de
-- "tela" dentro do MESMO widget via um Vide source, nunca um segundo
-- DockWidgetPluginGui — spec da tarefa original).
--
-- 2026-08-02 (reskin "Modux Companion", 7ª rodada): visual inteiro portado
-- 1:1 do mockup já aprovado (`design-preview/index.html`/`styles.css`) —
-- ver `Theme.luau` pros tokens de cor/fonte novos e
-- `.claude/agent-memory/ui-dev.md` pro raciocínio completo de cada decisão
-- não-óbvia desta rodada. Mudanças estruturais (não só cor):
--   - Layout deixou de ser "posições Y acumuladas manualmente" e passou a
--     usar `UIListLayout(VerticalAlignment = Center)` dentro de um Frame de
--     conteúdo — é a técnica nativa do Roblox equivalente a
--     `justify-content: center` do CSS, permite ao bloco inteiro (INFO/
--     Porta/Connect/ReSync/tabela) centralizar verticalmente no espaço
--     disponível do painel, em vez de ficar grudado no topo.
--   - `UICorner` removido de TUDO (painel, botões, campos, chip, líder) —
--     o reskin não usa cantos arredondados em nenhum elemento.
--   - Indicador de status (quadrado 10x10, sem corner) novo na titlebar.
--   - Negrito real via `Theme.applyBold` (Nunito não tem variante Bold no
--     enum) em títulos/labels/botões/campos — texto de linha da tabela
--     (username/info) é a ÚNICA exceção deliberada (dado dinâmico truncado,
--     pior de escanear centralizado/negrito).
--
-- Recebe de PluginUI.luau (dono do ciclo de vida/estado):
--   state = {
--     connectionStatus: Source<"disconnected" | "connecting" | "connected">,
--     port: Source<number>,
--     view: Source<"main" | "settings">,
--     notificationsEnabled: Source<boolean>,
--     autoReconnectEnabled: Source<boolean>,
--     customDisplayName: Source<string>,
--       -- "Nome de exibição customizado" (implementado de verdade em
--       -- 2026-08-02, saiu da lista de placeholders "em breve"). "" = usar o
--       -- nome do Roblox normalmente (default). Espelha
--       -- Config.CUSTOM_DISPLAY_NAME_SETTING_KEY, mesmo padrão de
--       -- notificationsEnabled/autoReconnectEnabled.
--     sessionsMap: Source<{ [clientId]: RowData }>,
--       -- RowData = { clientId, username, joinSequence, isLeader, info }
--     infoText: Source<string>,
--       -- última mensagem IMPRESSA por Logger.log/Logger.notify (ver
--       -- Logger.getLastMessage()), sem o prefixo "[SyncTeam HH:MM:SS]".
--       -- Placeholder até a 1ª mensagem desta sessão do plugin.
--     infoElapsedSeconds: Source<number>,
--       -- segundos decorridos desde infoText mudar pela última vez;
--       -- PluginUI.luau recalcula a cada 1s (task.wait(1)) a partir de
--       -- Logger.getLastMessage()'s `updatedAt` (os.clock()) — só volta a 0
--       -- quando uma mensagem NOVA de fato chega, nunca por conta própria.
--     resyncState: Source<"idle" | "syncing" | "done">,
--       -- estado visual do botão RESYNC; PluginUI.luau/init.server.luau controlam
--       -- as transições (enviar resyncRequest -> "syncing"; resposta resyncResult
--       -- -> "done" ou volta "idle"). Ver contrato em docs/DECISIONS.md (5ª rodada).
--     leaseStaleAfterSeconds: Source<number>,
--       -- "handoff quase-instantâneo de lease" (2026-08-02, docs/DECISIONS.md
--       -- "8ª rodada"): segundos de inatividade de Pulse antes de um intent de
--       -- edição ser considerado morto e a lease do arquivo liberar para o
--       -- próximo candidato (TeamCreateLease.luau/readLiveIntents). Espelha
--       -- Config.getPlaceSetting(pluginObject, "leaseStaleAfterSeconds")
--       -- (setting POR-PLACE, não global — mesmo mecanismo de
--       -- sourcePollIntervalSeconds/presencePollIntervalSeconds, ver
--       -- Config.luau). Default 2.
--   }
--   callbacks = {
--     onConnect: () -> (),
--     onDisconnect: () -> (),
--     onPortSubmit: (newPort: number) -> (),
--     onNotificationsToggle: (enabled: boolean) -> (),
--     onAutoReconnectToggle: (enabled: boolean) -> (),
--     onCustomDisplayNameSubmit: (newName: string) -> (),
--       -- confirmado (Enter/perda de foco) no TextBox de "Nome de exibição
--       -- customizado" — persiste a setting E atualiza o Username já
--       -- publicado na sessão atual, se houver (ver TeamCreateElection.setLocalUsername,
--       -- chamado por PluginUI.luau; este módulo só invoca, nunca reimplementa).
--     onModeChange: (mode: "individual" | "teams") -> (),
--       -- clique no seletor entre CONNECT e RESYNC. Passthrough puro: este
--       -- modulo nao decide nada sobre sincronizacao nem persiste a escolha,
--       -- so avisa. Quem reflete de volta e state.mode.
--     onResyncRequest: () -> (),
--       -- dispara o pedido de ReSync (limpar+repuxar Source do VS Code). Ver
--       -- contrato completo em docs/DECISIONS.md (5ª rodada) — você só chama,
--       -- quem envia a mensagem de verdade é PluginUI.luau/init.server.luau.
--     onLeaseStaleAfterSecondsSubmit: (newValue: number) -> (),
--       -- confirmado (Enter/perda de foco) no TextBox de "Liberar lease após
--       -- (s)". SELF-CONTAINED em PluginUI.luau (Config.setPlaceSetting) —
--       -- diferente de onCustomDisplayNameSubmit, não precisa de nenhuma
--       -- republicação em runtime: TeamCreateLease.readLiveIntents já relê
--       -- Config.getPlaceSetting a cada ciclo do líder (até 0.5s), então
--       -- persistir a setting já é suficiente para o próximo ciclo refletir.
--       -- Este módulo já valida/rejeita não-positivo ANTES de chamar (mesmo
--       -- padrão de PortRow.FocusLost) — o callback nunca recebe <= 0.
--   }
--
-- `state` é uma tabela de Sources do Vide — cada campo É a própria função
-- fonte (chamável sem args para ler, com 1 arg para escrever), não um par
-- getter/setter separado; StatusPanel só LÊ (exceto `view`,
-- `notificationsEnabled`, `autoReconnectEnabled` e `customDisplayName`, que
-- este módulo também escreve diretamente ao reagir a clique/edição — trocar
-- de tela e ligar/desligar/editar essas preferências são estado 100% local
-- de UI, não precisam de callback de volta pra PluginUI/init.server.luau só
-- para o VALOR mudar; `onNotificationsToggle`/`onAutoReconnectToggle`/
-- `onCustomDisplayNameSubmit` existem à parte porque a PERSISTÊNCIA (e, no
-- caso de auto-reconectar/nome customizado, a AÇÃO de tentar conectar já ou
-- de atualizar o Username já publicado — ver PluginUI.luau) precisa de
-- código que só quem chamou PluginUI.init conhece, nunca duplicado aqui).
--
-- NOTA (gap consciente, fora de escopo desta rodada): o mockup também
-- colore o TEXTO da linha INFO conforme a severidade da última mensagem
-- (neutro/sucesso/aviso/erro, ver `#main-info-box[data-color]` no CSS) —
-- isso exigiria um `state.infoColor` novo que PluginUI.luau/init.server.luau
-- ainda não fornecem (mesma disciplina de contrato negociado do RESYNC, ver
-- `.claude/agent-memory/ui-dev.md`). Por ora o texto da caixa INFO fica
-- sempre em `Theme.Color.SubText` (neutro) — sinalizar ao `luau-dev` se essa
-- coloração virar pedido real.
--
-- Decisões de layout (pixels/cores) não especificadas 100% pelo mockup do
-- usuário: documentadas em detalhe em .claude/agent-memory/ui-dev.md.

local TweenService = game:GetService("TweenService")

local vide = require(script.Parent.Parent.Packages.Vide)
local Theme = require(script.Parent.Theme)

local StatusPanel = {}

-- ------------------------------------------------------------------ layout
local TITLE_HEIGHT = Theme.Layout.TitleBarHeight
local ROW_GAP = 8 -- gap vertical entre linhas do bloco de conteúdo (mockup mistura 8/10px conforme a linha; uniformizado em 8, diferença de 2px aceita — ver memória do agente)
local ROW_INNER_GAP = 10 -- mockup: gap:10px entre label e campo dentro de InfoRow/PortRow
-- 2026-08-02: alturas das caixas (não mais iguais) bater com o Modux
-- Companion real — `portInput` = 28px (`UI.luau`), `infoBox` = 34px. A caixa
-- em si é `altura da linha - 10` (5px de margem fixa em cima/embaixo, ver
-- InfoRow/PortRow — `Position` y=5, `Size` "1,-10" relativo à própria linha),
-- então a linha precisa ser caixa+10.
local PORT_ROW_HEIGHT = 38 -- caixa de 28px (era 20px, linha de 30)
local INFO_ROW_HEIGHT = 44 -- caixa de 34px (era 20px, linha de 30 — desacoplada de PORT_ROW_HEIGHT)
-- 2026-08-02: reduzido de Theme.Layout.ButtonHeight (34, não usado em mais
-- nenhum lugar do plugin) pra bater com a altura dos botões de ação do
-- Modux Companion real (`actionRow` em UI.luau: Connect/Disconnect a 30px) —
-- pedido do usuário ("Connect... em tamanho menor").
local CONNECT_ROW_HEIGHT = 30
local MODE_ROW_HEIGHT = 26 -- entre CONNECT (30) e RESYNC (28): e um seletor, nao uma acao
-- 2026-09-19, 2ª passada: era `0.18, Quad, Out` e o usuário relatou que o
-- slider "parece que tá sendo criado" em vez de deslizar. Não era rebuild —
-- o ModeRow é construído UMA vez (`vide.switch` só reavalia em `state.view`,
-- nunca em `state.mode`). Era o easing: `Out` tem velocidade MÁXIMA em t=0,
-- então o bloco cruza metade do trilho no primeiro quarto do tempo. Com
-- 0,18s e a cor trocando no mesmo tween, o olho lê "sumiu um azul, apareceu
-- um vermelho" em vez de "o mesmo bloco andou".
--
-- `InOut` começa parado: o primeiro quadro mostra o bloco AINDA no lugar
-- antigo, que é o que amarra a identidade do objeto antes de ele se mover. O
-- tempo maior existe pelo mesmo motivo — o movimento precisa ser lido, não
-- só acontecer.
local MODE_TWEEN = TweenInfo.new(0.26, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut)
local RESYNC_ROW_HEIGHT = 28
local HEADER_HEIGHT = 22
-- Altura FIXA da tabela de sessões (era flex:1/preenchia o painel inteiro).
-- Mockup: `.st-table { flex: none; max-height: 150px; overflow-y: auto; }`
-- — Roblox não tem "AutomaticSize com teto", então a aproximação prática é
-- uma altura fixa (não cresce além disso, e o `ScrollingFrame` já rola
-- sozinho se o conteúdo passar). Só com uma altura FIXA (não "preenche o
-- resto") o bloco inteiro consegue centralizar como grupo dentro do
-- ContentWrapper — do contrário a tabela absorveria todo o espaço livre e
-- anularia o `VerticalAlignment = Center`.
local TABLE_HEIGHT = 150

local LABEL_COLUMN_WIDTH = 48 -- mockup: .st-inforow label / .st-portrow label { width: 48px } (era 44)
local STATUS_SQUARE_SIZE = 10 -- mockup: .st-status-square (10x10px, sem corner)
local INFO_SECONDS_CHIP_WIDTH = 40
-- 34 (era 18) — mesma altura da InfoBox agora, batendo com `infoTimestamp`
-- do Modux Companion real (mesma altura de `infoBox`, já que lá o
-- timestamp mora DENTRO da caixa de info, não numa caixa separada como aqui).
local INFO_SECONDS_CHIP_HEIGHT = 34

-- Split de colunas da tabela USER | INFO, em Scale (responsivo a
-- redimensionamento do painel/widget flutuante) — mesma fração usada pelo
-- cabeçalho e por cada linha, para colunas alinharem.
local USER_COLUMN_SCALE = 0.45
local ROW_LEADING_INSET = 24 -- espaço reservado pra bolinha/quadrado de líder antes do nome

-- ------------------------------------------------------- divisória (Divider)

-- Linha divisória sutil (mockup `.st-divider`, reusada em 2 lugares: topo do
-- bloco de conteúdo da tela principal, topo do corpo de Configurações) — 1px
-- na cor do texto com transparência alta, equivalente a
-- `rgba(242,242,242,0.12)` do CSS (Color3 não tem canal alpha).
-- Contorno fino das "caixas" (InfoBox/SecondsChip/PortInput) — 2026-08-02,
-- pedido do usuário: mesma transparência do Modux Companion real
-- (`infoBoxStroke`/`portInputStroke` em UI.luau: `Color = colors.text,
-- Transparency = 0.88, Thickness = 1`). Roblox NÃO tem transparência nativa
-- em `BorderSizePixel`/`BorderColor3` (só cor sólida) — por isso é preciso um
-- `UIStroke` de verdade, igual ao que o Modux já usa, em vez do
-- `BorderSizePixel=1`/`BorderColor3=Theme.Color.Border` de antes. Reaproveita
-- `Theme.Layout.DividerTransparency` (já 0.88, mesmo valor, mesmo usado no
-- `Divider`) em vez de um token novo.
--
-- `ApplyStrokeMode = Border` explícito (bug real reportado pelo usuário: a
-- borda do campo de Porta sumiu) — o padrão de `UIStroke.ApplyStrokeMode` é
-- `Contextual`, que fica praticamente invisível num `TextBox` (o Modux real
-- só seta `Border` explícito em `portInputStroke`, não em `infoBoxStroke` —
-- mas fixar `Border` sempre é seguro/idêntico visualmente num `Frame` também,
-- então usar num helper único evita o mesmo bug se este helper for reusado
-- em outro `TextBox`/`TextButton` no futuro).
local function BorderStroke()
	return vide.create "UIStroke" {
		Color = Theme.Color.Text,
		Transparency = Theme.Layout.DividerTransparency,
		Thickness = 1,
		ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
	}
end

local function Divider()
	return vide.create "Frame" {
		Name = "Divider",
		Size = UDim2.new(1, 0, 0, 1),
		BackgroundColor3 = Theme.Color.Text,
		BackgroundTransparency = Theme.Layout.DividerTransparency,
		BorderSizePixel = 0,
	}
end

-- ------------------------------------------------------- indicador de status

-- Quadrado 10x10, SEM corner, na barra de título — cor reflete a SAÚDE da
-- conexão (state.connectionStatus()), DELIBERADAMENTE com paleta diferente
-- do botão CONNECT logo abaixo (que reflete "ação disponível", não "saúde
-- da conexão"): disconnected=cinza/neutro, connecting=amarelo,
-- connected=verde; vermelho reservado para um futuro estado de erro
-- genuíno, ainda não modelado nos 3 valores atuais de connectionStatus. Ver
-- raciocínio completo em .claude/agent-memory/ui-dev.md.
local function StatusSquare(state)
	return vide.create "Frame" {
		Name = "StatusSquare",
		Size = UDim2.fromOffset(STATUS_SQUARE_SIZE, STATUS_SQUARE_SIZE),
		BackgroundColor3 = function()
			local status = state.connectionStatus()
			if status == "connected" then
				return Theme.Color.Green
			elseif status == "connecting" then
				return Theme.Color.Yellow
			end
			return Theme.Color.Muted
		end,
		BorderSizePixel = 0,
	}
end

-- ------------------------------------------------------------- título (main)

-- Barra de título: fundo é AGORA a MESMA cor do resto do painel (era um tom
-- mais escuro que destacava a barra como "card" — princípio do reskin: sem
-- blocos internos mais claros/escuros).
--
-- 2026-08-02: corrigido pra bater com o `UI.luau` REAL do Modux Companion
-- (apontado pelo usuário) — o indicador e o título NÃO formam um grupo
-- centralizado junto (era assim antes, via `UIListLayout(HorizontalAlignment
-- = Center)`, replicando uma leitura literal do CSS do mockup que também
-- estava errada nesse detalhe, já corrigida em design-preview/styles.css).
-- Na fonte real, `indicator` fica em posição FIXA no canto esquerdo
-- (`Position = UDim2.fromOffset(14, 17)`, independente do título) e `title`
-- ocupa a LARGURA INTEIRA do header com `TextXAlignment = Center` — ou seja,
-- o texto fica centralizado na barra TODA, ignorando a largura do quadrado,
-- em vez de "grupo quadrado+texto" centralizado como bloco (o que desloca o
-- texto pra direita do centro verdadeiro). `StatusSquare` recebe posição
-- fixa aqui em vez de participar de um layout horizontal.
-- 2026-08-02: header ganhou uma 2ª linha (resumo de status, igual ao
-- `status` TextLabel do UI.luau real — "Disconnected"/"Connected"/etc, ali
-- posicionado logo abaixo do título dentro do MESMO `header`) — por isso
-- este bloco cresceu de 1 linha (TITLE_HEIGHT=36, ainda usado por
-- SettingsView, que continua com só 1 linha) pra uma altura própria maior,
-- só pra este título. Ver `MAIN_HEADER_HEIGHT` abaixo.
local MAIN_HEADER_HEIGHT = 58

local function TitleBar(text, state)
	local titleLabel = vide.create "TextLabel" {
		Name = "Title",
		Size = UDim2.new(1, 0, 0, 24),
		Position = UDim2.new(0, 0, 0, 6),
		BackgroundTransparency = 1,
		Font = Theme.Font.Rounded,
		TextSize = 20,
		TextColor3 = Theme.Color.Text,
		TextXAlignment = Enum.TextXAlignment.Center,
		Text = text,
	}
	Theme.applyBold(titleLabel)

	-- Resumo de status (novo, pedido do usuário 2026-08-02) — mesma ideia do
	-- `status`/`setConnectionState` do UI.luau real: 1 linha central resumindo
	-- conexão + porta-alvo, fonte menor e cor mais acizentada que o título
	-- (Theme.Color.SubText, mesmo tom "secundário" já usado em outros labels
	-- do painel — não FieldText/Text, que são pro texto principal).
	local statusSummary = vide.create "TextLabel" {
		Name = "StatusSummary",
		Size = UDim2.new(1, 0, 0, 20),
		Position = UDim2.new(0, 0, 0, 32),
		BackgroundTransparency = 1,
		Font = Theme.Font.Rounded,
		TextSize = 16,
		TextColor3 = Theme.Color.SubText,
		TextXAlignment = Enum.TextXAlignment.Center,
		Text = function()
			local status = state.connectionStatus()
			local port = state.port()
			if status == "connected" then
				return `Connected - target port {port}`
			elseif status == "connecting" then
				return `Reconnecting - target port {port}`
			end
			return `Disconnected - target port {port}`
		end,
	}
	Theme.applyBold(statusSummary)

	-- Indicador fica alinhado com a linha do TÍTULO especificamente (não mais
	-- centralizado no header inteiro, que agora tem 2 linhas) — mesmo
	-- y aproximado do `indicator` real (`UDim2.fromOffset(14,17)` relativo ao
	-- header, que também é medido a partir do título, não do header todo).
	local statusSquare = StatusSquare(state)
	statusSquare.AnchorPoint = Vector2.new(0, 0)
	statusSquare.Position = UDim2.new(0, 12, 0, 13)

	return vide.create "Frame" {
		Name = "TitleBar",
		Size = UDim2.new(1, 0, 0, MAIN_HEADER_HEIGHT),
		BackgroundColor3 = Theme.Color.Background,
		BorderSizePixel = 0,

		titleLabel,
		statusSummary,
		statusSquare,
	}
end

-- ------------------------------------------------------------- linha de info

-- Label fixo "INFO:" (mesma largura/estilo do label "Porta" de PortRow, para
-- as duas colunas alinharem) + uma CAIXA somente-exibição (Frame+TextLabel,
-- NUNCA um TextBox — o usuário só vê a última mensagem, não digita ali) +
-- um selo ("chip") de segundos à direita da caixa. Campo/caixa usam o
-- MESMO fundo do painel (Theme.Color.Background) + borda fina padrão
-- (Theme.Color.Border) — nunca um tom de fundo diferente do resto
-- (princípio 1 do reskin).
local function InfoRow(state)
	local label = vide.create "TextLabel" {
		Name = "Label",
		Size = UDim2.new(0, LABEL_COLUMN_WIDTH, 1, 0),
		BackgroundTransparency = 1,
		Font = Theme.Font.Rounded,
		TextSize = 16,
		TextColor3 = Theme.Color.SubText,
		TextXAlignment = Enum.TextXAlignment.Center,
		Text = "Info",
	}
	Theme.applyBold(label)

	-- Cor sempre neutra (SubText) — ver NOTA no cabeçalho do arquivo sobre o
	-- gap consciente de `state.infoColor` (coloração por severidade do
	-- mockup, fora de escopo desta rodada).
	local boxText = vide.create "TextLabel" {
		Name = "Text",
		Size = UDim2.new(1, -16, 1, 0),
		Position = UDim2.new(0, 8, 0, 0),
		BackgroundTransparency = 1,
		Font = Theme.Font.Rounded,
		TextSize = 14,
		TextColor3 = Theme.Color.SubText,
		TextXAlignment = Enum.TextXAlignment.Center,
		TextTruncate = Enum.TextTruncate.AtEnd,
		Text = function()
			return state.infoText()
		end,
	}
	Theme.applyBold(boxText)

	local secondsText = vide.create "TextLabel" {
		Name = "Text",
		Size = UDim2.fromScale(1, 1),
		BackgroundTransparency = 1,
		Font = Theme.Font.Rounded,
		TextSize = 12,
		TextColor3 = Theme.Color.Muted,
		TextXAlignment = Enum.TextXAlignment.Center,
		Text = function()
			return ("%ds"):format(state.infoElapsedSeconds())
		end,
	}
	Theme.applyBold(secondsText)

	return vide.create "Frame" {
		Name = "InfoRow",
		Size = UDim2.new(1, 0, 0, INFO_ROW_HEIGHT),
		BackgroundTransparency = 1,

		label,

		vide.create "Frame" {
			Name = "InfoBox",
			Position = UDim2.new(0, LABEL_COLUMN_WIDTH + ROW_INNER_GAP, 0, 5),
			Size = UDim2.new(
				1,
				-(LABEL_COLUMN_WIDTH + ROW_INNER_GAP) - ROW_INNER_GAP - INFO_SECONDS_CHIP_WIDTH,
				1,
				-10
			),
			BackgroundColor3 = Theme.Color.Background,
			BorderSizePixel = 0,

			BorderStroke(),
			boxText,
		},

		vide.create "Frame" {
			Name = "SecondsChip",
			Position = UDim2.new(1, -INFO_SECONDS_CHIP_WIDTH, 0.5, -INFO_SECONDS_CHIP_HEIGHT / 2),
			Size = UDim2.new(0, INFO_SECONDS_CHIP_WIDTH, 0, INFO_SECONDS_CHIP_HEIGHT),
			BackgroundColor3 = Theme.Color.Background,
			BorderSizePixel = 0,

			BorderStroke(),
			secondsText,
		},
	}
end

-- --------------------------------------------------------------- campo de porta

local function PortRow(state, callbacks)
	-- Forward-declarado: o handler de FocusLost precisa ler `portBox.Text`
	-- (o valor JÁ EDITADO pelo usuário) depois que o evento dispara — closure
	-- captura o upvalue `portBox`, que já estará atribuído pelo momento em
	-- que o evento realmente for disparado (nunca antes: FocusLost só ocorre
	-- depois que este `vide.create` inteiro já retornou).
	local portBox
	portBox = vide.create "TextBox" {
		Name = "PortInput",
		Position = UDim2.new(0, LABEL_COLUMN_WIDTH + ROW_INNER_GAP, 0, 5),
		Size = UDim2.new(1, -(LABEL_COLUMN_WIDTH + ROW_INNER_GAP), 1, -10),
		BackgroundColor3 = Theme.Color.Background,
		BorderSizePixel = 0,
		-- Único campo monoespaçado do painel (mockup: "campos técnicos:
		-- porta, endereço") — `Enum.Font.Code` já era usado no projeto antes
		-- deste reskin, não é API nova.
		Font = Theme.Font.Mono,
		TextSize = 15,
		TextColor3 = Theme.Color.Text,
		TextXAlignment = Enum.TextXAlignment.Center,
		PlaceholderText = "porta",
		PlaceholderColor3 = Theme.Color.Muted,
		ClearTextOnFocus = false,
		Text = function()
			return tostring(state.port())
		end,

		-- Confirma em Enter OU perda de foco (os dois disparam FocusLost no
		-- Roblox; spec da tarefa pede os dois, sem distinguir motivo) —
		-- reusa a MESMA lógica de troca de porta já existente
		-- (onPortSubmit -> Config.PORT_SETTING_KEY + stop()+start(), ver
		-- PluginUI.luau/init.server.luau), nunca um mecanismo paralelo.
		FocusLost = function()
			local parsed = tonumber(portBox.Text)
			if parsed ~= nil and parsed > 0 and parsed == math.floor(parsed) then
				local newPort = math.floor(parsed)
				if newPort ~= state.port() then
					callbacks.onPortSubmit(newPort)
				end
			end
			-- Sempre volta a refletir o valor CONFIRMADO (não o texto cru
			-- digitado) — só muda de verdade quando PluginUI.setPort for
			-- chamado de volta, depois da reconexão de fato acontecer.
			-- Também corrige texto inválido (não-numérico, negativo etc.)
			-- sem travar o campo num estado ruim.
			portBox.Text = tostring(state.port())
		end,

		BorderStroke(),
	}
	Theme.applyBold(portBox) -- mockup: .st-textbox { font-weight: 700 } vale pro portBox E pro infoBox

	local label = vide.create "TextLabel" {
		Name = "Label",
		Size = UDim2.new(0, LABEL_COLUMN_WIDTH, 1, 0),
		BackgroundTransparency = 1,
		Font = Theme.Font.Rounded,
		TextSize = 16,
		TextColor3 = Theme.Color.SubText,
		TextXAlignment = Enum.TextXAlignment.Center,
		Text = "Porta",
	}
	Theme.applyBold(label)

	return vide.create "Frame" {
		Name = "PortRow",
		Size = UDim2.new(1, 0, 0, PORT_ROW_HEIGHT),
		BackgroundTransparency = 1,

		label,
		portBox,
	}
end

-- ----------------------------------------------------- CONNECT/DISCONNECT + engrenagem

local function ConnectRow(state, callbacks)
	-- Botão de 3 estados, cor+texto reativos a state.connectionStatus():
	--   "disconnected" -> verde-forte "Connect" (ação: conectar — call-to-action positiva)
	--   "connecting"   -> amarelo-forte "Conectando..." (tentando/reconectando em loop)
	--   "connected"    -> vermelho-forte "Disconnect" (ação: desconectar)
	-- Texto em Title Case (era ALL CAPS) — pedido do usuário 2026-08-02, mesmo
	-- estilo do UI.luau real ("Connect"/"Disconnect", nunca maiúsculo).
	-- Cores *Strong (nunca as claras, que são só texto/indicador — ver
	-- Theme.luau princípio 3) + texto branco PURO (ButtonText), regra nova
	-- do reskin sem exceção.
	local connectHovered = vide.source(false)
	local connectButton = vide.create "TextButton" {
		Name = "ConnectButton",
		Size = UDim2.new(1, -(Theme.Layout.IconButtonSize + 8), 1, 0),
		BackgroundColor3 = function()
			local status = state.connectionStatus()
			if status == "connected" then
				return if connectHovered() then Theme.Color.RedStrongHover else Theme.Color.RedStrong
			elseif status == "connecting" then
				return if connectHovered() then Theme.Color.YellowStrongHover else Theme.Color.YellowStrong
			end
			return if connectHovered() then Theme.Color.GreenStrongHover else Theme.Color.GreenStrong
		end,
		BorderSizePixel = 0,
		AutoButtonColor = false,
		Font = Theme.Font.Rounded,
		TextSize = 16,
		TextColor3 = Theme.Color.ButtonText,
		TextXAlignment = Enum.TextXAlignment.Center,
		Text = function()
			local status = state.connectionStatus()
			if status == "connected" then
				return "Disconnect"
			elseif status == "connecting" then
				return "Conectando..."
			end
			return "Connect"
		end,

		MouseEnter = function()
			connectHovered(true)
		end,
		MouseLeave = function()
			connectHovered(false)
		end,
		Activated = function()
			-- "disconnected" (parado) é o único estado cuja ação é CONECTAR;
			-- "connecting" e "connected" ambos oferecem PARAR — onDisconnect
			-- chama stop(), que zera o loop de runConnection (cancela a
			-- tentativa em voo ou encerra a conexão viva).
			if state.connectionStatus() == "disconnected" then
				callbacks.onConnect()
			else
				callbacks.onDisconnect()
			end
		end,
	}
	Theme.applyBold(connectButton)

	-- Engrenagem: virou botão-ícone TRANSPARENTE com borda fina (era fundo
	-- sólido cinza) — mockup `.st-icon-btn`: fundo transparente, borda
	-- padrão, hover = overlay claro sutil (rgba(242,242,242,0.08)). Sem
	-- negrito (mockup não define font-weight pra `.st-icon-btn`, diferente
	-- do `.st-back-btn` das Configurações, que É negrito — ver SettingsView).
	local gearHovered = vide.source(false)
	local gearButton = vide.create "TextButton" {
		Name = "SettingsGear",
		Size = UDim2.fromOffset(Theme.Layout.IconButtonSize, Theme.Layout.IconButtonSize),
		Position = UDim2.new(1, -Theme.Layout.IconButtonSize, 0.5, -Theme.Layout.IconButtonSize / 2),
		BackgroundColor3 = Theme.Color.Text,
		BackgroundTransparency = function()
			return if gearHovered() then Theme.Layout.HoverOverlayTransparency else 1
		end,
		BorderSizePixel = 1,
		BorderColor3 = Theme.Color.Border,
		Font = Theme.Font.Rounded,
		TextSize = 15,
		TextColor3 = Theme.Color.Text,
		Text = "\u{2699}", -- engrenagem (⚙) — abre a tela de configurações
		TextYAlignment = Enum.TextYAlignment.Center,

		MouseEnter = function()
			gearHovered(true)
		end,
		MouseLeave = function()
			gearHovered(false)
		end,
		Activated = function()
			state.view("settings")
		end,
	}

	return vide.create "Frame" {
		Name = "ConnectRow",
		Size = UDim2.new(1, 0, 0, CONNECT_ROW_HEIGHT),
		BackgroundTransparency = 1,

		connectButton,
		gearButton,
	}
end

-- ------------------------------------------------------------------ ReSync

-- Seletor de modo entre CONNECT e RESYNC: [ Individual | Teams ].
--
-- Um trilho com dois rotulos e UM frame deslizante por baixo marcando o
-- ativo. O frame anda com TweenPosition da esquerda para a direita — nao e
-- enfeite: o movimento e o que diz QUAL lado ficou ativo, num controle onde
-- os dois rotulos ficam visiveis o tempo todo e so a cor os distinguiria.
--
-- "individual" e o default e fica a esquerda, porque e o modo que nao depende
-- de mais ninguem estar online.
--
-- Este modulo so LE `state.mode()` e chama `callbacks.onModeChange(novo)` —
-- mesma disciplina do resto do StatusPanel. Quem decide o que cada modo faz
-- de verdade e quem persiste a escolha e PluginUI/init.server; clicar aqui
-- nao muda sincronizacao nenhuma por conta propria.
local function ModeRow(state, callbacks)
	local hovered = vide.source("")

	-- O deslizante e criado uma vez e movido por tween; recriar ou saltar a
	-- posicao mataria justamente a informacao que ele carrega.
	local slider = vide.create "Frame" {
		Name = "Slider",
		Size = UDim2.new(0.5, -2, 1, -4),
		Position = UDim2.fromOffset(2, 2),
		BackgroundColor3 = Theme.Color.BlueStrong, -- "individual" e o default; ver o efeito abaixo
		BorderSizePixel = 0,
	}
	vide.create "UICorner" { CornerRadius = UDim.new(0, 4), Parent = slider }

	-- CORREÇÃO (2026-09-19): a primeira versao usava `slider:TweenPosition(...)`
	-- e derrubava O PAINEL INTEIRO. O `TweenPosition` legado exige um GuiObject
	-- ja renderizado e lanca `Can only tween objects in the workspace` — e
	-- `vide.effect` roda a primeira avaliacao DURANTE a construcao, com o
	-- slider ainda sem pai. O erro subia pelo `vide.mount`, caia no `pcall` de
	-- `PluginUI.init` e sumia: desde 2026-08-02 nenhuma funcao do Logger
	-- imprime no Output, e nesse ponto nao ha nem painel nem WebSocket para
	-- receber o aviso. Plugin carregava limpo, sem UI e sem uma palavra.
	-- (`PluginUI.init` agora tambem faz `warn` nesse caminho, ver la.)
	--
	-- `TweenService:Create` nao tem essa restricao: anima qualquer Instance,
	-- com pai ou sem.
	local tweenAtual = nil
	local primeiraAvaliacao = true
	vide.effect(function()
		local aEsquerda = state.mode() ~= "teams"
		local destino = UDim2.new(if aEsquerda then 0 else 0.5, 2, 0, 2)
		-- Cor junto do movimento, no MESMO tween (2026-09-19, pedido do
		-- usuario): azul em "individual", vermelho em "teams". Sao os dois
		-- unicos estados, entao a cor nao esta repetindo o que a posicao ja
		-- diz — ela diz a mesma coisa por um canal que sobrevive a um olhar
		-- de relance, sem esperar o slide terminar.
		local cor = if aEsquerda then Theme.Color.BlueStrong else Theme.Color.RedStrong

		-- A avaliacao de construcao nao anima: nao ha de onde deslizar, e o
		-- destino ja e a posicao inicial. Animar so a partir da PRIMEIRA troca
		-- de modo de verdade e o que preserva o significado do movimento.
		if primeiraAvaliacao then
			primeiraAvaliacao = false
			slider.Position = destino
			slider.BackgroundColor3 = cor
			return
		end

		if tweenAtual ~= nil then
			tweenAtual:Cancel()
		end
		tweenAtual = TweenService:Create(slider, MODE_TWEEN, { Position = destino, BackgroundColor3 = cor })
		tweenAtual:Play()
	end)

	local function rotulo(texto, modo, xScale)
		local botao = vide.create "TextButton" {
			Name = texto,
			Size = UDim2.fromScale(0.5, 1),
			Position = UDim2.fromScale(xScale, 0),
			BackgroundTransparency = 1,
			AutoButtonColor = false,
			Font = Theme.Font.Rounded,
			TextSize = 13,
			-- O lado ativo usa ButtonText (mesma leitura do texto sobre
			-- botao colorido); o inativo cai para Muted e clareia no hover,
			-- para o alvo de clique ficar obvio sem competir com o ativo.
			TextColor3 = function()
				if state.mode() == modo then
					return Theme.Color.ButtonText
				end
				return if hovered() == modo then Theme.Color.SubText else Theme.Color.Muted
			end,
			Text = texto,

			MouseEnter = function()
				hovered(modo)
			end,
			MouseLeave = function()
				if hovered() == modo then
					hovered("")
				end
			end,
			Activated = function()
				if state.mode() ~= modo then
					callbacks.onModeChange(modo)
				end
			end,
		}
		Theme.applyBold(botao)
		return botao
	end

	local trilho = vide.create "Frame" {
		Name = "Trilho",
		Size = UDim2.fromScale(1, 1),
		BackgroundColor3 = Theme.Color.Border,
		BorderSizePixel = 0,

		slider,
		rotulo("Individual", "individual", 0),
		rotulo("Teams", "teams", 0.5),
	}
	vide.create "UICorner" { CornerRadius = UDim.new(0, 4), Parent = trilho }

	return vide.create "Frame" {
		Name = "ModeRow",
		Size = UDim2.new(1, 0, 0, MODE_ROW_HEIGHT),
		BackgroundTransparency = 1,

		trilho,
	}
end

-- Botão largura-cheia abaixo de ConnectRow, 3 estados visuais em
-- state.resyncState() ("idle"/"syncing"/"done" — este módulo só LÊ, mesma
-- disciplina do resto do StatusPanel). SEM lógica de envio de mensagem aqui
-- — Activated só chama callbacks.onResyncRequest(), passthrough puro
-- (contrato completo no cabeçalho do arquivo e em docs/DECISIONS.md).
--
-- Cores: "idle" = Theme.Color.Purple/PurpleHover (2026-08-02: era
-- Theme.Color.Border/cinza — pedido do usuário pra bater com o botão "Refresh
-- Snapshots" do Modux Companion real, que usa `colors.purple` — mesma leitura
-- semântica, ReSync É o "refresh" do SyncTeam). "syncing" = Theme.Color.
-- YellowStrong, "done" = Theme.Color.GreenStrong (nenhuma cor nova pra esses
-- dois — mesmas usadas pelo ConnectRow). SEM variação de hover em
-- "syncing"/"done" — mockup aprovado trata os dois como não-interativos
-- enquanto duram (clique só dispara callback no estado "idle").
local function ResyncRow(state, callbacks)
	local hovered = vide.source(false)

	local resyncButton = vide.create "TextButton" {
		Name = "ResyncButton",
		Size = UDim2.fromScale(1, 1),
		BackgroundColor3 = function()
			local resyncState = state.resyncState()
			if resyncState == "syncing" then
				return Theme.Color.YellowStrong
			elseif resyncState == "done" then
				return Theme.Color.GreenStrong
			end
			return if hovered() then Theme.Color.PurpleHover else Theme.Color.Purple
		end,
		BorderSizePixel = 0,
		AutoButtonColor = false,
		Font = Theme.Font.Rounded,
		-- 16 (era 15) — bate com Connect/Info/Porta/resumo de status, todos a
		-- 16px (pedido do usuário 2026-08-02: conferir consistência de fonte).
		TextSize = 16,
		TextColor3 = Theme.Color.ButtonText,
		TextXAlignment = Enum.TextXAlignment.Center,
		Text = function()
			local resyncState = state.resyncState()
			if resyncState == "syncing" then
				return "Syncing...."
			elseif resyncState == "done" then
				return "Done"
			end
			return "Resync"
		end,

		MouseEnter = function()
			hovered(true)
		end,
		MouseLeave = function()
			hovered(false)
		end,
		Activated = function()
			-- Só dispara em "idle" — "syncing"/"done" são estados transitórios
			-- não-clicáveis (mesmo espírito de CONNECT enquanto "connecting",
			-- que também não tem um terceiro clique paralelo escondido).
			if state.resyncState() == "idle" then
				callbacks.onResyncRequest()
			end
		end,
	}
	Theme.applyBold(resyncButton)

	return vide.create "Frame" {
		Name = "ResyncRow",
		Size = UDim2.new(1, 0, 0, RESYNC_ROW_HEIGHT),
		BackgroundTransparency = 1,

		resyncButton,
	}
end

-- --------------------------------------------------------- cabeçalho da tabela

-- Centralizado + negrito (era alinhado à esquerda) — mockup `.st-tableheader`.
local function TableHeader()
	local userLabel = vide.create "TextLabel" {
		Name = "User",
		Size = UDim2.new(USER_COLUMN_SCALE, -ROW_LEADING_INSET, 1, 0),
		Position = UDim2.new(0, ROW_LEADING_INSET, 0, 0),
		BackgroundTransparency = 1,
		Font = Theme.Font.Rounded,
		TextSize = 12,
		TextColor3 = Theme.Color.Muted,
		TextXAlignment = Enum.TextXAlignment.Center,
		Text = "USER",
	}
	Theme.applyBold(userLabel)

	local infoLabel = vide.create "TextLabel" {
		Name = "Info",
		Size = UDim2.new(1 - USER_COLUMN_SCALE, -8, 1, 0),
		Position = UDim2.new(USER_COLUMN_SCALE, 0, 0, 0),
		BackgroundTransparency = 1,
		Font = Theme.Font.Rounded,
		TextSize = 12,
		TextColor3 = Theme.Color.Muted,
		TextXAlignment = Enum.TextXAlignment.Center,
		Text = "INFO",
	}
	Theme.applyBold(infoLabel)

	return vide.create "Frame" {
		Name = "TableHeader",
		Size = UDim2.new(1, 0, 0, HEADER_HEIGHT),
		BackgroundTransparency = 1,

		userLabel,
		infoLabel,
	}
end

-- ------------------------------------------------------------- linha da tabela

-- rowSource: Source<RowData>, RowData = { clientId, username, joinSequence,
-- isLeader, info }. LayoutOrder = joinSequence garante ordem visual estável
-- via UIListLayout(SortOrder=LayoutOrder) — a ordem em que vide.indexes()
-- entrega os Instances (baseado em iteração de tabela/Map em Lua) NÃO é
-- garantida, então a ordem visual correta depende inteiramente do
-- LayoutOrder, nunca da ordem de criação/parenting.
local function SessionRow(rowSource)
	-- Alinhado à ESQUERDA (dado dinâmico truncado escaneia pior centralizado)
	-- mas NEGRITO como todo o resto — pedido explícito do usuário 2026-08-02
	-- ("negrito em todas as letras"), substitui a exceção antiga que deixava
	-- só estas 2 labels em peso normal (raciocínio anterior documentado em
	-- .claude/agent-memory/ui-dev.md, mantido só como histórico).
	local username = vide.create "TextLabel" {
		Name = "Username",
		Size = UDim2.new(USER_COLUMN_SCALE, -ROW_LEADING_INSET, 1, 0),
		Position = UDim2.new(0, ROW_LEADING_INSET, 0, 0),
		BackgroundTransparency = 1,
		Font = Theme.Font.Rounded,
		TextSize = 13,
		TextColor3 = Theme.Color.Text,
		TextXAlignment = Enum.TextXAlignment.Left,
		TextTruncate = Enum.TextTruncate.AtEnd,
		Text = function()
			return rowSource().username
		end,
	}
	Theme.applyBold(username)

	local info = vide.create "TextLabel" {
		Name = "Info",
		Size = UDim2.new(1 - USER_COLUMN_SCALE, -8, 1, 0),
		Position = UDim2.new(USER_COLUMN_SCALE, 0, 0, 0),
		BackgroundTransparency = 1,
		Font = Theme.Font.Rounded,
		TextSize = 13,
		TextColor3 = Theme.Color.SubText,
		TextXAlignment = Enum.TextXAlignment.Left,
		TextTruncate = Enum.TextTruncate.AtEnd,
		Text = function()
			return rowSource().info
		end,
	}
	Theme.applyBold(info)

	return vide.create "Frame" {
		Name = "Row",
		Size = UDim2.new(1, 0, 0, Theme.Layout.RowHeight),
		LayoutOrder = function()
			return rowSource().joinSequence
		end,
		-- MESMA cor em toda linha agora, sem alternância par/ímpar (era um
		-- tom mais claro nas linhas pares) — princípio 1 do reskin; a
		-- separação vem só da linha divisória abaixo.
		BackgroundColor3 = Theme.Color.Background,
		BorderSizePixel = 0,

		-- Quadrado de líder (era CÍRCULO/UICorner) — mesma linguagem "sem
		-- corner" do indicador de status da titlebar. Sempre criado (mesmo
		-- espaço reservado em toda linha, líder ou não — alinhamento
		-- consistente do nome entre linhas), só a visibilidade é reativa.
		vide.create "Frame" {
			Name = "LeaderDot",
			Size = UDim2.fromOffset(8, 8),
			Position = UDim2.new(0, 8, 0.5, -4),
			BackgroundColor3 = Theme.Color.Green,
			BorderSizePixel = 0,
			Visible = function()
				return rowSource().isLeader
			end,
		},

		username,
		info,

		-- Linha divisória sutil entre linhas (mockup: border-bottom
		-- rgba(242,242,242,0.08) em toda linha, exceto a última). Aplicada
		-- em TODA linha aqui, inclusive a última — simplificação deliberada:
		-- replicar o ":last-child" exigiria que cada linha soubesse sua
		-- posição relativa dentro do Map de vide.indexes (não é informação
		-- naturalmente disponível), complexidade desproporcional ao ganho
		-- visual de 1px — ver .claude/agent-memory/ui-dev.md.
		vide.create "Frame" {
			Name = "Divider",
			Position = UDim2.new(0, 0, 1, -1),
			Size = UDim2.new(1, 0, 0, 1),
			BackgroundColor3 = Theme.Color.Text,
			BackgroundTransparency = Theme.Layout.RowDividerTransparency,
			BorderSizePixel = 0,
		},
	}
end

-- Bug real reportado pelo usuário (screenshot, 2026-08-02): com a tabela
-- vazia (0 sessões, caso mais comum ao abrir o painel), os 150px fixos de
-- `TABLE_HEIGHT` viravam espaço morto ocupando quase metade do painel — o
-- bloco de conteúdo inteiro (Divider..TableHeader..Table) ficava ~367px
-- contra ~364px disponíveis no widget default (400 - TITLE_HEIGHT), ou seja,
-- SEM folga nenhuma pra `UIListLayout(VerticalAlignment=Center)` centralizar
-- de verdade — visualmente parecia "grudado no topo" com um vazio enorme
-- embaixo, exatamente o oposto do princípio do reskin ("tabela encolhe com
-- pouco conteúdo, cresce/rola só quando precisa" — mesma decisão já tomada
-- no mockup, `flex:none + max-height` em vez de `flex:1`). Fix: altura
-- REATIVA (função Vide, mesmo padrão de `BackgroundColor3` reativo já usado
-- em todo o arquivo) — cresce conforme o número real de sessões
-- (`Theme.Layout.RowHeight` + o gap de 2px do UIListLayout interno por
-- linha), até o teto de `TABLE_HEIGHT` (150px), onde passa a rolar
-- internamente (`AutomaticCanvasSize` já cobre isso). Com 0 sessões, altura
-- 0 — nenhum espaço morto, o resto do bloco fica livre pra centralizar de
-- verdade no espaço disponível.
local ROW_STRIDE = Theme.Layout.RowHeight + 2 -- +2 = UIListLayout Padding interno da tabela

local function SessionsTable(state)
	return vide.create "ScrollingFrame" {
		Name = "SessionsTable",
		Size = function()
			local rowCount = 0
			for _ in pairs(state.sessionsMap()) do
				rowCount += 1
			end
			return UDim2.new(1, 0, 0, math.min(rowCount * ROW_STRIDE, TABLE_HEIGHT))
		end,
		BackgroundTransparency = 1,
		BorderSizePixel = 0,
		ScrollBarThickness = 6,
		ScrollBarImageColor3 = Theme.Color.Muted,
		CanvasSize = UDim2.fromOffset(0, 0),
		AutomaticCanvasSize = Enum.AutomaticSize.Y,

		vide.create "UIListLayout" {
			SortOrder = Enum.SortOrder.LayoutOrder,
			Padding = UDim.new(0, 2),
		},

		-- Uma linha por sessão viva (vide.indexes: chave = clientId, mantém
		-- a MESMA Instance de linha entre refreshes em vez de recriar tudo
		-- do zero a cada poll — só linhas que entram/saem de Sessions/
		-- criam/destroem Instance).
		vide.indexes(state.sessionsMap, function(rowSource)
			return SessionRow(rowSource)
		end),
	}
end

-- ------------------------------------------------------------------ main view

-- Bug real reportado pelo usuário (screenshot, 2026-08-02, 2ª rodada de
-- correção): mesmo depois da tabela encolher, o título ficava "preso ao
-- topo" com um vão vazio grande antes da divisória — porque `TitleBar` era
-- um irmão FIXO no topo do widget inteiro, FORA do bloco que centraliza;
-- só `contentWrapper` (divisória pra baixo) centralizava. Isso diverge da
-- fonte real do Modux Companion (`UI.luau`, apontada pelo usuário): lá,
-- `header` (título) é o PRIMEIRO filho DENTRO do `panel` — o mesmo bloco de
-- altura fixa que centraliza inteiro via `AnchorPoint=(0,0.5)` +
-- `Position=(0, 0.5, 0)`. Corrigido: título agora é só mais um item dentro
-- do MESMO `UIListLayout`/`Panel`, que centraliza como bloco ÚNICO (título +
-- divisória + linhas + tabela) — técnica trocada de
-- `UIListLayout(VerticalAlignment=Center)` num frame de tamanho FIXO pra
-- `AutomaticSize.Y` + `AnchorPoint(0,0.5)`/`Position(0,0.5,0)` num frame de
-- altura VARIÁVEL (mesma técnica de `AnchorPoint`/`Position` que o
-- `UI.luau` real usa pro `panel`, só que com altura automática em vez de
-- fixa, já que a tabela de sessões deste painel pode crescer/encolher).
local function MainView(state, callbacks)
	local panel = vide.create "Frame" {
		Name = "Panel",
		Size = UDim2.new(1, 0, 0, 0),
		AutomaticSize = Enum.AutomaticSize.Y,
		AnchorPoint = Vector2.new(0, 0.5),
		Position = UDim2.new(0, 0, 0.5, 0),
		BackgroundTransparency = 1,

		vide.create "UIListLayout" {
			SortOrder = Enum.SortOrder.LayoutOrder,
			Padding = UDim.new(0, ROW_GAP),
		},

		TitleBar("Sync Team", state),
		Divider(),
		InfoRow(state),
		PortRow(state, callbacks),
		ConnectRow(state, callbacks),
		ModeRow(state, callbacks),
		ResyncRow(state, callbacks),
		TableHeader(),
		SessionsTable(state),
	}

	return vide.create "Frame" {
		Name = "MainView",
		Size = UDim2.fromScale(1, 1),
		BackgroundTransparency = 1,
		ClipsDescendants = true,

		vide.create "UIPadding" {
			PaddingLeft = UDim.new(0, Theme.Layout.Padding),
			PaddingRight = UDim.new(0, Theme.Layout.Padding),
			PaddingTop = UDim.new(0, Theme.Layout.Padding),
			PaddingBottom = UDim.new(0, Theme.Layout.Padding),
		},

		panel,
	}
end

-- -------------------------------------------------------------- settings view

local function SettingsRow(labelText, control, isDisabled)
	local label = vide.create "TextLabel" {
		Name = "Label",
		Size = UDim2.new(0.62, 0, 1, 0),
		BackgroundTransparency = 1,
		Font = Theme.Font.Rounded,
		TextSize = 15,
		TextColor3 = if isDisabled then Theme.Color.Muted else Theme.Color.Text,
		TextXAlignment = Enum.TextXAlignment.Left,
		TextWrapped = true,
		Text = labelText,
	}
	Theme.applyBold(label)

	return vide.create "Frame" {
		Name = "SettingsRow",
		Size = UDim2.new(1, 0, 0, 34), -- mockup: .st-settings-row { min-height: 34px }
		BackgroundTransparency = 1,

		label,
		control,
	}
end

-- Única opção real desta fatia: liga/desliga o toast; o encaminhamento por
-- WS NUNCA é condicionado a isso (Logger.notify sempre encaminha, ver
-- Logger.luau) — só a exibição visual passa por aqui. Fundo = Theme.Color.Border
-- (sólido, sem borda própria — mockup `.st-toggle { border: none; }`) +
-- texto branco puro negrito, hover = BorderHover.
local function NotificationsToggle(state, callbacks)
	local hovered = vide.source(false)
	local toggle = vide.create "TextButton" {
		Name = "NotificationsToggle",
		Size = UDim2.new(0.38, 0, 1, -6),
		Position = UDim2.new(0.62, 0, 0, 3),
		BackgroundColor3 = function()
			return if hovered() then Theme.Color.BorderHover else Theme.Color.Border
		end,
		BorderSizePixel = 0,
		AutoButtonColor = false,
		Font = Theme.Font.Rounded,
		TextSize = 13,
		TextColor3 = Theme.Color.ButtonText,
		TextXAlignment = Enum.TextXAlignment.Center,
		Text = function()
			return if state.notificationsEnabled() then "Ativado" else "Desativado"
		end,

		MouseEnter = function()
			hovered(true)
		end,
		MouseLeave = function()
			hovered(false)
		end,
		Activated = function()
			local newValue = not state.notificationsEnabled()
			state.notificationsEnabled(newValue)
			callbacks.onNotificationsToggle(newValue)
		end,
	}
	Theme.applyBold(toggle)
	return toggle
end

-- Segunda opção real desta fatia: liga/desliga Config.AUTOSTART_SETTING_KEY
-- — "conectar sozinho ao abrir a place/recarregar o plugin", sem relação
-- com reconexão após queda de sessão já viva (automática/incondicional, ver
-- Config.RECONNECT_SECONDS). Mesmo mirror visual de NotificationsToggle —
-- única diferença é que `callbacks.onAutoReconnectToggle` também pode AGIR
-- na hora (tentar conectar já, se ligado e desconectado — ver
-- init.server.luau, PluginUI.init).
local function AutoReconnectToggle(state, callbacks)
	local hovered = vide.source(false)
	local toggle = vide.create "TextButton" {
		Name = "AutoReconnectToggle",
		Size = UDim2.new(0.38, 0, 1, -6),
		Position = UDim2.new(0.62, 0, 0, 3),
		BackgroundColor3 = function()
			return if hovered() then Theme.Color.BorderHover else Theme.Color.Border
		end,
		BorderSizePixel = 0,
		AutoButtonColor = false,
		Font = Theme.Font.Rounded,
		TextSize = 13,
		TextColor3 = Theme.Color.ButtonText,
		TextXAlignment = Enum.TextXAlignment.Center,
		Text = function()
			return if state.autoReconnectEnabled() then "Ativado" else "Desativado"
		end,

		MouseEnter = function()
			hovered(true)
		end,
		MouseLeave = function()
			hovered(false)
		end,
		Activated = function()
			local newValue = not state.autoReconnectEnabled()
			state.autoReconnectEnabled(newValue)
			callbacks.onAutoReconnectToggle(newValue)
		end,
	}
	Theme.applyBold(toggle)
	return toggle
end

-- Terceira opção real desta fatia (2026-08-02, saiu da lista "em breve"):
-- TextBox editável — substitui, quando não-vazio, o nome resolvido via
-- Players:GetNameFromUserIdAsync como Username publicado em
-- Sessions/<clientId> (ver TeamCreateElection.luau/Config.resolveCustomDisplayName).
-- Mesmo visual/hover pattern dos 2 toggles booleanos acima (SettingsRow com
-- controle na coluna direita, 0.62/0.38, altura 34px da linha), adaptado
-- para um campo de TEXTO em vez de um TextButton binário — MESMA técnica de
-- FocusLost/forward-declaration já usada em PortRow acima (confirma em Enter
-- OU perda de foco, os dois disparam FocusLost no Roblox).
--
-- Fundo/borda seguem o padrão de "caixa somente-exibição vira editável"
-- (Theme.Color.Background + BorderStroke, mesmo do portBox) em vez do fundo
-- sólido Theme.Color.Border dos toggles — decisão: um TextBox editável já se
-- distingue visualmente de um TextButton de toggle só pelo cursor de texto/
-- caret, mas reforçar com a MESMA linguagem visual dos outros campos de
-- entrada do painel (PortRow) deixa claro "isto é um campo, não um botão" à
-- primeira vista.
local function CustomDisplayNameField(state, callbacks)
	local fieldBox
	fieldBox = vide.create "TextBox" {
		Name = "CustomDisplayNameField",
		Size = UDim2.new(0.38, 0, 1, -6),
		Position = UDim2.new(0.62, 0, 0, 3),
		BackgroundColor3 = Theme.Color.Background,
		BorderSizePixel = 0,
		Font = Theme.Font.Rounded,
		TextSize = 13,
		TextColor3 = Theme.Color.Text,
		TextXAlignment = Enum.TextXAlignment.Center,
		PlaceholderText = "nome do Roblox",
		PlaceholderColor3 = Theme.Color.Muted,
		ClearTextOnFocus = false,
		Text = function()
			return state.customDisplayName()
		end,

		-- Confirma em Enter OU perda de foco (mesmo padrão de PortRow.FocusLost
		-- acima) — grava a preferência E atualiza o Username já publicado na
		-- sessão atual sem exigir reconectar (callbacks.onCustomDisplayNameSubmit,
		-- contrato no cabeçalho do arquivo; quem executa de fato é
		-- PluginUI.luau/TeamCreateElection.setLocalUsername, este módulo só
		-- invoca). Campo vazio é um valor válido e INTENCIONAL (volta a usar o
		-- nome do Roblox) — só ignora se o texto não mudou de verdade, para não
		-- disparar o callback à toa a cada FocusLost sem edição real.
		FocusLost = function()
			local newValue = fieldBox.Text
			if newValue ~= state.customDisplayName() then
				state.customDisplayName(newValue)
				callbacks.onCustomDisplayNameSubmit(newValue)
			end
		end,

		BorderStroke(),
	}
	Theme.applyBold(fieldBox) -- mockup: .st-textbox { font-weight: 700 } — mesma regra do portBox/infoBox
	return fieldBox
end

-- Quarta opção real desta fatia (2026-08-02, "8ª rodada" — handoff
-- quase-instantâneo de lease, docs/DECISIONS.md): TextBox NUMÉRICO, mesmo
-- esqueleto visual/hover/FocusLost de CustomDisplayNameField acima (campo de
-- texto na SettingsRow, não toggle) — mas parse+validação de número seguem
-- PortRow (`tonumber` + rejeita não-positivo), já que aqui o valor é
-- segundos, não uma string livre. Diferente de PortRow: aceita fração
-- (2.5s é um valor razoável para este campo, ao contrário de porta) — não
-- força `math.floor`.
local function LeaseStaleAfterSecondsField(state, callbacks)
	local fieldBox
	fieldBox = vide.create "TextBox" {
		Name = "LeaseStaleAfterSecondsField",
		Size = UDim2.new(0.38, 0, 1, -6),
		Position = UDim2.new(0.62, 0, 0, 3),
		BackgroundColor3 = Theme.Color.Background,
		BorderSizePixel = 0,
		Font = Theme.Font.Mono, -- mesmo tratamento de PortRow: campo numérico/técnico
		TextSize = 13,
		TextColor3 = Theme.Color.Text,
		TextXAlignment = Enum.TextXAlignment.Center,
		PlaceholderText = "segundos",
		PlaceholderColor3 = Theme.Color.Muted,
		ClearTextOnFocus = false,
		Text = function()
			return tostring(state.leaseStaleAfterSeconds())
		end,

		-- Confirma em Enter OU perda de foco (mesmo padrão de
		-- PortRow/CustomDisplayNameField). Valida/rejeita não-positivo ANTES de
		-- chamar o callback (mesmo padrão de validação que
		-- Config.setPlaceSetting já aplica do lado do Config — checar aqui
		-- também evita mandar um valor claramente inválido pela ponte de
		-- callback só para ser rejeitado do outro lado). Texto inválido
		-- (não-numérico, <= 0) é descartado e o campo sempre volta a refletir
		-- o valor CONFIRMADO, nunca o texto cru digitado — mesma disciplina de
		-- PortRow.FocusLost.
		FocusLost = function()
			local parsed = tonumber(fieldBox.Text)
			if parsed ~= nil and parsed > 0 then
				if parsed ~= state.leaseStaleAfterSeconds() then
					state.leaseStaleAfterSeconds(parsed)
					callbacks.onLeaseStaleAfterSecondsSubmit(parsed)
				end
			end
			fieldBox.Text = tostring(state.leaseStaleAfterSeconds())
		end,

		BorderStroke(),
	}
	Theme.applyBold(fieldBox)
	return fieldBox
end

-- Itens "em breve" restantes (spec original tinha 3; "Nome de exibição
-- customizado" saiu desta lista em 2026-08-02, ver CustomDisplayNameField
-- acima — os outros 2 continuam sem funcionalidade real nesta fatia) —
-- texto estático à direita, sem controle nenhum.
local DISABLED_SETTINGS = {
	"Containers observados",
	"Nível de log",
}

local function DisabledSettingRow(labelText)
	local tag = vide.create "TextLabel" {
		Name = "Tag",
		Size = UDim2.new(0.38, 0, 1, 0),
		Position = UDim2.new(0.62, 0, 0, 0),
		BackgroundTransparency = 1,
		Font = Theme.Font.Rounded,
		TextSize = 12,
		TextColor3 = Theme.Color.Muted,
		TextXAlignment = Enum.TextXAlignment.Right,
		Text = "em breve",
	}
	Theme.applyBold(tag)
	return SettingsRow(labelText, tag, true)
end

local function SettingsView(state, callbacks)
	-- Botão-ícone transparente com borda fina, igual à engrenagem (mockup
	-- `.st-back-btn`) — ao contrário da engrenagem, ESTE é negrito (mockup:
	-- `.st-back-btn { font-weight: 700; }`).
	local backHovered = vide.source(false)
	local backButton = vide.create "TextButton" {
		Name = "BackButton",
		Size = UDim2.fromOffset(Theme.Layout.IconButtonSize, Theme.Layout.IconButtonSize),
		Position = UDim2.new(0, Theme.Layout.Padding, 0.5, -Theme.Layout.IconButtonSize / 2),
		BackgroundColor3 = Theme.Color.Text,
		BackgroundTransparency = function()
			return if backHovered() then Theme.Layout.HoverOverlayTransparency else 1
		end,
		BorderSizePixel = 1,
		BorderColor3 = Theme.Color.Border,
		Font = Theme.Font.Rounded,
		TextSize = 14,
		TextColor3 = Theme.Color.Text,
		Text = "\u{2190}", -- seta pra esquerda (←) — volta pra tela principal

		MouseEnter = function()
			backHovered(true)
		end,
		MouseLeave = function()
			backHovered(false)
		end,
		Activated = function()
			state.view("main")
		end,
	}
	Theme.applyBold(backButton)

	local titleLabel = vide.create "TextLabel" {
		Name = "Title",
		Size = UDim2.fromScale(1, 1),
		BackgroundTransparency = 1,
		Font = Theme.Font.Rounded,
		TextSize = 20,
		TextColor3 = Theme.Color.Text,
		TextXAlignment = Enum.TextXAlignment.Center,
		Text = "CONFIGURAÇÕES",
	}
	Theme.applyBold(titleLabel)

	local titleBar = vide.create "Frame" {
		Name = "TitleBar",
		Size = UDim2.new(1, 0, 0, TITLE_HEIGHT),
		BackgroundColor3 = Theme.Color.Background,
		BorderSizePixel = 0,

		titleLabel,
		backButton,
	}

	-- Mesma correção de MainView (ver comentário lá) — título deixa de ser
	-- irmão fixo no topo e vira o primeiro item do MESMO bloco centralizado
	-- (`AutomaticSize.Y` + `AnchorPoint`/`Position` 0.5), igual ao `panel` do
	-- `UI.luau` real.
	local panel = vide.create "Frame" {
		Name = "Panel",
		Size = UDim2.new(1, 0, 0, 0),
		AutomaticSize = Enum.AutomaticSize.Y,
		AnchorPoint = Vector2.new(0, 0.5),
		Position = UDim2.new(0, 0, 0.5, 0),
		BackgroundTransparency = 1,

		vide.create "UIListLayout" {
			SortOrder = Enum.SortOrder.LayoutOrder,
			Padding = UDim.new(0, 4),
		},

		titleBar,
		Divider(),
		SettingsRow("Mostrar notificações", NotificationsToggle(state, callbacks), false),
		SettingsRow("Reconectar automaticamente ao abrir a place", AutoReconnectToggle(state, callbacks), false),
		SettingsRow("Nome de exibição customizado", CustomDisplayNameField(state, callbacks), false),
		SettingsRow("Liberar lease após (s)", LeaseStaleAfterSecondsField(state, callbacks), false),
		DisabledSettingRow(DISABLED_SETTINGS[1]),
		DisabledSettingRow(DISABLED_SETTINGS[2]),
	}

	return vide.create "Frame" {
		Name = "SettingsView",
		Size = UDim2.fromScale(1, 1),
		BackgroundTransparency = 1,
		ClipsDescendants = true,

		vide.create "UIPadding" {
			PaddingLeft = UDim.new(0, Theme.Layout.Padding),
			PaddingRight = UDim.new(0, Theme.Layout.Padding),
			PaddingTop = UDim.new(0, Theme.Layout.Padding),
			PaddingBottom = UDim.new(0, Theme.Layout.Padding),
		},

		panel,
	}
end

-- ------------------------------------------------------------------- root

-- Constrói a árvore inteira (main view + settings view, trocadas por
-- vide.switch olhando state.view — NUNCA um segundo DockWidgetPluginGui,
-- spec da tarefa original) e devolve o rootFrame. Quem chama (PluginUI.luau)
-- parenta rootFrame dentro do DockWidgetPluginGui. O toast de notificação
-- NÃO é criado aqui: é um ScreenGui flutuante independente em CoreGui (ver
-- Toast.luau), montado por PluginUI.init fora deste vide.mount.
function StatusPanel.build(state, callbacks)
	return vide.create "Frame" {
		Name = "SyncTeamRoot",
		Size = UDim2.fromScale(1, 1),
		BackgroundColor3 = Theme.Color.Background,
		BorderSizePixel = 0,
		-- Higiene de painel: nada deve desenhar fora dos limites do widget.
		ClipsDescendants = true,

		vide.switch(state.view) {
			main = function()
				return MainView(state, callbacks)
			end,
			settings = function()
				return SettingsView(state, callbacks)
			end,
		},
	}
end

return StatusPanel
]]></string>
        </Properties>
      </Item>
      <Item class="ModuleScript" referent="13">
        <Properties>
          <string name="Name">Theme</string>
          <string name="Source"><![CDATA[-- SyncTeam — Constantes visuais do painel de status (M4.5)
--
-- Centraliza cor/espaçamento/fonte usados por StatusPanel.luau/Toast.luau/
-- PluginUI.luau — nenhum valor "mágico" solto nos componentes.
--
-- 2026-08-02 (reskin "Modux Companion", 7ª rodada): paleta INTEIRA
-- substituída pela aprovada no mockup `design-preview/styles.css` (bloco
-- `:root` com variáveis `--mc-*`, ver `.claude/agent-memory/ui-dev.md` pro
-- raciocínio completo de cada decisão). Fonte de verdade PIXEL-A-PIXEL é o
-- CSS do mockup — os valores abaixo são a tradução literal de cada
-- `--mc-*` para `Color3`. Paleta continua ESCURA FIXA (não reage ao tema
-- claro/escuro do Studio — mesma decisão pragmática de sempre, ver histórico
-- na memória do agente).
--
-- Princípios do reskin (valem para TODOS os componentes que usam este
-- Theme, não repetir em cada arquivo):
--   1. Fundo ÚNICO (`Background`) em TODO o painel — sem blocos internos
--      mais claros/escuros (titlebar, linhas de tabela, campos: todos a
--      MESMA cor de fundo; a separação visual vem de bordas finas/linhas
--      divisórias, nunca de tons de cinza diferentes).
--   2. Sem cantos arredondados em NENHUM elemento (por isso não existe mais
--      `Theme.Layout.CornerRadius` — o token foi removido, não zerado;
--      nenhum componente deveria referenciá-lo).
--   3. Toda cor semântica (verde/amarelo/vermelho) vem em PAR: a versão
--      clara (`Green`/`Yellow`/`Red`) é SÓ para texto/indicador sobre o
--      fundo escuro; a `*Strong` é SÓ para fundo de botão (sempre com
--      `ButtonText` branco puro em cima) — nunca inverter os dois papéis.
--      `Blue`/`Purple` não têm par `*Strong` (o mockup também não usa em
--      nenhum botão) — mantidos só como tokens disponíveis para o futuro.
--   4. Bordas finas (`BorderSizePixel = 1`, `BorderColor3 = Theme.Color.Border`)
--      são o ÚNICO contorno visual admitido (campos tipo "porta"/"info box",
--      botões-ícone transparentes) — nunca uma UIStroke customizada nem
--      cantos arredondados fazendo esse papel.
local Theme = {}

Theme.Color = {
	-- Fundo ÚNICO do painel inteiro (titlebar, conteúdo, linhas de tabela,
	-- campos tipo "porta"/"info box") — --mc-bg. Era um tom mais escuro na
	-- titlebar (destacava como "card"); agora tudo a mesma cor (princípio 1).
	Background = Color3.fromRGB(23, 23, 23),

	-- Borda padrão única, usada em TODO contorno do painel (campos, chip de
	-- segundos, botões-ícone transparentes, fundo sólido de RESYNC-idle/
	-- toggle) — --mc-border.
	Border = Color3.fromRGB(84, 84, 84),
	-- Hover de elementos cuja cor de fundo É o próprio Border (RESYNC idle,
	-- toggles de Configurações) — aproximação de `filter: brightness(1.2)`
	-- do CSS (Roblox não tem filtro de brilho; computado como
	-- canal*1.2, arredondado, já que não há um valor fixo equivalente no
	-- mockup — mesma técnica já aceita no projeto para hovers antigos).
	BorderHover = Color3.fromRGB(101, 101, 101),

	-- Texto principal (títulos, botões coloridos ficam com ButtonText à
	-- parte) — --mc-text. Também usado como base de linhas divisórias
	-- (ver Theme.Layout.*Transparency abaixo — Color3 não tem canal alpha).
	Text = Color3.fromRGB(242, 242, 242),
	-- Texto secundário (labels "INFO:"/"Porta", texto de linha INFO da
	-- tabela, corpo do texto de campos somente-leitura) — --mc-subtext.
	SubText = Color3.fromRGB(199, 199, 199),
	-- Texto discreto (selo de segundos, cabeçalho da tabela, opções "em
	-- breve") — --mc-muted.
	Muted = Color3.fromRGB(154, 154, 154),

	-- Cores de status/severidade, SEMPRE em par (ver princípio 3 acima).
	Green = Color3.fromRGB(150, 220, 150), -- --mc-green: indicador de status "connected", bolinha de líder
	GreenStrong = Color3.fromRGB(34, 130, 74), -- --mc-green-strong: fundo do CONNECT "disconnected" (ação = conectar) e do RESYNC "done"/toast "sucesso"
	GreenStrongHover = Color3.fromRGB(39, 150, 85), -- aproximação de brightness(1.15), mesmo método de BorderHover
	Yellow = Color3.fromRGB(235, 200, 120), -- --mc-yellow: indicador de status "connecting"
	YellowStrong = Color3.fromRGB(163, 120, 20), -- --mc-yellow-strong: fundo do CONNECT "connecting" e do RESYNC "syncing"/toast "aviso"
	YellowStrongHover = Color3.fromRGB(187, 138, 23),
	Red = Color3.fromRGB(230, 120, 120), -- --mc-red: indicador de status "error" (reservado, não modelado nos 3 estados atuais de connectionStatus)
	RedStrong = Color3.fromRGB(189, 45, 45), -- --mc-red-strong: fundo do CONNECT "connected" (ação = desconectar) e do toast "erro"
	RedStrongHover = Color3.fromRGB(217, 52, 52),
	-- --mc-blue / --mc-purple: SEM par "Strong" (o próprio mockup já avisa)
	-- e não usados em nenhum botão desta fatia — mantidos como tokens
	-- disponíveis, mesma decisão do CSS de origem.
	Blue = Color3.fromRGB(130, 170, 230),
	-- ÚNICA exceção à nota acima, aberta em 2026-09-19 para o seletor
	-- Individual/Teams (StatusPanel.ModeRow): o trilho é fundo CHEIO com
	-- texto branco, então precisa de um par `*Strong` como todo botão
	-- colorido do painel (princípio 3) — `Blue` puro é claro demais para
	-- carregar `ButtonText` em cima. Sem origem no CSS do mockup (que nunca
	-- usou azul como fundo), então derivado para casar de PESO com os Strong
	-- que já existem: luminância perceptual (0.299R + 0.587G + 0.114B) de
	-- 86,5 contra 88 do RedStrong e 95 do GreenStrong — as duas metades do
	-- seletor ficam com o mesmo peso visual, que é o ponto.
	BlueStrong = Color3.fromRGB(40, 92, 180),
	Purple = Color3.fromRGB(124, 77, 200), -- --mc-purple: fundo do RESYNC "idle" (2026-08-02 — era Border/cinza)
	PurpleHover = Color3.fromRGB(143, 89, 230), -- aproximação de brightness(1.15), mesma técnica de BorderHover/GreenStrongHover

	-- Regra nova (substitui a distinção antiga "idle discreto"): TODO botão
	-- colorido (fundo `*Strong` ou `Border`) usa texto branco PURO — nunca
	-- Theme.Color.Text (242,242,242), que é "quase branco" mas não put branco.
	ButtonText = Color3.fromRGB(255, 255, 255),
	-- ÚNICA exceção à regra acima: faixa "info"/genérica do Toast (fundo
	-- SubText claro + texto PRETO) — briefing explícito, ver Toast.luau.
	ToastInfoText = Color3.fromRGB(0, 0, 0),
}

Theme.Font = {
	-- Fonte "arredondada e amigável, mas sóbria" — confirmado por pesquisa
	-- que `Enum.Font.Nunito` EXISTE no enum atual do Roblox antes de
	-- depender dela (ver `.claude/research/2026-08-02-font-enum-fontface-
	-- fontweight-nunito.md`, regra do projeto para API de Font nova).
	-- Usada em TUDO no painel, exceto o campo técnico da porta (Mono).
	Rounded = Enum.Font.Nunito,
	-- Campo técnico (porta) — único lugar que foge da fonte arredondada
	-- (briefing: "campos técnicos: porta, endereço"). `Enum.Font.Code` já é
	-- usado no projeto há tempo (não é API nova, não precisa de confirmação
	-- de pesquisa) e é o mais próximo de "Cascadia Code" do mockup.
	Mono = Enum.Font.Code,
}

-- `Enum.Font.Nunito` não tem uma variante "*Bold" própria no enum (ao
-- contrário de `Gotham`/`GothamBold`) — pra negrito de verdade é preciso
-- `FontFace`/`Font.new` com `Enum.FontWeight.Bold`. Técnica CONFIRMADA por
-- pesquisa antes de codar (ver arquivo de pesquisa citado acima):
--   - NUNCA mutar `instance.FontFace.Weight` diretamente — `Font` é um
--     value type, a leitura devolve CÓPIA; mutar um campo da cópia não
--     reflete de volta na Instance (confirmado por 2 relatos de DevForum
--     independentes na pesquisa).
--   - Sempre montar um `Font` NOVO (`Font.new(family, weight, style)`) e
--     reatribuir o objeto INTEIRO de volta a `.FontFace`.
--   - Chamar SEMPRE depois que `.Font` (enum) já foi setado na Instance —
--     a engine mantém `.Font`/`.FontFace` sincronizados nos dois sentidos
--     (doc oficial: "kept in sync with"), então setar `.Font` DEPOIS
--     reconstruiria o `FontFace` a partir do enum (peso Regular),
--     desfazendo o override. `vide.create` já aplica todas as props
--     (inclusive `.Font`) antes de devolver a Instance, então chamar
--     `Theme.applyBold(instance)` logo após `vide.create "TextLabel" {...}`
--     retornar é sempre seguro.
function Theme.applyBold(instance)
	local face = instance.FontFace
	instance.FontFace = Font.new(face.Family, Enum.FontWeight.Bold, face.Style)
end

-- Espaçamentos/tamanhos usados em mais de um componente (evita repetir o
-- mesmo número mágico em StatusPanel/Toast/PluginUI). Layout exato em pixels
-- é decisão desta tarefa quando não especificado 1:1 pelo mockup — ver
-- memória do agente para o raciocínio completo.
Theme.Layout = {
	Padding = 12, -- mockup: .st-content/.st-settings-body { padding: 12px } (era 10)
	TitleBarHeight = 36,
	RowHeight = 30,
	ButtonHeight = 34,
	IconButtonSize = 28,
	-- SEM corner-radius: nenhum elemento do painel usa cantos arredondados
	-- no reskin (princípio 2 acima) — o token foi REMOVIDO de propósito
	-- (não zerado), nenhum componente deveria mais referenciá-lo.

	-- Transparências equivalentes às linhas/overlays `rgba(242,242,242,X)`
	-- do CSS — `Color3` do Roblox não tem canal alpha; o efeito é
	-- `BackgroundTransparency` numa Instance com `BackgroundColor3 = Theme.Color.Text`.
	DividerTransparency = 0.88, -- linha entre título e conteúdo (rgba(...,0.12))
	RowDividerTransparency = 0.92, -- linha entre linhas da tabela de sessões (rgba(...,0.08))
	HoverOverlayTransparency = 0.92, -- hover de botão-ícone transparente (rgba(...,0.08)) — MESMO valor de RowDividerTransparency por coincidência do CSS, não por relação semântica (nomes separados de propósito)
}

return Theme
]]></string>
        </Properties>
      </Item>
      <Item class="ModuleScript" referent="14">
        <Properties>
          <string name="Name">Toast</string>
          <string name="Source"><![CDATA[-- SyncTeam — Notificação flutuante (ScreenGui em CoreGui) do plugin (M4.5+)
--
-- Substitui o toast antigo, que vivia DENTRO do DockWidgetPluginGui do painel
-- sob a premissa (confirmada ERRADA por pesquisa) de que plugin de Studio só
-- tem DockWidgetPluginGui como superfície de UI. A pesquisa
-- (.claude/research/2026-07-15-plugin-floating-overlay-notification.md)
-- confirmou — inclusive no código-fonte público do Rojo — que um plugin PODE
-- criar um `ScreenGui` parentado direto em `game:GetService("CoreGui")`,
-- flutuando LIVREMENTE sobre o Studio (mesma técnica do popup do Rojo). Este
-- módulo cria esse ScreenGui + um Frame de aviso com botão de fechar ("X",
-- canto superior direito — ver item 3 da tarefa 2026-07-21 mais abaixo) que
-- some sozinho depois de HOLD_SECONDS (5s, item 2 da tarefa; era 4.5s no
-- toast antigo).
--
-- Independente do painel: aparece por conta própria, sem precisar mais forçar o
-- DockWidgetPluginGui a abrir (ver PluginUI.notify). Ressalva registrada na
-- pesquisa (a validar em teste real): o overlay via CoreGui provavelmente só
-- cobre a área do viewport 3D, não os painéis nativos (Explorer/Properties/
-- Output). O log no Output (Logger.notify, incondicional) continua sendo o
-- registro GARANTIDO de todo evento notificável.
--
-- SEM reatividade Vide: `vide.create` é usado só para montar Instances com
-- props ESTÁTICAS (estilo consistente via Theme) — o texto é setado
-- imperativamente em `show()`, o hover do botão de fechar idem, e a animação é
-- TweenService + task.delay. Assim este módulo NÃO depende de um escopo
-- `vide.root()`/`vide.mount` ativo (é criado direto por PluginUI.init, FORA do
-- vide.mount do painel), evitando o erro "effect() outside stable scope" que
-- confinava a versão anterior à árvore reativa do painel. (Eventos como
-- MouseEnter/Activated são apenas conexões de sinal — não criam effect, não
-- exigem escopo; só props com VALOR-função exigiriam.)
--
-- Só 1 notificação por vez (sem fila): uma nova chamada de `show()` reinicia
-- texto/tween/timer. Token de geração garante que um auto-dismiss antigo não
-- feche por cima de um `show()` mais novo, e que o botão fechar cancele o
-- auto-dismiss pendente.
--
-- Ciclo de vida: CoreGui persiste entre reloads do plugin (NÃO é auto-limpo
-- como o DockWidgetPluginGui) — por isso `mount()` faz self-heal (destrói um
-- ScreenGui vazado de um load anterior) e devolve `destroy()` para
-- PluginUI.stop() chamar em plugin.Unloading (mesma disciplina do vazamento de
-- WebStreamClient documentado em docs/DECISIONS.md).
--
-- 2026-08-02 (5ª rodada, aprovado no mockup design-preview/): redesenho
-- visual — antes o frame era UMA faixa (fundo único + aba/"Accent" de 4px
-- colorida à esquerda + botão X FLUTUANDO por cima do texto via
-- `Position = UDim2.new(1,-6,0,6)` sem reservar espaço no layout). Agora são
-- DUAS faixas verticais empilhadas, reproduzindo a mesma estrutura de
-- título+corpo do próprio painel (`Theme.Color.TitleBar` + fundo, ver
-- StatusPanel.TitleBar): a faixa de cima ("SYNC TEAM" + X, dentro do fluxo
-- normal — nenhum dos dois flutua por cima do outro) muda de cor conforme a
-- SEVERIDADE (`Toast.show(text, severity)`, novo 2º parâmetro OPCIONAL,
-- default `"info"` — chamadores existentes que só passam `text` continuam
-- funcionando sem editar nada); a faixa de baixo é o corpo da mensagem, fundo
-- neutro. Cores reaproveitadas das MESMAS 3 já usadas por ConnectRow
-- (`Theme.Color.ConnectIdle`/`ConnectConnecting`/`ConnectActive` — nenhuma
-- cor nova) e `Theme.Color.FieldBackground` do corpo (mesmo do portBox) — ver
-- `Theme.luau` para o comentário atualizado. Cantos retos (sem `UICorner`),
-- igual ao `.st-titlebar` do painel principal — o frame do toast antes tinha
-- `UICorner`, removido. O Frame "Accent" também foi removido (a cor de
-- severidade agora vive inteira na faixa de cima, sem precisar de uma aba
-- lateral separada).
--
-- 2026-08-02 (7ª rodada — reskin "Modux Companion", ver Theme.luau):
-- paleta/tokens do parágrafo acima foram SUBSTITUÍDOS (`ConnectIdle`/
-- `ConnectConnecting`/`ConnectActive`/`FieldBackground`/`ToastText`/
-- `Font.Title`/`Font.Body` não existem mais no Theme) — ver SEVERITY_STYLE
-- abaixo para o mapeamento atual (4 severidades: sucesso/aviso/erro em
-- `*Strong` + texto branco, "info" com fundo `SubText` + texto PRETO,
-- única exceção). Texto de título e corpo também passaram a centralizado +
-- negrito (princípio 3 do reskin).
--
-- Item 1 da tarefa 2026-07-21 (causa raiz de "o som nunca toca"): confirmado
-- contra a documentação oficial do Studio que, em modo Edit (fora de Play/Run
-- testing), o Studio só reproduz `Sound` quando a Instance é descendente do
-- `DockWidgetPluginGui` do PRÓPRIO plugin — um `Sound` parentado num
-- `ScreenGui` de `CoreGui` (o mesmo usado pelo toast visual acima) é
-- silenciosamente ignorado fora de Play/Run. Por isso `Toast.mount(pluginObject)`
-- passou a receber `pluginObject` (mesmo parâmetro que `PluginUI.init` já
-- recebe) e cria, 1x, um `DockWidgetPluginGui` "dummy" — sempre
-- `Enabled = false`, dimensões triviais, nunca aberto pelo usuário — que serve
-- SÓ como parent legal para o `Sound` de notificação (ver `soundWidget` e
-- `playNotificationSound` abaixo). Nada na UI visível do toast muda; é só o
-- destino do `Sound.Parent`.

local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local Debris = game:GetService("Debris")

local vide = require(script.Parent.Parent.Packages.Vide)
local Theme = require(script.Parent.Theme)
local Logger = require(script.Parent.Parent.Logger)
local log = Logger.log

local Toast = {}

local WIDTH = 300
-- 2026-08-02 (5ª rodada): duas faixas empilhadas — TITLE_BAND_HEIGHT reusa a
-- MESMA constante da barra de título do painel principal (Theme.Layout.
-- TitleBarHeight), para o toast ler como a mesma linguagem visual do resto
-- do produto; BODY_HEIGHT é decisão de layout desta tarefa (espaço suficiente
-- pra 2-3 linhas de mensagem curta, mesma leitura de antes).
local TITLE_BAND_HEIGHT = Theme.Layout.TitleBarHeight
local BODY_HEIGHT = 48
local HEIGHT = TITLE_BAND_HEIGHT + BODY_HEIGHT
local MARGIN = 16
local ENTER_SECONDS = 0.28
local EXIT_SECONDS = 0.22
local HOLD_SECONDS = 5 -- item 2 da tarefa: era 4.5 no toast antigo, agora 5

local SCREEN_GUI_NAME = "SyncTeamNotifications"

-- Severidade do toast (2026-08-02, 7ª rodada — reskin "Modux Companion",
-- substitui o esquema de 3 cores da 5ª rodada) — decide a cor de FUNDO e de
-- TEXTO da faixa de título; `Toast.show(text, severity)` tem `severity`
-- OPCIONAL, default "info". 4 severidades agora (fonte de verdade:
-- design-preview/styles.css, `.st-toast__titlebar[data-severity=...]`):
-- "sucesso"/"aviso"/"erro" usam a cor `*Strong` correspondente + texto branco
-- (Theme.Color.ButtonText); "info" é a ÚNICA exceção à regra de texto
-- branco — fundo claro neutro (Theme.Color.SubText) + texto PRETO
-- (Theme.Color.ToastInfoText), reservada pro caso genérico/sem severidade
-- reconhecida. `resolveSeverityStyle` cai para "info" tanto quando
-- `severity` é `nil` (chamador antigo, só passou `text`) quanto quando é uma
-- string desconhecida (defensivo).
local DEFAULT_SEVERITY = "info"
local SEVERITY_STYLE = {
	info = { background = Theme.Color.SubText, text = Theme.Color.ToastInfoText },
	sucesso = { background = Theme.Color.GreenStrong, text = Theme.Color.ButtonText },
	aviso = { background = Theme.Color.YellowStrong, text = Theme.Color.ButtonText },
	erro = { background = Theme.Color.RedStrong, text = Theme.Color.ButtonText },
}

local function resolveSeverityStyle(severity)
	return SEVERITY_STYLE[severity] or SEVERITY_STYLE[DEFAULT_SEVERITY]
end

-- Item 2 da tarefa 2026-07-21: nenhum código de Sound existia neste plugin
-- (grep em plugin/src inteiro sem resultado) — som nunca tocava porque nunca
-- foi implementado, não por um bug de wiring. Continua 1 som só, tocado em
-- TODA chamada de show() independente da severidade (2026-08-02, 5ª rodada,
-- introduziu variação de COR por severidade, não de som) — sem tabela por
-- nível como o ModuxWatcher tem hoje (id reaproveitado de lá,
-- ModuxWatcher/studio-plugin/src/Companion/Toast.luau linha 11, já
-- vetted pelo mesmo autor para esse mesmo uso "notification genérica").
local NOTIFICATION_SOUND_ID = "rbxassetid://131039887376992"

-- Ancorado no canto inferior-direito da tela (AnchorPoint 1,1). "Escondido" =
-- empurrado além da borda direita pela própria largura + margem; como a
-- posição é relativa à borda (Scale X = 1), o ScreenGui de tela cheia recorta
-- a parte fora e o slide-in funciona sem precisar de ClipsDescendants.
local HIDDEN_POSITION = UDim2.new(1, WIDTH + MARGIN + 40, 1, -MARGIN)
local VISIBLE_POSITION = UDim2.new(1, -MARGIN, 1, -MARGIN)

-- Cria o ScreenGui em CoreGui + o Frame de toast (oculto, fora da borda
-- direita) e devolve
-- { show = function(text: string, severity: ("info"|"aviso"|"erro")?), destroy = function() }.
-- `severity` é OPCIONAL (default "info", ver resolveSeverityColor acima) —
-- chamadas existentes que só passam `text` continuam funcionando sem editar
-- nada. Chamado 1x por PluginUI.init, fora do vide.mount do painel. `pluginObject` é
-- o mesmo parâmetro recebido por PluginUI.init — usado aqui SÓ para criar o
-- DockWidgetPluginGui dummy exigido pelo Studio como parent legal de `Sound`
-- em modo Edit (ver comentário no cabeçalho do arquivo, item 1 da tarefa
-- 2026-07-21).
-- Handle do ÚLTIMO Toast.mount() bem-sucedido — permite `Toast.show(...)`
-- ESTÁTICO (2026-08-02, 5ª rodada: init.server.luau chama Toast.show direto
-- na resposta de erro do ReSync, deliberadamente por FORA de PluginUI.notify/
-- toastHandle, pra sempre aparecer independente da preferência "Mostrar
-- notificações" — ver handleResyncResult) sem cada chamador precisar guardar/
-- repassar o handle retornado por mount(). Mesmo padrão já usado por Logger
-- (Logger.init guarda estado interno; chamadas estáticas subsequentes usam
-- esse estado) — só 1 instância montada por vez faz sentido aqui (o próprio
-- ScreenGui em CoreGui já é single-instance, ver self-heal abaixo).
local activeToast = nil -- { show, destroy }

function Toast.mount(pluginObject)
	-- Self-heal: remove um ScreenGui vazado de um load anterior do plugin
	-- (CoreGui não é limpo automaticamente ao recarregar o plugin — ao
	-- contrário do DockWidgetPluginGui, que o Studio gerencia).
	local existing = CoreGui:FindFirstChild(SCREEN_GUI_NAME)
	if existing then
		existing:Destroy()
	end

	-- Forward-declaradas: os handlers (tween/dismiss/hover) capturam estes
	-- upvalues, que já estarão atribuídos quando qualquer evento disparar (só
	-- ocorrem depois que este mount() inteiro já retornou). `titleBarFrame`/
	-- `titleText` (2026-08-02): `show()` precisa mudar fundo E cor do texto
	-- da faixa de título conforme a severidade a cada chamada (4 severidades,
	-- "info" é a única com texto preto — ver SEVERITY_STYLE acima).
	local frame
	local titleBarFrame
	local titleText
	local screenGui
	local currentToken = 0
	local soundWidget = nil

	local function tweenTo(position, seconds, direction)
		TweenService:Create(frame, TweenInfo.new(seconds, Enum.EasingStyle.Quad, direction), { Position = position })
			:Play()
	end

	-- Fecha imediatamente e invalida qualquer auto-dismiss pendente (bump de
	-- token, mesmo mecanismo do timer de show) — usado pelo botão de fechar.
	local function dismiss()
		currentToken += 1
		tweenTo(HIDDEN_POSITION, EXIT_SECONDS, Enum.EasingDirection.In)
	end

	-- Item 2 da tarefa 2026-07-21: toca o som de notificação toda vez que um
	-- toast é exibido (show(), abaixo). API de plataforma nova (Sound/Debris)
	-- em pcall (.claude/rules/luau.md) — uma falha aqui (asset removido,
	-- SoundService desabilitado etc.) nunca pode impedir o toast visual de
	-- aparecer. `Debris:AddItem(sound, 6)` garante o cleanup da Instance sem
	-- precisar de um handle/lista própria (mesmo padrão do ModuxWatcher).
	local function playNotificationSound()
		if soundWidget == nil then
			return
		end
		pcall(function()
			local sound = Instance.new("Sound")
			sound.SoundId = NOTIFICATION_SOUND_ID
			sound.Volume = 0.55
			sound.Parent = soundWidget
			sound:Play()
			Debris:AddItem(sound, 6)
		end)
	end

	-- Corpo da mensagem (faixa de baixo) — mesmo TextLabel de sempre, só
	-- reposicionado abaixo da faixa de título (2026-08-02, 5ª rodada): antes
	-- ocupava o frame inteiro (menos a folga do X flutuante), agora ocupa só
	-- BODY_HEIGHT, abaixo de TITLE_BAND_HEIGHT. Sem folga extra à direita —
	-- o X não flutua mais por cima do texto, mora na faixa de cima.
	-- 2026-08-02 (7ª rodada): texto centralizado + negrito (princípio 3 do
	-- reskin), fonte Rounded (era Body, removida do Theme), cor Text (era
	-- ToastText, removida do Theme) — mockup: `.st-toast__body` é
	-- `text-align:center; font-weight:700`.
	local textLabel = vide.create "TextLabel" {
		Name = "Text",
		Size = UDim2.new(1, -28, 0, BODY_HEIGHT - 20),
		Position = UDim2.new(0, 14, 0, TITLE_BAND_HEIGHT + 10),
		BackgroundTransparency = 1,
		Font = Theme.Font.Rounded,
		TextSize = 14,
		TextColor3 = Theme.Color.Text,
		TextXAlignment = Enum.TextXAlignment.Center,
		TextYAlignment = Enum.TextYAlignment.Center,
		TextWrapped = true,
		Text = "",
	}
	Theme.applyBold(textLabel)

	-- Botão de fechar — agora DENTRO do fluxo da faixa de título (não mais
	-- floating por cima do texto). Cor segue `resolveSeverityStyle(...).text`
	-- (branco nas 3 severidades coloridas, preto no fallback "info") —
	-- ToastCloseIdle/Hover (cinza sobre cinza) não faziam mais sentido aqui e
	-- foram removidos do Theme. Hover vira leve transparência (opacity ~0.75
	-- do mockup) em vez de troca de cor.
	local closeButton
	closeButton = vide.create "TextButton" {
		Name = "Close",
		Size = UDim2.fromOffset(22, 22),
		Position = UDim2.new(1, -6, 0, (TITLE_BAND_HEIGHT - 22) / 2),
		AnchorPoint = Vector2.new(1, 0),
		BackgroundTransparency = 1,
		AutoButtonColor = false,
		-- Item 3 da tarefa 2026-07-21: "\u{2715}" (✕ Unicode) renderizava como
		-- tofu/glifo quebrado no Studio — trocado por um "X" ASCII literal,
		-- que qualquer fonte do Studio sabe desenhar. Enum.Font.FredokaOne
		-- confirmado como nome exato do item no Font enum atual (consulta à
		-- documentação oficial, 2026-07-21: create.roblox.com/docs/reference/
		-- engine/enums/Font lista "FredokaOne" entre os itens válidos) — já é
		-- visualmente bold por design, sem precisar de Theme.Font.Bold aqui.
		Font = Enum.Font.FredokaOne,
		TextSize = 16,
		-- Segue a cor de texto da severidade ATUAL (branco nas 3 coloridas,
		-- preto no fallback "info") — valor inicial aqui é só o default antes
		-- da 1ª chamada de show(); show() atualiza a cada exibição.
		TextColor3 = resolveSeverityStyle(DEFAULT_SEVERITY).text,
		TextTransparency = 0,
		Text = "X", -- fechar

		-- Hover imperativo (sem vide.source, para não exigir escopo reativo).
		MouseEnter = function()
			closeButton.TextTransparency = 0.25
		end,
		MouseLeave = function()
			closeButton.TextTransparency = 0
		end,
		Activated = function()
			dismiss()
		end,
	}

	-- Faixa de cima ("SYNC TEAM" + X) — fundo e cor de texto são setados por
	-- severidade dentro de show() (resolveSeverityStyle); valores iniciais
	-- aqui são só o default "info" antes da primeira chamada de show().
	-- 2026-08-02 (7ª rodada): texto centralizado (era Left) + negrito, fonte
	-- Rounded (era Font.Title, removida do Theme) — mockup:
	-- `.st-toast__titlebar { justify-content:center; text-align:center;
	-- font-weight:700 }`. `Size` volta a ocupar a largura toda (era `-34` de
	-- folga fixa pro X) porque o X agora é posicionado ABSOLUTO por cima via
	-- AnchorPoint/Position (mesmo truque do CSS `position:absolute` +
	-- `justify-content:center` no pai) — center de verdade precisa considerar
	-- a largura inteira do frame, não só o espaço "sobrando" à esquerda do X.
	titleText = vide.create "TextLabel" {
		Name = "Title",
		Size = UDim2.fromScale(1, 1),
		BackgroundTransparency = 1,
		Font = Theme.Font.Rounded,
		TextSize = 15,
		TextColor3 = resolveSeverityStyle(DEFAULT_SEVERITY).text,
		TextXAlignment = Enum.TextXAlignment.Center,
		Text = "SYNC TEAM",
	}
	Theme.applyBold(titleText)

	titleBarFrame = vide.create "Frame" {
		Name = "TitleBar",
		Size = UDim2.new(1, 0, 0, TITLE_BAND_HEIGHT),
		Position = UDim2.fromOffset(0, 0),
		BackgroundColor3 = resolveSeverityStyle(DEFAULT_SEVERITY).background,
		BorderSizePixel = 0,

		titleText,
		closeButton,
	}

	-- Frame raiz — cantos RETOS (sem UICorner, igual ao .st-titlebar/
	-- .st-panel do painel principal); fundo é Theme.Color.Background (fundo
	-- ÚNICO do reskin, era Theme.Color.FieldBackground, removida — a faixa de
	-- cima cobre a área de cima com a cor de severidade, então o fundo do
	-- frame raiz só aparece mesmo na faixa de baixo/corpo). "Accent" removido
	-- — a cor de severidade vive inteira na faixa de título.
	frame = vide.create "Frame" {
		Name = "SyncTeamToast",
		Size = UDim2.fromOffset(WIDTH, HEIGHT),
		AnchorPoint = Vector2.new(1, 1),
		Position = HIDDEN_POSITION,
		BackgroundColor3 = Theme.Color.Background,
		BorderSizePixel = 0,
		ZIndex = 100,

		titleBarFrame,
		textLabel,
	}

	screenGui = vide.create "ScreenGui" {
		Name = SCREEN_GUI_NAME,
		ResetOnSpawn = false,
		ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
		DisplayOrder = 100, -- por cima de outros ScreenGuis (mesmo valor do Rojo)
		IgnoreGuiInset = true,

		frame,
	}
	screenGui.Parent = CoreGui

	-- Item 1 da tarefa 2026-07-21 (ver cabeçalho do arquivo): DockWidgetPluginGui
	-- dummy, único propósito é ser parent legal de `Sound` em modo Edit — nunca
	-- deve abrir/ficar visível. API de plataforma nova (DockWidgetPluginGuiInfo/
	-- CreateDockWidgetPluginGui) em pcall (.claude/rules/luau.md); falha aqui
	-- degrada para "toast visual funciona, som fica mudo" (ver guard em
	-- playNotificationSound abaixo), nunca derruba o toast inteiro.
	local okSoundWidget, errSoundWidget = pcall(function()
		local widgetInfo = DockWidgetPluginGuiInfo.new(
			Enum.InitialDockState.Float,
			false, -- initialEnabled: nunca deve abrir sozinho
			true, -- overrideEnabledRestore: força fechado TODA sessão, mesmo se
			-- o Studio tiver persistido um estado diferente (ao contrário
			-- do widget do painel em PluginUI.luau, que respeita o estado
			-- salvo do usuário de propósito)
			10,
			10,
			10,
			10 -- dimensões triviais: nunca deve ficar visível de verdade
		)
		soundWidget = pluginObject:CreateDockWidgetPluginGui("SyncTeamSoundPlayer", widgetInfo)
		soundWidget.Name = "SyncTeamSoundPlayer"
		soundWidget.Title = "SyncTeam Sound" -- distinto do título do painel ("SyncTeam")
		soundWidget.Enabled = false -- defensivo/redundante com override acima
	end)
	if not okSoundWidget then
		log(
			"toast: falha ao criar widget dummy p/ Sound (Edit mode); som de notificação ficará mudo:",
			tostring(errSoundWidget)
		)
	end

	-- `severity` OPCIONAL (2026-08-02) — default "info" via
	-- resolveSeverityStyle, então chamadas antigas que só passam `text`
	-- continuam funcionando sem qualquer ajuste no chamador. 4 severidades
	-- agora (7ª rodada): atualiza fundo E cor de texto da faixa (título +
	-- close), já que "info" é a exceção com texto preto.
	local function show(text, severity)
		currentToken += 1
		local myToken = currentToken
		textLabel.Text = text
		local style = resolveSeverityStyle(severity)
		titleBarFrame.BackgroundColor3 = style.background
		titleText.TextColor3 = style.text
		closeButton.TextColor3 = style.text
		playNotificationSound()

		TweenService:Create(
			frame,
			TweenInfo.new(ENTER_SECONDS, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
			{ Position = VISIBLE_POSITION }
		):Play()

		task.delay(HOLD_SECONDS, function()
			if currentToken ~= myToken then
				return -- um show()/dismiss() mais novo já assumiu; deixa o dele terminar
			end
			TweenService:Create(
				frame,
				TweenInfo.new(EXIT_SECONDS, Enum.EasingStyle.Quad, Enum.EasingDirection.In),
				{ Position = HIDDEN_POSITION }
			):Play()
		end)
	end

	-- Chamado por PluginUI.stop() em plugin.Unloading — destrói o ScreenGui de
	-- CoreGui (que não seria limpo sozinho). Bump de token para não deixar um
	-- auto-dismiss pendente tocar tween num Frame já destruído.
	local function destroy()
		currentToken += 1
		pcall(function()
			screenGui:Destroy()
		end)
	end

	local handle = { show = show, destroy = destroy }
	activeToast = handle
	return handle
end

-- Chamada ESTÁTICA de conveniência (2026-08-02, 5ª rodada) — ver comentário de
-- `activeToast` acima. Encaminha direto para o `show` da instância montada,
-- SEM nenhum gate de preferência (isso é responsabilidade de quem chama:
-- PluginUI.notify já checa notificationsEnabledSource ANTES de usar
-- toastHandle.show; init.server.luau usa Toast.show deliberadamente por fora
-- desse caminho quando quer uma notificação sempre visível). Nunca lança se
-- ainda não montado (ex.: PluginUI.init falhou/ainda não rodou) — só loga e
-- descarta, mesma disciplina defensiva do resto da UI do plugin.
function Toast.show(text, severity)
	if activeToast == nil then
		log("Toast.show chamado antes de Toast.mount(); notificação descartada:", tostring(text))
		return
	end
	activeToast.show(text, severity)
end

return Toast
]]></string>
        </Properties>
      </Item>
    </Item>
    <Item class="Folder" referent="15">
      <Properties>
        <string name="Name">Packages</string>
      </Properties>
      <Item class="ModuleScript" referent="16">
        <Properties>
          <string name="Name">Vide</string>
          <string name="Source"><![CDATA[return require(script.Parent._Index["centau_vide@0.4.1"]["vide"])
]]></string>
        </Properties>
      </Item>
      <Item class="Folder" referent="17">
        <Properties>
          <string name="Name">_Index</string>
        </Properties>
        <Item class="Folder" referent="18">
          <Properties>
            <string name="Name">centau_vide@0.4.1</string>
          </Properties>
          <Item class="ModuleScript" referent="19">
            <Properties>
              <string name="Name">vide</string>
              <string name="Source"><![CDATA[assert(game, "when using vide outside of Roblox, require lib.luau instead")

local vide = require(script.lib)

export type source<T> = vide.source<T>
export type Source<T> = vide.Source<T>
export type context<T> = vide.context<T>
export type Context<T> = vide.Context<T>

return vide
]]></string>
            </Properties>
            <Item class="ModuleScript" referent="20">
              <Properties>
                <string name="Name">action</string>
                <string name="Source"><![CDATA[type Action = {
    priority: number,
    callback: (Instance) -> ()
}

local ActionMT = table.freeze {}

local function is_action(v: any)
    return getmetatable(v) == ActionMT
end

local function action(callback: (Instance) -> (), priority: number?): Action
    local a = {
        priority = priority or 1,
        callback = callback
    }

    setmetatable(a :: any, ActionMT)

    return table.freeze(a)
end

return function()
    return action, is_action
end
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="21">
              <Properties>
                <string name="Name">apply</string>
                <string name="Source"><![CDATA[local typeof = game and typeof or require "../test/mock".typeof :: never

local flags = require "./flags"
local implicit_effect = require "./implicit_effect"
local _, is_action = require "./action"()
local graph = require "./graph"
type Node<T> = graph.Node<T>

type Array<V> = { V }
type ArrayOrV<V> = {ArrayOrV<V>} | V
type Map<K, V> = { [K]: V }

type Cache = {
    -- event listeners to connect after properties are set
    events: Array<
        | string -- 1. event name
        | () -> () -- 2. listener
    >,

    -- actions to run after events are connected
    actions: Map<
        number, -- priority
        Array<(Instance) -> ()> -- action callbacks
    >,

    -- what to parent the instance to after running actions
    parent: unknown,

    -- cache to detect duplicate property setting at same nesting depth
    nested_debug: Map<
        number, -- depth
        Map<string, true> -- set of property names
    >,

    -- each nested layer occupies two indexes: 1. table ref 2. nested depth
    -- e.g. { t1 = { t3 = {} }, t2 = {} } -> { t1, 1, t2, 1, t3, 2 }
    nested_stack: { {} | number }
}

local free_cache: Cache?

local function borrow_cache(): Cache
    if free_cache then
        local cache = free_cache
        free_cache = nil
        return cache
    else
        return {
            events = {},
            actions = setmetatable({} :: any, { -- lazy init
                __index = function(self, i) self[i] = {}; return self[i] end
            }),
            parent = nil,
            nested_debug = setmetatable({} :: any, {
                __index = function(self, i: number) self[i] = {}; return self[i] end
            }),
            nested_stack = {}
        }
    end
end

local function return_cache(cache: Cache )
    free_cache = cache
end

local function process_properties(properties: Map<unknown, unknown>, instance: Instance, cache: Cache, depth: number)
    for property, value in properties do
        if type(property) == "string" then
            if flags.strict then -- check for duplicate property assignment at nesting depth
                if cache.nested_debug[depth][property] then
                    error(`duplicate property {property} at depth {depth}`, 0)
                end
                cache.nested_debug[depth][property] = true
            end

            if property == "Parent" then
                cache.parent = value
                continue
            end

            if type(value) == "function" then 
                if typeof((instance :: any)[property]) == "RBXScriptSignal" then
                    table.insert(cache.events, property) -- add event name to buffer
                    table.insert(cache.events, value :: () -> ()) -- add event listener to buffer
                else
                    implicit_effect.property(instance, property, value :: () -> ()) -- create implicit effect for property
                end
            else
                (instance :: any)[property] = value -- set property
            end    
        elseif type(property) == "number" then
            if type(value) == "function" then
                implicit_effect.children(instance, value :: () -> ArrayOrV<Instance>) -- bind children
            elseif type(value) == "table" then
                if is_action(value) then
                    table.insert(cache.actions[(value :: any).priority], (value :: any).callback :: () -> ()) -- add action to buffer
                elseif flags.defer_nested_properties then
                    table.insert(cache.nested_stack, value :: {})
                    table.insert(cache.nested_stack, depth + 1) -- push table to stack for later processing
                else
                    process_properties(value :: Map<unknown, unknown>, instance, cache, depth + 1)
                end
            elseif type(value) == "userdata" then
                (value :: Instance).Parent = instance -- parent child
            end
        end
    end
end

-- applies table of nested properties to an instance using full vide semantics
local function apply<T>(instance: T & Instance, properties: { [unknown]: unknown }): T
    if not properties then
        error "attempt to call a constructor returned by create() with no properties"
    end

    local caches = borrow_cache()
    local events = caches.events
    local actions = caches.actions
    local nested_debug = caches.nested_debug
    local nested_stack = caches.nested_stack

    -- process all properties
    local depth = 1
    repeat
        process_properties(properties, instance, caches, depth)
        depth = table.remove(nested_stack) :: number
        properties = table.remove(nested_stack) :: {}
    until not properties

    for i = 1, #events, 2 do
        local event_name = events[i]
        local event_listener = events[i + 1]
        ;(instance :: any)[event_name]:Connect(event_listener)
    end

    for _, queued in actions do
        for _, callback in queued do
            callback(instance)
        end
    end

    local parent = caches.parent
    if parent then
        if type(parent) == "function" then
            implicit_effect.parent(instance, parent :: () -> Instance)
        else
            instance.Parent = parent :: Instance
        end
    end

    table.clear(events)
    for _, queued in actions do table.clear(queued) end
    caches.parent = nil
    if flags.strict then table.clear(nested_debug) end
    table.clear(nested_stack)

    return_cache(caches)

    return instance
end

return apply
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="22">
              <Properties>
                <string name="Name">batch</string>
                <string name="Source"><![CDATA[local flags = require "./flags"
local graph = require "./graph"

local function batch(setter: () -> ())
    local already_batching = flags.batch
    local from

    if not already_batching then
        flags.batch = true
        from = graph.get_update_queue_length()
    end

    local ok, err: string? = xpcall(setter, debug.traceback)

    if not already_batching then
        flags.batch = false
        graph.flush_update_queue(from)
    end

    if not ok then error(`error occured while batching updates: {err}`, 0) end
end

return batch
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="23">
              <Properties>
                <string name="Name">branch</string>
                <string name="Source"><![CDATA[local graph = require "./graph"
type Node<T> = graph.Node<T>
local create_node = graph.create_node
local push_scope = graph.push_scope
local pop_scope = graph.pop_scope
local destroy = graph.destroy
local get_scope = graph.get_scope

local function branch<T>(fn: () -> T): (() -> (), T)
    local current = get_scope()
    if not current then
        error(`cannot use branch() outside a stable or reactive scope`, 0)
    end

    local parent = current.owner
    if not parent then
        error(`current scope is not owned by a scope`, 0)
    end

    local node = create_node(parent, false, false)

    local destroy = function()
        destroy(node)
    end

    push_scope(node)

    local ok, result = xpcall(fn, debug.traceback)

    pop_scope()

    if not ok then
        destroy()
        error(`error while running branch():\n\n{result}`, 0)
    end

    return destroy, result
end

return branch
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="24">
              <Properties>
                <string name="Name">changed</string>
                <string name="Source"><![CDATA[local action = require "./action"()
local cleanup = require "./cleanup"

local function changed<T>(property: string, callback: (T) -> ())
    return action(function(instance)
        local con = instance:GetPropertyChangedSignal(property):Connect(function()
            callback((instance :: any)[property])
        end)

        cleanup(function()
            con:Disconnect()
        end)

        callback((instance :: any)[property])
    end)
end

return changed
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="25">
              <Properties>
                <string name="Name">cleanup</string>
                <string name="Source"><![CDATA[local typeof = game and typeof or require "../test/mock".typeof :: never

local graph = require "./graph"
local get_scope = graph.get_scope
local push_cleanup = graph.push_cleanup

local function helper(obj: any)
    return
        if typeof(obj) == "RBXScriptConnection" then function() obj:Disconnect() end
        elseif type(obj) == "thread" then function() task.cancel(obj) end
        elseif typeof(obj) == "Instance" then function() obj:Destroy() end
        elseif obj.destroy then function() obj:destroy() end
        elseif obj.disconnect then function() obj:disconnect() end
        elseif obj.Destroy then function() obj:Destroy() end
        elseif obj.Disconnect then function() obj:Disconnect() end
        else error "cannot cleanup given object"
end

local function cleanup(value: unknown)
    local scope = get_scope()

    if not scope then
        error "cannot cleanup outside a stable or reactive scope"
    end; assert(scope)

    if type(value) == "function" then
        push_cleanup(scope, value :: () -> ())
    else
        push_cleanup(scope, helper(value))
    end
end

type Destroyable = { destroy: (any) -> () } | { Destroy: (any) -> () }
type Disconnectable = { disconnect: (any) -> () } | { Disconnect: (any) -> () }

return cleanup ::
    ( (callback: () -> ()) -> () ) &
    ( (thread: thread) -> () ) &
    ( (instance: Destroyable) -> () ) &
    ( (connection: Disconnectable) -> () ) &
    ( (instance: Instance) -> () ) &
    ( (connection: RBXScriptConnection) -> () )

]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="26">
              <Properties>
                <string name="Name">context</string>
                <string name="Source"><![CDATA[local graph = require "./graph"
type Node<T> = graph.Node<T>
local create_node = graph.create_node
local get_scope = graph.get_scope
local push_scope = graph.push_scope
local pop_scope = graph.pop_scope
local set_context = graph.set_context

export type Context<T> = (() -> T) & (<U>(T, () -> U) -> U)

local nil_symbol = newproxy()
local count = 0

local function context<T>(...: T): Context<T>
    count += 1
    local id = count

    local has_default = select("#", ...) > 0
    local default_value = ...

    return function<T>(...): any -- todo: fix type error
        local scope: Node<unknown>? | false = get_scope()

        if select("#", ...) == 0 then -- get
            while scope do
                local ctx = scope.context
    
                if not ctx then
                    scope = scope.owner
                    continue
                end

                local value = (ctx :: { unknown })[id]

                if value == nil then
                    scope = scope.owner
                    continue
                end
                
                return (if value ~= nil_symbol then value else nil) :: T
            end

            if has_default ~= nil then
                return default_value
            else
                error("attempt to get context when no context is set and no default context is set", 0)
            end
        else -- set
            if not scope then return error("attempt to set context outside of a vide scope", 0) end

            local value, component = ...
            
            local new_scope = create_node(scope, false, false)
            set_context(new_scope, id, if value == nil then nil_symbol else value)

            push_scope(new_scope)

            local function efn(err: string) return debug.traceback(err, 3) end
            local ok, result = xpcall(component, efn)

            pop_scope()

            if not ok then
                error(`error while running context:\n\n{result}`, 0)
            end

            return result
        end

        return nil :: any
    end
end

return context
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="27">
              <Properties>
                <string name="Name">create</string>
                <string name="Source"><![CDATA[local typeof = game and typeof or require "../test/mock".typeof :: never
local Instance = game and Instance or require "../test/mock".Instance :: never

local defaults = require "./defaults"
local apply = require "./apply"
local flags = require "./flags"

local function create_constructor_for_class(class: string): ({ [unknown]: unknown }) -> Instance
    local function constructor(properties: { [unknown]: unknown }): Instance
        local ok, instance: Instance = pcall(Instance.new, class :: any)
        if not ok then error(`invalid class name {class}`, 0) end

        if flags.defaults then
            local default: { [string]: unknown }? = defaults[class]
            if default then
                for i, v in default do
                    (instance :: any)[i] = v
                end
            end
        end

        return apply(instance, properties)
    end

    return constructor
end

local constructor_cache = {} :: { [string]: ({ [unknown]: unknown }) -> Instance }

-- todo: remove support for different overloads
local function create(class_or_instance: string|Instance, properties: { [unknown]: unknown }?): unknown
    if type(class_or_instance) ~= "string" and typeof(class_or_instance) ~= "Instance" then
        error("bad argument #1, expected string or instance, got " .. typeof(class_or_instance), 0)
    end

    local constructor: ({ [unknown]: unknown }) -> Instance
    if type(class_or_instance) == "string" then
        constructor = constructor_cache[class_or_instance]
        if not constructor then
            constructor = create_constructor_for_class(class_or_instance)
            constructor_cache[class_or_instance] = constructor
        end
    else
        constructor = function(props)
            local clone = assert(class_or_instance:Clone(), "attempt to clone a non-archivable instance")
            return apply(clone, props)
        end
    end

    return if properties
        then constructor(properties)
        else constructor
end

type Instances = {
    Folder: Folder,
    BillboardGui: BillboardGui,
    CanvasGroup: CanvasGroup,
    Frame: Frame,
    ImageButton: ImageButton,
    ImageLabel: ImageLabel,
    ScreenGui: ScreenGui,
    ScrollingFrame: ScrollingFrame,
    SurfaceGui: SurfaceGui,
    TextBox: TextBox,
    TextButton: TextButton,
    TextLabel: TextLabel,
    UIAspectRatioConstraint: UIAspectRatioConstraint,
    UICorner: UICorner,
    UIGradient: UIGradient,
    UIGridLayout: UIGridLayout,
    UIListLayout: UIListLayout,
    Camera: Camera,
    WorldModel: WorldModel,
}

type function Properties(instance: type?)
    local properties = types.newtable()

    while instance do
        for i, v in instance:properties() do
            local connector = v.read and v.read.tag == "table" and v.read:readproperty(types.singleton("Connect"))
            if connector then
                local params = connector:parameters().head
                if not params then continue end
                local listener = params[2]
                if not listener then continue end
                properties:setproperty(i, types.optional(listener))
            elseif v.write then
                properties:setproperty(i, types.optional(types.unionof(
                    v.write,
                    types.newfunction({}, { head = { v.write } })
                )))
            end
        end

        instance = instance:readparent()
    end

    properties:setindexer(types.number, types.any)

    return properties
end

type Create = <Name>(Name|keyof<Instances>|"") -> (Properties<index<Instances, Name>>) -> index<Instances, Name>

return create :: Create
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="28">
              <Properties>
                <string name="Name">defaults</string>
                <string name="Source"><![CDATA[local Enum = game and Enum or require "../test/mock".Enum :: never
local Color3 = game and Color3 or require "../test/mock".Color3 :: never

return {
    Part = {
        Material = Enum.Material.SmoothPlastic,
        Size = vector.create(1, 1, 1),
        Anchored = true
    },

    BillboardGui = {
        ResetOnSpawn = false,
        ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    },

    CanvasGroup = nil,

    Frame = {
        BackgroundColor3 = Color3.new(1, 1, 1),
        BorderColor3 = Color3.new(0, 0, 0),
        BorderSizePixel = 0
    },

    ImageButton = {
        BackgroundColor3 = Color3.new(1, 1, 1),
        BorderColor3 = Color3.new(0, 0, 0),
        BorderSizePixel = 0,
        AutoButtonColor = false
    },

    ImageLabel = {
        BackgroundColor3 = Color3.new(1, 1, 1),
        BorderColor3 = Color3.new(0, 0, 0),
        BorderSizePixel = 0,
    },

    ScreenGui = {
        ResetOnSpawn = false,
        ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    },

    ScrollingFrame = {
        BackgroundColor3 = Color3.new(1, 1, 1),
        BorderColor3 = Color3.new(0, 0, 0),
        BorderSizePixel = 0,
        ScrollBarImageColor3 = Color3.new(0, 0, 0)
    },
    
    SurfaceGui = {
        ResetOnSpawn = false,
        ZIndexBehavior = Enum.ZIndexBehavior.Sibling,

        PixelsPerStud = 50,
        SizingMode = Enum.SurfaceGuiSizingMode.PixelsPerStud
    },
    
    TextBox = {
        BackgroundColor3 = Color3.new(1, 1, 1),
        BorderColor3 = Color3.new(0, 0, 0),
        BorderSizePixel = 0,
        ClearTextOnFocus = false,
        Font = Enum.Font.SourceSans,
        Text = "",
        TextColor3 = Color3.new(0, 0, 0)
    },

    TextButton = {
        BackgroundColor3 = Color3.new(1, 1, 1),
        BorderColor3 = Color3.new(0, 0, 0),
        BorderSizePixel = 0,
        AutoButtonColor = false,
        Font = Enum.Font.SourceSans,
        Text = "",
        TextColor3 = Color3.new(0, 0, 0)
    },
    
    TextLabel = {
        BackgroundColor3 = Color3.new(1, 1, 1),
        BorderColor3 = Color3.new(0, 0, 0),
        BorderSizePixel = 0,
        Font = Enum.Font.SourceSans,
        Text = "",
        TextColor3 = Color3.new(0, 0, 0)
    },
    
    UIListLayout = {
        SortOrder = Enum.SortOrder.LayoutOrder
    },
   
    UIGridLayout = {
        SortOrder = Enum.SortOrder.LayoutOrder
    },

    UITableLayout = {
        SortOrder = Enum.SortOrder.LayoutOrder
    },

    UIPageLayout = {
        SortOrder = Enum.SortOrder.LayoutOrder
    },
    
    VideoFrame = {
        BackgroundColor3 = Color3.new(1, 1, 1),
        BorderColor3 = Color3.new(0, 0, 0),
        BorderSizePixel = 0
    },
    
    ViewportFrame = {
        BackgroundColor3 = Color3.new(1, 1, 1),
        BorderColor3 = Color3.new(0, 0, 0),
        BorderSizePixel = 0
    }
} :: { [string]: { [string]: unknown} }
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="29">
              <Properties>
                <string name="Name">derive</string>
                <string name="Source"><![CDATA[local graph = require "./graph"
local create_node = graph.create_node
local push_scope_as_child_of = graph.push_scope_as_child_of
local assert_stable_scope = graph.assert_stable_scope
local evaluate_node = graph.evaluate_node

local function derive<T>(source: () -> T): () -> T
    local node = create_node(assert_stable_scope(), source, false :: any)

    evaluate_node(node)

    return function()
        push_scope_as_child_of(node)
        return node.cache
    end
end

return derive
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="30">
              <Properties>
                <string name="Name">effect</string>
                <string name="Source"><![CDATA[local graph = require "./graph"
local create_node = graph.create_node
local assert_stable_scope = graph.assert_stable_scope
local evaluate_node = graph.evaluate_node

local function effect<T>(callback: (T) -> T, initial_value: T)
    local node = create_node(assert_stable_scope(), callback, initial_value)

    evaluate_node(node)
end

return effect :: (<T>(callback: (T) -> T, initial_value: T) -> ()) & ((callback: () -> ()) -> ())
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="31">
              <Properties>
                <string name="Name">flags</string>
                <string name="Source"><![CDATA[local function inline_test(): string
    return debug.info(1, "n")
end

local is_O2 = inline_test() ~= "inline_test"

return {
    strict = not is_O2,
    defaults = true,
    defer_nested_properties = true,
    batch = false,
}
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="32">
              <Properties>
                <string name="Name">graph</string>
                <string name="Source"><![CDATA[local flags = require "./flags"

export type SourceNode<T> = {
    cache: T,
    [number]: Node<T>
}

export type Node<T> =  {
    cache: T,
    effect:  ((T) -> T) | false,
    cleanups: { () -> () } | false,

    context: { [number]: unknown } | false,

    owned: { Node<T> } | false,
    owner: Node<T> | false,

    parents: { SourceNode<T> },
    [number]: Node<T> -- children
}

local scopes = { n = 0 } :: { [number]: Node<any>, n: number } -- scopes stack

local function efn(err: string)
    local trace = debug.traceback(err, 2)

    if string.find(err, "^effect error stacktrace") then -- if effect error is nested
        trace = string.gsub("    " .. trace, "\n", function() -- indent entire error
            return "\n    "
        end)
    end

    trace ..= "\nsource update stacktrace:"
return trace
end

local function ycall<T, U>(fn: (T) -> U, arg: T): (boolean, string|U)

    local thread = coroutine.create(xpcall)
    --local function efn(err: string) return debug.traceback(err, 3) end
    local resume_ok, run_ok, result = coroutine.resume(thread, fn, efn, arg)

    assert(resume_ok)
        
    if coroutine.status(thread) ~= "dead" then
        return false, debug.traceback(thread, "attempt to yield in reactive scope")
    end

    return run_ok, result
end

local function get_scope(): Node<unknown>?
    return scopes[scopes.n]
end

local function assert_stable_scope(): Node<unknown>
    local scope = get_scope()

    if not scope then
        local caller_name = debug.info(2, "n")
        return error(`cannot use {caller_name}() outside a stable or reactive scope`, 0)
    elseif scope.effect then
        error("cannot create a new reactive scope inside another reactive scope", 0)
    end

    return scope
end

local function push_child<T>(parent: SourceNode<any>, child: Node<any>)
    table.insert(parent, child)
    table.insert(child.parents, parent)
end

local function push_scope<T>(node: Node<T>)
    local n = scopes.n + 1
    scopes.n = n
    scopes[n] = node
end

local function pop_scope()
    local n = scopes.n
    scopes.n = n - 1
    scopes[n] = nil
end

local function push_cleanup<T>(node: Node<T>, cleanup: () -> ())
    if node.cleanups then
        table.insert(node.cleanups, cleanup)
    else
        node.cleanups = { cleanup }
    end
end

local function flush_cleanups<T>(node: Node<T>)
    if node.cleanups then
        for _, fn in node.cleanups do
            local ok, err: string? = xpcall(fn, debug.traceback)
            if not ok then error(`cleanup error: {err}`, 0) end
        end

        table.clear(node.cleanups)
    end
end

local function find_and_swap_pop<T>(t: { T }, v: T)
    local i = table.find(t, v) :: number
    local n = #t
    t[i] = t[n]
    t[n] = nil
end

local function unparent<T>(node: Node<T>)
    local parents = node.parents

    for i, parent in parents do
        find_and_swap_pop(parent, node)
        parents[i] = nil
    end
end

local function destroy<T>(node: Node<T>)
    if flags.strict and table.find(scopes, node) then
        error("attempt to destroy an active scope", 0)
    end

    flush_cleanups(node)
    unparent(node)
    
    if node.owner then
        find_and_swap_pop(node.owner.owned :: { Node<T> }, node)
        node.owner = false
    end

    if node.owned then
        local owned = node.owned
        while owned[1] do destroy(owned[1]) end
    end
end

local function destroy_owned<T>(node: Node<T>)
    if node.owned then
        local owned = node.owned
        while owned[1] do destroy(owned[1]) end
    end
end

local update_queue = { n = 0 } :: { n: number, [number]: Node<any> }

local function evaluate_node<T>(node: Node<T>)
    if flags.strict then
        if table.find(scopes, node) then
            error("a scope, that should rerun due to the update of a source, is already active", 0)
        end

        local initial_value = node.cache

        for i = 1, 2 do
            local cur_value = node.cache

            flush_cleanups(node)
            destroy_owned(node)
    
            push_scope(node)
            local ok, new_value = ycall(node.effect :: (T) -> T, cur_value)
            pop_scope()
            
            if not ok then
                table.clear(update_queue)
                update_queue.n = 0
                error(`effect error stacktrace\n{new_value :: string}`, 0)
            end

            node.cache = new_value :: T
        end

        return initial_value ~= node.cache
    else
        local cur_value = node.cache

        flush_cleanups(node)
        destroy_owned(node)

        push_scope(node)
        local ok, new_value = pcall(node.effect :: (T) -> T, node.cache)
        pop_scope()

        if not ok then
            table.clear(update_queue)
            update_queue.n = 0
            error(`effect error:\n{new_value}\n`, 0)
        end
    
        node.cache = new_value
        return cur_value ~= new_value
    end
end

local function queue_children_for_update<T>(node: SourceNode<T>)
    local i = update_queue.n
    while node[1] do
        i += 1
        update_queue[i] = node[1]
        unparent(node[1])
    end
    update_queue.n = i
end

local function get_update_queue_length()
    return update_queue.n
end

local function flush_update_queue(from: number)
    local i = from + 1
    while i <= update_queue.n do
        local node = update_queue[i]
        --assert(node.effect)

        if node.owner and evaluate_node(node) then
            queue_children_for_update(node)
        end

        update_queue[i] = false :: any
        i += 1
    end
    
    update_queue.n = from
end

local function update_descendants<T>(root: SourceNode<T>)
    local n0 = update_queue.n
    queue_children_for_update(root)

    if flags.batch then return end

    local i = n0 + 1
    while i <= update_queue.n do
        local node = update_queue[i]
        --assert(node.effect)

        -- check if node is still owned in case destroyed after queued
        if node.owner and evaluate_node(node) then
            queue_children_for_update(node)
        end

        update_queue[i] = false :: any -- false instead of nil to avoid sparse
        i += 1
    end

    update_queue.n = n0
end

local function push_scope_as_child_of<T>(node: SourceNode<T>)
    local scope = get_scope()
    if scope and scope.effect then -- do not track nodes with no effect
        push_child(node, scope)
    end
end

local function create_node<T>(owner: false | Node<any>, effect: false | (T) -> T, value: T): Node<T>
    local node: Node<T> = {
        cache = value,
        effect = effect,
        cleanups = false,

        context = false,

        owner = owner,
        owned = false,

        parents = {},
    }

    if owner then
        if owner.owned then
            table.insert(owner.owned, node)
        else
            owner.owned = { node }
        end
    end

    return node
end

local function create_source_node<T>(value: T): SourceNode<T>
    return { cache = value }
end

local function get_children<T>(node: Node<T>): { Node<unknown> }
    return { unpack(node) } :: { Node<any> }
end

local function set_context<T>(node: Node<T>, key: number, value: unknown)
    if node.context then
        node.context[key] = value
    else
        node.context = { [key] = value }
    end
end

return table.freeze {
    push_scope = push_scope,
    pop_scope = pop_scope,
    evaluate_node = evaluate_node,
    get_scope = get_scope,
    assert_stable_scope = assert_stable_scope,
    push_cleanup = push_cleanup,
    destroy = destroy,
    flush_cleanups = flush_cleanups,
    push_scope_as_child_of = push_scope_as_child_of,
    update_descendants = update_descendants,
    push_child = push_child,
    create_node = create_node,
    create_source_node = create_source_node,
    get_children = get_children,
    flush_update_queue = flush_update_queue,
    get_update_queue_length = get_update_queue_length,
    set_context = set_context,
    scopes = scopes,

    q = update_queue
}
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="33">
              <Properties>
                <string name="Name">implicit_effect</string>
                <string name="Source"><![CDATA[local graph = require "./graph"
type Node<T> = graph.Node<T>
local create_node = graph.create_node
local assert_stable_scope = graph.assert_stable_scope
local get_scope = graph.get_scope
local evaluate_node = graph.evaluate_node
local push_cleanup = graph.push_cleanup

local function update_property_effect(p: {
    instance: Instance,
    property: string,
    source: () -> unknown  
})
    (p.instance :: any)[p.property] = p.source()
    return p
end

local function update_parent_effect(p: {
    instance: Instance,
    source: () -> Instance
})
    p.instance.Parent = p.source()
    return p
end

-- todo: investigate if "count" method used in indexes() and values() can improve performance here
local function update_children_effect(p: {
    instance: Instance,
    cur_children_set: { [Instance]: true },
    new_children_set: { [Instance]: true },
    source: () -> Instance | { Instance }
})
    local cur_children_set: { [Instance]: true } = p.cur_children_set -- cache of all children parented before update
    local new_children_set: { [Instance]: true } = p.new_children_set -- cache of all children parented after update

    local new_children = p.source() -- all (and only) children that should be parented after this update
    
    local function process_child(child: Instance | { Instance })
        if type(child) == "userdata" then
            if new_children_set[child] then return end -- stops redundant reparenting

            new_children_set[child] = true -- record child set from this update
            
            if not cur_children_set[child] then
                child.Parent = p.instance -- if child wasn't already parented then parent it
            else 
                cur_children_set[child] = nil -- remove child from cache if it was already in cache
            end  
        elseif type(child) == "table" then
            for _, child in child do
                process_child(child)
            end
        elseif type(child) == "function" then
            local node = create_node(assert(get_scope()), update_children_effect, {
                instance = p.instance,
                cur_children_set = {},
                new_children_set = {},
                source = child   
            })
    
            evaluate_node(node)

            push_cleanup(assert(get_scope()), function()
                for child in node.cache.cur_children_set do
                    child.Parent = nil
                end
            end)
        end
    end

    process_child(new_children)

    for child in cur_children_set do
        child.Parent = nil -- unparent all children that weren't in the new children set
    end

    table.clear(cur_children_set) -- clear cache, preserve capacity
    p.cur_children_set, p.new_children_set = new_children_set, cur_children_set

    return p
end

return {
    property = function(instance, property, source)
        local node = create_node(assert_stable_scope(), update_property_effect, {
            instance = instance,
            property = property,
            source = source
        })

        evaluate_node(node)

        return node
    end,

    parent = function(instance, parent)
        local node = create_node(assert_stable_scope(), update_parent_effect, {
            instance = instance,
            source = parent
        })

        evaluate_node(node)
        
        return node
    end,

    children = function(instance, children)
        local node = create_node(assert_stable_scope(), update_children_effect, {
            instance = instance,
            cur_children_set = {},
            new_children_set = {},
            source = children
        })

        evaluate_node(node)

        push_cleanup(assert_stable_scope(), function()
            for child in node.cache.cur_children_set do
                child.Parent = nil
            end
        end)

        return node
    end
}
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="34">
              <Properties>
                <string name="Name">indexes</string>
                <string name="Source"><![CDATA[local flags = require "./flags"
local branch = require "./branch"
local source = require "./source"
local effect = require "./effect"
local timeout = require "./timeout" ()

type Array<T> = { T }
type Map<K, V> = { [K]: V }
type Source<T> = () -> T

local function indexes<K, V, Obj>(
    input: Source<Map<K, V>>,
    component: (Source<V>, K, Source<boolean>) -> (Obj, ...number)
): Source<Array<Obj>>
    local update_count = 0
    local scopes = {} :: Map<K, {
        destroy: () -> (),
        object: Obj,
        value: V?,
        value_source: (V?) -> V,
        count: number,
        delay: number,
        present: (boolean?) -> boolean,
        timeout: { cancel: boolean }?,
    }>

    local output = source({} :: Array<Obj>)
    local function update_output()
        local objects = table.create(4)

        for _, scope in scopes do
            table.insert(objects, scope.object)
        end

        output(objects)
    end

    effect(function()
        local data = input()

        local count = update_count
        update_count += 1

        local children_need_update = false -- set to true if a scope is created or destroyed

        -- create or update scopes
        for i, v in data do
            local scope = scopes[i]

            if scope == nil then -- create new scope and create component
                local value_source = source(v)
                local present = source(false)

                local delay = nil :: number?
                local destroy, object = branch(function()
                    local object, t = component(value_source, i, present)
                    delay = t
                    return object
                end)

                present(true)
                
                children_need_update = true

                scopes[i] = {
                    destroy = destroy,
                    object = object,
                    value = v,
                    value_source = value_source,
                    count = count,
                    delay = delay or 0,
                    present = present,
                    timeout = nil,
                }
            else -- update scope
                scope.count = count

                if scope.value ~= v then
                    if scope.timeout then -- index is in input table again; cancel destruction
                        scope.timeout.cancel = true
                        scope.timeout = nil
                        scope.present(true)
                    end
                    
                    scope.value = v
                    scope.value_source(v)
                end
            end
        end

        -- destroy scopes
        for i, scope in scopes do
            if scope.count < count then -- if count is not latest then index is no longer in the input table
                scope.present(false)

                if scope.delay == 0 then
                    scope.destroy()
                    scopes[i] = nil
                    children_need_update = true
                else
                    scope.value = nil -- set to nil for the `scope.value ~= v` check
                    if scope.timeout == nil then
                        scope.timeout = timeout(scope.delay, function() -- todo: possible redundant updates
                            scope.destroy()
                            scopes[i] = nil
                            update_output()
                        end)
                    end
                end
            end
        end

        if children_need_update then
            update_output()
        end
    end)

    return output
end

return indexes
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="35">
              <Properties>
                <string name="Name">lib</string>
                <string name="Source"><![CDATA[local version = { major = 0, minor = 4, patch = 1 }

local root = require "./root"
local branch = require "./branch"
local mount = require "./mount"
local create = require "./create"
local apply = require "./apply"
local source = require "./source"
local effect = require "./effect"
local derive = require "./derive"
local cleanup = require "./cleanup"
local untrack = require "./untrack"
local read = require "./read"
local batch = require "./batch"
local context = require "./context"
local switch = require "./switch"
local show = require "./show"
local indexes = require "./indexes"
local values = require "./values"
local spring, update_springs = require "./spring"()
local action = require "./action"()
local changed = require "./changed"
local timeout, update_timeouts = require "./timeout"()
local flags = require "./flags"

export type Source<T> = source.Source<T>
export type source<T> = Source<T>
export type Context<T> = context.Context<T>
export type context<T> = Context<T>

local function step(dt: number)
    if game then debug.profilebegin("VIDE STEP") end

    if game then debug.profilebegin("VIDE SPRING") end
    update_springs(dt)
    if game then debug.profileend() end

    if game then debug.profilebegin("VIDE SCHEDULER") end
    update_timeouts(dt)
    if game then debug.profileend() end

    if game then debug.profileend() end
end

local stepped = game and game:GetService("RunService").Heartbeat:Connect(function(dt: number)
    task.defer(step, dt)
end)

local vide = {
    version = version,

    -- core
    root = root,
    --branch = branch,
    mount = mount,
    create = create,
    source = source,
    effect = effect,
    derive = derive,
    switch = switch,
    show = show,
    indexes = indexes,
    values = values,

    -- util
    cleanup = cleanup,
    untrack = untrack,
    read = read,
    batch = batch,
    context = context,

    -- animations
    spring = spring,

    -- actions
    action = action,
    changed = changed,

    -- flags
    strict = (nil :: any) :: boolean,
    defaults = (nil :: any) :: boolean,
    defer_nested_properties = (nil :: any) :: boolean,

    -- temporary
    apply = function(instance: Instance)
        return function(props: { [any]: any })
            apply(instance, props)
            return instance
        end
    end,

    -- runtime
    step = function(dt: number)
        if stepped then
            stepped:Disconnect()
            stepped = nil
        end
        step(dt)
    end
}

setmetatable(vide :: any, {
    __index = function(_, index: unknown): ()
        if flags[index] == nil then
            error(`{tostring(index)} is not a valid member of vide`, 0)
        else
            return flags[index]
        end
    end,

    __newindex = function(_, index: unknown, value: unknown)
        if flags[index] == nil then
            error(`{tostring(index)} is not a valid member of vide, 0`)
        else
            flags[index] = value
        end
    end
})

return vide
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="36">
              <Properties>
                <string name="Name">mount</string>
                <string name="Source"><![CDATA[local root = require "./root"
local apply = require "./apply"

local function mount<T>(component: () -> T, target: Instance?): () -> ()
    return root(function()
        local result = component()
        if target then apply(target, { result }) end
    end)
end

return mount :: (<T>(component: () -> T, target: Instance) -> () -> ()) & ((component: () -> ()) -> () -> ())
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="37">
              <Properties>
                <string name="Name">read</string>
                <string name="Source"><![CDATA[local function read<T>(value: T | () -> T): T
    return if type(value) == "function" then value() else value
end

return read
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="38">
              <Properties>
                <string name="Name">root</string>
                <string name="Source"><![CDATA[local graph = require "./graph"
type Node<T> = graph.Node<T>
local create_node = graph.create_node
local push_scope = graph.push_scope
local pop_scope = graph.pop_scope
local destroy = graph.destroy

local refs = {}

local function root<T...>(fn: (destroy: () -> ()) -> T...): (() -> (), T...)
    local node = create_node(false, false, false)

    refs[node] = true -- prevent gc of root node

    local destroy = function()
        if not refs[node] then error "root already destroyed" end
        refs[node] = nil
        destroy(node)
    end

    push_scope(node)

    local result = { xpcall(fn, debug.traceback, destroy) }

    pop_scope()

    if not result[1] then
        destroy()
        error(`error while running root():\n\n{result[2]}`, 0)
    end

    return destroy, unpack(result :: any, 2)
end

return root :: <T...>(fn: (destroy: () -> ()) -> T...) -> (() -> (), T...)
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="39">
              <Properties>
                <string name="Name">show</string>
                <string name="Source"><![CDATA[local source = require "./source"
local derive = require "./derive"
local effect = require "./effect"
local untrack = require "./untrack"
local switch = require "./switch"

type Array<T> = { T }
type Source<T> = () -> T

local function show<T, Obj>(
    input: Source<T?>,
    component: (Source<T>, Source<boolean>) -> (Obj, ...number),
    fallback: ((Source<boolean>) -> (Obj, ...number))?
): Source<nil | Obj | Array<Obj>>
    local filtered_input = source()

    effect(function()
        local v = input() 
        if v then
            filtered_input(v)
        end
    end)

    local input_is_truthy = derive(function()
        return not not input()
    end)

    return switch(input_is_truthy) {
        [true] = function(present)
            return component(filtered_input, present)
        end,
        [false] = fallback
    }
end

return show
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="40">
              <Properties>
                <string name="Name">source</string>
                <string name="Source"><![CDATA[local graph = require "./graph"
type Node<T> = graph.Node<T>
local create_source_node = graph.create_source_node
local push_scope_as_child_of = graph.push_scope_as_child_of
local update_descendants = graph.update_descendants

export type Source<T> = (() -> T) & ((value: T) -> T)

local function source<T>(initial_value: T): Source<T>
    local node = create_source_node(initial_value)

    local function update_source(...): T
        if select("#", ...) == 0 then -- no args were given
            push_scope_as_child_of(node)
            return node.cache
        end

        local v = ... :: T
        if node.cache == v and (type(v) ~= "table" or table.isfrozen(v)) then 
            return v
        end

        node.cache = v
        update_descendants(node)
        return v
    end

    return update_source
end

return source :: (<T>(initial_value: T) -> Source<T>) & (<T>() -> Source<T>)
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="41">
              <Properties>
                <string name="Name">spring</string>
                <string name="Source"><![CDATA[local graph = require "./graph"
type Node<T> = graph.Node<T>
type SourceNode<T> = graph.SourceNode<T>
local create_node = graph.create_node
local create_source_node = graph.create_source_node
local assert_stable_scope = graph.assert_stable_scope
local evaluate_node = graph.evaluate_node
local update_descendants = graph.update_descendants
local push_scope_as_child_of = graph.push_scope_as_child_of

local UPDATE_RATE = 120
local TOLERANCE_FACTOR = 10_000

type Animatable = number | CFrame | Color3 | UDim | UDim2 | Vector2 | Vector3

--[[
Unsupported datatypes:
- bool
- Vector2int16
- Vector3int16
- EnumItem
]]

type SpringState<T> = {
    k: number, -- spring constant
    c: number, -- damping coeff

    x0_123: vector, x0_456: vector, -- initial position
    x_123: vector, x_456: vector, -- current position
    x1_123: vector, x1_456: vector, -- target position
    v_123: vector, v_456: vector, -- current velocity

    source_value: T -- current value of spring input source
}

type SpringSettings<T> = ({
    position: T?,
    velocity: T?,
    impulse: T?
}) -> ()

type TypeToVec6<T> = (T) -> (vector, vector)
type Vec6ToType<T> = (vector, vector) -> T

local type_to_vec6 = {
    number = function(v)
        return vector.create(v, 0, 0), vector.zero
    end :: TypeToVec6<number>,

    CFrame = function(v)
        return v.Position, vector.create(v:ToEulerAnglesXYZ())
    end :: TypeToVec6<CFrame>,

    Color3 = function(v)
        -- todo: hsv, oklab?
        return vector.create(v.R, v.G, v.B), vector.zero
    end :: TypeToVec6<Color3>,

    UDim = function(v)
        return vector.create(v.Scale, v.Offset, 0), vector.zero
    end :: TypeToVec6<UDim>,
    
    UDim2 = function(v)
        return vector.create(v.X.Scale, v.X.Offset, v.Y.Scale), vector.create(v.Y.Offset, 0, 0)
    end :: TypeToVec6<UDim2>,

    Vector2 = function(v)
        return vector.create(v.X, v.Y, 0), vector.zero
    end :: TypeToVec6<Vector2>,

    Vector3 = function(v)
        return v, vector.zero
    end :: TypeToVec6<Vector3>,

    Rect = function(v)
        return vector.create(v.Min.X, v.Min.Y, v.Max.X), vector.create(v.Max.Y, 0, 0)
    end :: TypeToVec6<Rect>,

    table = function(v)
        return vector.create(v[1] or 0, v[2] or 0, v[3] or 0), vector.create(v[4] or 0, 0, 0)
    end :: TypeToVec6<{ number }>
}

local vec6_to_type = {
    number = function(a, b)
        return a.X
    end :: Vec6ToType<number>,

    CFrame = function(a, b)
        return CFrame.new(a) * CFrame.fromEulerAnglesXYZ(b.X, b.Y, b.Z)
    end :: Vec6ToType<CFrame>,

    Color3 = function(v)
        return Color3.new(math.clamp(v.X, 0, 1), math.clamp(v.Y, 0, 1), math.clamp(v.Z, 0, 1))
    end :: Vec6ToType<Color3>,

    UDim = function(v)
        return UDim.new(v.X, math.round(v.Y))
    end :: Vec6ToType<UDim>,
    
    UDim2 = function(a, b)
        return UDim2.new(a.X, math.round(a.Y), a.Z, math.round(b.X))
    end :: Vec6ToType<UDim2>,

    Vector2 = function(v)
        return Vector2.new(v.X, v.Y)
    end :: Vec6ToType<Vector2>,

    Vector3 = function(v)
        return v
    end :: Vec6ToType<Vector3>,

    Rect = function(a, b)
        return Rect.new(a.X, a.Y, a.Z, b.X)
    end :: Vec6ToType<Rect>,

    table = function(a, b)
        return { a.X, a.Y, a.Z, b.X }
    end :: Vec6ToType<{ number }>
}

local invalid_type = {
    __index = function(_, t: string)
        error(`cannot spring type {t}`, 0)
    end
}

setmetatable(type_to_vec6, invalid_type)
setmetatable(vec6_to_type, invalid_type)

-- maps spring data to its corresponding output node
-- lifetime of spring data is tied to output node
local springs: { [SpringState<unknown>]: SourceNode<unknown> } = {}
setmetatable(springs :: any, { __mode = "v" })

local function spring<T>(source: () -> T, period: number?, damping_ratio: number?): (() -> T, SpringSettings<T>)
    local owner = assert_stable_scope()

    -- https://en.wikipedia.org/wiki/Damping
    local w_n = 2*math.pi / (period or 1)
    local z = damping_ratio or 1
    local k = w_n^2
    local c_c = 2*w_n
    local c = z * c_c

    -- todo: is there a solution other than reducing step size?
    -- todo: this does not catch all solver exploding cases
    if c > UPDATE_RATE*2 then -- solver will explode if this is true
        error("spring damping too high, consider reducing damping or increasing period", 0)
    end

    local data: SpringState<T> = {
        k = k,
        c = c,

        x0_123 = vector.zero,
        x_123 = vector.zero,
        x1_123 = vector.zero,
        v_123 = vector.zero,

        x0_456 = vector.zero,
        x_456 = vector.zero,
        x1_456 = vector.zero,
        v_456 = vector.zero,

        source_value = false :: any,
    }
    
    local output = create_source_node(false :: any)

    local function updater_effect()
        local value = source()
        data.x1_123, data.x1_456 = type_to_vec6[typeof(value)](value)
        data.source_value = value
        springs[data] = output
        return value
    end

    local updater = create_node(owner, updater_effect, false :: any)
    evaluate_node(updater)

    -- set initial position to goal
    data.x_123, data.x_456 = data.x1_123, data.x1_456

    -- set output to goal
    output.cache = data.source_value

    local config = function(p)
        local x = p.position
        local v = p.velocity
        local dv = p.impulse

        if x then
            local x_123, x_456 = type_to_vec6[typeof(x)](x)
            data.x_123, data.x_456 = x_123, x_456
            data.x0_123, data.x0_456 = x_123, x_456
        end

        if v then
            data.v_123, data.v_456 = type_to_vec6[typeof(v)](v)
        end

        if dv then
            local dv_123, dv_456 = type_to_vec6[typeof(dv)](dv)
            data.v_123 += dv_123
            data.v_456 += dv_456
        end

        -- schedule spring
        springs[data] = output
    end :: SpringSettings<T>

    return function(...)
        if select("#", ...) == 0 then -- no args were given
            push_scope_as_child_of(output)
            return output.cache
        end

        -- set current position to value
        local v = ... :: T
        data.x_123, data.x_456 = type_to_vec6[typeof(v)](v)

        -- reset velocity
        data.v_123 = vector.zero
        data.v_456 = vector.zero

        -- schedule spring
        springs[data] = output

        -- set output to value
        output.cache = v

        return v
    end, config
end

-- calculates a float tolerance, based on the magnitude of the float
local function get_min_step(x: number): number
    return x/TOLERANCE_FACTOR
end
local function get_min_vector_step(direction: vector): vector
    return vector.create(
        get_min_step(direction.x),
        get_min_step(direction.y),
        get_min_step(direction.z)
    )
end

local function step_springs(dt: number)
    for s in springs do
        local k = s.k
        local c = s.c
        local x_123, x_456 = s.x_123, s.x_456
        local x1_123, x1_456 = s.x1_123, s.x1_456
        local u_123, u_456 = s.v_123, s.v_456

        -- calculate displacement from target
        local dx_123 = x_123 - x1_123
        local dx_456 = x_456 - x1_456

        -- calculate spring force
        local fs_123 = dx_123*-k
        local fs_456 = dx_456*-k

        -- calculate friction force
        local ff_123 = u_123*-c
        local ff_456 = u_456*-c

        -- calculate acceleration
        local a_123 = (fs_123 + ff_123)
        local a_456 = (fs_456 + ff_456)

        -- step acceleration
        local v_123 = u_123 + a_123*dt
        local v_456 = u_456 + a_456*dt

        -- step velocity
        local y_123 = x_123 + v_123*dt
        local y_456 = x_456 + v_456*dt

        s.x_123, s.x_456 = y_123, y_456
        s.v_123, s.v_456 = v_123, v_456
    end
end

local function update_spring_sources()
    for data, output in springs do
        local x0_123, x0_456 = data.x0_123, data.x0_456
        local x_123, x_456 = data.x_123, data.x_456
        local x1_123, x1_456 = data.x1_123, data.x1_456
        local v_123, v_456 = data.v_123, data.v_456

        local tol_123 = vector.abs(get_min_vector_step(x0_123 - x1_123))
        local tol_456 = vector.abs(get_min_vector_step(x0_456 - x1_456))

        if
            -- position is at goal (within tolerance)
            vector.max(vector.abs(x_123 - x1_123), tol_123) == tol_123
            and vector.max(vector.abs(x_456 - x1_456), tol_456) == tol_456

            -- velocity is at 0 (within tolerance)
            and vector.max(vector.abs(v_123/10), tol_123) == tol_123
            and vector.max(vector.abs(v_456/10), tol_456) == tol_456
        then
            springs[data] = nil
            output.cache = data.source_value
        else
            output.cache = vec6_to_type[typeof(data.source_value)](x_123, x_456)
        end

        update_descendants(output)
    end
end

return function()
    local time_elapsed = 0

    return spring, function(dt: number)
        time_elapsed += dt

        while time_elapsed > 1 / UPDATE_RATE do
            time_elapsed -= 1 / UPDATE_RATE
            step_springs(1 / UPDATE_RATE)
        end

        update_spring_sources()
    end
end
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="42">
              <Properties>
                <string name="Name">switch</string>
                <string name="Source"><![CDATA[local branch = require "./branch"
local source = require "./source"
local effect = require "./effect"
local timeout = require "./timeout" ()

type Array<T> = { T }
type Map<K, V> = { [K]: V }
type Source<T> = () -> T
type Component<T> = (Source<boolean>) -> (T, ...number)

local function switch_map<K, Obj>(
    input: Source<K>,
    map: Map<K, Component<Obj>>
): Source<nil | Obj | Array<Obj>>
    local scopes = {} :: Map<K, {
        destroy: () -> (),
        object: Obj,
        delay: number,
        present: (boolean?) -> boolean,
        timeout: { cancel: boolean }?
    }>

    local output = source(nil :: nil | Obj | Array<Obj>)
    local function update_output()
        local objects = {}

        for _, scope in scopes do
            table.insert(objects, scope.object)
        end

        output(
            if objects[2] then objects
            elseif objects[1] then objects[1]
            else nil
        )
    end

    effect(function()
        local key: K? = input()

        -- destroy (or queue destroy) all scopes not associated with the input key
        for k, scope in scopes do
            if k == key then continue end

            scope.present(false)

            if scope.delay == 0 then
                scope.destroy()
                scopes[k] = nil
            else
                if scope.timeout == nil then
                    scope.timeout = timeout(scope.delay, function()
                        scope.destroy()
                        scopes[k] = nil
                        update_output()
                    end)
                end
            end
        end

        -- create new scope or abort destruction of existing scope if key exists
        if key ~= nil then
            local scope = scopes[key]

            if scope then
                scope.present(true)

                if scope.timeout then
                    scope.timeout.cancel = true
                    scope.timeout = nil
                end
            else
                local component = map[key]

                if component ~= nil then
                    if type(component) ~= "function" then
                        error("map must map a value to a function", 0)
                    end

                    local present = source(false)

                    local delay = nil :: number?
                    local destroy, object = branch(function()
                        local object, t = component(present)
                        delay = t
                        return object
                    end)

                    present(true)

                    scopes[key] = {
                        destroy = destroy,
                        object = object,
                        delay = delay or 0,
                        present = present,
                        timeout = nil
                    }
                end
            end
        end

        update_output()
    end)

    return output
end

local function switch<K, Obj>(input: Source<K>): (map: Map<K, Component<Obj>>) -> Source<nil | Obj | Array<Obj>>
    return function(map)
        return switch_map(input, map)
    end
end

return switch
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="43">
              <Properties>
                <string name="Name">timeout</string>
                <string name="Source"><![CDATA[local queue = {} :: {
    { t: number, fn: () -> (), cancel: boolean }
}

local function timeout(t: number, fn: () -> ())
    local handle = { t = t, fn = fn, cancel = false }
    table.insert(queue, handle)
    return handle
end

local function update_timeouts(dt: number)
    for i = #queue, 1, -1 do
        local handle = queue[i]
        handle.t -= dt
        
        if handle.cancel or handle.t <= 0 then
            queue[i] = queue[#queue]
            queue[#queue] = nil

            if not handle.cancel then
                handle.fn()
            end
        end
    end
end

return function() return timeout, update_timeouts end
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="44">
              <Properties>
                <string name="Name">untrack</string>
                <string name="Source"><![CDATA[local graph = require "./graph"
type Node<T> = graph.Node<T>
local get_scope = graph.get_scope

local function untrack<T>(source: () -> T): T
    local scope = get_scope()
    
    if scope then
        -- sources are only tracked if the node in scope has an effect
        local effect = scope.effect
        scope.effect = false

        local ok, result = xpcall(source, debug.traceback)

        scope.effect = effect :: () -> ()

        if not ok then error(result, 0) end

        return result :: T
    else
        return source()
    end
end

return untrack :: ( <T>(fn: () -> T) -> T ) & ( (fn: () -> ()) -> () )
]]></string>
              </Properties>
            </Item>
            <Item class="ModuleScript" referent="45">
              <Properties>
                <string name="Name">values</string>
                <string name="Source"><![CDATA[local flags = require "./flags"
local branch = require "./branch"
local source = require "./source"
local effect = require "./effect"
local timeout = require "./timeout" ()

type Array<T> = { T }
type Map<K, V> = { [K]: V }
type Source<T> = () -> T

local function values<K, V, Obj>(
    input: Source<Map<K, V>>,
    component: (V, Source<K>, Source<boolean>) -> (Obj, ...number)
): Source<Array<Obj>>
    local update_count = 0
    local scopes = {} :: Map<V, {
        destroy: () -> (),
        object: Obj,
        index: K?,
        index_source: (K?) -> K,
        count: number,
        delay: number,
        present: (boolean?) -> boolean,
        timeout: { cancel: boolean }?,
    }>

    local output = source({} :: Array<Obj>)
    local function update_output()
        local objects = table.create(4)

        for _, scope in scopes do
            table.insert(objects, scope.object)
        end

        output(objects)
    end

    effect(function()
        local data = input()

        local count = update_count
        update_count += 1

        local children_need_update = false -- set to true if a scope is created or destroyed

        if flags.strict then -- check for duplicate values
            local map = {}
            for _, v in data do
                if map[v] then
                    error("table source passed to `values()` contains duplicate values", 0)
                end
                map[v] = true
            end
        end

        -- create or update scopes
        for i, v in data do
            local scope = scopes[v]

            if scope == nil then -- create new scope and create component
                local index_source = source(i)
                local present = source(false)

                local delay = nil :: number?
                local destroy, object = branch(function()
                    local object, t = component(v, index_source, present)
                    delay = t
                    return object
                end)

                present(true)
                
                children_need_update = true

                scopes[v] = {
                    destroy = destroy,
                    object = object,
                    index = i,
                    index_source = index_source,
                    count = count,
                    delay = delay or 0,
                    present = present,
                    timeout = nil,
                }
            else -- update scope
                scope.count = count

                if scope.index ~= i then
                    if scope.timeout then -- value is in input table again; cancel destruction
                        scope.timeout.cancel = true
                        scope.timeout = nil
                        scope.present(true)
                    end
                    
                    scope.index = i
                    scope.index_source(i)
                end
            end
        end

        -- destroy scopes
        for v, scope in scopes do
            if scope.count < count then -- if count is not latest then value is no longer in the input table
                scope.present(false)

                if scope.delay == 0 then
                    scope.destroy()
                    scopes[v] = nil
                    children_need_update = true
                else
                    scope.index = nil -- set to nil for the `scope.index ~= i` check
                    if scope.timeout == nil then
                        scope.timeout = timeout(scope.delay, function() -- todo: possible redundant updates
                            scope.destroy()
                            scopes[v] = nil
                            update_output()
                        end)
                    end
                end
            end
        end

        if children_need_update then
            update_output()
        end
    end)

    return output
end

return values
]]></string>
              </Properties>
            </Item>
          </Item>
        </Item>
      </Item>
    </Item>
  </Item>
</roblox>